# P3-23 audit: IEEE facets and composite value identity

Copyright (C) 2026 Qore Technologies, s.r.o.

Final reviewed scope on develop; all applicable commit gates below have completed.

Scope: WSDL IEEE facet/list/union/fixed-value integration and provider metadata; the new Qore/Python/worker regressions; shared rational reference and strict corpus promotion; release notes, design, evidence, current reports, README and execution record. The complete audit-changes skill and applicable module-structure, sandboxing, cooperative cancellation and DataProvider guides were reviewed. There are no C++ or core edits.

The audit found and corrected two regressions: decimal fixed-value comparison through short display strings, and fixed digit-count comparison in a bounded scalar value space. Focused reproductions fail before correction, and new schema/provider/independent regressions pass after it.

Checklist: **21 Pass, 41 N/A, 0 Fail**. The final diff, current reports, source/runtime hashes and corrected strict-gate assertions were verified.

| Check | Status | Evidence |
| --- | --- | --- |
| 1. Entry exists in `doxygen/lang/120_modules.dox.tmpl` (for modules in the Qore repo; N/A for external module repos) | N/A | Existing external single-file WSDL module; no new module, separated source or second entry point. |
| 2. Entry exists in `doxygen/lang/900_release_notes.dox.tmpl` (for modules in the Qore repo; external modules have release notes in their .qm) | Pass | WSDL 0.5.8 release notes describe rounded IEEE facets, primitive identity, metadata and adjacent examples. |
| 3. `qore_user_module()` or `qore_external_user_module()` call in `CMakeLists.txt` | Pass | Existing WSDL CMake registration builds the updated AOT module and Qdx/Doxygen output; no registration change is required. |
| 4. Module added to QMOD list in `CMakeLists.txt` | N/A | Existing external single-file WSDL module; no new module, separated source or second entry point. |
| 5. `.qm` file has `@section <lowercasemodname>intro` as first doc section — **must be all lowercase** (e.g., `avrodataproviderintro`, not `AvroDataProviderintro`) | Pass | The existing first documentation section remains lowercase wsdlintro. |
| 6. `%modern` in `.qm` file — no redundant `%new-style`, `%require-types`, `%strict-args`, `%enable-all-warnings` | Pass | WSDL and the new qtest use %modern; the .qr worker obtains modern semantics from its extension. |
| 7. No parse directives (`%requires`, `%modern`, `%new-style`) in separated `.qc` files (check OUTSIDE of `@code` blocks only) | N/A | Existing external single-file WSDL module; no new module, separated source or second entry point. |
| 8. No `%include` usage (deprecated for modules) | Pass | No %include directive is introduced. |
| 9. Copyright 2026 on all new files | Pass | All new authored code, tests, design and evidence files have 2026 copyright. |
| 10. Directory layout: `.qm` inside `qlib/<ModuleName>/` directory (not at `qlib/<ModuleName>.qm` for multi-file modules) | N/A | Existing external single-file WSDL module; no new module, separated source or second entry point. |
| 11. No second `.qm` for the same module at `qlib/<ModuleName>.qm` | N/A | Existing external single-file WSDL module; no new module, separated source or second entry point. |
| 12. `ns=Qore::XX` matches the QoreNamespace constructor path | N/A | No C++/QPP changes, native operations or blocking I/O are introduced. Existing native IEEE cancellation/environment handling is unchanged. |
| 13. `%modern` directive present | Pass | The qtest uses explicit %modern, the .qr worker implicit modern semantics, and Python tests use unittest. |
| 14. Executable permission set (`chmod +x`) | Pass | The new qtest, .qr worker and Python test entry point are executable (755). The shared Python reference is an imported utility. |
| 15. Uses %prepend-module-path  before %requires for in-repo modules (Qore and Qore modules only; not Qorus) | Pass | Qore tests prepend local qlib and require the relative WSDL source. A separate execution check explicitly loads the built AOT module. |
| 16. External module dependencies use `%try-module` — except modules delivered with the project itself (Qore ex: DataProvider, ConnectionProvider, QUnit, etc.) which use hard `%requires` | Pass | Repository xml/WSDL and Qore QUnit are hard requirements; external json uses the %try-module pattern and reports a missing dependency. |
| 17. No filesystem operations (fopen, open, creat, unlink, remove, rename, mkdir, rmdir, stat, chmod) without sandbox checks | N/A | No C++/QPP changes, native operations or blocking I/O are introduced. Existing native IEEE cancellation/environment handling is unchanged. |
| 18. No network operations (connect, bind, socket, getaddrinfo, gethostbyname) without sandbox checks | N/A | No C++/QPP changes, native operations or blocking I/O are introduced. Existing native IEEE cancellation/environment handling is unchanged. |
| 19. If filesystem/network ops exist, verify `QoreSandboxManagerHelper` usage | N/A | No C++/QPP changes, native operations or blocking I/O are introduced. Existing native IEEE cancellation/environment handling is unchanged. |
| 20. No `File::`, `Dir::`, `Socket::`, `HTTPClient::` usage without justification | Pass | Production comparison/provider code performs no I/O. The .qr test reads its explicit manifest through ReadOnlyFile; SOAP fixtures are local and temporary. |
| 21. All `for`/`while` loops that could iterate >100 times have `qore_check_cancel()` checks | N/A | No C++/QPP changes, native operations or blocking I/O are introduced. Existing native IEEE cancellation/environment handling is unchanged. |
| 22. Uses `qore_check_cancel()` (NOT deprecated `qore_check_io_interrupt()`) | N/A | No C++/QPP changes, native operations or blocking I/O are introduced. Existing native IEEE cancellation/environment handling is unchanged. |
| 23. Check frequency: every 100 iterations for tight loops, every 10 for expensive iterations | N/A | No C++/QPP changes, native operations or blocking I/O are introduced. Existing native IEEE cancellation/environment handling is unchanged. |
| 24. No blocking operations without cancellation support | N/A | No C++/QPP changes, native operations or blocking I/O are introduced. Existing native IEEE cancellation/environment handling is unchanged. |
| 25. Every action has `display_name`, `short_desc` (plain text, <80 chars), `desc` (markdown) | N/A | No app/action registration, request/response record type, secret, factory, presentation catalog or JAR is added; this change extends existing scalar providers and schema-generated fields. |
| 26. Every action has `options` populated via `getActionOptionFromFields()` — without this, the action shows an empty, unusable form | N/A | No app/action registration, request/response record type, secret, factory, presentation catalog or JAR is added; this change extends existing scalar providers and schema-generated fields. |
| 27. Every action has `output_type` set to a typed data type constant (e.g., `MyResponseDataType`) — not omitted | N/A | No app/action registration, request/response record type, secret, factory, presentation catalog or JAR is added; this change extends existing scalar providers and schema-generated fields. |
| 28. DPAT_API actions: provider has `"supports_request": True` and implements `doRequestImpl()` | N/A | No app/action registration, request/response record type, secret, factory, presentation catalog or JAR is added; this change extends existing scalar providers and schema-generated fields. |
| 29. DPAT_FIND actions: every option exists in `SearchOptions`, `getRecordTypeImpl()` returns `*hash<string, AbstractDataField>` | N/A | No app/action registration, request/response record type, secret, factory, presentation catalog or JAR is added; this change extends existing scalar providers and schema-generated fields. |
| 30. Scheme-based apps (with `"scheme"` in registerApp): actions use `"path"` and do NOT use `"cls"` — having both `scheme` and `cls` causes a runtime error | N/A | No app/action registration, request/response record type, secret, factory, presentation catalog or JAR is added; this change extends existing scalar providers and schema-generated fields. |
| 31. Single-key hash slices use trailing comma: `Fields{"key",}` (without trailing comma, `Fields{"key"}` returns the value, not a hash) | N/A | No app/action registration, request/response record type, secret, factory, presentation catalog or JAR is added; this change extends existing scalar providers and schema-generated fields. |
| 32. **Typed data type classes exist** for request and response types — inherit `HashDataType`, have `const Fields` hash, call `addQoreFields(Fields)` in constructor, export public constant at bottom (e.g., `public const MyDataType = new MyDataType();`) | N/A | No app/action registration, request/response record type, secret, factory, presentation catalog or JAR is added; this change extends existing scalar providers and schema-generated fields. |
| 33. Request/input types use `public` Fields (enables `ClassName::Fields` in action registration) | N/A | No app/action registration, request/response record type, secret, factory, presentation catalog or JAR is added; this change extends existing scalar providers and schema-generated fields. |
| 34. Response/output types use `private` Fields | N/A | No app/action registration, request/response record type, secret, factory, presentation catalog or JAR is added; this change extends existing scalar providers and schema-generated fields. |
| 35. Each field in data types has `display_name`, `type`, and `desc` (markdown-formatted) | N/A | No app/action registration, request/response record type, secret, factory, presentation catalog or JAR is added; this change extends existing scalar providers and schema-generated fields. |
| 36. Input fields have `example_value` where useful (string fields, endpoint URIs, SQL queries, etc.) | N/A | No app/action registration, request/response record type, secret, factory, presentation catalog or JAR is added; this change extends existing scalar providers and schema-generated fields. |
| 37. Fields with finite allowed values use `allowed_values` with `AllowedValueInfo` containing both `value` and `display_name` (Title Case, human-readable) — never bare values, never described only in text | Pass | The existing numeric field resolves choices to AllowedValueInfo entries with value/display_name. IEEE keys affect comparison only; authored display spellings and atomic failed updates are tested. |
| 38. Password/secret fields have `"sensitive": True` | N/A | No app/action registration, request/response record type, secret, factory, presentation catalog or JAR is added; this change extends existing scalar providers and schema-generated fields. |
| 39. `groups` uses `AppGroup` enum values from `qlib/DataProvider/AppGroup.qc` | N/A | No app/action registration, request/response record type, secret, factory, presentation catalog or JAR is added; this change extends existing scalar providers and schema-generated fields. |
| 40. App `logo` stored as separate file, loaded at module level in `Priv` namespace | N/A | No app/action registration, request/response record type, secret, factory, presentation catalog or JAR is added; this change extends existing scalar providers and schema-generated fields. |
| 41. App `desc` uses markdown: bullet list of capabilities, links to project website, business-language explanation of value | N/A | No app/action registration, request/response record type, secret, factory, presentation catalog or JAR is added; this change extends existing scalar providers and schema-generated fields. |
| 42. `display_name` is user-friendly ("Apache Avro" not "avro") | N/A | No app/action registration, request/response record type, secret, factory, presentation catalog or JAR is added; this change extends existing scalar providers and schema-generated fields. |
| 43. `short_desc` is plain text, under 80 chars, single sentence — no markdown | N/A | No app/action registration, request/response record type, secret, factory, presentation catalog or JAR is added; this change extends existing scalar providers and schema-generated fields. |
| 44. `desc` uses markdown: backticks for code/field refs (`` `field_name` ``, `` `True` ``, `` `pdf` ``), `\n\n` for paragraphs, `- ` bullet lists for enumerations, `**bold**` for caveats | N/A | No app/action registration, request/response record type, secret, factory, presentation catalog or JAR is added; this change extends existing scalar providers and schema-generated fields. |
| 45. Descriptions use plain business language relating to common challenges — not just technical "what" but "why" and "when to use" | N/A | No app/action registration, request/response record type, secret, factory, presentation catalog or JAR is added; this change extends existing scalar providers and schema-generated fields. |
| 46. No bare `True`/`False`/`NOTHING` — must be backtick-wrapped in `desc` | N/A | No app/action registration, request/response record type, secret, factory, presentation catalog or JAR is added; this change extends existing scalar providers and schema-generated fields. |
| 47. No bare field/option names in prose — must use backticks | N/A | No app/action registration, request/response record type, secret, factory, presentation catalog or JAR is added; this change extends existing scalar providers and schema-generated fields. |
| 48. Long descriptions (>500 chars) use bold section headers and bullet lists | N/A | No app/action registration, request/response record type, secret, factory, presentation catalog or JAR is added; this change extends existing scalar providers and schema-generated fields. |
| 49. **Factory registration in Qore repo**: every factory name registered in `qlib/DataProvider/DataProvider.qc` → `FactoryMap` (without this, module loads but doesn't appear in Qorus apps) | N/A | No app/action registration, request/response record type, secret, factory, presentation catalog or JAR is added; this change extends existing scalar providers and schema-generated fields. |
| 50. **`getRecordTypeImpl()` signature**: must be `private *hash<string, AbstractDataField> getRecordTypeImpl(*hash<auto> search_options)` — NOT returning `*AbstractDataProviderType` | N/A | No app/action registration, request/response record type, secret, factory, presentation catalog or JAR is added; this change extends existing scalar providers and schema-generated fields. |
| 51. **Dependency JARs committed** (for JNI modules): JAR files in `qlib/*/jar/` may be gitignored — use `git add -f` to ensure they're tracked, otherwise CI compilation fails | N/A | No app/action registration, request/response record type, secret, factory, presentation catalog or JAR is added; this change extends existing scalar providers and schema-generated fields. |
| 52. JAR install rules in CMakeLists.txt for all dependency JARs | N/A | No app/action registration, request/response record type, secret, factory, presentation catalog or JAR is added; this change extends existing scalar providers and schema-generated fields. |
| 53. **No workarounds**: No TODOs, FIXMEs, stubs, or partially-implemented features | Pass | Root fixes use target value identity and count identity; no tolerance, fixture-specific production behavior, lexical cap or retry is introduced. Precise validator defects remain explicit oracle disagreements. |
| 54. **Exception safety**: C++ uses `ReferenceHolder` for Qore allocations, `std::unique_ptr` for C++ allocations, `*xsink` checked after every fallible operation | Pass | Qore owns all allocations. Numeric metadata validates before member assignment; choice updates build complete temporary data before replacement. Conversion/cancellation errors propagate through the existing narrow remapping paths. |
| 55. **Thread safety**: All mutable shared state protected by `std::lock_guard<std::mutex>` or documented as immutable-after-construction | Pass | No new global mutable state is added. Provider builtin metadata is immutable after construction/reconstruction. Field setters retain the existing caller-synchronization contract; per-call keys and sample candidates are local. |
| 56. **Type safety**: Strongly-typed `code<return(args)>` instead of untyped `code`; `static_cast` instead of C casts; typed hashdecls for results; enums where appropriate | Pass | Builtin names, optionality, partial comparisons and facet/list/union metadata are typed. Heterogeneous inputs stay at established scalar boundaries. Known conflicting provider identities reject during reconstruction. |
| 57. **Performance**: No O(n²) where O(n) is possible; no unnecessary copies; coordinate descent uses incremental residuals not full matrix multiply | Pass | Adjacent-value generation tests at most two candidates per bound and never searches the numeric value space. Facet validation remains linear in declared facets/choices and scalar text size; no new exponential traversal is introduced. |
| 58. **Error handling**: All inputs validated (dimensions, empty data, unfitted models); C++ I/O handles EAGAIN/EINTR if applicable | Pass | Tests cover malformed lexical input, NaN partial order, impossible intervals, forbidden/inherited/fixed facets, wrong precision metadata, optional values and failed choice updates with the intended error categories. |
| 59. **Documentation**: Doxygen `@param`, `@return`, `@throw` on all public methods; `@par Example` with realistic business scenarios; `@note` for important caveats | Pass | Public builtin getters have return/since documentation. Provider docs explain IEEE comparison and custom conversion responsibility; the design has an executed example. AOT and Qdx/Doxygen builds pass without diagnostics. |
| 60. **QPP flags**: `[flags=CONSTANT]` on methods that never throw; `[flags=RET_VALUE_ONLY]` on methods that throw but have no side effects | N/A | No C++/QPP changes, native operations or blocking I/O are introduced. Existing native IEEE cancellation/environment handling is unchanged. |
| 61. **Security**: No user-controlled format strings; no buffer overflows; bounds checking on array indices; no credentials in code | Pass | No credentials, unmanaged buffers or user-controlled format strings are added. Strict native conversion validates numeric text; XML output retains existing escaping and temporary test files remain local. |
| 62. **Correctness**: Algorithms verified against reference implementations; edge cases tested (empty data, single sample, all-zero features) | Pass | The 13-case Qore suite passes 4837 assertions in AST/IR/JIT/tiered and AOT; all 66 affected suites pass 761 cases/20957 assertions. Independent SOAP matrices, encoded-adjacency reference and fixed-count schemas pass; the corpus has no new failure and 24 resolved requirements. The full 169-method run retained 33 known P4/P5/P6 failures plus a stale selected-scope assertion; after correcting that test, its complete 10-method file has only the same two P6 failures. Combined results retain exactly the previous 33 later-phase signatures and no new failures or errors. No production code changed after the full run. |

Verification evidence is recorded in [EXECUTION.md](../EXECUTION.md) and
[IEEE facet evidence](../ieee-facets-evidence.md). The full run's original failure
log is preserved; the corrected test is verified independently rather than
relabeled as passing in that original run. `/tmp/wsdl-p3-23-reviewed-python-comparison.json`
checks both logs against the preceding baseline. The strict corpus contains
97 WSDLs / 828 directions with no selected failures and 196 remaining broad
failures (24 resolved, zero added). No C++ changes require a new Valgrind run.
