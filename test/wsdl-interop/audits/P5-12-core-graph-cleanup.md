# P5-12 core prerequisite evidence audit

Copyright (C) 2026 Qore Technologies, s.r.o.

Audited against XML parent `99565bb` on 2026-09-11 using the full audit-changes
skill and all five referenced design guides. This XML commit contains evidence
and planning documents only. The implementation, test, release note and native
62-item audit are in Qore develop commit `0eb8abb81` (19 Pass, 43 N/A, 0 Fail).
This documentation scope has all 62 items below: **5 Pass, 57 N/A, 0 Fail**.

The complete [inventory](../P5-12-validation.json) and
[evidence](../core-graph-cleanup-evidence.md) retain the final tests, original
failing reproducer, known DWARF-reader warning and remaining phase requirements.
No implementation code, fixtures, selection expectations, installation or remote
branch is changed by this XML commit. The independent native wildcard type
finding is reproduced and assigned to P5-13; its temporary prototype is explicitly
separate from production. Historical P5-11 evidence remains unchanged.

| # | Check | Status | Evidence |
| --- | --- | --- | --- |
| 1 | Entry exists in `doxygen/lang/120_modules.dox.tmpl` (for modules in the Qore repo; N/A for external module repos) | N/A | Documentation and evidence only: no new or changed module, QPP/C++ implementation, executable test, DataProvider registration, factory, I/O or JNI dependency in this XML commit. The core implementation has its own complete audit. |
| 2 | Entry exists in `doxygen/lang/900_release_notes.dox.tmpl` (for modules in the Qore repo; external modules have release notes in their .qm) | N/A | Documentation and evidence only: no new or changed module, QPP/C++ implementation, executable test, DataProvider registration, factory, I/O or JNI dependency in this XML commit. The core implementation has its own complete audit. |
| 3 | `qore_user_module()` or `qore_external_user_module()` call in `CMakeLists.txt` | N/A | Documentation and evidence only: no new or changed module, QPP/C++ implementation, executable test, DataProvider registration, factory, I/O or JNI dependency in this XML commit. The core implementation has its own complete audit. |
| 4 | Module added to QMOD list in `CMakeLists.txt` | N/A | Documentation and evidence only: no new or changed module, QPP/C++ implementation, executable test, DataProvider registration, factory, I/O or JNI dependency in this XML commit. The core implementation has its own complete audit. |
| 5 | `.qm` file has `@section <lowercasemodname>intro` as first doc section — **must be all lowercase** (e.g., `avrodataproviderintro`, not `AvroDataProviderintro`) | N/A | Documentation and evidence only: no new or changed module, QPP/C++ implementation, executable test, DataProvider registration, factory, I/O or JNI dependency in this XML commit. The core implementation has its own complete audit. |
| 6 | `%modern` in `.qm` file — no redundant `%new-style`, `%require-types`, `%strict-args`, `%enable-all-warnings` | N/A | Documentation and evidence only: no new or changed module, QPP/C++ implementation, executable test, DataProvider registration, factory, I/O or JNI dependency in this XML commit. The core implementation has its own complete audit. |
| 7 | No parse directives (`%requires`, `%modern`, `%new-style`) in separated `.qc` files (check OUTSIDE of `@code` blocks only) | N/A | Documentation and evidence only: no new or changed module, QPP/C++ implementation, executable test, DataProvider registration, factory, I/O or JNI dependency in this XML commit. The core implementation has its own complete audit. |
| 8 | No `%include` usage (deprecated for modules) | N/A | Documentation and evidence only: no new or changed module, QPP/C++ implementation, executable test, DataProvider registration, factory, I/O or JNI dependency in this XML commit. The core implementation has its own complete audit. |
| 9 | Copyright 2026 on all new files | Pass | All new authored evidence, finding and audit documents and the inventory carry Copyright 2026. |
| 10 | Directory layout: `.qm` inside `qlib/<ModuleName>/` directory (not at `qlib/<ModuleName>.qm` for multi-file modules) | N/A | Documentation and evidence only: no new or changed module, QPP/C++ implementation, executable test, DataProvider registration, factory, I/O or JNI dependency in this XML commit. The core implementation has its own complete audit. |
| 11 | No second `.qm` for the same module at `qlib/<ModuleName>.qm` | N/A | Documentation and evidence only: no new or changed module, QPP/C++ implementation, executable test, DataProvider registration, factory, I/O or JNI dependency in this XML commit. The core implementation has its own complete audit. |
| 12 | `ns=Qore::XX` matches the QoreNamespace constructor path | N/A | Documentation and evidence only: no new or changed module, QPP/C++ implementation, executable test, DataProvider registration, factory, I/O or JNI dependency in this XML commit. The core implementation has its own complete audit. |
| 13 | `%modern` directive present | N/A | Documentation and evidence only: no new or changed module, QPP/C++ implementation, executable test, DataProvider registration, factory, I/O or JNI dependency in this XML commit. The core implementation has its own complete audit. |
| 14 | Executable permission set (`chmod +x`) | N/A | Documentation and evidence only: no new or changed module, QPP/C++ implementation, executable test, DataProvider registration, factory, I/O or JNI dependency in this XML commit. The core implementation has its own complete audit. |
| 15 | Uses %prepend-module-path  before %requires for in-repo modules (Qore and Qore modules only; not Qorus) | N/A | Documentation and evidence only: no new or changed module, QPP/C++ implementation, executable test, DataProvider registration, factory, I/O or JNI dependency in this XML commit. The core implementation has its own complete audit. |
| 16 | External module dependencies use `%try-module` — except modules delivered with the project itself (Qore ex: DataProvider, ConnectionProvider, QUnit, etc.) which use hard `%requires` | N/A | Documentation and evidence only: no new or changed module, QPP/C++ implementation, executable test, DataProvider registration, factory, I/O or JNI dependency in this XML commit. The core implementation has its own complete audit. |
| 17 | No filesystem operations (fopen, open, creat, unlink, remove, rename, mkdir, rmdir, stat, chmod) without sandbox checks | N/A | Documentation and evidence only: no new or changed module, QPP/C++ implementation, executable test, DataProvider registration, factory, I/O or JNI dependency in this XML commit. The core implementation has its own complete audit. |
| 18 | No network operations (connect, bind, socket, getaddrinfo, gethostbyname) without sandbox checks | N/A | Documentation and evidence only: no new or changed module, QPP/C++ implementation, executable test, DataProvider registration, factory, I/O or JNI dependency in this XML commit. The core implementation has its own complete audit. |
| 19 | If filesystem/network ops exist, verify `QoreSandboxManagerHelper` usage | N/A | Documentation and evidence only: no new or changed module, QPP/C++ implementation, executable test, DataProvider registration, factory, I/O or JNI dependency in this XML commit. The core implementation has its own complete audit. |
| 20 | No `File::`, `Dir::`, `Socket::`, `HTTPClient::` usage without justification | N/A | Documentation and evidence only: no new or changed module, QPP/C++ implementation, executable test, DataProvider registration, factory, I/O or JNI dependency in this XML commit. The core implementation has its own complete audit. |
| 21 | All `for`/`while` loops that could iterate >100 times have `qore_check_cancel()` checks | N/A | Documentation and evidence only: no new or changed module, QPP/C++ implementation, executable test, DataProvider registration, factory, I/O or JNI dependency in this XML commit. The core implementation has its own complete audit. |
| 22 | Uses `qore_check_cancel()` (NOT deprecated `qore_check_io_interrupt()`) | N/A | Documentation and evidence only: no new or changed module, QPP/C++ implementation, executable test, DataProvider registration, factory, I/O or JNI dependency in this XML commit. The core implementation has its own complete audit. |
| 23 | Check frequency: every 100 iterations for tight loops, every 10 for expensive iterations | N/A | Documentation and evidence only: no new or changed module, QPP/C++ implementation, executable test, DataProvider registration, factory, I/O or JNI dependency in this XML commit. The core implementation has its own complete audit. |
| 24 | No blocking operations without cancellation support | N/A | Documentation and evidence only: no new or changed module, QPP/C++ implementation, executable test, DataProvider registration, factory, I/O or JNI dependency in this XML commit. The core implementation has its own complete audit. |
| 25 | Every action has `display_name`, `short_desc` (plain text, <80 chars), `desc` (markdown) | N/A | Documentation and evidence only: no new or changed module, QPP/C++ implementation, executable test, DataProvider registration, factory, I/O or JNI dependency in this XML commit. The core implementation has its own complete audit. |
| 26 | Every action has `options` populated via `getActionOptionFromFields()` — without this, the action shows an empty, unusable form | N/A | Documentation and evidence only: no new or changed module, QPP/C++ implementation, executable test, DataProvider registration, factory, I/O or JNI dependency in this XML commit. The core implementation has its own complete audit. |
| 27 | Every action has `output_type` set to a typed data type constant (e.g., `MyResponseDataType`) — not omitted | N/A | Documentation and evidence only: no new or changed module, QPP/C++ implementation, executable test, DataProvider registration, factory, I/O or JNI dependency in this XML commit. The core implementation has its own complete audit. |
| 28 | DPAT_API actions: provider has `"supports_request": True` and implements `doRequestImpl()` | N/A | Documentation and evidence only: no new or changed module, QPP/C++ implementation, executable test, DataProvider registration, factory, I/O or JNI dependency in this XML commit. The core implementation has its own complete audit. |
| 29 | DPAT_FIND actions: every option exists in `SearchOptions`, `getRecordTypeImpl()` returns `*hash<string, AbstractDataField>` | N/A | Documentation and evidence only: no new or changed module, QPP/C++ implementation, executable test, DataProvider registration, factory, I/O or JNI dependency in this XML commit. The core implementation has its own complete audit. |
| 30 | Scheme-based apps (with `"scheme"` in registerApp): actions use `"path"` and do NOT use `"cls"` — having both `scheme` and `cls` causes a runtime error | N/A | Documentation and evidence only: no new or changed module, QPP/C++ implementation, executable test, DataProvider registration, factory, I/O or JNI dependency in this XML commit. The core implementation has its own complete audit. |
| 31 | Single-key hash slices use trailing comma: `Fields{"key",}` (without trailing comma, `Fields{"key"}` returns the value, not a hash) | N/A | Documentation and evidence only: no new or changed module, QPP/C++ implementation, executable test, DataProvider registration, factory, I/O or JNI dependency in this XML commit. The core implementation has its own complete audit. |
| 32 | **Typed data type classes exist** for request and response types — inherit `HashDataType`, have `const Fields` hash, call `addQoreFields(Fields)` in constructor, export public constant at bottom (e.g., `public const MyDataType = new MyDataType();`) | N/A | Documentation and evidence only: no new or changed module, QPP/C++ implementation, executable test, DataProvider registration, factory, I/O or JNI dependency in this XML commit. The core implementation has its own complete audit. |
| 33 | Request/input types use `public` Fields (enables `ClassName::Fields` in action registration) | N/A | Documentation and evidence only: no new or changed module, QPP/C++ implementation, executable test, DataProvider registration, factory, I/O or JNI dependency in this XML commit. The core implementation has its own complete audit. |
| 34 | Response/output types use `private` Fields | N/A | Documentation and evidence only: no new or changed module, QPP/C++ implementation, executable test, DataProvider registration, factory, I/O or JNI dependency in this XML commit. The core implementation has its own complete audit. |
| 35 | Each field in data types has `display_name`, `type`, and `desc` (markdown-formatted) | N/A | Documentation and evidence only: no new or changed module, QPP/C++ implementation, executable test, DataProvider registration, factory, I/O or JNI dependency in this XML commit. The core implementation has its own complete audit. |
| 36 | Input fields have `example_value` where useful (string fields, endpoint URIs, SQL queries, etc.) | N/A | Documentation and evidence only: no new or changed module, QPP/C++ implementation, executable test, DataProvider registration, factory, I/O or JNI dependency in this XML commit. The core implementation has its own complete audit. |
| 37 | Fields with finite allowed values use `allowed_values` with `AllowedValueInfo` containing both `value` and `display_name` (Title Case, human-readable) — never bare values, never described only in text | N/A | Documentation and evidence only: no new or changed module, QPP/C++ implementation, executable test, DataProvider registration, factory, I/O or JNI dependency in this XML commit. The core implementation has its own complete audit. |
| 38 | Password/secret fields have `"sensitive": True` | N/A | Documentation and evidence only: no new or changed module, QPP/C++ implementation, executable test, DataProvider registration, factory, I/O or JNI dependency in this XML commit. The core implementation has its own complete audit. |
| 39 | `groups` uses `AppGroup` enum values from `qlib/DataProvider/AppGroup.qc` | N/A | Documentation and evidence only: no new or changed module, QPP/C++ implementation, executable test, DataProvider registration, factory, I/O or JNI dependency in this XML commit. The core implementation has its own complete audit. |
| 40 | App `logo` stored as separate file, loaded at module level in `Priv` namespace | N/A | Documentation and evidence only: no new or changed module, QPP/C++ implementation, executable test, DataProvider registration, factory, I/O or JNI dependency in this XML commit. The core implementation has its own complete audit. |
| 41 | App `desc` uses markdown: bullet list of capabilities, links to project website, business-language explanation of value | N/A | Documentation and evidence only: no new or changed module, QPP/C++ implementation, executable test, DataProvider registration, factory, I/O or JNI dependency in this XML commit. The core implementation has its own complete audit. |
| 42 | `display_name` is user-friendly ("Apache Avro" not "avro") | N/A | Documentation and evidence only: no new or changed module, QPP/C++ implementation, executable test, DataProvider registration, factory, I/O or JNI dependency in this XML commit. The core implementation has its own complete audit. |
| 43 | `short_desc` is plain text, under 80 chars, single sentence — no markdown | N/A | Documentation and evidence only: no new or changed module, QPP/C++ implementation, executable test, DataProvider registration, factory, I/O or JNI dependency in this XML commit. The core implementation has its own complete audit. |
| 44 | `desc` uses markdown: backticks for code/field refs (`` `field_name` ``, `` `True` ``, `` `pdf` ``), `\n\n` for paragraphs, `- ` bullet lists for enumerations, `**bold**` for caveats | N/A | Documentation and evidence only: no new or changed module, QPP/C++ implementation, executable test, DataProvider registration, factory, I/O or JNI dependency in this XML commit. The core implementation has its own complete audit. |
| 45 | Descriptions use plain business language relating to common challenges — not just technical "what" but "why" and "when to use" | N/A | Documentation and evidence only: no new or changed module, QPP/C++ implementation, executable test, DataProvider registration, factory, I/O or JNI dependency in this XML commit. The core implementation has its own complete audit. |
| 46 | No bare `True`/`False`/`NOTHING` — must be backtick-wrapped in `desc` | N/A | Documentation and evidence only: no new or changed module, QPP/C++ implementation, executable test, DataProvider registration, factory, I/O or JNI dependency in this XML commit. The core implementation has its own complete audit. |
| 47 | No bare field/option names in prose — must use backticks | N/A | Documentation and evidence only: no new or changed module, QPP/C++ implementation, executable test, DataProvider registration, factory, I/O or JNI dependency in this XML commit. The core implementation has its own complete audit. |
| 48 | Long descriptions (>500 chars) use bold section headers and bullet lists | N/A | Documentation and evidence only: no new or changed module, QPP/C++ implementation, executable test, DataProvider registration, factory, I/O or JNI dependency in this XML commit. The core implementation has its own complete audit. |
| 49 | **Factory registration in Qore repo**: every factory name registered in `qlib/DataProvider/DataProvider.qc` → `FactoryMap` (without this, module loads but doesn't appear in Qorus apps) | N/A | Documentation and evidence only: no new or changed module, QPP/C++ implementation, executable test, DataProvider registration, factory, I/O or JNI dependency in this XML commit. The core implementation has its own complete audit. |
| 50 | **`getRecordTypeImpl()` signature**: must be `private *hash<string, AbstractDataField> getRecordTypeImpl(*hash<auto> search_options)` — NOT returning `*AbstractDataProviderType` | N/A | Documentation and evidence only: no new or changed module, QPP/C++ implementation, executable test, DataProvider registration, factory, I/O or JNI dependency in this XML commit. The core implementation has its own complete audit. |
| 51 | **Dependency JARs committed** (for JNI modules): JAR files in `qlib/*/jar/` may be gitignored — use `git add -f` to ensure they're tracked, otherwise CI compilation fails | N/A | Documentation and evidence only: no new or changed module, QPP/C++ implementation, executable test, DataProvider registration, factory, I/O or JNI dependency in this XML commit. The core implementation has its own complete audit. |
| 52 | JAR install rules in CMakeLists.txt for all dependency JARs | N/A | Documentation and evidence only: no new or changed module, QPP/C++ implementation, executable test, DataProvider registration, factory, I/O or JNI dependency in this XML commit. The core implementation has its own complete audit. |
| 53 | **No workarounds**: No TODOs, FIXMEs, stubs, or partially-implemented features | Pass | No implementation or corpus expectation changes in this XML commit. The fixed core failure and newly reproduced independent native defect retain explicit evidence and separate implementation ownership. |
| 54 | **Exception safety**: C++ uses `ReferenceHolder` for Qore allocations, `std::unique_ptr` for C++ allocations, `*xsink` checked after every fallible operation | N/A | Documentation and evidence only: no new or changed module, QPP/C++ implementation, executable test, DataProvider registration, factory, I/O or JNI dependency in this XML commit. The core implementation has its own complete audit. |
| 55 | **Thread safety**: All mutable shared state protected by `std::lock_guard<std::mutex>` or documented as immutable-after-construction | N/A | Documentation and evidence only: no new or changed module, QPP/C++ implementation, executable test, DataProvider registration, factory, I/O or JNI dependency in this XML commit. The core implementation has its own complete audit. |
| 56 | **Type safety**: Strongly-typed `code<return(args)>` instead of untyped `code`; `static_cast` instead of C casts; typed hashdecls for results; enums where appropriate | N/A | Documentation and evidence only: no new or changed module, QPP/C++ implementation, executable test, DataProvider registration, factory, I/O or JNI dependency in this XML commit. The core implementation has its own complete audit. |
| 57 | **Performance**: No O(n²) where O(n) is possible; no unnecessary copies; coordinate descent uses incremental residuals not full matrix multiply | N/A | Documentation and evidence only: no new or changed module, QPP/C++ implementation, executable test, DataProvider registration, factory, I/O or JNI dependency in this XML commit. The core implementation has its own complete audit. |
| 58 | **Error handling**: All inputs validated (dimensions, empty data, unfitted models); C++ I/O handles EAGAIN/EINTR if applicable | N/A | Documentation and evidence only: no new or changed module, QPP/C++ implementation, executable test, DataProvider registration, factory, I/O or JNI dependency in this XML commit. The core implementation has its own complete audit. |
| 59 | **Documentation**: Doxygen `@param`, `@return`, `@throw` on all public methods; `@par Example` with realistic business scenarios; `@note` for important caveats | Pass | Execution record, README, plan, closed core finding, evidence and the next native finding identify exact scope, commands, results, limitations and commit. Durable implementation design and release notes accompany Qore commit 0eb8abb81. |
| 60 | **QPP flags**: `[flags=CONSTANT]` on methods that never throw; `[flags=RET_VALUE_ONLY]` on methods that throw but have no side effects | N/A | Documentation and evidence only: no new or changed module, QPP/C++ implementation, executable test, DataProvider registration, factory, I/O or JNI dependency in this XML commit. The core implementation has its own complete audit. |
| 61 | **Security**: No user-controlled format strings; no buffer overflows; bounds checking on array indices; no credentials in code | Pass | Only reviewed source paths, commands, fixture data and diagnostic summaries are recorded; no credentials or production user data. |
| 62 | **Correctness**: Algorithms verified against reference implementations; edge cases tested (empty data, single sample, all-zero features) | Pass | 384 original source/log/artifact hashes plus documentation and next-finding hashes verify. Fourteen clean memory checks, 20 core runs, 127 XML suites and 19 supplements are retained. Both corpus files are byte-identical to P5-11; selected failures remain zero and 60 broader failures stay visible. The native prototype uses a temporary source and preserves all 24 expected outcomes. |
