# P9a-08 WS-Addressing ledger rows audit

Copyright (C) 2026 Qore Technologies, s.r.o.

Applied `/home/david/.claude/skills/audit-changes/SKILL.md` to P9a-08 on `develop` based on 5075308. Scope: `WsAddressingHelper::HeaderBlocks` and the addressing node (R1041), the explicit empty SOAP action (R2745) and the Basic Profile 2.0 R2901 check in `qlib/WSDL.qm`; the `send_soapaction` option in `qlib/SoapClient.qm` (client, call and SoapConnection), `qlib/SoapClientIo` and `qlib/SoapDataProvider/SoapRequestDataProvider.qc`, with documentation in `qlib/SalesforceSoapClient.qm`; `test/soap-action-header.qtest`; the updated handler, messages, client, decoupled, metadata, soap-actions and wsdl-empty-body-routing tests and `test_soap_actions.py`; `XsdRepeatedElementDataType` (MISSING-VALUE-ERROR for an absent mandatory repeated element after Qore a4049e8ad); `assertion-ledger.json`, `verify_ledger.py` and `assertion-ledger-evidence.md`; the design documents; `PLAN.md` and `EXECUTION.md`.

All 62 checks are explicitly accounted for:

| # | Check | Status | Evidence |
| --- | --- | --- | --- |
| 1 | Entry exists in `doxygen/lang/120_modules.dox.tmpl` (for modules in the Qore repo; N/A for external module repos) | N/A | External module repository. |
| 2 | Entry exists in `doxygen/lang/900_release_notes.dox.tmpl` (for modules in the Qore repo; external modules have release notes in their .qm) | Pass | Release notes: WSDL v0.5.8 (explicit empty SOAP action sends SOAPAction ""; BP 2.0 R2901 for SOAP 1.2), SoapClient v1.0.4, SoapClientIo v1.0, SoapDataProvider v2.0 and SalesforceSoapClient v1.5 (send_soapaction and the empty soapaction behavior); all entries are for unreleased versions (added after tag qore-cross-repo-2.1.2). |
| 3 | `qore_user_module()` or `qore_external_user_module()` call in `CMakeLists.txt` | N/A | No module added. |
| 4 | Module added to QMOD list in `CMakeLists.txt` | N/A | No module added. |
| 5 | `.qm` file has `@section <lowercasemodname>intro` as first doc section — **must be all lowercase** (e.g., `avrodataproviderintro`, not `AvroDataProviderintro`) | N/A | No module added. |
| 6 | `%modern` in `.qm` file — no redundant `%new-style`, `%require-types`, `%strict-args`, `%enable-all-warnings` | Pass | The changed .qm files keep %modern. |
| 7 | No parse directives (`%requires`, `%modern`, `%new-style`) in separated `.qc` files (check OUTSIDE of `@code` blocks only) | Pass | qlib/SoapClientIo/SoapClientIo.qc and qlib/SoapDataProvider/SoapRequestDataProvider.qc gain no parse directives. |
| 8 | No `%include` usage (deprecated for modules) | Pass | No %include usage. |
| 9 | Copyright 2026 on all new files | Pass | The new test/soap-action-header.qtest and this record carry the 2026 copyright. |
| 10 | Directory layout: `.qm` inside `qlib/<ModuleName>/` directory (not at `qlib/<ModuleName>.qm` for multi-file modules) | N/A | No module added. |
| 11 | No second `.qm` for the same module at `qlib/<ModuleName>.qm` | N/A | No module added. |
| 12 | `ns=Qore::XX` matches the QoreNamespace constructor path | N/A | No C++ or QPP change; the changes are Qore module code in qlib/WSDL.qm, qlib/SoapClient.qm, qlib/SoapClientIo, qlib/SoapDataProvider and qlib/SalesforceSoapClient.qm, tests, the ledger and its verifier, and documentation. |
| 13 | `%modern` directive present | Pass | test/soap-action-header.qtest uses %modern. |
| 14 | Executable permission set (`chmod +x`) | Pass | test/soap-action-header.qtest is executable. |
| 15 | Uses %prepend-module-path  before %requires for in-repo modules (Qore and Qore modules only; not Qorus) | Pass | The new test uses %prepend-module-path before its relative %requires of the in-repository modules. |
| 16 | External module dependencies use `%try-module` — except modules delivered with the project itself (Qore ex: DataProvider, ConnectionProvider, QUnit, etc.) which use hard `%requires` | Pass | QUnit, xml and HttpServer are delivered with Qore; in-repository modules use relative %requires. |
| 17 | No filesystem operations (fopen, open, creat, unlink, remove, rename, mkdir, rmdir, stat, chmod) without sandbox checks | N/A | No C++ or QPP change; the changes are Qore module code in qlib/WSDL.qm, qlib/SoapClient.qm, qlib/SoapClientIo, qlib/SoapDataProvider and qlib/SalesforceSoapClient.qm, tests, the ledger and its verifier, and documentation. |
| 18 | No network operations (connect, bind, socket, getaddrinfo, gethostbyname) without sandbox checks | N/A | No C++ or QPP change; the changes are Qore module code in qlib/WSDL.qm, qlib/SoapClient.qm, qlib/SoapClientIo, qlib/SoapDataProvider and qlib/SalesforceSoapClient.qm, tests, the ledger and its verifier, and documentation. |
| 19 | If filesystem/network ops exist, verify `QoreSandboxManagerHelper` usage | N/A | No C++ or QPP change; the changes are Qore module code in qlib/WSDL.qm, qlib/SoapClient.qm, qlib/SoapClientIo, qlib/SoapDataProvider and qlib/SalesforceSoapClient.qm, tests, the ledger and its verifier, and documentation. |
| 20 | No `File::`, `Dir::`, `Socket::`, `HTTPClient::` usage without justification | Pass | No new File/Socket/HTTPClient use in module code. The test writes its WSDL to tmp_location() for the SoapConnection case and removes it with on_exit. |
| 21 | All `for`/`while` loops that could iterate >100 times have `qore_check_cancel()` checks | N/A | No C++ or QPP change; the changes are Qore module code in qlib/WSDL.qm, qlib/SoapClient.qm, qlib/SoapClientIo, qlib/SoapDataProvider and qlib/SalesforceSoapClient.qm, tests, the ledger and its verifier, and documentation. |
| 22 | Uses `qore_check_cancel()` (NOT deprecated `qore_check_io_interrupt()`) | N/A | No C++ or QPP change; the changes are Qore module code in qlib/WSDL.qm, qlib/SoapClient.qm, qlib/SoapClientIo, qlib/SoapDataProvider and qlib/SalesforceSoapClient.qm, tests, the ledger and its verifier, and documentation. |
| 23 | Check frequency: every 100 iterations for tight loops, every 10 for expensive iterations | N/A | No C++ or QPP change; the changes are Qore module code in qlib/WSDL.qm, qlib/SoapClient.qm, qlib/SoapClientIo, qlib/SoapDataProvider and qlib/SalesforceSoapClient.qm, tests, the ledger and its verifier, and documentation. |
| 24 | No blocking operations without cancellation support | Pass | No new blocking operations; tests use blocking queues with timeouts. |
| 25 | Every action has `display_name`, `short_desc` (plain text, <80 chars), `desc` (markdown) | N/A | No DataProvider action, factory, application registration or JNI change. |
| 26 | Every action has `options` populated via `getActionOptionFromFields()` — without this, the action shows an empty, unusable form | N/A | No DataProvider action, factory, application registration or JNI change. |
| 27 | Every action has `output_type` set to a typed data type constant (e.g., `MyResponseDataType`) — not omitted | N/A | No DataProvider action, factory, application registration or JNI change. |
| 28 | DPAT_API actions: provider has `"supports_request": True` and implements `doRequestImpl()` | N/A | No DataProvider action, factory, application registration or JNI change. |
| 29 | DPAT_FIND actions: every option exists in `SearchOptions`, `getRecordTypeImpl()` returns `*hash<string, AbstractDataField>` | N/A | No DataProvider action, factory, application registration or JNI change. |
| 30 | Scheme-based apps (with `"scheme"` in registerApp): actions use `"path"` and do NOT use `"cls"` — having both `scheme` and `cls` causes a runtime error | N/A | No DataProvider action, factory, application registration or JNI change. |
| 31 | Single-key hash slices use trailing comma: `Fields{"key",}` (without trailing comma, `Fields{"key"}` returns the value, not a hash) | N/A | No DataProvider action, factory, application registration or JNI change. |
| 32 | **Typed data type classes exist** for request and response types — inherit `HashDataType`, have `const Fields` hash, call `addQoreFields(Fields)` in constructor, export public constant at bottom (e.g., `public const MyDataType = new MyDataType();`) | N/A | No DataProvider action, factory, application registration or JNI change. |
| 33 | Request/input types use `public` Fields (enables `ClassName::Fields` in action registration) | N/A | No DataProvider action, factory, application registration or JNI change. |
| 34 | Response/output types use `private` Fields | N/A | No DataProvider action, factory, application registration or JNI change. |
| 35 | Each field in data types has `display_name`, `type`, and `desc` (markdown-formatted) | Pass | The new send_soapaction request option of SoapRequestDataProvider has display_name, short_desc and a markdown desc, like the existing options. |
| 36 | Input fields have `example_value` where useful (string fields, endpoint URIs, SQL queries, etc.) | N/A | A bool option has no useful example value. |
| 37 | Fields with finite allowed values use `allowed_values` with `AllowedValueInfo` containing both `value` and `display_name` (Title Case, human-readable) — never bare values, never described only in text | N/A | A bool option has no enumerated values. |
| 38 | Password/secret fields have `"sensitive": True` | N/A | No secret fields. |
| 39 | `groups` uses `AppGroup` enum values from `qlib/DataProvider/AppGroup.qc` | N/A | No DataProvider action, factory, application registration or JNI change. |
| 40 | App `logo` stored as separate file, loaded at module level in `Priv` namespace | N/A | No DataProvider action, factory, application registration or JNI change. |
| 41 | App `desc` uses markdown: bullet list of capabilities, links to project website, business-language explanation of value | N/A | No DataProvider action, factory, application registration or JNI change. |
| 42 | `display_name` is user-friendly ("Apache Avro" not "avro") | Pass | The option's display_name is "Send SOAPAction"; the connection option's is the same. |
| 43 | `short_desc` is plain text, under 80 chars, single sentence — no markdown | Pass | short_desc values are plain text under 80 characters. |
| 44 | `desc` uses markdown: backticks for code/field refs (`` `field_name` ``, `` `True` ``, `` `pdf` ``), `\n\n` for paragraphs, `- ` bullet lists for enumerations, `**bold**` for caveats | Pass | desc values use backticks for `SOAPAction`, `False` and `""`, and `\n\n` before the bold note. |
| 45 | Descriptions use plain business language relating to common challenges — not just technical "what" but "why" and "when to use" | Pass | The desc explains when to use the option (a server that rejects the header) and its conformance cost. |
| 46 | No bare `True`/`False`/`NOTHING` — must be backtick-wrapped in `desc` | Pass | `False` and `True` are backtick-wrapped in the descs. |
| 47 | No bare field/option names in prose — must use backticks | Pass | Option and header names are backtick-wrapped. |
| 48 | Long descriptions (>500 chars) use bold section headers and bullet lists | N/A | The descriptions are short. |
| 49 | **Factory registration in Qore repo**: every factory name registered in `qlib/DataProvider/DataProvider.qc` → `FactoryMap` (without this, module loads but doesn't appear in Qorus apps) | N/A | No DataProvider action, factory, application registration or JNI change. |
| 50 | **`getRecordTypeImpl()` signature**: must be `private *hash<string, AbstractDataField> getRecordTypeImpl(*hash<auto> search_options)` — NOT returning `*AbstractDataProviderType` | N/A | No DataProvider action, factory, application registration or JNI change. |
| 51 | **Dependency JARs committed** (for JNI modules): JAR files in `qlib/*/jar/` may be gitignored — use `git add -f` to ensure they're tracked, otherwise CI compilation fails | N/A | No DataProvider action, factory, application registration or JNI change. |
| 52 | JAR install rules in CMakeLists.txt for all dependency JARs | N/A | No DataProvider action, factory, application registration or JNI change. |
| 53 | **No workarounds**: No TODOs, FIXMEs, stubs, or partially-implemented features | Pass | No stubs. The one open P9a item (CXF-client decoupled exchanges) is documented with its core dependency; R1203/R1204 are not applicable with an approved specification-based rationale, not skipped. |
| 54 | **Exception safety**: C++ uses `ReferenceHolder` for Qore allocations, `std::unique_ptr` for C++ allocations, `*xsink` checked after every fallible operation | Pass | send_soapaction is validated before any request is sent; the WS-Addressing refusal happens before transmission; the test closes SoapClientIo clients and stops servers with on_exit. |
| 55 | **Thread safety**: All mutable shared state protected by `std::lock_guard<std::mutex>` or documented as immutable-after-construction | Pass | The new client member is set in the constructor and restored when a saved client is deserialized; no shared mutable state is added. |
| 56 | **Type safety**: Strongly-typed `code<return(args)>` instead of untyped `code`; `static_cast` instead of C casts; typed hashdecls for results; enums where appropriate | Pass | send_soapaction is a typed bool member and type-checked as a constructor and call option; checkActions() gains a typed bool parameter. |
| 57 | **Performance**: No O(n²) where O(n) is possible; no unnecessary copies; coordinate descent uses incremental residuals not full matrix multiply | Pass | Constant-time checks only. |
| 58 | **Error handling**: All inputs validated (dimensions, empty data, unfitted models); C++ I/O handles EAGAIN/EINTR if applicable | Pass | Invalid option types raise SOAP-CLIENT-ERROR or SOAP-CLIENT-IO-ERROR, WS-Addressing with suppression raises them with R1144, and getSoapResponse() rejects the request-only option; all are asserted. |
| 59 | **Documentation**: Doxygen `@param`, `@return`, `@throw` on all public methods; `@par Example` with realistic business scenarios; `@note` for important caveats | Pass | Constructor, call and connection options are documented; WSDL module docs (SoapNodeOptions.addressing, wsdl_addressing, R2901), design/soap-addressing.md, soap-envelope-processing.md and wsdl-component-references.md are updated; docs-WSDL, docs-SoapClient, docs-SoapClientIo, docs-SoapDataProvider and docs-SalesforceSoapClient build without warnings. |
| 60 | **QPP flags**: `[flags=CONSTANT]` on methods that never throw; `[flags=RET_VALUE_ONLY]` on methods that throw but have no side effects | N/A | No QPP change. |
| 61 | **Security**: No user-controlled format strings; no buffer overflows; bounds checking on array indices; no credentials in code | Pass | Suppressing SOAPAction is an explicit opt-in, documented as non-conforming and refused where WS-Addressing requires the header; no credentials. |
| 62 | **Correctness**: Algorithms verified against reference implementations; edge cases tested (empty data, single sample, all-zero features) | Pass | In a clean environment against the new libqore (NaN-boxing fixed), all 292 qtests and all 498 Python tests pass. Targeted suites: soap-action-header 6 cases / 84 assertions (twice), soap-addressing-handler 233, -messages 350, -client 136, -decoupled 111, -metadata 151, soap-actions 2122, wsdl-particle-occurrences 219; verify_ledger.py OK with 459 covered, 34 not applicable and 993 mappings, and rejects an injected WS-Addressing gap. |

Result: **27 Pass / 35 N/A / zero Fail**.
