# P6-35 WSDL URI constraints audit

Copyright (C) 2026 Qore Technologies, s.r.o.

Applied `/home/david/.codex/skills/audit-changes/SKILL.md` to the complete production,
private native dependency, probe, regression, fixture and documentation changes.
All 62 checks below were reviewed. Native changes have affected Valgrind evidence;
PCRE2 JIT is disabled only for the documented Qore memory-checking configuration.

| # | Check | Status | Evidence |
| --- | --- | --- | --- |
| 1 | Entry exists in `doxygen/lang/120_modules.dox.tmpl` (for modules in the Qore repo; N/A for external module repos) | N/A | No new module, separated class file, registration or layout change. |
| 2 | Entry exists in `doxygen/lang/900_release_notes.dox.tmpl` (for modules in the Qore repo; external modules have release notes in their .qm) | N/A | No new module, separated class file, registration or layout change. |
| 3 | `qore_user_module()` or `qore_external_user_module()` call in `CMakeLists.txt` | N/A | No new module, separated class file, registration or layout change. |
| 4 | Module added to QMOD list in `CMakeLists.txt` | N/A | No new module, separated class file, registration or layout change. |
| 5 | `.qm` file has `@section <lowercasemodname>intro` as first doc section — **must be all lowercase** (e.g., `avrodataproviderintro`, not `AvroDataProviderintro`) | Pass | WSDL retains its lowercase introduction section. |
| 6 | `%modern` in `.qm` file — no redundant `%new-style`, `%require-types`, `%strict-args`, `%enable-all-warnings` | Pass | WSDL retains %modern without redundant directives. |
| 7 | No parse directives (`%requires`, `%modern`, `%new-style`) in separated `.qc` files (check OUTSIDE of `@code` blocks only) | N/A | No new module, separated class file, registration or layout change. |
| 8 | No `%include` usage (deprecated for modules) | Pass | No %include introduced. |
| 9 | Copyright 2026 on all new files | Pass | All new authored CMake, C, Qore, Python, JSON and Markdown files carry copyright 2026; third-party fixture notices are preserved. |
| 10 | Directory layout: `.qm` inside `qlib/<ModuleName>/` directory (not at `qlib/<ModuleName>.qm` for multi-file modules) | N/A | No new module, separated class file, registration or layout change. |
| 11 | No second `.qm` for the same module at `qlib/<ModuleName>.qm` | N/A | No new module, separated class file, registration or layout change. |
| 12 | `ns=Qore::XX` matches the QoreNamespace constructor path | N/A | No QPP class or namespace change. |
| 13 | `%modern` directive present | Pass | The new and updated Qore suites use %modern. |
| 14 | Executable permission set (`chmod +x`) | Pass | All changed Qore suites and the independent Python runner are executable. |
| 15 | Uses %prepend-module-path  before %requires for in-repo modules (Qore and Qore modules only; not Qorus) | Pass | Tests prepend local qlib and use relative in-repo module requirements. Validation selects local binary/user modules. |
| 16 | External module dependencies use `%try-module` — except modules delivered with the project itself (Qore ex: DataProvider, ConnectionProvider, QUnit, etc.) which use hard `%requires` | Pass | Repository-owned xml and core QUnit use hard requirements; external json uses %try-module with a clear missing-dependency exception. |
| 17 | No filesystem operations (fopen, open, creat, unlink, remove, rename, mkdir, rmdir, stat, chmod) without sandbox checks | Pass | Native datatype validation and the offline probe add no filesystem operations. CMake writes checksum-verified copies only in the build tree. |
| 18 | No network operations (connect, bind, socket, getaddrinfo, gethostbyname) without sandbox checks | Pass | Native validation adds no network operations. Dependency source retrieval retains checksum/TLS verification and offline overrides. |
| 19 | If filesystem/network ops exist, verify `QoreSandboxManagerHelper` usage | N/A | No native runtime filesystem or network operation requires a new sandbox helper. |
| 20 | No `File::`, `Dir::`, `Socket::`, `HTTPClient::` usage without justification | Pass | Production adds no I/O primitives. Tests read local fixtures, use ephemeral loopback listeners, bounded queue/request completion and on_exit cleanup. |
| 21 | All `for`/`while` loops that could iterate >100 times have `qore_check_cancel()` checks | N/A | The native production correction adds no loop; its single scheme-length scan is linear. The configure-only probe has 16 bounded fixtures. |
| 22 | Uses `qore_check_cancel()` (NOT deprecated `qore_check_io_interrupt()`) | Pass | normalize_xsd_uri retains qore_check_cancel before/after native conversion and every 100 decoded characters. The cancellation/recovery test passes. |
| 23 | Check frequency: every 100 iterations for tight loops, every 10 for expensive iterations | N/A | No new runtime polling loop or changed cancellation frequency; the existing character loop retains its 100-character checks. |
| 24 | No blocking operations without cancellation support | Pass | No blocking native operation added. WSDL classification uses Qore URI mapping without fetching resources. |
| 25 | Every action has `display_name`, `short_desc` (plain text, <80 chars), `desc` (markdown) | N/A | No DataProvider action, app, field-catalog, factory or provider implementation changes; consumer assertions exercise the existing provider API. |
| 26 | Every action has `options` populated via `getActionOptionFromFields()` — without this, the action shows an empty, unusable form | N/A | No DataProvider action, app, field-catalog, factory or provider implementation changes; consumer assertions exercise the existing provider API. |
| 27 | Every action has `output_type` set to a typed data type constant (e.g., `MyResponseDataType`) — not omitted | N/A | No DataProvider action, app, field-catalog, factory or provider implementation changes; consumer assertions exercise the existing provider API. |
| 28 | DPAT_API actions: provider has `"supports_request": True` and implements `doRequestImpl()` | N/A | No DataProvider action, app, field-catalog, factory or provider implementation changes; consumer assertions exercise the existing provider API. |
| 29 | DPAT_FIND actions: every option exists in `SearchOptions`, `getRecordTypeImpl()` returns `*hash<string, AbstractDataField>` | N/A | No DataProvider action, app, field-catalog, factory or provider implementation changes; consumer assertions exercise the existing provider API. |
| 30 | Scheme-based apps (with `"scheme"` in registerApp): actions use `"path"` and do NOT use `"cls"` — having both `scheme` and `cls` causes a runtime error | N/A | No DataProvider action, app, field-catalog, factory or provider implementation changes; consumer assertions exercise the existing provider API. |
| 31 | Single-key hash slices use trailing comma: `Fields{"key",}` (without trailing comma, `Fields{"key"}` returns the value, not a hash) | N/A | No DataProvider action, app, field-catalog, factory or provider implementation changes; consumer assertions exercise the existing provider API. |
| 32 | **Typed data type classes exist** for request and response types — inherit `HashDataType`, have `const Fields` hash, call `addQoreFields(Fields)` in constructor, export public constant at bottom (e.g., `public const MyDataType = new MyDataType();`) | N/A | No DataProvider action, app, field-catalog, factory or provider implementation changes; consumer assertions exercise the existing provider API. |
| 33 | Request/input types use `public` Fields (enables `ClassName::Fields` in action registration) | N/A | No DataProvider action, app, field-catalog, factory or provider implementation changes; consumer assertions exercise the existing provider API. |
| 34 | Response/output types use `private` Fields | N/A | No DataProvider action, app, field-catalog, factory or provider implementation changes; consumer assertions exercise the existing provider API. |
| 35 | Each field in data types has `display_name`, `type`, and `desc` (markdown-formatted) | N/A | No DataProvider action, app, field-catalog, factory or provider implementation changes; consumer assertions exercise the existing provider API. |
| 36 | Input fields have `example_value` where useful (string fields, endpoint URIs, SQL queries, etc.) | N/A | No DataProvider action, app, field-catalog, factory or provider implementation changes; consumer assertions exercise the existing provider API. |
| 37 | Fields with finite allowed values use `allowed_values` with `AllowedValueInfo` containing both `value` and `display_name` (Title Case, human-readable) — never bare values, never described only in text | N/A | No DataProvider action, app, field-catalog, factory or provider implementation changes; consumer assertions exercise the existing provider API. |
| 38 | Password/secret fields have `"sensitive": True` | N/A | No DataProvider action, app, field-catalog, factory or provider implementation changes; consumer assertions exercise the existing provider API. |
| 39 | `groups` uses `AppGroup` enum values from `qlib/DataProvider/AppGroup.qc` | N/A | No DataProvider action, app, field-catalog, factory or provider implementation changes; consumer assertions exercise the existing provider API. |
| 40 | App `logo` stored as separate file, loaded at module level in `Priv` namespace | N/A | No DataProvider action, app, field-catalog, factory or provider implementation changes; consumer assertions exercise the existing provider API. |
| 41 | App `desc` uses markdown: bullet list of capabilities, links to project website, business-language explanation of value | N/A | No DataProvider action, app, field-catalog, factory or provider implementation changes; consumer assertions exercise the existing provider API. |
| 42 | `display_name` is user-friendly ("Apache Avro" not "avro") | N/A | No DataProvider action, app, field-catalog, factory or provider implementation changes; consumer assertions exercise the existing provider API. |
| 43 | `short_desc` is plain text, under 80 chars, single sentence — no markdown | N/A | No DataProvider action, app, field-catalog, factory or provider implementation changes; consumer assertions exercise the existing provider API. |
| 44 | `desc` uses markdown: backticks for code/field refs (`` `field_name` ``, `` `True` ``, `` `pdf` ``), `\n\n` for paragraphs, `- ` bullet lists for enumerations, `**bold**` for caveats | N/A | No DataProvider action, app, field-catalog, factory or provider implementation changes; consumer assertions exercise the existing provider API. |
| 45 | Descriptions use plain business language relating to common challenges — not just technical "what" but "why" and "when to use" | N/A | No DataProvider action, app, field-catalog, factory or provider implementation changes; consumer assertions exercise the existing provider API. |
| 46 | No bare `True`/`False`/`NOTHING` — must be backtick-wrapped in `desc` | N/A | No DataProvider action, app, field-catalog, factory or provider implementation changes; consumer assertions exercise the existing provider API. |
| 47 | No bare field/option names in prose — must use backticks | N/A | No DataProvider action, app, field-catalog, factory or provider implementation changes; consumer assertions exercise the existing provider API. |
| 48 | Long descriptions (>500 chars) use bold section headers and bullet lists | N/A | No DataProvider action, app, field-catalog, factory or provider implementation changes; consumer assertions exercise the existing provider API. |
| 49 | **Factory registration in Qore repo**: every factory name registered in `qlib/DataProvider/DataProvider.qc` → `FactoryMap` (without this, module loads but doesn't appear in Qorus apps) | N/A | No DataProvider action, app, field-catalog, factory or provider implementation changes; consumer assertions exercise the existing provider API. |
| 50 | **`getRecordTypeImpl()` signature**: must be `private *hash<string, AbstractDataField> getRecordTypeImpl(*hash<auto> search_options)` — NOT returning `*AbstractDataProviderType` | N/A | No DataProvider action, app, field-catalog, factory or provider implementation changes; consumer assertions exercise the existing provider API. |
| 51 | **Dependency JARs committed** (for JNI modules): JAR files in `qlib/*/jar/` may be gitignored — use `git add -f` to ensure they're tracked, otherwise CI compilation fails | N/A | No JAR changes; existing pinned offline dependencies are reused. |
| 52 | JAR install rules in CMakeLists.txt for all dependency JARs | N/A | No JAR changes; new private native inputs are distributed through Makefile.am. |
| 53 | **No workarounds**: No TODOs, FIXMEs, stubs, or partially-implemented features | Pass | Missing WSDL absolute/relative semantic checks and a mismatch between XSD 1.0 RFC 2396 validity and the shared RFC 3986 parser are root caused. Validation is fixed in its owning layers; no alternate resolver, fixture-specific workaround, stub or TODO is added. |
| 54 | **Exception safety**: C++ uses `ReferenceHolder` for Qore allocations, `std::unique_ptr` for C++ allocations, `*xsink` checked after every fallible operation | Pass | The native rejection frees the parsed URI before its existing failure path. Existing normalization/parse allocation errors still propagate. Native probe and URI Qore tests pass Valgrind with no lost allocations; allocation fault-injection passes. Construction state is private until publication and HTTP tests use on_exit cleanup. |
| 55 | **Thread safety**: All mutable shared state protected by `std::lock_guard<std::mutex>` or documented as immutable-after-construction | Pass | URI classification and native validation use call-local state; probe inputs are static const. No mutable global cache is introduced. |
| 56 | **Type safety**: Strongly-typed `code<return(args)>` instead of untyped `code`; `static_cast` instead of C casts; typed hashdecls for results; enums where appropriate | Pass | The helper accepts string and returns bool. Existing typed URI parts and exception hashes are retained; tests use typed callbacks. The dependency correction is C, uses the parsed scheme and a const pointer into the validated normalized value. |
| 57 | **Performance**: No O(n²) where O(n) is possible; no unnecessary copies; coordinate descent uses incremental residuals not full matrix multiply | Pass | Classification uses the existing linear URI mapping and component access. The datatype check scans the parsed scheme once, adds no allocations and retains stored lexical identity. There is no recursive or quadratic traversal. |
| 58 | **Error handling**: All inputs validated (dimensions, empty data, unfitted models); C++ I/O handles EAGAIN/EINTR if applicable | Pass | The 33-document WSDL matrix covers omitted/empty/whitespace/relative/absolute/opaque/Unicode-escaped namespace and operation values. Sixteen additional datatype boundaries cover valid/invalid bare schemes and fragments, native/schema/provider conversion and recovery. CMake rejects unexpected source hashes and missing/duplicate target sources. |
| 59 | **Documentation**: Doxygen `@param`, `@return`, `@throw` on all public methods; `@par Example` with realistic business scenarios; `@note` for important caveats | Pass | Release notes and durable WSDL grammar, HTTP, datatype and dependency design documents explain as-implemented rules with examples. No public signature or DGC architecture changes; no design/dgc.md exists in this repository. Standards and reproduction evidence is committed. |
| 60 | **QPP flags**: `[flags=CONSTANT]` on methods that never throw; `[flags=RET_VALUE_ONLY]` on methods that throw but have no side effects | N/A | No QPP declaration changed; normalize_xsd_uri retains RET_VALUE_ONLY and its documented errors. |
| 61 | **Security**: No user-controlled format strings; no buffer overflows; bounds checking on array indices; no credentials in code | Pass | The native pointer is bounded by the successfully parsed scheme prefix in the normalized source. All error paths retain ownership. Input URIs are classified without retrieval; no user-controlled format strings or credentials are added. HTTP tests use loopback and bounded completion. |
| 62 | **Correctness**: Algorithms verified against reference implementations; edge cases tested (empty data, single sample, all-zero features) | Pass | All 30 affected Qore suites pass (1254 cases / 25660 reported assertions), including 35 URI constraint cases / 754 assertions and 21 HTTP calls. Pinned Xerces checks all 33 WSDL and 89 datatype documents; six peer tests, 12 CMake tests, docs/parser and all 16 corpus gates pass. Native and configured-Qore Valgrinds are clean; six semantic reports match P6-34. |

All 62 checks: **22 Pass / 40 N/A / zero Fail**.
