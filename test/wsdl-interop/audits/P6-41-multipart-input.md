# P6-41 multipart input audit

Copyright (C) 2026 Qore Technologies, s.r.o.

Applied `/home/david/.codex/skills/audit-changes/SKILL.md` to the working-tree production modules, tests, fixtures and documentation. Every checklist item is recorded. All upstream-dependent regressions now pass with installed Qore 79348ce8c / Mime 1.9. The review also restored an unintended URI-space regex edit and removed redundant root decoding for native handler calls; affected URI and retained XML tests pass. No main Qore changes were made by this increment.

| # | Check | Status | Evidence |
| --- | --- | --- | --- |
| 1 | Entry exists in `doxygen/lang/120_modules.dox.tmpl` (for modules in the Qore repo; N/A for external module repos) | N/A | No new module, CMake registration, separated module file or layout change. |
| 2 | Entry exists in `doxygen/lang/900_release_notes.dox.tmpl` (for modules in the Qore repo; external modules have release notes in their .qm) | N/A | No new module, CMake registration, separated module file or layout change. |
| 3 | `qore_user_module()` or `qore_external_user_module()` call in `CMakeLists.txt` | N/A | No new module, CMake registration, separated module file or layout change. |
| 4 | Module added to QMOD list in `CMakeLists.txt` | N/A | No new module, CMake registration, separated module file or layout change. |
| 5 | `.qm` file has `@section <lowercasemodname>intro` as first doc section — **must be all lowercase** (e.g., `avrodataproviderintro`, not `AvroDataProviderintro`) | Pass | Existing WSDL, SoapClient and SoapHandler introduction sections remain lowercase. |
| 6 | `%modern` in `.qm` file — no redundant `%new-style`, `%require-types`, `%strict-args`, `%enable-all-warnings` | Pass | All changed modules retain %modern; no redundant parse directives. |
| 7 | No parse directives (`%requires`, `%modern`, `%new-style`) in separated `.qc` files (check OUTSIDE of `@code` blocks only) | N/A | No new module, CMake registration, separated module file or layout change. |
| 8 | No `%include` usage (deprecated for modules) | Pass | No %include introduced. |
| 9 | Copyright 2026 on all new files | Pass | New Qore, Python, JSON and Markdown files have copyright 2026; module notices already include 2026. |
| 10 | Directory layout: `.qm` inside `qlib/<ModuleName>/` directory (not at `qlib/<ModuleName>.qm` for multi-file modules) | N/A | No new module, CMake registration, separated module file or layout change. |
| 11 | No second `.qm` for the same module at `qlib/<ModuleName>.qm` | N/A | No new module, CMake registration, separated module file or layout change. |
| 12 | `ns=Qore::XX` matches the QoreNamespace constructor path | N/A | No C++/QPP changes; native sandboxing and cancellation checks do not apply. |
| 13 | `%modern` directive present | Pass | Both the qtest and peer.qr use %modern. |
| 14 | Executable permission set (`chmod +x`) | Pass | qtest, peer.qr and Python runner are executable. |
| 15 | Uses %prepend-module-path  before %requires for in-repo modules (Qore and Qore modules only; not Qorus) | Pass | Both Qore test entry points prepend the local qlib before relative module requirements; execution selects build-debug. |
| 16 | External module dependencies use `%try-module` — except modules delivered with the project itself (Qore ex: DataProvider, ConnectionProvider, QUnit, etc.) which use hard `%requires` | Pass | The external json dependency uses %try-module; repository/core dependencies remain hard requirements. |
| 17 | No filesystem operations (fopen, open, creat, unlink, remove, rename, mkdir, rmdir, stat, chmod) without sandbox checks | N/A | No C++/QPP changes; native sandboxing and cancellation checks do not apply. |
| 18 | No network operations (connect, bind, socket, getaddrinfo, gethostbyname) without sandbox checks | N/A | No C++/QPP changes; native sandboxing and cancellation checks do not apply. |
| 19 | If filesystem/network ops exist, verify `QoreSandboxManagerHelper` usage | N/A | No C++/QPP changes; native sandboxing and cancellation checks do not apply. |
| 20 | No `File::`, `Dir::`, `Socket::`, `HTTPClient::` usage without justification | Pass | Production uses existing Mime/resource helpers and adds no I/O primitive. Tests use fixture files, loopback listeners, readiness events, bounded process completion and deterministic teardown. |
| 21 | All `for`/`while` loops that could iterate >100 times have `qore_check_cancel()` checks | N/A | No C++/QPP changes; native sandboxing and cancellation checks do not apply. |
| 22 | Uses `qore_check_cancel()` (NOT deprecated `qore_check_io_interrupt()`) | N/A | No C++/QPP changes; native sandboxing and cancellation checks do not apply. |
| 23 | Check frequency: every 100 iterations for tight loops, every 10 for expensive iterations | N/A | No C++/QPP changes; native sandboxing and cancellation checks do not apply. |
| 24 | No blocking operations without cancellation support | N/A | No C++/QPP changes; native sandboxing and cancellation checks do not apply. |
| 25 | Every action has `display_name`, `short_desc` (plain text, <80 chars), `desc` (markdown) | N/A | No DataProvider registration/type implementation, JNI dependency or installation changes. |
| 26 | Every action has `options` populated via `getActionOptionFromFields()` — without this, the action shows an empty, unusable form | N/A | No DataProvider registration/type implementation, JNI dependency or installation changes. |
| 27 | Every action has `output_type` set to a typed data type constant (e.g., `MyResponseDataType`) — not omitted | N/A | No DataProvider registration/type implementation, JNI dependency or installation changes. |
| 28 | DPAT_API actions: provider has `"supports_request": True` and implements `doRequestImpl()` | N/A | No DataProvider registration/type implementation, JNI dependency or installation changes. |
| 29 | DPAT_FIND actions: every option exists in `SearchOptions`, `getRecordTypeImpl()` returns `*hash<string, AbstractDataField>` | N/A | No DataProvider registration/type implementation, JNI dependency or installation changes. |
| 30 | Scheme-based apps (with `"scheme"` in registerApp): actions use `"path"` and do NOT use `"cls"` — having both `scheme` and `cls` causes a runtime error | N/A | No DataProvider registration/type implementation, JNI dependency or installation changes. |
| 31 | Single-key hash slices use trailing comma: `Fields{"key",}` (without trailing comma, `Fields{"key"}` returns the value, not a hash) | N/A | No DataProvider registration/type implementation, JNI dependency or installation changes. |
| 32 | **Typed data type classes exist** for request and response types — inherit `HashDataType`, have `const Fields` hash, call `addQoreFields(Fields)` in constructor, export public constant at bottom (e.g., `public const MyDataType = new MyDataType();`) | N/A | No DataProvider registration/type implementation, JNI dependency or installation changes. |
| 33 | Request/input types use `public` Fields (enables `ClassName::Fields` in action registration) | N/A | No DataProvider registration/type implementation, JNI dependency or installation changes. |
| 34 | Response/output types use `private` Fields | N/A | No DataProvider registration/type implementation, JNI dependency or installation changes. |
| 35 | Each field in data types has `display_name`, `type`, and `desc` (markdown-formatted) | N/A | No DataProvider registration/type implementation, JNI dependency or installation changes. |
| 36 | Input fields have `example_value` where useful (string fields, endpoint URIs, SQL queries, etc.) | N/A | No DataProvider registration/type implementation, JNI dependency or installation changes. |
| 37 | Fields with finite allowed values use `allowed_values` with `AllowedValueInfo` containing both `value` and `display_name` (Title Case, human-readable) — never bare values, never described only in text | N/A | No DataProvider registration/type implementation, JNI dependency or installation changes. |
| 38 | Password/secret fields have `"sensitive": True` | N/A | No DataProvider registration/type implementation, JNI dependency or installation changes. |
| 39 | `groups` uses `AppGroup` enum values from `qlib/DataProvider/AppGroup.qc` | N/A | No DataProvider registration/type implementation, JNI dependency or installation changes. |
| 40 | App `logo` stored as separate file, loaded at module level in `Priv` namespace | N/A | No DataProvider registration/type implementation, JNI dependency or installation changes. |
| 41 | App `desc` uses markdown: bullet list of capabilities, links to project website, business-language explanation of value | N/A | No DataProvider registration/type implementation, JNI dependency or installation changes. |
| 42 | `display_name` is user-friendly ("Apache Avro" not "avro") | N/A | No DataProvider registration/type implementation, JNI dependency or installation changes. |
| 43 | `short_desc` is plain text, under 80 chars, single sentence — no markdown | N/A | No DataProvider registration/type implementation, JNI dependency or installation changes. |
| 44 | `desc` uses markdown: backticks for code/field refs (`` `field_name` ``, `` `True` ``, `` `pdf` ``), `\n\n` for paragraphs, `- ` bullet lists for enumerations, `**bold**` for caveats | N/A | No DataProvider registration/type implementation, JNI dependency or installation changes. |
| 45 | Descriptions use plain business language relating to common challenges — not just technical "what" but "why" and "when to use" | N/A | No DataProvider registration/type implementation, JNI dependency or installation changes. |
| 46 | No bare `True`/`False`/`NOTHING` — must be backtick-wrapped in `desc` | N/A | No DataProvider registration/type implementation, JNI dependency or installation changes. |
| 47 | No bare field/option names in prose — must use backticks | N/A | No DataProvider registration/type implementation, JNI dependency or installation changes. |
| 48 | Long descriptions (>500 chars) use bold section headers and bullet lists | N/A | No DataProvider registration/type implementation, JNI dependency or installation changes. |
| 49 | **Factory registration in Qore repo**: every factory name registered in `qlib/DataProvider/DataProvider.qc` → `FactoryMap` (without this, module loads but doesn't appear in Qorus apps) | N/A | No DataProvider registration/type implementation, JNI dependency or installation changes. |
| 50 | **`getRecordTypeImpl()` signature**: must be `private *hash<string, AbstractDataField> getRecordTypeImpl(*hash<auto> search_options)` — NOT returning `*AbstractDataProviderType` | N/A | No DataProvider registration/type implementation, JNI dependency or installation changes. |
| 51 | **Dependency JARs committed** (for JNI modules): JAR files in `qlib/*/jar/` may be gitignored — use `git add -f` to ensure they're tracked, otherwise CI compilation fails | N/A | No DataProvider registration/type implementation, JNI dependency or installation changes. |
| 52 | JAR install rules in CMakeLists.txt for all dependency JARs | N/A | No DataProvider registration/type implementation, JNI dependency or installation changes. |
| 53 | **No workarounds**: No TODOs, FIXMEs, stubs, or partially-implemented features | Pass | The ad hoc reader and handler MIME regexes are removed. Framing and transfer decoding use Qore Mime 1.9. All dependency regressions are enabled and pass; no wire rewriting or fixture-specific fallback. This increment implements transport normalization; WSDL attachment-part binding remains separate P6 work. |
| 54 | **Exception safety**: C++ uses `ReferenceHolder` for Qore allocations, `std::unique_ptr` for C++ allocations, `*xsink` checked after every fallible operation | Pass | Root, part maps and normalized values remain local until complete. Exceptions publish no partial result; cancellation is rethrown. HTTP rejection/recovery and on_exit teardown are tested. No C++ allocation changes. |
| 55 | **Thread safety**: All mutable shared state protected by `std::lock_guard<std::mutex>` or documented as immutable-after-construction | Pass | All new production state is call-local; shared WebService descriptors are not changed. Header inspection modifies local parsed values only. |
| 56 | **Type safety**: Strongly-typed `code<return(args)>` instead of untyped `code`; `static_cast` instead of C casts; typed hashdecls for results; enums where appropriate | Pass | Core hash<Mime::MessageInfo>, typed part maps/lists and typed callbacks. Root bytes are decoded before retained XML string APIs. All Qore debug suites and astparser pass. |
| 57 | **Performance**: No O(n²) where O(n) is possible; no unnecessary copies; coordinate descent uses incremental residuals not full matrix multiply | Pass | One pass over MIME parts with hash-based identifier lookup and linear header checks. Framing remains in Qore. Native handler calls do not decode a second root string; retained XML consumers use the common decoder. |
| 58 | **Error handling**: All inputs validated (dimensions, empty data, unfitted models); C++ I/O handles EAGAIN/EINTR if applicable | Pass | Missing/duplicate roots, duplicate singleton headers, invalid media parameters, false delimiters, malformed encodings and bad bodies reject. Shared-parser fixes are verified by all 53 reader cases / 798 assertions. HTTP malformed-request/response and recovery tests pass. |
| 59 | **Documentation**: Doxygen `@param`, `@return`, `@throw` on all public methods; `@par Example` with realistic business scenarios; `@note` for important caveats | Pass | Public decoder parameters, result, errors, example and caveat are documented. Module/repository release notes and durable multipart-input design updated; Mime 1.9 is required. No DGC behavior changes. |
| 60 | **QPP flags**: `[flags=CONSTANT]` on methods that never throw; `[flags=RET_VALUE_ONLY]` on methods that throw but have no side effects | N/A | No C++/QPP changes; native sandboxing and cancellation checks do not apply. |
| 61 | **Security**: No user-controlled format strings; no buffer overflows; bounds checking on array indices; no credentials in code | Pass | No user-controlled format strings, raw allocations, credentials or new I/O primitives. Identifier slices are bounded; duplicates are rejected after the fixed shared parser preserves all field values. HTTP tests use loopback endpoints. |
| 62 | **Correctness**: Algorithms verified against reference implementations; edge cases tested (empty data, single sample, all-zero features) | Pass | All 50 gates pass: 34 Qore suites, 1608 cases / 52945 assertions; 50 wire fixtures, 53 reader cases, 96 independent HTTP exchanges. Python independently verifies framing and bytes. All 16 corpus commands meet expected outcomes; six semantic reports differ only in runtime versions. |

All 62 checks: **18 Pass / 44 N/A / zero Fail**. Validation: [P6-41-validation.json](../P6-41-validation.json).
