# P5-16b element constraint declaration audit

Copyright (C) 2026 Qore Technologies, s.r.o.

2026-09-12, parent `1ea1370`. Applied the complete audit-changes skill and its
five referenced guides to the final WSDL changes, tests and documentation. Guide
hashes are unchanged from the full read and are recorded in the inventory.

All 137 source suites pass (1,332 cases/65,931 reported assertions), with fourteen
passing supplements, a clean documentation build and example, and zero Valgrind
errors/lost blocks on the complete new suite. The final test-only expansion was
rerun in the default runtime, four source modes, AOT and Valgrind; production and
compiled artifacts stayed unchanged. Corpus outcomes match P5-16a exactly.

The independently reproduced QName AOT stack finding remains a P9 runtime failure.
Remaining P5 instance default/fixed/nil behavior and P6–P9 requirements remain open,
as required by the execution prompt. No native or main-Qore edit was made.

See [requirement evidence](../element-constraints-evidence.md) and
[validation inventory](../P5-16b-validation.json).

| # | Check | Status | Evidence |
| --- | --- | --- | --- |
| 1 | Entry exists in `doxygen/lang/120_modules.dox.tmpl` (for modules in the Qore repo; N/A for external module repos) | N/A | Existing single-file WSDL module; no new entry point, separated file, CMake/QMOD registration or directory layout. |
| 2 | Entry exists in `doxygen/lang/900_release_notes.dox.tmpl` (for modules in the Qore repo; external modules have release notes in their .qm) | Pass | WSDL 0.5.8 release notes, README and implemented design document describe validated declaration constraints and the public metadata API. |
| 3 | `qore_user_module()` or `qore_external_user_module()` call in `CMakeLists.txt` | N/A | Existing single-file WSDL module; no new entry point, separated file, CMake/QMOD registration or directory layout. |
| 4 | Module added to QMOD list in `CMakeLists.txt` | N/A | Existing single-file WSDL module; no new entry point, separated file, CMake/QMOD registration or directory layout. |
| 5 | `.qm` file has `@section <lowercasemodname>intro` as first doc section — **must be all lowercase** (e.g., `avrodataproviderintro`, not `AvroDataProviderintro`) | Pass | Existing wsdlintro and %modern remain; no redundant parse directives or deprecated includes added. |
| 6 | `%modern` in `.qm` file — no redundant `%new-style`, `%require-types`, `%strict-args`, `%enable-all-warnings` | Pass | Existing wsdlintro and %modern remain; no redundant parse directives or deprecated includes added. |
| 7 | No parse directives (`%requires`, `%modern`, `%new-style`) in separated `.qc` files (check OUTSIDE of `@code` blocks only) | N/A | Existing single-file WSDL module; no new entry point, separated file, CMake/QMOD registration or directory layout. |
| 8 | No `%include` usage (deprecated for modules) | Pass | Existing wsdlintro and %modern remain; no redundant parse directives or deprecated includes added. |
| 9 | Copyright 2026 on all new files | Pass | New Qore/Python tests, design, evidence and audit use copyright 2026; JSON inventory has the same notice. |
| 10 | Directory layout: `.qm` inside `qlib/<ModuleName>/` directory (not at `qlib/<ModuleName>.qm` for multi-file modules) | N/A | Existing single-file WSDL module; no new entry point, separated file, CMake/QMOD registration or directory layout. |
| 11 | No second `.qm` for the same module at `qlib/<ModuleName>.qm` | N/A | Existing single-file WSDL module; no new entry point, separated file, CMake/QMOD registration or directory layout. |
| 12 | `ns=Qore::XX` matches the QoreNamespace constructor path | N/A | No C++/QPP source, native I/O, native loop or method flags changed; native XML artifact unchanged from P5-16a. |
| 13 | `%modern` directive present | Pass | Executable %modern qtest prepends local qlib before relative WSDL requires. xml is delivered by this repository; QUnit and Serializable are core dependencies. No new external Qore dependency. |
| 14 | Executable permission set (`chmod +x`) | Pass | Executable %modern qtest prepends local qlib before relative WSDL requires. xml is delivered by this repository; QUnit and Serializable are core dependencies. No new external Qore dependency. |
| 15 | Uses %prepend-module-path  before %requires for in-repo modules (Qore and Qore modules only; not Qorus) | Pass | Executable %modern qtest prepends local qlib before relative WSDL requires. xml is delivered by this repository; QUnit and Serializable are core dependencies. No new external Qore dependency. |
| 16 | External module dependencies use `%try-module` — except modules delivered with the project itself (Qore ex: DataProvider, ConnectionProvider, QUnit, etc.) which use hard `%requires` | Pass | Executable %modern qtest prepends local qlib before relative WSDL requires. xml is delivered by this repository; QUnit and Serializable are core dependencies. No new external Qore dependency. |
| 17 | No filesystem operations (fopen, open, creat, unlink, remove, rename, mkdir, rmdir, stat, chmod) without sandbox checks | N/A | No C++/QPP source, native I/O, native loop or method flags changed; native XML artifact unchanged from P5-16a. |
| 18 | No network operations (connect, bind, socket, getaddrinfo, gethostbyname) without sandbox checks | N/A | No C++/QPP source, native I/O, native loop or method flags changed; native XML artifact unchanged from P5-16a. |
| 19 | If filesystem/network ops exist, verify `QoreSandboxManagerHelper` usage | N/A | No C++/QPP source, native I/O, native loop or method flags changed; native XML artifact unchanged from P5-16a. |
| 20 | No `File::`, `Dir::`, `Socket::`, `HTTPClient::` usage without justification | Pass | Constraint conversion adds no I/O. Import recovery uses an explicit offline cache with async_only; the Python matrix uses bounded temporary fixtures and the existing pinned oracle/worker. |
| 21 | All `for`/`while` loops that could iterate >100 times have `qore_check_cancel()` checks | N/A | No C++/QPP source, native I/O, native loop or method flags changed; native XML artifact unchanged from P5-16a. |
| 22 | Uses `qore_check_cancel()` (NOT deprecated `qore_check_io_interrupt()`) | N/A | No C++/QPP source, native I/O, native loop or method flags changed; native XML artifact unchanged from P5-16a. |
| 23 | Check frequency: every 100 iterations for tight loops, every 10 for expensive iterations | N/A | No C++/QPP source, native I/O, native loop or method flags changed; native XML artifact unchanged from P5-16a. |
| 24 | No blocking operations without cancellation support | Pass | No blocking production operation. Real SandboxManager interruption and controlled cancellation categories propagate, retain metadata, restore namespace scope and permit subsequent validation. |
| 25 | Every action has `display_name`, `short_desc` (plain text, <80 chars), `desc` (markdown) | N/A | No provider/action/app registration, request/response field class, secret, FactoryMap/getRecordTypeImpl change or JNI dependency. This increment validates schema declarations. |
| 26 | Every action has `options` populated via `getActionOptionFromFields()` — without this, the action shows an empty, unusable form | N/A | No provider/action/app registration, request/response field class, secret, FactoryMap/getRecordTypeImpl change or JNI dependency. This increment validates schema declarations. |
| 27 | Every action has `output_type` set to a typed data type constant (e.g., `MyResponseDataType`) — not omitted | N/A | No provider/action/app registration, request/response field class, secret, FactoryMap/getRecordTypeImpl change or JNI dependency. This increment validates schema declarations. |
| 28 | DPAT_API actions: provider has `"supports_request": True` and implements `doRequestImpl()` | N/A | No provider/action/app registration, request/response field class, secret, FactoryMap/getRecordTypeImpl change or JNI dependency. This increment validates schema declarations. |
| 29 | DPAT_FIND actions: every option exists in `SearchOptions`, `getRecordTypeImpl()` returns `*hash<string, AbstractDataField>` | N/A | No provider/action/app registration, request/response field class, secret, FactoryMap/getRecordTypeImpl change or JNI dependency. This increment validates schema declarations. |
| 30 | Scheme-based apps (with `"scheme"` in registerApp): actions use `"path"` and do NOT use `"cls"` — having both `scheme` and `cls` causes a runtime error | N/A | No provider/action/app registration, request/response field class, secret, FactoryMap/getRecordTypeImpl change or JNI dependency. This increment validates schema declarations. |
| 31 | Single-key hash slices use trailing comma: `Fields{"key",}` (without trailing comma, `Fields{"key"}` returns the value, not a hash) | N/A | No provider/action/app registration, request/response field class, secret, FactoryMap/getRecordTypeImpl change or JNI dependency. This increment validates schema declarations. |
| 32 | **Typed data type classes exist** for request and response types — inherit `HashDataType`, have `const Fields` hash, call `addQoreFields(Fields)` in constructor, export public constant at bottom (e.g., `public const MyDataType = new MyDataType();`) | N/A | No provider/action/app registration, request/response field class, secret, FactoryMap/getRecordTypeImpl change or JNI dependency. This increment validates schema declarations. |
| 33 | Request/input types use `public` Fields (enables `ClassName::Fields` in action registration) | N/A | No provider/action/app registration, request/response field class, secret, FactoryMap/getRecordTypeImpl change or JNI dependency. This increment validates schema declarations. |
| 34 | Response/output types use `private` Fields | N/A | No provider/action/app registration, request/response field class, secret, FactoryMap/getRecordTypeImpl change or JNI dependency. This increment validates schema declarations. |
| 35 | Each field in data types has `display_name`, `type`, and `desc` (markdown-formatted) | N/A | No provider/action/app registration, request/response field class, secret, FactoryMap/getRecordTypeImpl change or JNI dependency. This increment validates schema declarations. |
| 36 | Input fields have `example_value` where useful (string fields, endpoint URIs, SQL queries, etc.) | N/A | No provider/action/app registration, request/response field class, secret, FactoryMap/getRecordTypeImpl change or JNI dependency. This increment validates schema declarations. |
| 37 | Fields with finite allowed values use `allowed_values` with `AllowedValueInfo` containing both `value` and `display_name` (Title Case, human-readable) — never bare values, never described only in text | N/A | No provider/action/app registration, request/response field class, secret, FactoryMap/getRecordTypeImpl change or JNI dependency. This increment validates schema declarations. |
| 38 | Password/secret fields have `"sensitive": True` | N/A | No provider/action/app registration, request/response field class, secret, FactoryMap/getRecordTypeImpl change or JNI dependency. This increment validates schema declarations. |
| 39 | `groups` uses `AppGroup` enum values from `qlib/DataProvider/AppGroup.qc` | N/A | No provider/action/app registration, request/response field class, secret, FactoryMap/getRecordTypeImpl change or JNI dependency. This increment validates schema declarations. |
| 40 | App `logo` stored as separate file, loaded at module level in `Priv` namespace | N/A | No provider/action/app registration, request/response field class, secret, FactoryMap/getRecordTypeImpl change or JNI dependency. This increment validates schema declarations. |
| 41 | App `desc` uses markdown: bullet list of capabilities, links to project website, business-language explanation of value | N/A | No provider/action/app registration, request/response field class, secret, FactoryMap/getRecordTypeImpl change or JNI dependency. This increment validates schema declarations. |
| 42 | `display_name` is user-friendly ("Apache Avro" not "avro") | N/A | No provider/action/app registration, request/response field class, secret, FactoryMap/getRecordTypeImpl change or JNI dependency. This increment validates schema declarations. |
| 43 | `short_desc` is plain text, under 80 chars, single sentence — no markdown | N/A | No provider/action/app registration, request/response field class, secret, FactoryMap/getRecordTypeImpl change or JNI dependency. This increment validates schema declarations. |
| 44 | `desc` uses markdown: backticks for code/field refs (`` `field_name` ``, `` `True` ``, `` `pdf` ``), `\n\n` for paragraphs, `- ` bullet lists for enumerations, `**bold**` for caveats | N/A | No provider/action/app registration, request/response field class, secret, FactoryMap/getRecordTypeImpl change or JNI dependency. This increment validates schema declarations. |
| 45 | Descriptions use plain business language relating to common challenges — not just technical "what" but "why" and "when to use" | N/A | No provider/action/app registration, request/response field class, secret, FactoryMap/getRecordTypeImpl change or JNI dependency. This increment validates schema declarations. |
| 46 | No bare `True`/`False`/`NOTHING` — must be backtick-wrapped in `desc` | N/A | No provider/action/app registration, request/response field class, secret, FactoryMap/getRecordTypeImpl change or JNI dependency. This increment validates schema declarations. |
| 47 | No bare field/option names in prose — must use backticks | N/A | No provider/action/app registration, request/response field class, secret, FactoryMap/getRecordTypeImpl change or JNI dependency. This increment validates schema declarations. |
| 48 | Long descriptions (>500 chars) use bold section headers and bullet lists | N/A | No provider/action/app registration, request/response field class, secret, FactoryMap/getRecordTypeImpl change or JNI dependency. This increment validates schema declarations. |
| 49 | **Factory registration in Qore repo**: every factory name registered in `qlib/DataProvider/DataProvider.qc` → `FactoryMap` (without this, module loads but doesn't appear in Qorus apps) | N/A | No provider/action/app registration, request/response field class, secret, FactoryMap/getRecordTypeImpl change or JNI dependency. This increment validates schema declarations. |
| 50 | **`getRecordTypeImpl()` signature**: must be `private *hash<string, AbstractDataField> getRecordTypeImpl(*hash<auto> search_options)` — NOT returning `*AbstractDataProviderType` | N/A | No provider/action/app registration, request/response field class, secret, FactoryMap/getRecordTypeImpl change or JNI dependency. This increment validates schema declarations. |
| 51 | **Dependency JARs committed** (for JNI modules): JAR files in `qlib/*/jar/` may be gitignored — use `git add -f` to ensure they're tracked, otherwise CI compilation fails | N/A | No provider/action/app registration, request/response field class, secret, FactoryMap/getRecordTypeImpl change or JNI dependency. This increment validates schema declarations. |
| 52 | JAR install rules in CMakeLists.txt for all dependency JARs | N/A | No provider/action/app registration, request/response field class, secret, FactoryMap/getRecordTypeImpl change or JNI dependency. This increment validates schema declarations. |
| 53 | **No workarounds**: No TODOs, FIXMEs, stubs, or partially-implemented features | Pass | Root cause fixed in late declaration validation; no input rewriting, fixture special case, skip or validation bypass. Instance conversion is the explicitly retained next P5 increment, not claimed by this API. |
| 54 | **Exception safety**: C++ uses `ReferenceHolder` for Qore allocations, `std::unique_ptr` for C++ allocations, `*xsink` checked after every fallible operation | Pass | Scoped QName state is restored on failure; converted values publish only after success. Failed-addition rollback, controlled conversion failures, real cancellation, detached reconstruction and zero-error/zero-lost Valgrind pass. |
| 55 | **Thread safety**: All mutable shared state protected by `std::lock_guard<std::mutex>` or documented as immutable-after-construction | Pass | Constraint state is finalized during schema construction and read by consumers. The getter uses Qore copy-on-write hashes and existing immutable value objects. Revalidation is a construction operation; no concurrent mutation contract added. |
| 56 | **Type safety**: Strongly-typed `code<return(args)>` instead of untyped `code`; `static_cast` instead of C casts; typed hashdecls for results; enums where appropriate | Pass | XsdElementValueConstraintInfo has typed fixed/lexical/namespaces members and an intentionally heterogeneous typed-value member. Typed schema/type references and code<nothing()> callbacks are used. |
| 57 | **Performance**: No O(n²) where O(n) is possible; no unnecessary copies; coordinate descent uses incremental residuals not full matrix multiply | Pass | Only constrained declarations perform one datatype conversion after type finalization. Mixed emptiability reuses the existing bounded particle matcher; no repeated schema parse or quadratic scan introduced. |
| 58 | **Error handling**: All inputs validated (dimensions, empty data, unfitted models); C++ I/O handles EAGAIN/EINTR if applicable | Pass | Empty versus absent constraints, invalid lexical forms, facet bounds, ID-derived prohibition, incompatible complex content and namespace failures have exact WSDL-ERROR checks; cancellation retains its original category. |
| 59 | **Documentation**: Doxygen `@param`, `@return`, `@throw` on all public methods; `@par Example` with realistic business scenarios; `@note` for important caveats | Pass | New public hash members/methods have return/error/scope documentation as applicable; the implemented design includes an executable quantity example. qdx/Doxygen and example pass without warnings. |
| 60 | **QPP flags**: `[flags=CONSTANT]` on methods that never throw; `[flags=RET_VALUE_ONLY]` on methods that throw but have no side effects | N/A | No C++/QPP source, native I/O, native loop or method flags changed; native XML artifact unchanged from P5-16a. |
| 61 | **Security**: No user-controlled format strings; no buffer overflows; bounds checking on array indices; no credentials in code | Pass | Constant diagnostic formats; no credentials, new raw I/O, unchecked index operation or externally supplied format string. Existing datatype lexical validation remains authoritative. |
| 62 | **Correctness**: Algorithms verified against reference implementations; edge cases tested (empty data, single sample, all-zero features) | Pass | 188 independent Xerces schema decisions and 376 actual SOAP binding descriptions agree. Nine-case/153-assertion unit, four source modes, default AOT, affected consumers and the full 137-suite gate pass; corpus outcomes unchanged. |

Result: 20 Pass, 42 N/A, zero Fail. No approval exception or workaround.
