# P6-28 independent CXF binding audit

Copyright (C) 2026 Qore Technologies, s.r.o.

Applied `/home/david/.codex/skills/audit-changes/SKILL.md` to the exact production,
fixture, offline-peer, regression and documentation changes. No C++ changes;
Valgrind is not required. Upstream notice whitespace is preserved by a narrowly
scoped `.gitattributes` rule, matching the checksummed bytes in the manifest.

| # | Check | Status | Evidence |
| --- | --- | --- | --- |
| 1 | Entry exists in `doxygen/lang/120_modules.dox.tmpl` (for modules in the Qore repo; N/A for external module repos) | N/A | No new module, separated class file, registration or layout change. |
| 2 | Entry exists in `doxygen/lang/900_release_notes.dox.tmpl` (for modules in the Qore repo; external modules have release notes in their .qm) | N/A | No new module, separated class file, registration or layout change. |
| 3 | `qore_user_module()` or `qore_external_user_module()` call in `CMakeLists.txt` | N/A | No new module, separated class file, registration or layout change. |
| 4 | Module added to QMOD list in `CMakeLists.txt` | N/A | No new module, separated class file, registration or layout change. |
| 5 | `.qm` file has `@section <lowercasemodname>intro` as first doc section — **must be all lowercase** (e.g., `avrodataproviderintro`, not `AvroDataProviderintro`) | Pass | WSDL and SoapHandler retain lowercase introduction sections. |
| 6 | `%modern` in `.qm` file — no redundant `%new-style`, `%require-types`, `%strict-args`, `%enable-all-warnings` | Pass | Both existing modules retain %modern. |
| 7 | No parse directives (`%requires`, `%modern`, `%new-style`) in separated `.qc` files (check OUTSIDE of `@code` blocks only) | N/A | No new module, separated class file, registration or layout change. |
| 8 | No `%include` usage (deprecated for modules) | Pass | No %include introduced. |
| 9 | Copyright 2026 on all new files | Pass | Authored Java/Python/Qore/XML/Markdown/JSON/POM files carry copyright 2026. Checksummed upstream JARs, licenses and notices remain unmodified; the derivative retains the original Apache license and adds a 2026 modification notice. |
| 10 | Directory layout: `.qm` inside `qlib/<ModuleName>/` directory (not at `qlib/<ModuleName>.qm` for multi-file modules) | N/A | No new module, separated class file, registration or layout change. |
| 11 | No second `.qm` for the same module at `qlib/<ModuleName>.qm` | N/A | No new module, separated class file, registration or layout change. |
| 12 | `ns=Qore::XX` matches the QoreNamespace constructor path | N/A | No C++ changes; native sandboxing, cancellation and QPP checks do not apply. |
| 13 | `%modern` directive present | Pass | All three new Qore suites and the peer script use %modern. |
| 14 | Executable permission set (`chmod +x`) | Pass | New qtests, peer script and Python runner are executable. |
| 15 | Uses %prepend-module-path  before %requires for in-repo modules (Qore and Qore modules only; not Qorus) | Pass | New tests prepend local qlib before relative WSDL/SoapClient/SoapHandler requirements. |
| 16 | External module dependencies use `%try-module` — except modules delivered with the project itself (Qore ex: DataProvider, ConnectionProvider, QUnit, etc.) which use hard `%requires` | Pass | xml belongs to this repository and core dependencies are required; the external json module uses %try-module with an explicit missing-dependency error. |
| 17 | No filesystem operations (fopen, open, creat, unlink, remove, rename, mkdir, rmdir, stat, chmod) without sandbox checks | N/A | No C++ changes; native sandboxing, cancellation and QPP checks do not apply. |
| 18 | No network operations (connect, bind, socket, getaddrinfo, gethostbyname) without sandbox checks | N/A | No C++ changes; native sandboxing, cancellation and QPP checks do not apply. |
| 19 | If filesystem/network ops exist, verify `QoreSandboxManagerHelper` usage | N/A | No C++ changes; native sandboxing, cancellation and QPP checks do not apply. |
| 20 | No `File::`, `Dir::`, `Socket::`, `HTTPClient::` usage without justification | Pass | Production adds no filesystem or network primitives. Test fixtures read pinned files and use bounded loopback HTTP, owned subprocesses, readiness pipes and unconditional cleanup. |
| 21 | All `for`/`while` loops that could iterate >100 times have `qore_check_cancel()` checks | N/A | No C++ changes; native sandboxing, cancellation and QPP checks do not apply. |
| 22 | Uses `qore_check_cancel()` (NOT deprecated `qore_check_io_interrupt()`) | N/A | No C++ changes; native sandboxing, cancellation and QPP checks do not apply. |
| 23 | Check frequency: every 100 iterations for tight loops, every 10 for expensive iterations | N/A | No C++ changes; native sandboxing, cancellation and QPP checks do not apply. |
| 24 | No blocking operations without cancellation support | N/A | No C++ changes; native sandboxing, cancellation and QPP checks do not apply. |
| 25 | Every action has `display_name`, `short_desc` (plain text, <80 chars), `desc` (markdown) | N/A | No DataProvider action, app, field-catalog or factory registration changes; changes are schema value/provider internals. |
| 26 | Every action has `options` populated via `getActionOptionFromFields()` — without this, the action shows an empty, unusable form | N/A | No DataProvider action, app, field-catalog or factory registration changes; changes are schema value/provider internals. |
| 27 | Every action has `output_type` set to a typed data type constant (e.g., `MyResponseDataType`) — not omitted | N/A | No DataProvider action, app, field-catalog or factory registration changes; changes are schema value/provider internals. |
| 28 | DPAT_API actions: provider has `"supports_request": True` and implements `doRequestImpl()` | N/A | No DataProvider action, app, field-catalog or factory registration changes; changes are schema value/provider internals. |
| 29 | DPAT_FIND actions: every option exists in `SearchOptions`, `getRecordTypeImpl()` returns `*hash<string, AbstractDataField>` | N/A | No DataProvider action, app, field-catalog or factory registration changes; changes are schema value/provider internals. |
| 30 | Scheme-based apps (with `"scheme"` in registerApp): actions use `"path"` and do NOT use `"cls"` — having both `scheme` and `cls` causes a runtime error | N/A | No DataProvider action, app, field-catalog or factory registration changes; changes are schema value/provider internals. |
| 31 | Single-key hash slices use trailing comma: `Fields{"key",}` (without trailing comma, `Fields{"key"}` returns the value, not a hash) | N/A | No DataProvider action, app, field-catalog or factory registration changes; changes are schema value/provider internals. |
| 32 | **Typed data type classes exist** for request and response types — inherit `HashDataType`, have `const Fields` hash, call `addQoreFields(Fields)` in constructor, export public constant at bottom (e.g., `public const MyDataType = new MyDataType();`) | N/A | No DataProvider action, app, field-catalog or factory registration changes; changes are schema value/provider internals. |
| 33 | Request/input types use `public` Fields (enables `ClassName::Fields` in action registration) | N/A | No DataProvider action, app, field-catalog or factory registration changes; changes are schema value/provider internals. |
| 34 | Response/output types use `private` Fields | N/A | No DataProvider action, app, field-catalog or factory registration changes; changes are schema value/provider internals. |
| 35 | Each field in data types has `display_name`, `type`, and `desc` (markdown-formatted) | N/A | No DataProvider action, app, field-catalog or factory registration changes; changes are schema value/provider internals. |
| 36 | Input fields have `example_value` where useful (string fields, endpoint URIs, SQL queries, etc.) | N/A | No DataProvider action, app, field-catalog or factory registration changes; changes are schema value/provider internals. |
| 37 | Fields with finite allowed values use `allowed_values` with `AllowedValueInfo` containing both `value` and `display_name` (Title Case, human-readable) — never bare values, never described only in text | N/A | No DataProvider action, app, field-catalog or factory registration changes; changes are schema value/provider internals. |
| 38 | Password/secret fields have `"sensitive": True` | N/A | No DataProvider action, app, field-catalog or factory registration changes; changes are schema value/provider internals. |
| 39 | `groups` uses `AppGroup` enum values from `qlib/DataProvider/AppGroup.qc` | N/A | No DataProvider action, app, field-catalog or factory registration changes; changes are schema value/provider internals. |
| 40 | App `logo` stored as separate file, loaded at module level in `Priv` namespace | N/A | No DataProvider action, app, field-catalog or factory registration changes; changes are schema value/provider internals. |
| 41 | App `desc` uses markdown: bullet list of capabilities, links to project website, business-language explanation of value | N/A | No DataProvider action, app, field-catalog or factory registration changes; changes are schema value/provider internals. |
| 42 | `display_name` is user-friendly ("Apache Avro" not "avro") | N/A | No DataProvider action, app, field-catalog or factory registration changes; changes are schema value/provider internals. |
| 43 | `short_desc` is plain text, under 80 chars, single sentence — no markdown | N/A | No DataProvider action, app, field-catalog or factory registration changes; changes are schema value/provider internals. |
| 44 | `desc` uses markdown: backticks for code/field refs (`` `field_name` ``, `` `True` ``, `` `pdf` ``), `\n\n` for paragraphs, `- ` bullet lists for enumerations, `**bold**` for caveats | N/A | No DataProvider action, app, field-catalog or factory registration changes; changes are schema value/provider internals. |
| 45 | Descriptions use plain business language relating to common challenges — not just technical "what" but "why" and "when to use" | N/A | No DataProvider action, app, field-catalog or factory registration changes; changes are schema value/provider internals. |
| 46 | No bare `True`/`False`/`NOTHING` — must be backtick-wrapped in `desc` | N/A | No DataProvider action, app, field-catalog or factory registration changes; changes are schema value/provider internals. |
| 47 | No bare field/option names in prose — must use backticks | N/A | No DataProvider action, app, field-catalog or factory registration changes; changes are schema value/provider internals. |
| 48 | Long descriptions (>500 chars) use bold section headers and bullet lists | N/A | No DataProvider action, app, field-catalog or factory registration changes; changes are schema value/provider internals. |
| 49 | **Factory registration in Qore repo**: every factory name registered in `qlib/DataProvider/DataProvider.qc` → `FactoryMap` (without this, module loads but doesn't appear in Qorus apps) | N/A | No DataProvider action, app, field-catalog or factory registration changes; changes are schema value/provider internals. |
| 50 | **`getRecordTypeImpl()` signature**: must be `private *hash<string, AbstractDataField> getRecordTypeImpl(*hash<auto> search_options)` — NOT returning `*AbstractDataProviderType` | N/A | No DataProvider action, app, field-catalog or factory registration changes; changes are schema value/provider internals. |
| 51 | **Dependency JARs committed** (for JNI modules): JAR files in `qlib/*/jar/` may be gitignored — use `git add -f` to ensure they're tracked, otherwise CI compilation fails | Pass | All 55 unmodified CXF/JAXB/Jetty dependency JARs are staged despite ignore rules. SHA-256, size, Maven URLs and embedded/supplemental notices are recorded; runtime tests use only the verified offline closure. |
| 52 | JAR install rules in CMakeLists.txt for all dependency JARs | N/A | Test-only JARs are not JNI runtime dependencies and are not installed with module-xml. The standalone offline peer runner builds its classes in an owned temporary directory. |
| 53 | **No workarounds**: No TODOs, FIXMEs, stubs, or partially-implemented features | Pass | Independent CXF replay exposed incorrect empty-provider shapes, type-part empty capture, invented SOAP actions and bare dispatch failures. Fixes operate on schema/binding rules; no fixture names or message values enter production. The corrected header WSDL is a separately named, exactly tested derivative; its original remains an expected rejection. |
| 54 | **Exception safety**: C++ uses `ReferenceHolder` for Qore allocations, `std::unique_ptr` for C++ allocations, `*xsink` checked after every fallible operation | Pass | Native validation completes before empty-record normalization; namespace/identity scopes retain cleanup. Handler locks unwind on exceptions. Java clients/endpoints and buses use finally cleanup; Python reaps owned processes even on startup/call/shutdown errors, with a negative startup regression. |
| 55 | **Thread safety**: All mutable shared state protected by `std::lock_guard<std::mutex>` or documented as immutable-after-construction | Pass | Native provider graphs remain immutable after completion. Empty-route selection uses the handler read lock and existing registration maps; removal uses the existing write lock. Test callback input tables are immutable and completion uses Queue. |
| 56 | **Type safety**: Strongly-typed `code<return(args)>` instead of untyped `code`; `static_cast` instead of C casts; typed hashdecls for results; enums where appropriate | Pass | Native conversions use XsdComplexType checks/casts and typed provider objects. Routing uses OperationalBinding and a typed identity set. Qore callbacks have code<auto(hash<auto>, auto)> signatures. Java interfaces are CXF generated and all peer sources compile with --release 17 -Xlint:all -Werror. |
| 57 | **Performance**: No O(n²) where O(n) is possible; no unnecessary copies; coordinate descent uses incremental residuals not full matrix multiply | Pass | Empty-body routing scans registered aliases once and deduplicates operation/binding identities. Ordinary nonempty routing retains its existing lookup. Provider special handling applies only to fieldless complex records; normal field sample traversal is reused and validated once at the complete-message boundary. |
| 58 | **Error handling**: All inputs validated (dimensions, empty data, unfitted models); C++ I/O handles EAGAIN/EINTR if applicable | Pass | Tests cover source/saved/detached schemas and operations, empty records versus nil/absence, unknown content/attributes, optional/mandatory/soft providers, generated examples, zero-part messages, absent/empty/authored/overridden actions, scalar roots, ambiguous empty routing and service removal. Four identity fixtures now explicitly represent present empty records as {}; their 1,211-assertion Qore suite and the independent 340-stage/278-document identity gate pass. Substitution-root and wildcard-attribute tests retain their ordinary NOTHING checks and separately assert native empty records, explicit nil, mandatory/optional and saved/soft providers. Peer inventory and artifact hashes prevent missing/duplicate cases. |
| 59 | **Documentation**: Doxygen `@param`, `@return`, `@throw` on all public methods; `@par Example` with realistic business scenarios; `@note` for important caveats | Pass | The new message example method has parameter/return/error/example documentation. Module release notes, native empty-record design, component/binding design, offline peer instructions, exact derivative provenance and dependency notices document the implemented behavior. No DGC code/design changes. |
| 60 | **QPP flags**: `[flags=CONSTANT]` on methods that never throw; `[flags=RET_VALUE_ONLY]` on methods that throw but have no side effects | N/A | No C++ changes; native sandboxing, cancellation and QPP checks do not apply. |
| 61 | **Security**: No user-controlled format strings; no buffer overflows; bounds checking on array indices; no credentials in code | Pass | The independent runner loads only SHA-256-verified local dependencies and pins original CXF resource hashes. Requests use loopback listeners with bounded calls; no credentials or dynamic shell construction. Expanded XML names, string whitespace and ordered values remain significant in the independent comparator. |
| 62 | **Correctness**: Algorithms verified against reference implementations; edge cases tested (empty data, single sample, all-zero features) | Pass | All 179 Qore suites pass (1758 cases / 87379 reported assertions), with intentional negative comparator assertions distinguished. Six offline CXF tests cover 19 captured message pairs, 76 live Qore/CXF calls and 19 reference replays. All 16 corpus commands meet expected outcomes; six semantic reports are unchanged. Docs and astparser are clean. |

All 62 checks: **19 Pass / 43 N/A / zero Fail**.
