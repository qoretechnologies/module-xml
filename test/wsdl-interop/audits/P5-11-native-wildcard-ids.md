# P5-11 native wildcard ID audit

Copyright (C) 2026 Qore Technologies, s.r.o.

Audited against parent `ccc7820` on 2026-09-11 using the full audit-changes skill
and all five referenced design guides. All 62 individual checks: **19 Pass,
43 N/A, 0 Fail for these native changes**. The separate consumer Valgrind failure
in Qore core is explicitly retained in the inventory and assigned to the next
P5-12 prerequisite; this audit does not call that run clean.

The native unit, complete probe and all 30 injected allocation failures pass
Valgrind with zero errors/lost blocks. The full 127-suite gate passes 1,274 cases
and 63,311 assertions. Four execution modes, independent matrices, all 19 AOT/
consumer supplements and 40 distinct provider tests pass. Native documentation
and the shipment example pass. Both-version corpus results are unchanged.
See [evidence](../native-wildcard-ids-evidence.md),
[core finding](../p5-deserialization-cycle-finding.md) and
[inventory](../P5-11-validation.json). No pushes, installs or main-Qore edits.

| # | Check | Status | Evidence |
| --- | --- | --- | --- |
| 1 | Entry exists in `doxygen/lang/120_modules.dox.tmpl` (for modules in the Qore repo; N/A for external module repos) | N/A | No new or modified .qm/.qc module. Existing module registration and layout remain unchanged. |
| 2 | Entry exists in `doxygen/lang/900_release_notes.dox.tmpl` (for modules in the Qore repo; external modules have release notes in their .qm) | Pass | Native xml 2.3.0 release notes, README and implemented design describe wildcard ID rejection, ancestry, constraints and provider selection. |
| 3 | `qore_user_module()` or `qore_external_user_module()` call in `CMakeLists.txt` | N/A | No new or modified .qm/.qc module. Existing module registration and layout remain unchanged. |
| 4 | Module added to QMOD list in `CMakeLists.txt` | N/A | No new or modified .qm/.qc module. Existing module registration and layout remain unchanged. |
| 5 | `.qm` file has `@section <lowercasemodname>intro` as first doc section — **must be all lowercase** (e.g., `avrodataproviderintro`, not `AvroDataProviderintro`) | N/A | No new or modified .qm/.qc module. Existing module registration and layout remain unchanged. |
| 6 | `%modern` in `.qm` file — no redundant `%new-style`, `%require-types`, `%strict-args`, `%enable-all-warnings` | N/A | No new or modified .qm/.qc module. Existing module registration and layout remain unchanged. |
| 7 | No parse directives (`%requires`, `%modern`, `%new-style`) in separated `.qc` files (check OUTSIDE of `@code` blocks only) | N/A | No new or modified .qm/.qc module. Existing module registration and layout remain unchanged. |
| 8 | No `%include` usage (deprecated for modules) | N/A | No new or modified .qm/.qc module. Existing module registration and layout remain unchanged. |
| 9 | Copyright 2026 on all new files | Pass | All authored C/CMake/Qore/Python/design/evidence/audit files state Copyright 2026; third-party source notices remain untouched. |
| 10 | Directory layout: `.qm` inside `qlib/<ModuleName>/` directory (not at `qlib/<ModuleName>.qm` for multi-file modules) | N/A | No new or modified .qm/.qc module. Existing module registration and layout remain unchanged. |
| 11 | No second `.qm` for the same module at `qlib/<ModuleName>.qm` | N/A | No new or modified .qm/.qc module. Existing module registration and layout remain unchanged. |
| 12 | `ns=Qore::XX` matches the QoreNamespace constructor path | N/A | No QPP class/method/signature/flag changed. |
| 13 | `%modern` directive present | Pass | Native qtest, worker and embedded cancellation Program use %modern. |
| 14 | Executable permission set (`chmod +x`) | Pass | Native qtest, worker and Python integration matrix have mode 0755. |
| 15 | Uses %prepend-module-path  before %requires for in-repo modules (Qore and Qore modules only; not Qorus) | Pass | Local qlib is prepended before requires; QORE_MODULE_DIR loads the local binary XML. Source and AOT consumer paths are recorded. |
| 16 | External module dependencies use `%try-module` — except modules delivered with the project itself (Qore ex: DataProvider, ConnectionProvider, QUnit, etc.) which use hard `%requires` | Pass | Project xml and QUnit use hard requires; external json uses %try-module with a mandatory missing-dependency error. |
| 17 | No filesystem operations (fopen, open, creat, unlink, remove, rename, mkdir, rmdir, stat, chmod) without sandbox checks | Pass | The production correction adds no filesystem operations; CMake modifies only hash-checked build copies and tests use explicit local artifacts. |
| 18 | No network operations (connect, bind, socket, getaddrinfo, gethostbyname) without sandbox checks | Pass | The production correction adds no network operations. Independent schemas use bounded offline Xerces resources. |
| 19 | If filesystem/network ops exist, verify `QoreSandboxManagerHelper` usage | N/A | No new native filesystem/network calls require a sandbox manager helper. |
| 20 | No `File::`, `Dir::`, `Socket::`, `HTTPClient::` usage without justification | Pass | The Qore worker reads its explicit generated manifest; no production Qore I/O changed. Python tests use bounded subprocesses. |
| 21 | All `for`/`while` loops that could iterate >100 times have `qore_check_cancel()` checks | N/A | No C++ loop changed. Private libxml2 C has no Qore dependency; no new runtime loop or blocking operation. Probe loops are finite generated tests; existing Qore cancellation boundaries are tested. |
| 22 | Uses `qore_check_cancel()` (NOT deprecated `qore_check_io_interrupt()`) | N/A | No C++ loop changed. Private libxml2 C has no Qore dependency; no new runtime loop or blocking operation. Probe loops are finite generated tests; existing Qore cancellation boundaries are tested. |
| 23 | Check frequency: every 100 iterations for tight loops, every 10 for expensive iterations | N/A | No C++ loop changed. Private libxml2 C has no Qore dependency; no new runtime loop or blocking operation. Probe loops are finite generated tests; existing Qore cancellation boundaries are tested. |
| 24 | No blocking operations without cancellation support | Pass | No blocking native operation added. Cancellation/clear/reuse and concurrent readers pass; existing Qore entry/resource cancellation boundaries remain. |
| 25 | Every action has `display_name`, `short_desc` (plain text, <80 chars), `desc` (markdown) | N/A | No DataProvider action/app/datatype/factory registrations or JNI dependencies changed. Existing source/AOT provider consumers are tested. |
| 26 | Every action has `options` populated via `getActionOptionFromFields()` — without this, the action shows an empty, unusable form | N/A | No DataProvider action/app/datatype/factory registrations or JNI dependencies changed. Existing source/AOT provider consumers are tested. |
| 27 | Every action has `output_type` set to a typed data type constant (e.g., `MyResponseDataType`) — not omitted | N/A | No DataProvider action/app/datatype/factory registrations or JNI dependencies changed. Existing source/AOT provider consumers are tested. |
| 28 | DPAT_API actions: provider has `"supports_request": True` and implements `doRequestImpl()` | N/A | No DataProvider action/app/datatype/factory registrations or JNI dependencies changed. Existing source/AOT provider consumers are tested. |
| 29 | DPAT_FIND actions: every option exists in `SearchOptions`, `getRecordTypeImpl()` returns `*hash<string, AbstractDataField>` | N/A | No DataProvider action/app/datatype/factory registrations or JNI dependencies changed. Existing source/AOT provider consumers are tested. |
| 30 | Scheme-based apps (with `"scheme"` in registerApp): actions use `"path"` and do NOT use `"cls"` — having both `scheme` and `cls` causes a runtime error | N/A | No DataProvider action/app/datatype/factory registrations or JNI dependencies changed. Existing source/AOT provider consumers are tested. |
| 31 | Single-key hash slices use trailing comma: `Fields{"key",}` (without trailing comma, `Fields{"key"}` returns the value, not a hash) | N/A | No DataProvider action/app/datatype/factory registrations or JNI dependencies changed. Existing source/AOT provider consumers are tested. |
| 32 | **Typed data type classes exist** for request and response types — inherit `HashDataType`, have `const Fields` hash, call `addQoreFields(Fields)` in constructor, export public constant at bottom (e.g., `public const MyDataType = new MyDataType();`) | N/A | No DataProvider action/app/datatype/factory registrations or JNI dependencies changed. Existing source/AOT provider consumers are tested. |
| 33 | Request/input types use `public` Fields (enables `ClassName::Fields` in action registration) | N/A | No DataProvider action/app/datatype/factory registrations or JNI dependencies changed. Existing source/AOT provider consumers are tested. |
| 34 | Response/output types use `private` Fields | N/A | No DataProvider action/app/datatype/factory registrations or JNI dependencies changed. Existing source/AOT provider consumers are tested. |
| 35 | Each field in data types has `display_name`, `type`, and `desc` (markdown-formatted) | N/A | No DataProvider action/app/datatype/factory registrations or JNI dependencies changed. Existing source/AOT provider consumers are tested. |
| 36 | Input fields have `example_value` where useful (string fields, endpoint URIs, SQL queries, etc.) | N/A | No DataProvider action/app/datatype/factory registrations or JNI dependencies changed. Existing source/AOT provider consumers are tested. |
| 37 | Fields with finite allowed values use `allowed_values` with `AllowedValueInfo` containing both `value` and `display_name` (Title Case, human-readable) — never bare values, never described only in text | N/A | No DataProvider action/app/datatype/factory registrations or JNI dependencies changed. Existing source/AOT provider consumers are tested. |
| 38 | Password/secret fields have `"sensitive": True` | N/A | No DataProvider action/app/datatype/factory registrations or JNI dependencies changed. Existing source/AOT provider consumers are tested. |
| 39 | `groups` uses `AppGroup` enum values from `qlib/DataProvider/AppGroup.qc` | N/A | No DataProvider action/app/datatype/factory registrations or JNI dependencies changed. Existing source/AOT provider consumers are tested. |
| 40 | App `logo` stored as separate file, loaded at module level in `Priv` namespace | N/A | No DataProvider action/app/datatype/factory registrations or JNI dependencies changed. Existing source/AOT provider consumers are tested. |
| 41 | App `desc` uses markdown: bullet list of capabilities, links to project website, business-language explanation of value | N/A | No DataProvider action/app/datatype/factory registrations or JNI dependencies changed. Existing source/AOT provider consumers are tested. |
| 42 | `display_name` is user-friendly ("Apache Avro" not "avro") | N/A | No DataProvider action/app/datatype/factory registrations or JNI dependencies changed. Existing source/AOT provider consumers are tested. |
| 43 | `short_desc` is plain text, under 80 chars, single sentence — no markdown | N/A | No DataProvider action/app/datatype/factory registrations or JNI dependencies changed. Existing source/AOT provider consumers are tested. |
| 44 | `desc` uses markdown: backticks for code/field refs (`` `field_name` ``, `` `True` ``, `` `pdf` ``), `\n\n` for paragraphs, `- ` bullet lists for enumerations, `**bold**` for caveats | N/A | No DataProvider action/app/datatype/factory registrations or JNI dependencies changed. Existing source/AOT provider consumers are tested. |
| 45 | Descriptions use plain business language relating to common challenges — not just technical "what" but "why" and "when to use" | N/A | No DataProvider action/app/datatype/factory registrations or JNI dependencies changed. Existing source/AOT provider consumers are tested. |
| 46 | No bare `True`/`False`/`NOTHING` — must be backtick-wrapped in `desc` | N/A | No DataProvider action/app/datatype/factory registrations or JNI dependencies changed. Existing source/AOT provider consumers are tested. |
| 47 | No bare field/option names in prose — must use backticks | N/A | No DataProvider action/app/datatype/factory registrations or JNI dependencies changed. Existing source/AOT provider consumers are tested. |
| 48 | Long descriptions (>500 chars) use bold section headers and bullet lists | N/A | No DataProvider action/app/datatype/factory registrations or JNI dependencies changed. Existing source/AOT provider consumers are tested. |
| 49 | **Factory registration in Qore repo**: every factory name registered in `qlib/DataProvider/DataProvider.qc` → `FactoryMap` (without this, module loads but doesn't appear in Qorus apps) | N/A | No DataProvider action/app/datatype/factory registrations or JNI dependencies changed. Existing source/AOT provider consumers are tested. |
| 50 | **`getRecordTypeImpl()` signature**: must be `private *hash<string, AbstractDataField> getRecordTypeImpl(*hash<auto> search_options)` — NOT returning `*AbstractDataProviderType` | N/A | No DataProvider action/app/datatype/factory registrations or JNI dependencies changed. Existing source/AOT provider consumers are tested. |
| 51 | **Dependency JARs committed** (for JNI modules): JAR files in `qlib/*/jar/` may be gitignored — use `git add -f` to ensure they're tracked, otherwise CI compilation fails | N/A | No DataProvider action/app/datatype/factory registrations or JNI dependencies changed. Existing source/AOT provider consumers are tested. |
| 52 | JAR install rules in CMakeLists.txt for all dependency JARs | N/A | No DataProvider action/app/datatype/factory registrations or JNI dependencies changed. Existing source/AOT provider consumers are tested. |
| 53 | **No workarounds**: No TODOs, FIXMEs, stubs, or partially-implemented features | Pass | Three concrete root causes are fixed: omitted reports, wrong ancestry edge and inverted typed-constraint guard. No fixture-based production exceptions or weakened native expectations. The independent core cycle failure is explicitly routed to P5-12 with a reproducer that loads no XML. |
| 54 | **Exception safety**: C++ uses `ReferenceHolder` for Qore allocations, `std::unique_ptr` for C++ allocations, `*xsink` checked after every fallible operation | Pass | Native schema/parser/reader/value ownership is released on every test and error path. All 30 allocation faults, native unit and full probe pass Valgrind with no lost blocks. Additional consumer Valgrind failure is a reproduced independent Qore core cycle, explicitly retained as failing evidence for P5-12. |
| 55 | **Thread safety**: All mutable shared state protected by `std::lock_guard<std::mutex>` or documented as immutable-after-construction | Pass | No new mutable global production state. Ancestry reads resolved types and reports through the current validation context; computed constraints belong to their schema. Two concurrent readers and interruption recovery pass. |
| 56 | **Type safety**: Strongly-typed `code<return(args)>` instead of untyped `code`; `static_cast` instead of C casts; typed hashdecls for results; enums where appropriate | Pass | Native pointers keep existing component types. Probe iteration indices/buffers are bounded. Qore helpers have typed parameters and explicitly initialized heterogeneous result hashes; exception categories are checked. |
| 57 | **Performance**: No O(n²) where O(n) is possible; no unnecessary copies; coordinate descent uses incremental residuals not full matrix multiply | Pass | Ancestry follows one base edge per recursion and reporting reuses existing passes. Referenced constraints compute once. Fixed-size configure/test matrices have bounded subprocess deadlines; no Release benchmark claim. |
| 58 | **Error handling**: All inputs validated (dimensions, empty data, unfitted models); C++ I/O handles EAGAIN/EINTR if applicable | Pass | Required negative cases cover both wildcard clauses, absent declared uses, ID restrictions and ID schema constraints. Positive skip/namespace/list/union cases, invalid integer constraint allocation paths, reuse and exact Qore error categories pass. |
| 59 | **Documentation**: Doxygen `@param`, `@return`, `@throw` on all public methods; `@par Example` with realistic business scenarios; `@note` for important caveats | Pass | No public API added. Implemented design explains normative scope, provider detection, ownership/cancellation boundaries and validator differences; shipment example runs and native documentation builds without warnings. |
| 60 | **QPP flags**: `[flags=CONSTANT]` on methods that never throw; `[flags=RET_VALUE_ONLY]` on methods that throw but have no side effects | N/A | No QPP class/method/signature/flag changed. |
| 61 | **Security**: No user-controlled format strings; no buffer overflows; bounds checking on array indices; no credentials in code | Pass | Fixed native diagnostic/format strings; generated probe formats come only from static arrays, bounded snprintf outputs, no credentials. CMake verifies input/output hashes and leaves original sources unchanged. |
| 62 | **Correctness**: Algorithms verified against reference implementations; edge cases tested (empty data, single sample, all-zero features) | Pass | 127 suites/1274 cases/63311 assertions, four-mode native/HTTP/independent checks, 19 AOT and consumer supplements, 40 distinct provider tests and unchanged both-version corpus pass. Native cleanup is clean; the separate core-cycle Valgrind failure is retained and assigned to the next prerequisite. |
