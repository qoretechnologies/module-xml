# P6-09 shared Qore URI audit

Copyright (C) 2026 Qore Technologies, s.r.o.

Applied `/home/david/.codex/skills/audit-changes/SKILL.md` to the complete
migration and documentation repairs. Reviewed the Qore module-structure,
module-sandboxing and cooperative-cancellation design requirements. Provider
changes are comments only; provider registration/metadata checks are not applicable.
The old `doxygen/lang/interruptible-io-module-guide.md` path is absent in Qore;
the current sandboxing and cancellation design guides provide the applicable rules.

| # | Check | Status | Evidence |
| --- | --- | --- | --- |
| 1 | Entry exists in `doxygen/lang/120_modules.dox.tmpl` (for modules in the Qore repo; N/A for external module repos) | N/A | No new module, QPP class, registration or layout change. |
| 2 | Entry exists in `doxygen/lang/900_release_notes.dox.tmpl` (for modules in the Qore repo; external modules have release notes in their .qm) | N/A | No new module, QPP class, registration or layout change. |
| 3 | `qore_user_module()` or `qore_external_user_module()` call in `CMakeLists.txt` | N/A | No new module, QPP class, registration or layout change. |
| 4 | Module added to QMOD list in `CMakeLists.txt` | N/A | No new module, QPP class, registration or layout change. |
| 5 | `.qm` file has `@section <lowercasemodname>intro` as first doc section — **must be all lowercase** (e.g., `avrodataproviderintro`, not `AvroDataProviderintro`) | Pass | Existing WSDL and WebContentUtil retain lowercase wsdlintro and webcontentutilintro sections. |
| 6 | `%modern` in `.qm` file — no redundant `%new-style`, `%require-types`, `%strict-args`, `%enable-all-warnings` | Pass | Both functional modules use %modern without redundant directives. |
| 7 | No parse directives (`%requires`, `%modern`, `%new-style`) in separated `.qc` files (check OUTSIDE of `@code` blocks only) | Pass | Changed separated .qc files contain no parse directives; other module edits only repair documentation. |
| 8 | No `%include` usage (deprecated for modules) | Pass | No %include introduced. |
| 9 | Copyright 2026 on all new files | Pass | New evidence, audit and validation records carry 2026 copyright; changed authored code retains 2026 notices. |
| 10 | Directory layout: `.qm` inside `qlib/<ModuleName>/` directory (not at `qlib/<ModuleName>.qm` for multi-file modules) | N/A | No new module, QPP class, registration or layout change. |
| 11 | No second `.qm` for the same module at `qlib/<ModuleName>.qm` | N/A | No new module, QPP class, registration or layout change. |
| 12 | `ns=Qore::XX` matches the QoreNamespace constructor path | N/A | No new module, QPP class, registration or layout change. |
| 13 | `%modern` directive present | Pass | All three changed Qore tests declare %modern. |
| 14 | Executable permission set (`chmod +x`) | Pass | WebContentUtil.qtest, wsdl-location-resolution.qtest and wsdl-xml-context.qtest are executable (0755). |
| 15 | Uses %prepend-module-path  before %requires for in-repo modules (Qore and Qore modules only; not Qorus) | Pass | Changed tests prepend local qlib before relative in-repository requires; WebContentUtil now does so explicitly. |
| 16 | External module dependencies use `%try-module` — except modules delivered with the project itself (Qore ex: DataProvider, ConnectionProvider, QUnit, etc.) which use hard `%requires` | Pass | No new external binary dependency; tests use local XML/WSDL/WebContentUtil and core QUnit/HTTP modules. |
| 17 | No filesystem operations (fopen, open, creat, unlink, remove, rename, mkdir, rmdir, stat, chmod) without sandbox checks | Pass | No new native filesystem operation. Existing file policy checks and callback ownership remain intact; native resource policy tests pass. |
| 18 | No network operations (connect, bind, socket, getaddrinfo, gethostbyname) without sandbox checks | Pass | HTTP retrieval still uses QoreHttpClientObject with TLS verification, pre-encoded URLs and existing network policy enforcement. Redirect destination policy tests pass. |
| 19 | If filesystem/network ops exist, verify `QoreSandboxManagerHelper` usage | Pass | The URI function is pure; network and filesystem enforcement remain in the core I/O APIs and existing loader checks. The eight-case native resource suite includes denied access and recovery. |
| 20 | No `File::`, `Dir::`, `Socket::`, `HTTPClient::` usage without justification | Pass | No new production Qore I/O. Loopback HTTP fixtures and temporary TLS files have bounded cleanup; async tests use socket readiness and deadlines. |
| 21 | All `for`/`while` loops that could iterate >100 times have `qore_check_cancel()` checks | Pass | The native redirect replay loop is removed. qore_resolve_url performs its own cooperative cancellation checks. |
| 22 | Uses `qore_check_cancel()` (NOT deprecated `qore_check_io_interrupt()`) | Pass | Existing qore_check_cancel at resource loading entry remains; no deprecated interrupt API introduced. |
| 23 | Check frequency: every 100 iterations for tight loops, every 10 for expensive iterations | N/A | No new native loop requiring an iteration interval. |
| 24 | No blocking operations without cancellation support | Pass | No new blocking I/O; Qore HTTP retrieval retains cancellation support and the shared URI implementation is cancellable. |
| 25 | Every action has `display_name`, `short_desc` (plain text, <80 chars), `desc` (markdown) | N/A | No DataProvider metadata, factory registration, JNI dependency or packaging change; provider-file edits are documentation only. |
| 26 | Every action has `options` populated via `getActionOptionFromFields()` — without this, the action shows an empty, unusable form | N/A | No DataProvider metadata, factory registration, JNI dependency or packaging change; provider-file edits are documentation only. |
| 27 | Every action has `output_type` set to a typed data type constant (e.g., `MyResponseDataType`) — not omitted | N/A | No DataProvider metadata, factory registration, JNI dependency or packaging change; provider-file edits are documentation only. |
| 28 | DPAT_API actions: provider has `"supports_request": True` and implements `doRequestImpl()` | N/A | No DataProvider metadata, factory registration, JNI dependency or packaging change; provider-file edits are documentation only. |
| 29 | DPAT_FIND actions: every option exists in `SearchOptions`, `getRecordTypeImpl()` returns `*hash<string, AbstractDataField>` | N/A | No DataProvider metadata, factory registration, JNI dependency or packaging change; provider-file edits are documentation only. |
| 30 | Scheme-based apps (with `"scheme"` in registerApp): actions use `"path"` and do NOT use `"cls"` — having both `scheme` and `cls` causes a runtime error | N/A | No DataProvider metadata, factory registration, JNI dependency or packaging change; provider-file edits are documentation only. |
| 31 | Single-key hash slices use trailing comma: `Fields{"key",}` (without trailing comma, `Fields{"key"}` returns the value, not a hash) | N/A | No DataProvider metadata, factory registration, JNI dependency or packaging change; provider-file edits are documentation only. |
| 32 | **Typed data type classes exist** for request and response types — inherit `HashDataType`, have `const Fields` hash, call `addQoreFields(Fields)` in constructor, export public constant at bottom (e.g., `public const MyDataType = new MyDataType();`) | N/A | No DataProvider metadata, factory registration, JNI dependency or packaging change; provider-file edits are documentation only. |
| 33 | Request/input types use `public` Fields (enables `ClassName::Fields` in action registration) | N/A | No DataProvider metadata, factory registration, JNI dependency or packaging change; provider-file edits are documentation only. |
| 34 | Response/output types use `private` Fields | N/A | No DataProvider metadata, factory registration, JNI dependency or packaging change; provider-file edits are documentation only. |
| 35 | Each field in data types has `display_name`, `type`, and `desc` (markdown-formatted) | N/A | No DataProvider metadata, factory registration, JNI dependency or packaging change; provider-file edits are documentation only. |
| 36 | Input fields have `example_value` where useful (string fields, endpoint URIs, SQL queries, etc.) | N/A | No DataProvider metadata, factory registration, JNI dependency or packaging change; provider-file edits are documentation only. |
| 37 | Fields with finite allowed values use `allowed_values` with `AllowedValueInfo` containing both `value` and `display_name` (Title Case, human-readable) — never bare values, never described only in text | N/A | No DataProvider metadata, factory registration, JNI dependency or packaging change; provider-file edits are documentation only. |
| 38 | Password/secret fields have `"sensitive": True` | N/A | No DataProvider metadata, factory registration, JNI dependency or packaging change; provider-file edits are documentation only. |
| 39 | `groups` uses `AppGroup` enum values from `qlib/DataProvider/AppGroup.qc` | N/A | No DataProvider metadata, factory registration, JNI dependency or packaging change; provider-file edits are documentation only. |
| 40 | App `logo` stored as separate file, loaded at module level in `Priv` namespace | N/A | No DataProvider metadata, factory registration, JNI dependency or packaging change; provider-file edits are documentation only. |
| 41 | App `desc` uses markdown: bullet list of capabilities, links to project website, business-language explanation of value | N/A | No DataProvider metadata, factory registration, JNI dependency or packaging change; provider-file edits are documentation only. |
| 42 | `display_name` is user-friendly ("Apache Avro" not "avro") | N/A | No DataProvider metadata, factory registration, JNI dependency or packaging change; provider-file edits are documentation only. |
| 43 | `short_desc` is plain text, under 80 chars, single sentence — no markdown | N/A | No DataProvider metadata, factory registration, JNI dependency or packaging change; provider-file edits are documentation only. |
| 44 | `desc` uses markdown: backticks for code/field refs (`` `field_name` ``, `` `True` ``, `` `pdf` ``), `\n\n` for paragraphs, `- ` bullet lists for enumerations, `**bold**` for caveats | N/A | No DataProvider metadata, factory registration, JNI dependency or packaging change; provider-file edits are documentation only. |
| 45 | Descriptions use plain business language relating to common challenges — not just technical "what" but "why" and "when to use" | N/A | No DataProvider metadata, factory registration, JNI dependency or packaging change; provider-file edits are documentation only. |
| 46 | No bare `True`/`False`/`NOTHING` — must be backtick-wrapped in `desc` | N/A | No DataProvider metadata, factory registration, JNI dependency or packaging change; provider-file edits are documentation only. |
| 47 | No bare field/option names in prose — must use backticks | N/A | No DataProvider metadata, factory registration, JNI dependency or packaging change; provider-file edits are documentation only. |
| 48 | Long descriptions (>500 chars) use bold section headers and bullet lists | N/A | No DataProvider metadata, factory registration, JNI dependency or packaging change; provider-file edits are documentation only. |
| 49 | **Factory registration in Qore repo**: every factory name registered in `qlib/DataProvider/DataProvider.qc` → `FactoryMap` (without this, module loads but doesn't appear in Qorus apps) | N/A | No DataProvider metadata, factory registration, JNI dependency or packaging change; provider-file edits are documentation only. |
| 50 | **`getRecordTypeImpl()` signature**: must be `private *hash<string, AbstractDataField> getRecordTypeImpl(*hash<auto> search_options)` — NOT returning `*AbstractDataProviderType` | N/A | No DataProvider metadata, factory registration, JNI dependency or packaging change; provider-file edits are documentation only. |
| 51 | **Dependency JARs committed** (for JNI modules): JAR files in `qlib/*/jar/` may be gitignored — use `git add -f` to ensure they're tracked, otherwise CI compilation fails | N/A | No DataProvider metadata, factory registration, JNI dependency or packaging change; provider-file edits are documentation only. |
| 52 | JAR install rules in CMakeLists.txt for all dependency JARs | N/A | No DataProvider metadata, factory registration, JNI dependency or packaging change; provider-file edits are documentation only. |
| 53 | **No workarounds**: No TODOs, FIXMEs, stubs, or partially-implemented features | Pass | Duplicate merging/dot removal and redirect replay are removed. File compatibility remains an explicit API adapter. The TLS fixture owns its OpenSSL config rather than depending on a missing host config. No test deadline or assertion was relaxed. |
| 54 | **Exception safety**: C++ uses `ReferenceHolder` for Qore allocations, `std::unique_ptr` for C++ allocations, `*xsink` checked after every fallible operation | Pass | Native URI results use ReferenceHolder with the caller ExceptionSink, checked immediately after qore_resolve_url. Existing response/stream RAII owns cleanup. Qore catches translate only documented location errors and rethrow other exceptions. Both affected native tests pass Valgrind. |
| 55 | **Thread safety**: All mutable shared state protected by `std::lock_guard<std::mutex>` or documented as immutable-after-construction | Pass | Resolvers and adapters keep per-call state. No shared last-response state or cache is introduced; effective URL metadata belongs to the response hash. |
| 56 | **Type safety**: Strongly-typed `code<return(args)>` instead of untyped `code`; `static_cast` instead of C casts; typed hashdecls for results; enums where appropriate | Pass | Typed Qore signatures/exception hashes retained; native strings use QoreString and ReferenceHolder<QoreStringNode>. No untyped callbacks or casts added. |
| 57 | **Performance**: No O(n²) where O(n) is possible; no unnecessary copies; coordinate descent uses incremental residuals not full matrix multiply | Pass | Qore owns linear-time URI resolution. Native redirect replay and two Qore path algorithms are removed. The native redirect test batches ten cases to avoid repeated process startup under Valgrind. |
| 58 | **Error handling**: All inputs validated (dimensions, empty data, unfitted models); C++ I/O handles EAGAIN/EINTR if applicable | Pass | Malformed escapes/characters and invalid bases reject with documented errors; later valid use is checked. Empty delimiters, raw XML Base LEIRIs, legacy paths and fragment-free HTTP targets are tested. |
| 59 | **Documentation**: Doxygen `@param`, `@return`, `@throw` on all public methods; `@par Example` with realistic business scenarios; `@note` for important caveats | Pass | API error/return docs, requirements, release notes and three durable designs describe the implementation. Full documentation and final WSDL docs build without warnings; unresolved references discovered by the full build are fixed. |
| 60 | **QPP flags**: `[flags=CONSTANT]` on methods that never throw; `[flags=RET_VALUE_ONLY]` on methods that throw but have no side effects | N/A | No QPP methods or flags changed. |
| 61 | **Security**: No user-controlled format strings; no buffer overflows; bounds checking on array indices; no credentials in code | Pass | No new raw I/O, user-controlled format strings or credential logging. Core policies and TLS verification remain exercised; URI buffers are managed. Loopback peer checks exact request targets. |
| 62 | **Correctness**: Algorithms verified against reference implementations; edge cases tested (empty data, single sample, all-zero features) | Pass | Final 36-suite gate, XML context and WebContentUtil tests pass. Native resource/URI suites and both targeted Valgrind cases pass. Six corpus reports retain the previous semantic results; see P6-09-validation.json. |

All 62 checks: **25 Pass / 37 N/A / zero Fail**.

See [validation](../P6-09-validation.json) and [evidence](../qore-uri-evidence.md).
