# P3-29 audit: QName lexical values

Copyright (C) 2026 Qore Technologies, s.r.o.

All 62 audit-changes checks and the referenced module-structure, sandboxing,
cooperative-cancellation and DataProvider checklist/development guides were applied.
Scope: WSDL scalar/provider/example changes, two new Qore suites, the shared
boundary worker, the new independent Python matrix, current reports and docs.
No core-Qore or native source changed.

The review removed an enum check through the old string comparison path, which
could reject namespace-equivalent aliases. It also corrected optional sample
candidate insertion. Both cases have regressions. Final test runs use frozen
implementation/test inputs; the overlapping development read/save failure is
retained in evidence and superseded by the complete clean rerun.

Checklist: **20 Pass, 42 N/A, 0 Fail**. Exact final evidence and source/artifact
hashes are recorded in [EXECUTION.md](../EXECUTION.md).

| Check | Status | Evidence |
| --- | --- | --- |
| 1. Entry exists in `doxygen/lang/120_modules.dox.tmpl` (for modules in the Qore repo; N/A for external module repos) | N/A | Existing single-file external WSDL module; no new module, separated sources or registration. |
| 2. Entry exists in `doxygen/lang/900_release_notes.dox.tmpl` (for modules in the Qore repo; external modules have release notes in their .qm) | Pass | WSDL 0.5.8 release notes describe QName grammar, provider patterns and checked examples. |
| 3. `qore_user_module()` or `qore_external_user_module()` call in `CMakeLists.txt` | Pass | Existing WSDL, SoapDataProvider, SoapClient and SoapHandler qmod targets and WSDL documentation build pass; compiled tests and the design example execute. |
| 4. Module added to QMOD list in `CMakeLists.txt` | N/A | Existing single-file external WSDL module; no new module, separated sources or registration. |
| 5. `.qm` file has `@section <lowercasemodname>intro` as first doc section — **must be all lowercase** (e.g., `avrodataproviderintro`, not `AvroDataProviderintro`) | Pass | The existing first section is lowercase wsdlintro. |
| 6. `%modern` in `.qm` file — no redundant `%new-style`, `%require-types`, `%strict-args`, `%enable-all-warnings` | Pass | WSDL uses %modern; no redundant legacy parse directives were introduced. |
| 7. No parse directives (`%requires`, `%modern`, `%new-style`) in separated `.qc` files (check OUTSIDE of `@code` blocks only) | N/A | Existing single-file external WSDL module; no new module, separated sources or registration. |
| 8. No `%include` usage (deprecated for modules) | Pass | No %include was added. |
| 9. Copyright 2026 on all new files | Pass | All authored new files carry 2026 Qore Technologies copyright notices. |
| 10. Directory layout: `.qm` inside `qlib/<ModuleName>/` directory (not at `qlib/<ModuleName>.qm` for multi-file modules) | N/A | Existing single-file external WSDL module; no new module, separated sources or registration. |
| 11. No second `.qm` for the same module at `qlib/<ModuleName>.qm` | N/A | Existing single-file external WSDL module; no new module, separated sources or registration. |
| 12. `ns=Qore::XX` matches the QoreNamespace constructor path | N/A | No C++ or QPP change, native I/O or native loop. Qore owns allocations and cancellation; Valgrind is not required. |
| 13. `%modern` directive present | Pass | Both new qtests and the changed boundary worker use %modern. |
| 14. Executable permission set (`chmod +x`) | Pass | Both new qtests, the Python entry point and boundary worker are executable. |
| 15. Uses %prepend-module-path  before %requires for in-repo modules (Qore and Qore modules only; not Qorus) | Pass | The Qore tests/worker prepend local qlib before relative WSDL requirements. HTTP tests also require relative SoapClient/SoapHandler; AOT variants explicitly load built qmods. |
| 16. External module dependencies use `%try-module` — except modules delivered with the project itself (Qore ex: DataProvider, ConnectionProvider, QUnit, etc.) which use hard `%requires` | Pass | XML belongs to this repository; QUnit and HttpServer are Qore modules. The shared boundary worker retains its guarded external json dependency and explicit missing-module diagnostic. |
| 17. No filesystem operations (fopen, open, creat, unlink, remove, rename, mkdir, rmdir, stat, chmod) without sandbox checks | N/A | No C++ or QPP change, native I/O or native loop. Qore owns allocations and cancellation; Valgrind is not required. |
| 18. No network operations (connect, bind, socket, getaddrinfo, gethostbyname) without sandbox checks | N/A | No C++ or QPP change, native I/O or native loop. Qore owns allocations and cancellation; Valgrind is not required. |
| 19. If filesystem/network ops exist, verify `QoreSandboxManagerHelper` usage | N/A | No C++ or QPP change, native I/O or native loop. Qore owns allocations and cancellation; Valgrind is not required. |
| 20. No `File::`, `Dir::`, `Socket::`, `HTTPClient::` usage without justification | Pass | No production I/O added. HTTP tests use loopback, queues, 30-second request deadlines and on_exit server teardown. Python workers have bounded subprocess deadlines and TemporaryDirectory cleanup. |
| 21. All `for`/`while` loops that could iterate >100 times have `qore_check_cancel()` checks | N/A | No C++ or QPP change, native I/O or native loop. Qore owns allocations and cancellation; Valgrind is not required. |
| 22. Uses `qore_check_cancel()` (NOT deprecated `qore_check_io_interrupt()`) | N/A | No C++ or QPP change, native I/O or native loop. Qore owns allocations and cancellation; Valgrind is not required. |
| 23. Check frequency: every 100 iterations for tight loops, every 10 for expensive iterations | N/A | No C++ or QPP change, native I/O or native loop. Qore owns allocations and cancellation; Valgrind is not required. |
| 24. No blocking operations without cancellation support | N/A | No C++ or QPP change, native I/O or native loop. Qore owns allocations and cancellation; Valgrind is not required. |
| 25. Every action has `display_name`, `short_desc` (plain text, <80 chars), `desc` (markdown) | N/A | No app/action registration, static request/response catalog, finite-choice field, credential, connection or JAR change. QName providers reuse the existing dynamic scalar descriptor. |
| 26. Every action has `options` populated via `getActionOptionFromFields()` — without this, the action shows an empty, unusable form | N/A | No app/action registration, static request/response catalog, finite-choice field, credential, connection or JAR change. QName providers reuse the existing dynamic scalar descriptor. |
| 27. Every action has `output_type` set to a typed data type constant (e.g., `MyResponseDataType`) — not omitted | N/A | No app/action registration, static request/response catalog, finite-choice field, credential, connection or JAR change. QName providers reuse the existing dynamic scalar descriptor. |
| 28. DPAT_API actions: provider has `"supports_request": True` and implements `doRequestImpl()` | N/A | No app/action registration, static request/response catalog, finite-choice field, credential, connection or JAR change. QName providers reuse the existing dynamic scalar descriptor. |
| 29. DPAT_FIND actions: every option exists in `SearchOptions`, `getRecordTypeImpl()` returns `*hash<string, AbstractDataField>` | N/A | No app/action registration, static request/response catalog, finite-choice field, credential, connection or JAR change. QName providers reuse the existing dynamic scalar descriptor. |
| 30. Scheme-based apps (with `"scheme"` in registerApp): actions use `"path"` and do NOT use `"cls"` — having both `scheme` and `cls` causes a runtime error | N/A | No app/action registration, static request/response catalog, finite-choice field, credential, connection or JAR change. QName providers reuse the existing dynamic scalar descriptor. |
| 31. Single-key hash slices use trailing comma: `Fields{"key",}` (without trailing comma, `Fields{"key"}` returns the value, not a hash) | N/A | No app/action registration, static request/response catalog, finite-choice field, credential, connection or JAR change. QName providers reuse the existing dynamic scalar descriptor. |
| 32. **Typed data type classes exist** for request and response types — inherit `HashDataType`, have `const Fields` hash, call `addQoreFields(Fields)` in constructor, export public constant at bottom (e.g., `public const MyDataType = new MyDataType();`) | N/A | No app/action registration, static request/response catalog, finite-choice field, credential, connection or JAR change. QName providers reuse the existing dynamic scalar descriptor. |
| 33. Request/input types use `public` Fields (enables `ClassName::Fields` in action registration) | N/A | No app/action registration, static request/response catalog, finite-choice field, credential, connection or JAR change. QName providers reuse the existing dynamic scalar descriptor. |
| 34. Response/output types use `private` Fields | N/A | No app/action registration, static request/response catalog, finite-choice field, credential, connection or JAR change. QName providers reuse the existing dynamic scalar descriptor. |
| 35. Each field in data types has `display_name`, `type`, and `desc` (markdown-formatted) | N/A | No app/action registration, static request/response catalog, finite-choice field, credential, connection or JAR change. QName providers reuse the existing dynamic scalar descriptor. |
| 36. Input fields have `example_value` where useful (string fields, endpoint URIs, SQL queries, etc.) | N/A | No app/action registration, static request/response catalog, finite-choice field, credential, connection or JAR change. QName providers reuse the existing dynamic scalar descriptor. |
| 37. Fields with finite allowed values use `allowed_values` with `AllowedValueInfo` containing both `value` and `display_name` (Title Case, human-readable) — never bare values, never described only in text | N/A | No app/action registration, static request/response catalog, finite-choice field, credential, connection or JAR change. QName providers reuse the existing dynamic scalar descriptor. |
| 38. Password/secret fields have `"sensitive": True` | N/A | No app/action registration, static request/response catalog, finite-choice field, credential, connection or JAR change. QName providers reuse the existing dynamic scalar descriptor. |
| 39. `groups` uses `AppGroup` enum values from `qlib/DataProvider/AppGroup.qc` | N/A | No app/action registration, static request/response catalog, finite-choice field, credential, connection or JAR change. QName providers reuse the existing dynamic scalar descriptor. |
| 40. App `logo` stored as separate file, loaded at module level in `Priv` namespace | N/A | No app/action registration, static request/response catalog, finite-choice field, credential, connection or JAR change. QName providers reuse the existing dynamic scalar descriptor. |
| 41. App `desc` uses markdown: bullet list of capabilities, links to project website, business-language explanation of value | N/A | No app/action registration, static request/response catalog, finite-choice field, credential, connection or JAR change. QName providers reuse the existing dynamic scalar descriptor. |
| 42. `display_name` is user-friendly ("Apache Avro" not "avro") | N/A | No app/action registration, static request/response catalog, finite-choice field, credential, connection or JAR change. QName providers reuse the existing dynamic scalar descriptor. |
| 43. `short_desc` is plain text, under 80 chars, single sentence — no markdown | N/A | No app/action registration, static request/response catalog, finite-choice field, credential, connection or JAR change. QName providers reuse the existing dynamic scalar descriptor. |
| 44. `desc` uses markdown: backticks for code/field refs (`` `field_name` ``, `` `True` ``, `` `pdf` ``), `\n\n` for paragraphs, `- ` bullet lists for enumerations, `**bold**` for caveats | N/A | No app/action registration, static request/response catalog, finite-choice field, credential, connection or JAR change. QName providers reuse the existing dynamic scalar descriptor. |
| 45. Descriptions use plain business language relating to common challenges — not just technical "what" but "why" and "when to use" | N/A | No app/action registration, static request/response catalog, finite-choice field, credential, connection or JAR change. QName providers reuse the existing dynamic scalar descriptor. |
| 46. No bare `True`/`False`/`NOTHING` — must be backtick-wrapped in `desc` | N/A | No app/action registration, static request/response catalog, finite-choice field, credential, connection or JAR change. QName providers reuse the existing dynamic scalar descriptor. |
| 47. No bare field/option names in prose — must use backticks | N/A | No app/action registration, static request/response catalog, finite-choice field, credential, connection or JAR change. QName providers reuse the existing dynamic scalar descriptor. |
| 48. Long descriptions (>500 chars) use bold section headers and bullet lists | N/A | No app/action registration, static request/response catalog, finite-choice field, credential, connection or JAR change. QName providers reuse the existing dynamic scalar descriptor. |
| 49. **Factory registration in Qore repo**: every factory name registered in `qlib/DataProvider/DataProvider.qc` → `FactoryMap` (without this, module loads but doesn't appear in Qorus apps) | N/A | No app/action registration, static request/response catalog, finite-choice field, credential, connection or JAR change. QName providers reuse the existing dynamic scalar descriptor. |
| 50. **`getRecordTypeImpl()` signature**: must be `private *hash<string, AbstractDataField> getRecordTypeImpl(*hash<auto> search_options)` — NOT returning `*AbstractDataProviderType` | N/A | No app/action registration, static request/response catalog, finite-choice field, credential, connection or JAR change. QName providers reuse the existing dynamic scalar descriptor. |
| 51. **Dependency JARs committed** (for JNI modules): JAR files in `qlib/*/jar/` may be gitignored — use `git add -f` to ensure they're tracked, otherwise CI compilation fails | N/A | No app/action registration, static request/response catalog, finite-choice field, credential, connection or JAR change. QName providers reuse the existing dynamic scalar descriptor. |
| 52. JAR install rules in CMakeLists.txt for all dependency JARs | N/A | No app/action registration, static request/response catalog, finite-choice field, credential, connection or JAR change. QName providers reuse the existing dynamic scalar descriptor. |
| 53. **No workarounds**: No TODOs, FIXMEs, stubs, or partially-implemented features | Pass | The lexical increment contains no fixture branches, tolerance, stub or workaround. Namespace identity remains an explicit open requirement of the full plan. Generated samples validate all existing scalar restrictions and reject impossible generation. |
| 54. **Exception safety**: C++ uses `ReferenceHolder` for Qore allocations, `std::unique_ptr` for C++ allocations, `*xsink` checked after every fallible operation | Pass | Lexical validation precedes provider conversion. Existing reconstruction validates descriptor metadata before publication. Candidate state is local and absent samples are excluded. Cancellation exceptions propagate unchanged; recovery is tested. |
| 55. **Thread safety**: All mutable shared state protected by `std::lock_guard<std::mutex>` or documented as immutable-after-construction | Pass | New QName grammar is immutable constant metadata. Sample candidates and declaration checks use call-local state; no shared namespace maps or mutable global caches are changed. |
| 56. **Type safety**: Strongly-typed `code<return(args)>` instead of untyped `code`; `static_cast` instead of C casts; typed hashdecls for results; enums where appropriate | Pass | Existing XsdSizedFacetInfo carries QName lexical metadata. Typed lists, schema/provider classes and checked casts are used; auto is confined to the public heterogeneous scalar input contract. Malformed metadata rejects. |
| 57. **Performance**: No O(n²) where O(n) is possible; no unnecessary copies; coordinate descent uses incremental residuals not full matrix multiply | Pass | Anchored QName grammar is two NCName components with at most one colon, without nested ambiguous repetition. Sampling visits declared alternatives and uses the existing bounded pattern generator; deprecated lengths never allocate padding. |
| 58. **Error handling**: All inputs validated (dimensions, empty data, unfitted models); C++ I/O handles EAGAIN/EINTR if applicable | Pass | Tests cover empty/omitted values, malformed components, XML versus non-XML whitespace, 4036 character boundaries, malformed enumeration/metadata, inherited patterns, optionality, list/union fallback, cancellation and reuse with exact error categories. |
| 59. **Documentation**: Doxygen `@param`, `@return`, `@throw` on all public methods; `@par Example` with realistic business scenarios; `@note` for important caveats | Pass | The implemented design explains normalization, errors, patterns, examples and the boundary between lexical validation and namespace identity. The complete example executes against compiled WSDL. No new public methods or classes require additional API entries. |
| 60. **QPP flags**: `[flags=CONSTANT]` on methods that never throw; `[flags=RET_VALUE_ONLY]` on methods that throw but have no side effects | N/A | No C++ or QPP change, native I/O or native loop. Qore owns allocations and cancellation; Valgrind is not required. |
| 61. **Security**: No user-controlled format strings; no buffer overflows; bounds checking on array indices; no credentials in code | Pass | No credentials, native buffers or user-controlled format strings. Complete anchored validation rejects trailing text and malformed separators; namespace equivalence is not inferred by string alias comparison. |
| 62. **Correctness**: Algorithms verified against reference implementations; edge cases tested (empty data, single sample, all-zero features) | Pass | Final Qore, independent schema/value, compiled/mode/no-JIT and both-version strict corpus results are recorded in EXECUTION.md. All new checks pass; pre-existing diagnostic failures remain explicitly tracked. |

Normative and root-cause evidence: [qname-lexical-evidence.md](../qname-lexical-evidence.md).
