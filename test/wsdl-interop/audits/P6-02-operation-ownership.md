# P6-02 operation ownership audit

Copyright (C) 2026 Qore Technologies, s.r.o.

Applied `/home/david/.codex/skills/audit-changes/SKILL.md` before committing this increment.
All five guides were consulted for P6-01 and verified byte-identical for this audit:
`qore-module-structure.md`, `module-sandboxing-audit-guide.md`, `cooperative-cancellation.md`,
`data-provider-checklist.md`, and `data-provider-development-guide.md`.
No C++ changes; the affected Qore suite nevertheless runs under Valgrind because ownership is the fix.
No main-Qore mutation, install, push or delegation.

| # | Check | Status | Evidence |
| --- | --- | --- | --- |
| 1 | Entry exists in `doxygen/lang/120_modules.dox.tmpl` (for modules in the Qore repo; N/A for external module repos) | N/A | No new user module, QPP class, namespace, registration or module layout. |
| 2 | Entry exists in `doxygen/lang/900_release_notes.dox.tmpl` (for modules in the Qore repo; external modules have release notes in their .qm) | N/A | No new user module, QPP class, namespace, registration or module layout. |
| 3 | `qore_user_module()` or `qore_external_user_module()` call in `CMakeLists.txt` | N/A | No new user module, QPP class, namespace, registration or module layout. |
| 4 | Module added to QMOD list in `CMakeLists.txt` | N/A | No new user module, QPP class, namespace, registration or module layout. |
| 5 | `.qm` file has `@section <lowercasemodname>intro` as first doc section — **must be all lowercase** (e.g., `avrodataproviderintro`, not `AvroDataProviderintro`) | Pass | Existing WSDL mainpage begins with wsdlintro; no new module or registration. |
| 6 | `%modern` in `.qm` file — no redundant `%new-style`, `%require-types`, `%strict-args`, `%enable-all-warnings` | Pass | WSDL.qm retains %modern and its existing weak-reference permission for scoped borrows. |
| 7 | No parse directives (`%requires`, `%modern`, `%new-style`) in separated `.qc` files (check OUTSIDE of `@code` blocks only) | N/A | No new user module, QPP class, namespace, registration or module layout. |
| 8 | No `%include` usage (deprecated for modules) | Pass | No %include or separated module introduced. |
| 9 | Copyright 2026 on all new files | Pass | New test, authored WSDL fixture, design, evidence and audit/validation files carry 2026 copyright. |
| 10 | Directory layout: `.qm` inside `qlib/<ModuleName>/` directory (not at `qlib/<ModuleName>.qm` for multi-file modules) | N/A | No new user module, QPP class, namespace, registration or module layout. |
| 11 | No second `.qm` for the same module at `qlib/<ModuleName>.qm` | N/A | No new user module, QPP class, namespace, registration or module layout. |
| 12 | `ns=Qore::XX` matches the QoreNamespace constructor path | N/A | No new user module, QPP class, namespace, registration or module layout. |
| 13 | `%modern` directive present | Pass | New Qore test uses %modern. |
| 14 | Executable permission set (`chmod +x`) | Pass | New qtest is executable (0755). |
| 15 | Uses %prepend-module-path  before %requires for in-repo modules (Qore and Qore modules only; not Qorus) | Pass | Local qlib is prepended before relative in-repo module requires. |
| 16 | External module dependencies use `%try-module` — except modules delivered with the project itself (Qore ex: DataProvider, ConnectionProvider, QUnit, etc.) which use hard `%requires` | Pass | Core QUnit/HttpServer and this repo’s WSDL, SoapClient, SoapHandler and xml use hard requires; no new external binary dependency. |
| 17 | No filesystem operations (fopen, open, creat, unlink, remove, rename, mkdir, rmdir, stat, chmod) without sandbox checks | N/A | No native code, native I/O or C++ cancellation loop change. Qore cleanup/cancellation is covered by tracked dependency graphs and core loop cancellation points. |
| 18 | No network operations (connect, bind, socket, getaddrinfo, gethostbyname) without sandbox checks | N/A | No native code, native I/O or C++ cancellation loop change. Qore cleanup/cancellation is covered by tracked dependency graphs and core loop cancellation points. |
| 19 | If filesystem/network ops exist, verify `QoreSandboxManagerHelper` usage | N/A | No native code, native I/O or C++ cancellation loop change. Qore cleanup/cancellation is covered by tracked dependency graphs and core loop cancellation points. |
| 20 | No `File::`, `Dir::`, `Socket::`, `HTTPClient::` usage without justification | Pass | Production changes add no I/O. Tests read one committed fixture and use bounded local HttpServer/SoapClient through sandbox-aware core facilities, with on_exit cleanup. |
| 21 | All `for`/`while` loops that could iterate >100 times have `qore_check_cancel()` checks | N/A | No native code, native I/O or C++ cancellation loop change. Qore cleanup/cancellation is covered by tracked dependency graphs and core loop cancellation points. |
| 22 | Uses `qore_check_cancel()` (NOT deprecated `qore_check_io_interrupt()`) | N/A | No native code, native I/O or C++ cancellation loop change. Qore cleanup/cancellation is covered by tracked dependency graphs and core loop cancellation points. |
| 23 | Check frequency: every 100 iterations for tight loops, every 10 for expensive iterations | N/A | No native code, native I/O or C++ cancellation loop change. Qore cleanup/cancellation is covered by tracked dependency graphs and core loop cancellation points. |
| 24 | No blocking operations without cancellation support | N/A | No native code, native I/O or C++ cancellation loop change. Qore cleanup/cancellation is covered by tracked dependency graphs and core loop cancellation points. |
| 25 | Every action has `display_name`, `short_desc` (plain text, <80 chars), `desc` (markdown) | N/A | No DataProvider app/action/type metadata, factory, REST client or JNI packaging change; saved provider consumers are regression-tested. |
| 26 | Every action has `options` populated via `getActionOptionFromFields()` — without this, the action shows an empty, unusable form | N/A | No DataProvider app/action/type metadata, factory, REST client or JNI packaging change; saved provider consumers are regression-tested. |
| 27 | Every action has `output_type` set to a typed data type constant (e.g., `MyResponseDataType`) — not omitted | N/A | No DataProvider app/action/type metadata, factory, REST client or JNI packaging change; saved provider consumers are regression-tested. |
| 28 | DPAT_API actions: provider has `"supports_request": True` and implements `doRequestImpl()` | N/A | No DataProvider app/action/type metadata, factory, REST client or JNI packaging change; saved provider consumers are regression-tested. |
| 29 | DPAT_FIND actions: every option exists in `SearchOptions`, `getRecordTypeImpl()` returns `*hash<string, AbstractDataField>` | N/A | No DataProvider app/action/type metadata, factory, REST client or JNI packaging change; saved provider consumers are regression-tested. |
| 30 | Scheme-based apps (with `"scheme"` in registerApp): actions use `"path"` and do NOT use `"cls"` — having both `scheme` and `cls` causes a runtime error | N/A | No DataProvider app/action/type metadata, factory, REST client or JNI packaging change; saved provider consumers are regression-tested. |
| 31 | Single-key hash slices use trailing comma: `Fields{"key",}` (without trailing comma, `Fields{"key"}` returns the value, not a hash) | N/A | No DataProvider app/action/type metadata, factory, REST client or JNI packaging change; saved provider consumers are regression-tested. |
| 32 | **Typed data type classes exist** for request and response types — inherit `HashDataType`, have `const Fields` hash, call `addQoreFields(Fields)` in constructor, export public constant at bottom (e.g., `public const MyDataType = new MyDataType();`) | N/A | No DataProvider app/action/type metadata, factory, REST client or JNI packaging change; saved provider consumers are regression-tested. |
| 33 | Request/input types use `public` Fields (enables `ClassName::Fields` in action registration) | N/A | No DataProvider app/action/type metadata, factory, REST client or JNI packaging change; saved provider consumers are regression-tested. |
| 34 | Response/output types use `private` Fields | N/A | No DataProvider app/action/type metadata, factory, REST client or JNI packaging change; saved provider consumers are regression-tested. |
| 35 | Each field in data types has `display_name`, `type`, and `desc` (markdown-formatted) | N/A | No DataProvider app/action/type metadata, factory, REST client or JNI packaging change; saved provider consumers are regression-tested. |
| 36 | Input fields have `example_value` where useful (string fields, endpoint URIs, SQL queries, etc.) | N/A | No DataProvider app/action/type metadata, factory, REST client or JNI packaging change; saved provider consumers are regression-tested. |
| 37 | Fields with finite allowed values use `allowed_values` with `AllowedValueInfo` containing both `value` and `display_name` (Title Case, human-readable) — never bare values, never described only in text | N/A | No DataProvider app/action/type metadata, factory, REST client or JNI packaging change; saved provider consumers are regression-tested. |
| 38 | Password/secret fields have `"sensitive": True` | N/A | No DataProvider app/action/type metadata, factory, REST client or JNI packaging change; saved provider consumers are regression-tested. |
| 39 | `groups` uses `AppGroup` enum values from `qlib/DataProvider/AppGroup.qc` | N/A | No DataProvider app/action/type metadata, factory, REST client or JNI packaging change; saved provider consumers are regression-tested. |
| 40 | App `logo` stored as separate file, loaded at module level in `Priv` namespace | N/A | No DataProvider app/action/type metadata, factory, REST client or JNI packaging change; saved provider consumers are regression-tested. |
| 41 | App `desc` uses markdown: bullet list of capabilities, links to project website, business-language explanation of value | N/A | No DataProvider app/action/type metadata, factory, REST client or JNI packaging change; saved provider consumers are regression-tested. |
| 42 | `display_name` is user-friendly ("Apache Avro" not "avro") | N/A | No DataProvider app/action/type metadata, factory, REST client or JNI packaging change; saved provider consumers are regression-tested. |
| 43 | `short_desc` is plain text, under 80 chars, single sentence — no markdown | N/A | No DataProvider app/action/type metadata, factory, REST client or JNI packaging change; saved provider consumers are regression-tested. |
| 44 | `desc` uses markdown: backticks for code/field refs (`` `field_name` ``, `` `True` ``, `` `pdf` ``), `\n\n` for paragraphs, `- ` bullet lists for enumerations, `**bold**` for caveats | N/A | No DataProvider app/action/type metadata, factory, REST client or JNI packaging change; saved provider consumers are regression-tested. |
| 45 | Descriptions use plain business language relating to common challenges — not just technical "what" but "why" and "when to use" | N/A | No DataProvider app/action/type metadata, factory, REST client or JNI packaging change; saved provider consumers are regression-tested. |
| 46 | No bare `True`/`False`/`NOTHING` — must be backtick-wrapped in `desc` | N/A | No DataProvider app/action/type metadata, factory, REST client or JNI packaging change; saved provider consumers are regression-tested. |
| 47 | No bare field/option names in prose — must use backticks | N/A | No DataProvider app/action/type metadata, factory, REST client or JNI packaging change; saved provider consumers are regression-tested. |
| 48 | Long descriptions (>500 chars) use bold section headers and bullet lists | N/A | No DataProvider app/action/type metadata, factory, REST client or JNI packaging change; saved provider consumers are regression-tested. |
| 49 | **Factory registration in Qore repo**: every factory name registered in `qlib/DataProvider/DataProvider.qc` → `FactoryMap` (without this, module loads but doesn't appear in Qorus apps) | N/A | No DataProvider app/action/type metadata, factory, REST client or JNI packaging change; saved provider consumers are regression-tested. |
| 50 | **`getRecordTypeImpl()` signature**: must be `private *hash<string, AbstractDataField> getRecordTypeImpl(*hash<auto> search_options)` — NOT returning `*AbstractDataProviderType` | N/A | No DataProvider app/action/type metadata, factory, REST client or JNI packaging change; saved provider consumers are regression-tested. |
| 51 | **Dependency JARs committed** (for JNI modules): JAR files in `qlib/*/jar/` may be gitignored — use `git add -f` to ensure they're tracked, otherwise CI compilation fails | N/A | No DataProvider app/action/type metadata, factory, REST client or JNI packaging change; saved provider consumers are regression-tested. |
| 52 | JAR install rules in CMakeLists.txt for all dependency JARs | N/A | No DataProvider app/action/type metadata, factory, REST client or JNI packaging change; saved provider consumers are regression-tested. |
| 53 | **No workarounds**: No TODOs, FIXMEs, stubs, or partially-implemented features | Pass | Owning edges fix the lifetime root cause; no service back-reference workaround. Legacy _weak references are explicitly promoted. Empty maps are initialized at construction/restoration and typed QName attributes produce fresh records. No stubs or new bypasses. |
| 54 | **Exception safety**: C++ uses `ReferenceHolder` for Qore allocations, `std::unique_ptr` for C++ allocations, `*xsink` checked after every fallible operation | Pass | Tracked destructors verify namespace/type cycles and four message dependencies release exactly once after success, failed serialization, raised construction error and cancellation. Invalid restoration is followed by successful reuse. HTTP listeners and worker counters use on_exit cleanup. |
| 55 | **Thread safety**: All mutable shared state protected by `std::lock_guard<std::mutex>` or documented as immutable-after-construction | Pass | Construction precedes sharing; namespace output remains per call. Four queue-synchronized restored graphs retain distinct private namespaces and values. No new mutable global cache. |
| 56 | **Type safety**: Strongly-typed `code<return(args)>` instead of untyped `code`; `static_cast` instead of C casts; typed hashdecls for results; enums where appropriate | Pass | Dependencies retain Namespaces/WSMessage types. Required reconstructed objects are type-checked, optional message absence remains valid for one-way operations. Typed constructor attribute values no longer mutate scalar-valued hashes into records. |
| 57 | **Performance**: No O(n²) where O(n) is possible; no unnecessary copies; coordinate descent uses incremental residuals not full matrix multiply | Pass | Ownership adds references without cloning service graphs. Shared headers stay identical. QName attribute normalization is linear; no new repeated scan or unbounded generator. |
| 58 | **Error handling**: All inputs validated (dimensions, empty data, unfitted models); C++ I/O handles EAGAIN/EINTR if applicable | Pass | Missing saved dependencies reject with DESERIALIZATION-ERROR and valid retry succeeds. Recursive values, invalid strict wildcard values, invalid scalar output, declared faults, one-way/zero-part shapes and old weak graphs are exercised. |
| 59 | **Documentation**: Doxygen `@param`, `@return`, `@throw` on all public methods; `@par Example` with realistic business scenarios; `@note` for important caveats | Pass | Class/API ownership notes, legacy restoration policy, zero-part semantics, realistic caching example, durable ownership table, release notes and runnable evidence are included. WSDL docs are built and inspected. |
| 60 | **QPP flags**: `[flags=CONSTANT]` on methods that never throw; `[flags=RET_VALUE_ONLY]` on methods that throw but have no side effects | N/A | No QPP methods or flags changed. |
| 61 | **Security**: No user-controlled format strings; no buffer overflows; bounds checking on array indices; no credentials in code | Pass | Fixed format strings only; no unchecked memory or new resource access policy. Test endpoints are local ephemeral ports, queues and HTTP have bounded deadlines, and malformed saved graphs fail explicitly. |
| 62 | **Correctness**: Algorithms verified against reference implementations; edge cases tested (empty data, single sample, all-zero features) | Pass | Initial four-case baseline fails for each borrowed public handle. New tests assert values and namespaces, shared identity, exact release counts, concurrent restoration and both HTTP binding directions. Existing suites and complete corpus reports are compared to the accepted parent; Valgrind checks cleanup. |

All 62 checks: **18 Pass / 44 N/A / zero Fail**. See
[validation](../P6-02-validation.json) and [evidence](../operation-ownership-evidence.md).
