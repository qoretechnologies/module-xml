# P5-22a typed observer audit

Copyright (C) 2026 Qore Technologies, s.r.o.

Applied `/home/david/.codex/skills/audit-changes/SKILL.md` to the final harness
increment. Consulted Qore’s `design/qore-module-structure.md`,
`design/module-sandboxing-audit-guide.md`, `design/cooperative-cancellation.md`,
`design/data-provider-checklist.md` and `design/data-provider-development-guide.md`.
No main-Qore mutation, C++ change, install or push. Valgrind is not required for
this Java/Python-only increment. Existing known P6 failures are retained under
the execution prompt’s explicit later-phase exception; they are not passing tests.

| # | Check | Status | Evidence |
| --- | --- | --- | --- |
| 1 | Entry exists in `doxygen/lang/120_modules.dox.tmpl` (for modules in the Qore repo; N/A for external module repos) | N/A | No production module, QPP class, registration, namespace or build change; this increment is confined to the independent test harness. |
| 2 | Entry exists in `doxygen/lang/900_release_notes.dox.tmpl` (for modules in the Qore repo; external modules have release notes in their .qm) | N/A | No production module, QPP class, registration, namespace or build change; this increment is confined to the independent test harness. |
| 3 | `qore_user_module()` or `qore_external_user_module()` call in `CMakeLists.txt` | N/A | No production module, QPP class, registration, namespace or build change; this increment is confined to the independent test harness. |
| 4 | Module added to QMOD list in `CMakeLists.txt` | N/A | No production module, QPP class, registration, namespace or build change; this increment is confined to the independent test harness. |
| 5 | `.qm` file has `@section <lowercasemodname>intro` as first doc section — **must be all lowercase** (e.g., `avrodataproviderintro`, not `AvroDataProviderintro`) | N/A | No production module, QPP class, registration, namespace or build change; this increment is confined to the independent test harness. |
| 6 | `%modern` in `.qm` file — no redundant `%new-style`, `%require-types`, `%strict-args`, `%enable-all-warnings` | N/A | No production module, QPP class, registration, namespace or build change; this increment is confined to the independent test harness. |
| 7 | No parse directives (`%requires`, `%modern`, `%new-style`) in separated `.qc` files (check OUTSIDE of `@code` blocks only) | N/A | No production module, QPP class, registration, namespace or build change; this increment is confined to the independent test harness. |
| 8 | No `%include` usage (deprecated for modules) | N/A | No production module, QPP class, registration, namespace or build change; this increment is confined to the independent test harness. |
| 9 | Copyright 2026 on all new files | Pass | New Java/Python files carry 2026 notices; authored JSON fixture copyright is recorded in the companion evidence. |
| 10 | Directory layout: `.qm` inside `qlib/<ModuleName>/` directory (not at `qlib/<ModuleName>.qm` for multi-file modules) | N/A | No production module, QPP class, registration, namespace or build change; this increment is confined to the independent test harness. |
| 11 | No second `.qm` for the same module at `qlib/<ModuleName>.qm` | N/A | No production module, QPP class, registration, namespace or build change; this increment is confined to the independent test harness. |
| 12 | `ns=Qore::XX` matches the QoreNamespace constructor path | N/A | No production module, QPP class, registration, namespace or build change; this increment is confined to the independent test harness. |
| 13 | `%modern` directive present | N/A | Changed tests are Python; no Qore test directives or module dependencies added. |
| 14 | Executable permission set (`chmod +x`) | Pass | New test_typed_reference.py is executable (0755); the updated existing coverage test remains executable. |
| 15 | Uses %prepend-module-path  before %requires for in-repo modules (Qore and Qore modules only; not Qorus) | N/A | Changed tests are Python; no Qore test directives or module dependencies added. |
| 16 | External module dependencies use `%try-module` — except modules delivered with the project itself (Qore ex: DataProvider, ConnectionProvider, QUnit, etc.) which use hard `%requires` | N/A | Changed tests are Python; no Qore test directives or module dependencies added. |
| 17 | No filesystem operations (fopen, open, creat, unlink, remove, rename, mkdir, rmdir, stat, chmod) without sandbox checks | N/A | No C++ or Qore source changes. Standalone Java/Python harness I/O and cancellation are assessed under checks 54, 58 and 61. |
| 18 | No network operations (connect, bind, socket, getaddrinfo, gethostbyname) without sandbox checks | N/A | No C++ or Qore source changes. Standalone Java/Python harness I/O and cancellation are assessed under checks 54, 58 and 61. |
| 19 | If filesystem/network ops exist, verify `QoreSandboxManagerHelper` usage | N/A | No C++ or Qore source changes. Standalone Java/Python harness I/O and cancellation are assessed under checks 54, 58 and 61. |
| 20 | No `File::`, `Dir::`, `Socket::`, `HTTPClient::` usage without justification | N/A | No C++ or Qore source changes. Standalone Java/Python harness I/O and cancellation are assessed under checks 54, 58 and 61. |
| 21 | All `for`/`while` loops that could iterate >100 times have `qore_check_cancel()` checks | N/A | No C++ or Qore source changes. Standalone Java/Python harness I/O and cancellation are assessed under checks 54, 58 and 61. |
| 22 | Uses `qore_check_cancel()` (NOT deprecated `qore_check_io_interrupt()`) | N/A | No C++ or Qore source changes. Standalone Java/Python harness I/O and cancellation are assessed under checks 54, 58 and 61. |
| 23 | Check frequency: every 100 iterations for tight loops, every 10 for expensive iterations | N/A | No C++ or Qore source changes. Standalone Java/Python harness I/O and cancellation are assessed under checks 54, 58 and 61. |
| 24 | No blocking operations without cancellation support | N/A | No C++ or Qore source changes. Standalone Java/Python harness I/O and cancellation are assessed under checks 54, 58 and 61. |
| 25 | Every action has `display_name`, `short_desc` (plain text, <80 chars), `desc` (markdown) | N/A | No DataProvider app, action, type, presentation, factory or JNI module changes. Existing pinned test-oracle JAR is unchanged. |
| 26 | Every action has `options` populated via `getActionOptionFromFields()` — without this, the action shows an empty, unusable form | N/A | No DataProvider app, action, type, presentation, factory or JNI module changes. Existing pinned test-oracle JAR is unchanged. |
| 27 | Every action has `output_type` set to a typed data type constant (e.g., `MyResponseDataType`) — not omitted | N/A | No DataProvider app, action, type, presentation, factory or JNI module changes. Existing pinned test-oracle JAR is unchanged. |
| 28 | DPAT_API actions: provider has `"supports_request": True` and implements `doRequestImpl()` | N/A | No DataProvider app, action, type, presentation, factory or JNI module changes. Existing pinned test-oracle JAR is unchanged. |
| 29 | DPAT_FIND actions: every option exists in `SearchOptions`, `getRecordTypeImpl()` returns `*hash<string, AbstractDataField>` | N/A | No DataProvider app, action, type, presentation, factory or JNI module changes. Existing pinned test-oracle JAR is unchanged. |
| 30 | Scheme-based apps (with `"scheme"` in registerApp): actions use `"path"` and do NOT use `"cls"` — having both `scheme` and `cls` causes a runtime error | N/A | No DataProvider app, action, type, presentation, factory or JNI module changes. Existing pinned test-oracle JAR is unchanged. |
| 31 | Single-key hash slices use trailing comma: `Fields{"key",}` (without trailing comma, `Fields{"key"}` returns the value, not a hash) | N/A | No DataProvider app, action, type, presentation, factory or JNI module changes. Existing pinned test-oracle JAR is unchanged. |
| 32 | **Typed data type classes exist** for request and response types — inherit `HashDataType`, have `const Fields` hash, call `addQoreFields(Fields)` in constructor, export public constant at bottom (e.g., `public const MyDataType = new MyDataType();`) | N/A | No DataProvider app, action, type, presentation, factory or JNI module changes. Existing pinned test-oracle JAR is unchanged. |
| 33 | Request/input types use `public` Fields (enables `ClassName::Fields` in action registration) | N/A | No DataProvider app, action, type, presentation, factory or JNI module changes. Existing pinned test-oracle JAR is unchanged. |
| 34 | Response/output types use `private` Fields | N/A | No DataProvider app, action, type, presentation, factory or JNI module changes. Existing pinned test-oracle JAR is unchanged. |
| 35 | Each field in data types has `display_name`, `type`, and `desc` (markdown-formatted) | N/A | No DataProvider app, action, type, presentation, factory or JNI module changes. Existing pinned test-oracle JAR is unchanged. |
| 36 | Input fields have `example_value` where useful (string fields, endpoint URIs, SQL queries, etc.) | N/A | No DataProvider app, action, type, presentation, factory or JNI module changes. Existing pinned test-oracle JAR is unchanged. |
| 37 | Fields with finite allowed values use `allowed_values` with `AllowedValueInfo` containing both `value` and `display_name` (Title Case, human-readable) — never bare values, never described only in text | N/A | No DataProvider app, action, type, presentation, factory or JNI module changes. Existing pinned test-oracle JAR is unchanged. |
| 38 | Password/secret fields have `"sensitive": True` | N/A | No DataProvider app, action, type, presentation, factory or JNI module changes. Existing pinned test-oracle JAR is unchanged. |
| 39 | `groups` uses `AppGroup` enum values from `qlib/DataProvider/AppGroup.qc` | N/A | No DataProvider app, action, type, presentation, factory or JNI module changes. Existing pinned test-oracle JAR is unchanged. |
| 40 | App `logo` stored as separate file, loaded at module level in `Priv` namespace | N/A | No DataProvider app, action, type, presentation, factory or JNI module changes. Existing pinned test-oracle JAR is unchanged. |
| 41 | App `desc` uses markdown: bullet list of capabilities, links to project website, business-language explanation of value | N/A | No DataProvider app, action, type, presentation, factory or JNI module changes. Existing pinned test-oracle JAR is unchanged. |
| 42 | `display_name` is user-friendly ("Apache Avro" not "avro") | N/A | No DataProvider app, action, type, presentation, factory or JNI module changes. Existing pinned test-oracle JAR is unchanged. |
| 43 | `short_desc` is plain text, under 80 chars, single sentence — no markdown | N/A | No DataProvider app, action, type, presentation, factory or JNI module changes. Existing pinned test-oracle JAR is unchanged. |
| 44 | `desc` uses markdown: backticks for code/field refs (`` `field_name` ``, `` `True` ``, `` `pdf` ``), `\n\n` for paragraphs, `- ` bullet lists for enumerations, `**bold**` for caveats | N/A | No DataProvider app, action, type, presentation, factory or JNI module changes. Existing pinned test-oracle JAR is unchanged. |
| 45 | Descriptions use plain business language relating to common challenges — not just technical "what" but "why" and "when to use" | N/A | No DataProvider app, action, type, presentation, factory or JNI module changes. Existing pinned test-oracle JAR is unchanged. |
| 46 | No bare `True`/`False`/`NOTHING` — must be backtick-wrapped in `desc` | N/A | No DataProvider app, action, type, presentation, factory or JNI module changes. Existing pinned test-oracle JAR is unchanged. |
| 47 | No bare field/option names in prose — must use backticks | N/A | No DataProvider app, action, type, presentation, factory or JNI module changes. Existing pinned test-oracle JAR is unchanged. |
| 48 | Long descriptions (>500 chars) use bold section headers and bullet lists | N/A | No DataProvider app, action, type, presentation, factory or JNI module changes. Existing pinned test-oracle JAR is unchanged. |
| 49 | **Factory registration in Qore repo**: every factory name registered in `qlib/DataProvider/DataProvider.qc` → `FactoryMap` (without this, module loads but doesn't appear in Qorus apps) | N/A | No DataProvider app, action, type, presentation, factory or JNI module changes. Existing pinned test-oracle JAR is unchanged. |
| 50 | **`getRecordTypeImpl()` signature**: must be `private *hash<string, AbstractDataField> getRecordTypeImpl(*hash<auto> search_options)` — NOT returning `*AbstractDataProviderType` | N/A | No DataProvider app, action, type, presentation, factory or JNI module changes. Existing pinned test-oracle JAR is unchanged. |
| 51 | **Dependency JARs committed** (for JNI modules): JAR files in `qlib/*/jar/` may be gitignored — use `git add -f` to ensure they're tracked, otherwise CI compilation fails | N/A | No DataProvider app, action, type, presentation, factory or JNI module changes. Existing pinned test-oracle JAR is unchanged. |
| 52 | JAR install rules in CMakeLists.txt for all dependency JARs | N/A | No DataProvider app, action, type, presentation, factory or JNI module changes. Existing pinned test-oracle JAR is unchanged. |
| 53 | **No workarounds**: No TODOs, FIXMEs, stubs, or partially-implemented features | Pass | Complete standalone observer/protocol/comparator increment; no fixture-name branches, fallback passes, skips or altered source fixtures. Mandatory coverage integration remains a separately recorded task. |
| 54 | **Exception safety**: C++ uses `ReferenceHolder` for Qore allocations, `std::unique_ptr` for C++ allocations, `*xsink` checked after every fallible operation | Pass | Java owns per-document graphs; exceptions propagate. Python temporary contexts clean up on timeout, worker failure and KeyboardInterrupt, verified by deterministic injected failures. |
| 55 | **Thread safety**: All mutable shared state protected by `std::lock_guard<std::mutex>` or documented as immutable-after-construction | Pass | Each worker is a separate process; each schema observer belongs to its sequential worker. Anonymous type IDs are schema-local, and document state is fresh per call. No cross-thread mutable state. |
| 56 | **Type safety**: Strongly-typed `code<return(args)>` instead of untyped `code`; `static_cast` instead of C casts; typed hashdecls for results; enums where appropriate | Pass | Java generic collections and explicit primitive representations; Python validates exact field sets, scalar kinds, booleans, status/cardinality, namespaces and bounded trees before comparison. |
| 57 | **Performance**: No O(n²) where O(n) is possible; no unnecessary copies; coordinate descent uses incremental residuals not full matrix multiply | Pass | Single local namespace declaration storage, incremental scope undo, StringBuilder character coalescing, iterative JSON writer; depth and 8192-fragment tests pass. Sorting is limited to the requested element-only ordering contract. |
| 58 | **Error handling**: All inputs validated (dimensions, empty data, unfitted models); C++ I/O handles EAGAIN/EINTR if applicable | Pass | Malformed/missing/duplicate/reordered records and unknown values fail closed. Offline missing imports, invalid XML and DOCTYPE rejection recover for the next document. Reference-limit failures do not become production verdicts. |
| 59 | **Documentation**: Doxygen `@param`, `@return`, `@throw` on all public methods; `@par Example` with realistic business scenarios; `@note` for important caveats | Pass | Runner/predicate docstrings and evidence document contracts, exceptions, limits, schema-local identities, ordering, reference limitations and a concrete API example; no production public API changed. |
| 60 | **QPP flags**: `[flags=CONSTANT]` on methods that never throw; `[flags=RET_VALUE_ONLY]` on methods that throw but have no side effects | N/A | No QPP method or flags changed. |
| 61 | **Security**: No user-controlled format strings; no buffer overflows; bounds checking on array indices; no credentials in code | Pass | Pinned dependency verification and secure offline XML source/resolver are reused. Byte/count/heap/time/depth bounds remain explicit. No credentials, network access or user-supplied commands added. |
| 62 | **Correctness**: Algorithms verified against reference implementations; edge cases tested (empty data, single sample, all-zero features) | Pass | 57 positive/negative comparison pairs, checked 2096 native corpus pairs, protocol/cleanup/depth/fragment tests and affected reference/Qore suites. Existing P6 binding failures remain failing assertions under the plan’s explicit later-phase rule; stale IDREF count is corrected with exact family assertions. |

All 62 checks: **11 Pass / 51 N/A / zero Fail**. Exact final validation and
source provenance are recorded in [P5-22a-validation.json](../P5-22a-validation.json).
