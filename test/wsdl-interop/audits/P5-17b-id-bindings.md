# P5-17b WSDL ID bindings audit

Copyright (C) 2026 Qore Technologies, s.r.o.

Applied the full `/home/david/.codex/skills/audit-changes/SKILL.md` checklist on
2026-09-13 to the changes based on `e1c58e8`. All five previously read referenced
Qore design guides have unchanged hashes (recorded in the validation inventory).
Scope: WSDL Qore implementation, schema-provider wrapper, authored fixtures/tests,
API/release documentation and acceptance records. No C++ or build-provider change.
Unrelated `test/cmake/__pycache__/` is excluded.

See [evidence](../id-bindings-evidence.md) and [inventory](../P5-17b-validation.json).

| # | Check | Status | Evidence |
| --- | --- | --- | --- |
| 1 | Entry exists in `doxygen/lang/120_modules.dox.tmpl` (for modules in the Qore repo; N/A for external module repos) | N/A | No new module, separated source, QPP class, registration or layout. Existing WSDL remains %modern and uses no new parse directive. |
| 2 | Entry exists in `doxygen/lang/900_release_notes.dox.tmpl` (for modules in the Qore repo; external modules have release notes in their .qm) | N/A | No new module, separated source, QPP class, registration or layout. Existing WSDL remains %modern and uses no new parse directive. |
| 3 | `qore_user_module()` or `qore_external_user_module()` call in `CMakeLists.txt` | N/A | No new module, separated source, QPP class, registration or layout. Existing WSDL remains %modern and uses no new parse directive. |
| 4 | Module added to QMOD list in `CMakeLists.txt` | N/A | No new module, separated source, QPP class, registration or layout. Existing WSDL remains %modern and uses no new parse directive. |
| 5 | `.qm` file has `@section <lowercasemodname>intro` as first doc section — **must be all lowercase** (e.g., `avrodataproviderintro`, not `AvroDataProviderintro`) | N/A | No new module, separated source, QPP class, registration or layout. Existing WSDL remains %modern and uses no new parse directive. |
| 6 | `%modern` in `.qm` file — no redundant `%new-style`, `%require-types`, `%strict-args`, `%enable-all-warnings` | N/A | No new module, separated source, QPP class, registration or layout. Existing WSDL remains %modern and uses no new parse directive. |
| 7 | No parse directives (`%requires`, `%modern`, `%new-style`) in separated `.qc` files (check OUTSIDE of `@code` blocks only) | N/A | No new module, separated source, QPP class, registration or layout. Existing WSDL remains %modern and uses no new parse directive. |
| 8 | No `%include` usage (deprecated for modules) | N/A | No new module, separated source, QPP class, registration or layout. Existing WSDL remains %modern and uses no new parse directive. |
| 9 | Copyright 2026 on all new files | Pass | All new authored fixtures, scripts, tests, design and evidence carry copyright 2026. |
| 10 | Directory layout: `.qm` inside `qlib/<ModuleName>/` directory (not at `qlib/<ModuleName>.qm` for multi-file modules) | N/A | No new module, separated source, QPP class, registration or layout. Existing WSDL remains %modern and uses no new parse directive. |
| 11 | No second `.qm` for the same module at `qlib/<ModuleName>.qm` | N/A | No new module, separated source, QPP class, registration or layout. Existing WSDL remains %modern and uses no new parse directive. |
| 12 | `ns=Qore::XX` matches the QoreNamespace constructor path | N/A | No new module, separated source, QPP class, registration or layout. Existing WSDL remains %modern and uses no new parse directive. |
| 13 | `%modern` directive present | Pass | Both new Qore suites and the worker use %modern; the Python harness is an executable unittest entry point. |
| 14 | Executable permission set (`chmod +x`) | Pass | wsdl-id-bindings.qtest, wsdl-id-bindings-http.qtest, id-bindings.qr and test_id_bindings.py are mode 0755. |
| 15 | Uses %prepend-module-path  before %requires for in-repo modules (Qore and Qore modules only; not Qorus) | Pass | Tests prepend local qlib before requirements; relative requirements select local WSDL/SoapClient/SoapHandler/SoapDataProvider; QORE_MODULE_DIR selects Debug xml. |
| 16 | External module dependencies use `%try-module` — except modules delivered with the project itself (Qore ex: DataProvider, ConnectionProvider, QUnit, etc.) which use hard `%requires` | Pass | Same-project xml and Qore-supplied runtime modules use hard requirements. External json uses %try-module with an explicit dependency error. |
| 17 | No filesystem operations (fopen, open, creat, unlink, remove, rename, mkdir, rmdir, stat, chmod) without sandbox checks | N/A | No C++/QPP change or new runtime I/O entry point; Qore runtime cancellation and scope cleanup apply. |
| 18 | No network operations (connect, bind, socket, getaddrinfo, gethostbyname) without sandbox checks | N/A | No C++/QPP change or new runtime I/O entry point; Qore runtime cancellation and scope cleanup apply. |
| 19 | If filesystem/network ops exist, verify `QoreSandboxManagerHelper` usage | N/A | No C++/QPP change or new runtime I/O entry point; Qore runtime cancellation and scope cleanup apply. |
| 20 | No `File::`, `Dir::`, `Socket::`, `HTTPClient::` usage without justification | Pass | New production code adds no I/O. Tests read committed local fixtures and use bounded loopback HTTP with on_exit server shutdown; no external service or credentials. |
| 21 | All `for`/`while` loops that could iterate >100 times have `qore_check_cancel()` checks | N/A | No C++/QPP change or new runtime I/O entry point; Qore runtime cancellation and scope cleanup apply. |
| 22 | Uses `qore_check_cancel()` (NOT deprecated `qore_check_io_interrupt()`) | N/A | No C++/QPP change or new runtime I/O entry point; Qore runtime cancellation and scope cleanup apply. |
| 23 | Check frequency: every 100 iterations for tight loops, every 10 for expensive iterations | N/A | No C++/QPP change or new runtime I/O entry point; Qore runtime cancellation and scope cleanup apply. |
| 24 | No blocking operations without cancellation support | N/A | No C++/QPP change or new runtime I/O entry point; Qore runtime cancellation and scope cleanup apply. |
| 25 | Every action has `display_name`, `short_desc` (plain text, <80 chars), `desc` (markdown) | N/A | No app/action, fixed request/response DTO, field declaration, FactoryMap or JNI change. This schema-derived provider forwards existing metadata; general provider safety and behavior are reviewed in checks 54–59. |
| 26 | Every action has `options` populated via `getActionOptionFromFields()` — without this, the action shows an empty, unusable form | N/A | No app/action, fixed request/response DTO, field declaration, FactoryMap or JNI change. This schema-derived provider forwards existing metadata; general provider safety and behavior are reviewed in checks 54–59. |
| 27 | Every action has `output_type` set to a typed data type constant (e.g., `MyResponseDataType`) — not omitted | N/A | No app/action, fixed request/response DTO, field declaration, FactoryMap or JNI change. This schema-derived provider forwards existing metadata; general provider safety and behavior are reviewed in checks 54–59. |
| 28 | DPAT_API actions: provider has `"supports_request": True` and implements `doRequestImpl()` | N/A | No app/action, fixed request/response DTO, field declaration, FactoryMap or JNI change. This schema-derived provider forwards existing metadata; general provider safety and behavior are reviewed in checks 54–59. |
| 29 | DPAT_FIND actions: every option exists in `SearchOptions`, `getRecordTypeImpl()` returns `*hash<string, AbstractDataField>` | N/A | No app/action, fixed request/response DTO, field declaration, FactoryMap or JNI change. This schema-derived provider forwards existing metadata; general provider safety and behavior are reviewed in checks 54–59. |
| 30 | Scheme-based apps (with `"scheme"` in registerApp): actions use `"path"` and do NOT use `"cls"` — having both `scheme` and `cls` causes a runtime error | N/A | No app/action, fixed request/response DTO, field declaration, FactoryMap or JNI change. This schema-derived provider forwards existing metadata; general provider safety and behavior are reviewed in checks 54–59. |
| 31 | Single-key hash slices use trailing comma: `Fields{"key",}` (without trailing comma, `Fields{"key"}` returns the value, not a hash) | N/A | No app/action, fixed request/response DTO, field declaration, FactoryMap or JNI change. This schema-derived provider forwards existing metadata; general provider safety and behavior are reviewed in checks 54–59. |
| 32 | **Typed data type classes exist** for request and response types — inherit `HashDataType`, have `const Fields` hash, call `addQoreFields(Fields)` in constructor, export public constant at bottom (e.g., `public const MyDataType = new MyDataType();`) | N/A | No app/action, fixed request/response DTO, field declaration, FactoryMap or JNI change. This schema-derived provider forwards existing metadata; general provider safety and behavior are reviewed in checks 54–59. |
| 33 | Request/input types use `public` Fields (enables `ClassName::Fields` in action registration) | N/A | No app/action, fixed request/response DTO, field declaration, FactoryMap or JNI change. This schema-derived provider forwards existing metadata; general provider safety and behavior are reviewed in checks 54–59. |
| 34 | Response/output types use `private` Fields | N/A | No app/action, fixed request/response DTO, field declaration, FactoryMap or JNI change. This schema-derived provider forwards existing metadata; general provider safety and behavior are reviewed in checks 54–59. |
| 35 | Each field in data types has `display_name`, `type`, and `desc` (markdown-formatted) | N/A | No app/action, fixed request/response DTO, field declaration, FactoryMap or JNI change. This schema-derived provider forwards existing metadata; general provider safety and behavior are reviewed in checks 54–59. |
| 36 | Input fields have `example_value` where useful (string fields, endpoint URIs, SQL queries, etc.) | N/A | No app/action, fixed request/response DTO, field declaration, FactoryMap or JNI change. This schema-derived provider forwards existing metadata; general provider safety and behavior are reviewed in checks 54–59. |
| 37 | Fields with finite allowed values use `allowed_values` with `AllowedValueInfo` containing both `value` and `display_name` (Title Case, human-readable) — never bare values, never described only in text | N/A | No app/action, fixed request/response DTO, field declaration, FactoryMap or JNI change. This schema-derived provider forwards existing metadata; general provider safety and behavior are reviewed in checks 54–59. |
| 38 | Password/secret fields have `"sensitive": True` | N/A | No app/action, fixed request/response DTO, field declaration, FactoryMap or JNI change. This schema-derived provider forwards existing metadata; general provider safety and behavior are reviewed in checks 54–59. |
| 39 | `groups` uses `AppGroup` enum values from `qlib/DataProvider/AppGroup.qc` | N/A | No app/action, fixed request/response DTO, field declaration, FactoryMap or JNI change. This schema-derived provider forwards existing metadata; general provider safety and behavior are reviewed in checks 54–59. |
| 40 | App `logo` stored as separate file, loaded at module level in `Priv` namespace | N/A | No app/action, fixed request/response DTO, field declaration, FactoryMap or JNI change. This schema-derived provider forwards existing metadata; general provider safety and behavior are reviewed in checks 54–59. |
| 41 | App `desc` uses markdown: bullet list of capabilities, links to project website, business-language explanation of value | N/A | No app/action, fixed request/response DTO, field declaration, FactoryMap or JNI change. This schema-derived provider forwards existing metadata; general provider safety and behavior are reviewed in checks 54–59. |
| 42 | `display_name` is user-friendly ("Apache Avro" not "avro") | N/A | No app/action, fixed request/response DTO, field declaration, FactoryMap or JNI change. This schema-derived provider forwards existing metadata; general provider safety and behavior are reviewed in checks 54–59. |
| 43 | `short_desc` is plain text, under 80 chars, single sentence — no markdown | N/A | No app/action, fixed request/response DTO, field declaration, FactoryMap or JNI change. This schema-derived provider forwards existing metadata; general provider safety and behavior are reviewed in checks 54–59. |
| 44 | `desc` uses markdown: backticks for code/field refs (`` `field_name` ``, `` `True` ``, `` `pdf` ``), `\n\n` for paragraphs, `- ` bullet lists for enumerations, `**bold**` for caveats | N/A | No app/action, fixed request/response DTO, field declaration, FactoryMap or JNI change. This schema-derived provider forwards existing metadata; general provider safety and behavior are reviewed in checks 54–59. |
| 45 | Descriptions use plain business language relating to common challenges — not just technical "what" but "why" and "when to use" | N/A | No app/action, fixed request/response DTO, field declaration, FactoryMap or JNI change. This schema-derived provider forwards existing metadata; general provider safety and behavior are reviewed in checks 54–59. |
| 46 | No bare `True`/`False`/`NOTHING` — must be backtick-wrapped in `desc` | N/A | No app/action, fixed request/response DTO, field declaration, FactoryMap or JNI change. This schema-derived provider forwards existing metadata; general provider safety and behavior are reviewed in checks 54–59. |
| 47 | No bare field/option names in prose — must use backticks | N/A | No app/action, fixed request/response DTO, field declaration, FactoryMap or JNI change. This schema-derived provider forwards existing metadata; general provider safety and behavior are reviewed in checks 54–59. |
| 48 | Long descriptions (>500 chars) use bold section headers and bullet lists | N/A | No app/action, fixed request/response DTO, field declaration, FactoryMap or JNI change. This schema-derived provider forwards existing metadata; general provider safety and behavior are reviewed in checks 54–59. |
| 49 | **Factory registration in Qore repo**: every factory name registered in `qlib/DataProvider/DataProvider.qc` → `FactoryMap` (without this, module loads but doesn't appear in Qorus apps) | N/A | No app/action, fixed request/response DTO, field declaration, FactoryMap or JNI change. This schema-derived provider forwards existing metadata; general provider safety and behavior are reviewed in checks 54–59. |
| 50 | **`getRecordTypeImpl()` signature**: must be `private *hash<string, AbstractDataField> getRecordTypeImpl(*hash<auto> search_options)` — NOT returning `*AbstractDataProviderType` | N/A | No app/action, fixed request/response DTO, field declaration, FactoryMap or JNI change. This schema-derived provider forwards existing metadata; general provider safety and behavior are reviewed in checks 54–59. |
| 51 | **Dependency JARs committed** (for JNI modules): JAR files in `qlib/*/jar/` may be gitignored — use `git add -f` to ensure they're tracked, otherwise CI compilation fails | N/A | No app/action, fixed request/response DTO, field declaration, FactoryMap or JNI change. This schema-derived provider forwards existing metadata; general provider safety and behavior are reviewed in checks 54–59. |
| 52 | JAR install rules in CMakeLists.txt for all dependency JARs | N/A | No app/action, fixed request/response DTO, field declaration, FactoryMap or JNI change. This schema-derived provider forwards existing metadata; general provider safety and behavior are reviewed in checks 54–59. |
| 53 | **No workarounds**: No TODOs, FIXMEs, stubs, or partially-implemented features | Pass | Selected datatype capture feeds a real root binding table; no fixture-specific branch, relaxed validation or workaround. The optional-record metadata correction uses the actual base kind. Remaining defaults/keys/P6-P9 scope stays explicit. |
| 54 | **Exception safety**: C++ uses `ReferenceHolder` for Qore allocations, `std::unique_ptr` for C++ allocations, `*xsink` checked after every fallible operation | Pass | Qore c203380c4 fixes lexical release of assigned IR-only scopes; 15 original mode cases pass on installed 8c0c22c15 with immediate compilation. XML scope frames restore on success, error and simulated cancellation without workaround. No C++ change requires Valgrind in this increment. |
| 55 | **Thread safety**: All mutable shared state protected by `std::lock_guard<std::mutex>` or documented as immutable-after-construction | Pass | Per-validation bindings/counters and thread-local scopes isolate concurrent saved providers; forced AST/IR/JIT/tiered and AOT regressions pass on the corrected runtime. Schema metadata remains read-only during use. |
| 56 | **Type safety**: Strongly-typed `code<return(args)>` instead of untyped `code`; `static_cast` instead of C casts; typed hashdecls for results; enums where appropriate | Pass | Typed owner maps, frame/scope classes, XsdUnionValueIdentity fields and typed callbacks. Provider restoration verifies declaration, underlying provider and optionality. The polymorphic native contract intentionally remains auto. |
| 57 | **Performance**: No O(n²) where O(n) is possible; no unnecessary copies; coordinate descent uses incremental residuals not full matrix multiply | Pass | Hash token lookup and one root closure scan; iterative visited type traversal; no retained input trees. Existing conversion capture supplies selected union/list identities without repeating scalar conversion to identify their type. |
| 58 | **Error handling**: All inputs validated (dimensions, empty data, unfitted models); C++ I/O handles EAGAIN/EINTR if applicable | Pass | Negative tests assert serialization/deserialization/runtime categories and missing/conflicting bindings. Tests cover root/same-parent owners, forward refs, lists/unions, wildcards, defaults, dynamic types, nil, rejection recovery and converter exceptions. Owner counter overflow is checked. |
| 59 | **Documentation**: Doxygen `@param`, `@return`, `@throw` on all public methods; `@par Example` with realistic business scenarios; `@note` for important caveats | Pass | Public provider methods and identity fields are documented. Release notes, durable design, executed shipment example, reproduction commands, source hashes and limitations accompany the implementation. Affected WSDL/native API documentation is warning-free; broad unrelated documentation warnings are explicitly recorded for P9. |
| 60 | **QPP flags**: `[flags=CONSTANT]` on methods that never throw; `[flags=RET_VALUE_ONLY]` on methods that throw but have no side effects | N/A | No C++/QPP change or new runtime I/O entry point; Qore runtime cancellation and scope cleanup apply. |
| 61 | **Security**: No user-controlled format strings; no buffer overflows; bounds checking on array indices; no credentials in code | Pass | Constant diagnostic format strings, checked owner counter, no credentials or new production I/O. Fixture subprocesses have deadlines; HTTP uses loopback ephemeral ports and bounded requests. |
| 62 | **Correctness**: Algorithms verified against reference implementations; edge cases tested (empty data, single sample, all-zero features) | Pass | All 149 regression suites and 15 forced-runtime/AOT/Python supplements pass. The 68-fixture/272-SOAP-direction matrix, real HTTP tests, pinned independent validator and both complete corpus modes retain expected values/errors. Ten Xerces input disagreements remain adjudicated; unassessed preservation and remaining P5-P9 scope stay explicit. |

Result: 15 Pass, 47 N/A, 0 Fail. The three previous core-prerequisite failures are resolved and retested.
