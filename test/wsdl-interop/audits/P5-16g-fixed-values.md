# P5-16g receiving-element fixed-value audit

Copyright (C) 2026 Qore Technologies, s.r.o.

Applied all 62 audit-changes checks and the five referenced design guides to the
final changes on 2026-09-12; parent `46f73cd`. No C/C++ or main-Qore changes.
[Evidence](../fixed-values-evidence.md) and [inventory](../P5-16g-validation.json)
record source hashes, commands, results and complete corpus comparison.

| # | Check | Status | Evidence |
| --- | --- | --- | --- |
| 1 | Entry exists in `doxygen/lang/120_modules.dox.tmpl` (for modules in the Qore repo; N/A for external module repos) | N/A | No new module, split entry point, QPP class or build registration; existing external WSDL targets are retained. |
| 2 | Entry exists in `doxygen/lang/900_release_notes.dox.tmpl` (for modules in the Qore repo; external modules have release notes in their .qm) | Pass | WSDL 0.5.8 release notes and README describe explicit nonempty fixed-value checking and constrained examples; the durable design includes an executed invoice example. |
| 3 | `qore_user_module()` or `qore_external_user_module()` call in `CMakeLists.txt` | N/A | No new module, split entry point, QPP class or build registration; existing external WSDL targets are retained. |
| 4 | Module added to QMOD list in `CMakeLists.txt` | N/A | No new module, split entry point, QPP class or build registration; existing external WSDL targets are retained. |
| 5 | `.qm` file has `@section <lowercasemodname>intro` as first doc section — **must be all lowercase** (e.g., `avrodataproviderintro`, not `AvroDataProviderintro`) | Pass | Existing wsdlintro and %modern remain; no separated directives or %include are introduced. |
| 6 | `%modern` in `.qm` file — no redundant `%new-style`, `%require-types`, `%strict-args`, `%enable-all-warnings` | Pass | Existing wsdlintro and %modern remain; no separated directives or %include are introduced. |
| 7 | No parse directives (`%requires`, `%modern`, `%new-style`) in separated `.qc` files (check OUTSIDE of `@code` blocks only) | N/A | No new module, split entry point, QPP class or build registration; existing external WSDL targets are retained. |
| 8 | No `%include` usage (deprecated for modules) | Pass | Existing wsdlintro and %modern remain; no separated directives or %include are introduced. |
| 9 | Copyright 2026 on all new files | Pass | All authored new sources and documents carry copyright 2026; the opaque frozen binary fixture has its notice and exact provenance in the adjacent README. |
| 10 | Directory layout: `.qm` inside `qlib/<ModuleName>/` directory (not at `qlib/<ModuleName>.qm` for multi-file modules) | N/A | No new module, split entry point, QPP class or build registration; existing external WSDL targets are retained. |
| 11 | No second `.qm` for the same module at `qlib/<ModuleName>.qm` | N/A | No new module, split entry point, QPP class or build registration; existing external WSDL targets are retained. |
| 12 | `ns=Qore::XX` matches the QoreNamespace constructor path | N/A | No new module, split entry point, QPP class or build registration; existing external WSDL targets are retained. |
| 13 | `%modern` directive present | Pass | Both new executable qtests use %modern, prepend local qlib before relative WSDL/SoapClient/SoapHandler imports and hard-require same-project xml and core QUnit/HttpServer. The unchanged matrix worker guards external JSON. |
| 14 | Executable permission set (`chmod +x`) | Pass | Both new executable qtests use %modern, prepend local qlib before relative WSDL/SoapClient/SoapHandler imports and hard-require same-project xml and core QUnit/HttpServer. The unchanged matrix worker guards external JSON. |
| 15 | Uses %prepend-module-path  before %requires for in-repo modules (Qore and Qore modules only; not Qorus) | Pass | Both new executable qtests use %modern, prepend local qlib before relative WSDL/SoapClient/SoapHandler imports and hard-require same-project xml and core QUnit/HttpServer. The unchanged matrix worker guards external JSON. |
| 16 | External module dependencies use `%try-module` — except modules delivered with the project itself (Qore ex: DataProvider, ConnectionProvider, QUnit, etc.) which use hard `%requires` | Pass | Both new executable qtests use %modern, prepend local qlib before relative WSDL/SoapClient/SoapHandler imports and hard-require same-project xml and core QUnit/HttpServer. The unchanged matrix worker guards external JSON. |
| 17 | No filesystem operations (fopen, open, creat, unlink, remove, rename, mkdir, rmdir, stat, chmod) without sandbox checks | N/A | No C/C++ or native blocking operations changed. Qore loops retain runtime cancellation points; cancellation, AutoLock unwinding and retry are exercised. |
| 18 | No network operations (connect, bind, socket, getaddrinfo, gethostbyname) without sandbox checks | N/A | No C/C++ or native blocking operations changed. Qore loops retain runtime cancellation points; cancellation, AutoLock unwinding and retry are exercised. |
| 19 | If filesystem/network ops exist, verify `QoreSandboxManagerHelper` usage | N/A | No C/C++ or native blocking operations changed. Qore loops retain runtime cancellation points; cancellation, AutoLock unwinding and retry are exercised. |
| 20 | No `File::`, `Dir::`, `Socket::`, `HTTPClient::` usage without justification | Pass | No production I/O added. The test reads a pinned local provider fixture and uses ephemeral loopback HTTP listeners, bounded queues/requests and deterministic shutdown without sleeps or polling. |
| 21 | All `for`/`while` loops that could iterate >100 times have `qore_check_cancel()` checks | N/A | No C/C++ or native blocking operations changed. Qore loops retain runtime cancellation points; cancellation, AutoLock unwinding and retry are exercised. |
| 22 | Uses `qore_check_cancel()` (NOT deprecated `qore_check_io_interrupt()`) | N/A | No C/C++ or native blocking operations changed. Qore loops retain runtime cancellation points; cancellation, AutoLock unwinding and retry are exercised. |
| 23 | Check frequency: every 100 iterations for tight loops, every 10 for expensive iterations | N/A | No C/C++ or native blocking operations changed. Qore loops retain runtime cancellation points; cancellation, AutoLock unwinding and retry are exercised. |
| 24 | No blocking operations without cancellation support | N/A | No C/C++ or native blocking operations changed. Qore loops retain runtime cancellation points; cancellation, AutoLock unwinding and retry are exercised. |
| 25 | Every action has `display_name`, `short_desc` (plain text, <80 chars), `desc` (markdown) | N/A | No app/action registration, request/response field declaration, factory or JNI changes. This is an AbstractDataProviderType validation adapter; existing field metadata is delegated and tested through ordinary/native, soft/mandatory, saved, sample and SoapDataProvider consumers. |
| 26 | Every action has `options` populated via `getActionOptionFromFields()` — without this, the action shows an empty, unusable form | N/A | No app/action registration, request/response field declaration, factory or JNI changes. This is an AbstractDataProviderType validation adapter; existing field metadata is delegated and tested through ordinary/native, soft/mandatory, saved, sample and SoapDataProvider consumers. |
| 27 | Every action has `output_type` set to a typed data type constant (e.g., `MyResponseDataType`) — not omitted | N/A | No app/action registration, request/response field declaration, factory or JNI changes. This is an AbstractDataProviderType validation adapter; existing field metadata is delegated and tested through ordinary/native, soft/mandatory, saved, sample and SoapDataProvider consumers. |
| 28 | DPAT_API actions: provider has `"supports_request": True` and implements `doRequestImpl()` | N/A | No app/action registration, request/response field declaration, factory or JNI changes. This is an AbstractDataProviderType validation adapter; existing field metadata is delegated and tested through ordinary/native, soft/mandatory, saved, sample and SoapDataProvider consumers. |
| 29 | DPAT_FIND actions: every option exists in `SearchOptions`, `getRecordTypeImpl()` returns `*hash<string, AbstractDataField>` | N/A | No app/action registration, request/response field declaration, factory or JNI changes. This is an AbstractDataProviderType validation adapter; existing field metadata is delegated and tested through ordinary/native, soft/mandatory, saved, sample and SoapDataProvider consumers. |
| 30 | Scheme-based apps (with `"scheme"` in registerApp): actions use `"path"` and do NOT use `"cls"` — having both `scheme` and `cls` causes a runtime error | N/A | No app/action registration, request/response field declaration, factory or JNI changes. This is an AbstractDataProviderType validation adapter; existing field metadata is delegated and tested through ordinary/native, soft/mandatory, saved, sample and SoapDataProvider consumers. |
| 31 | Single-key hash slices use trailing comma: `Fields{"key",}` (without trailing comma, `Fields{"key"}` returns the value, not a hash) | N/A | No app/action registration, request/response field declaration, factory or JNI changes. This is an AbstractDataProviderType validation adapter; existing field metadata is delegated and tested through ordinary/native, soft/mandatory, saved, sample and SoapDataProvider consumers. |
| 32 | **Typed data type classes exist** for request and response types — inherit `HashDataType`, have `const Fields` hash, call `addQoreFields(Fields)` in constructor, export public constant at bottom (e.g., `public const MyDataType = new MyDataType();`) | N/A | No app/action registration, request/response field declaration, factory or JNI changes. This is an AbstractDataProviderType validation adapter; existing field metadata is delegated and tested through ordinary/native, soft/mandatory, saved, sample and SoapDataProvider consumers. |
| 33 | Request/input types use `public` Fields (enables `ClassName::Fields` in action registration) | N/A | No app/action registration, request/response field declaration, factory or JNI changes. This is an AbstractDataProviderType validation adapter; existing field metadata is delegated and tested through ordinary/native, soft/mandatory, saved, sample and SoapDataProvider consumers. |
| 34 | Response/output types use `private` Fields | N/A | No app/action registration, request/response field declaration, factory or JNI changes. This is an AbstractDataProviderType validation adapter; existing field metadata is delegated and tested through ordinary/native, soft/mandatory, saved, sample and SoapDataProvider consumers. |
| 35 | Each field in data types has `display_name`, `type`, and `desc` (markdown-formatted) | N/A | No app/action registration, request/response field declaration, factory or JNI changes. This is an AbstractDataProviderType validation adapter; existing field metadata is delegated and tested through ordinary/native, soft/mandatory, saved, sample and SoapDataProvider consumers. |
| 36 | Input fields have `example_value` where useful (string fields, endpoint URIs, SQL queries, etc.) | N/A | No app/action registration, request/response field declaration, factory or JNI changes. This is an AbstractDataProviderType validation adapter; existing field metadata is delegated and tested through ordinary/native, soft/mandatory, saved, sample and SoapDataProvider consumers. |
| 37 | Fields with finite allowed values use `allowed_values` with `AllowedValueInfo` containing both `value` and `display_name` (Title Case, human-readable) — never bare values, never described only in text | N/A | No app/action registration, request/response field declaration, factory or JNI changes. This is an AbstractDataProviderType validation adapter; existing field metadata is delegated and tested through ordinary/native, soft/mandatory, saved, sample and SoapDataProvider consumers. |
| 38 | Password/secret fields have `"sensitive": True` | N/A | No app/action registration, request/response field declaration, factory or JNI changes. This is an AbstractDataProviderType validation adapter; existing field metadata is delegated and tested through ordinary/native, soft/mandatory, saved, sample and SoapDataProvider consumers. |
| 39 | `groups` uses `AppGroup` enum values from `qlib/DataProvider/AppGroup.qc` | N/A | No app/action registration, request/response field declaration, factory or JNI changes. This is an AbstractDataProviderType validation adapter; existing field metadata is delegated and tested through ordinary/native, soft/mandatory, saved, sample and SoapDataProvider consumers. |
| 40 | App `logo` stored as separate file, loaded at module level in `Priv` namespace | N/A | No app/action registration, request/response field declaration, factory or JNI changes. This is an AbstractDataProviderType validation adapter; existing field metadata is delegated and tested through ordinary/native, soft/mandatory, saved, sample and SoapDataProvider consumers. |
| 41 | App `desc` uses markdown: bullet list of capabilities, links to project website, business-language explanation of value | N/A | No app/action registration, request/response field declaration, factory or JNI changes. This is an AbstractDataProviderType validation adapter; existing field metadata is delegated and tested through ordinary/native, soft/mandatory, saved, sample and SoapDataProvider consumers. |
| 42 | `display_name` is user-friendly ("Apache Avro" not "avro") | N/A | No app/action registration, request/response field declaration, factory or JNI changes. This is an AbstractDataProviderType validation adapter; existing field metadata is delegated and tested through ordinary/native, soft/mandatory, saved, sample and SoapDataProvider consumers. |
| 43 | `short_desc` is plain text, under 80 chars, single sentence — no markdown | N/A | No app/action registration, request/response field declaration, factory or JNI changes. This is an AbstractDataProviderType validation adapter; existing field metadata is delegated and tested through ordinary/native, soft/mandatory, saved, sample and SoapDataProvider consumers. |
| 44 | `desc` uses markdown: backticks for code/field refs (`` `field_name` ``, `` `True` ``, `` `pdf` ``), `\n\n` for paragraphs, `- ` bullet lists for enumerations, `**bold**` for caveats | N/A | No app/action registration, request/response field declaration, factory or JNI changes. This is an AbstractDataProviderType validation adapter; existing field metadata is delegated and tested through ordinary/native, soft/mandatory, saved, sample and SoapDataProvider consumers. |
| 45 | Descriptions use plain business language relating to common challenges — not just technical "what" but "why" and "when to use" | N/A | No app/action registration, request/response field declaration, factory or JNI changes. This is an AbstractDataProviderType validation adapter; existing field metadata is delegated and tested through ordinary/native, soft/mandatory, saved, sample and SoapDataProvider consumers. |
| 46 | No bare `True`/`False`/`NOTHING` — must be backtick-wrapped in `desc` | N/A | No app/action registration, request/response field declaration, factory or JNI changes. This is an AbstractDataProviderType validation adapter; existing field metadata is delegated and tested through ordinary/native, soft/mandatory, saved, sample and SoapDataProvider consumers. |
| 47 | No bare field/option names in prose — must use backticks | N/A | No app/action registration, request/response field declaration, factory or JNI changes. This is an AbstractDataProviderType validation adapter; existing field metadata is delegated and tested through ordinary/native, soft/mandatory, saved, sample and SoapDataProvider consumers. |
| 48 | Long descriptions (>500 chars) use bold section headers and bullet lists | N/A | No app/action registration, request/response field declaration, factory or JNI changes. This is an AbstractDataProviderType validation adapter; existing field metadata is delegated and tested through ordinary/native, soft/mandatory, saved, sample and SoapDataProvider consumers. |
| 49 | **Factory registration in Qore repo**: every factory name registered in `qlib/DataProvider/DataProvider.qc` → `FactoryMap` (without this, module loads but doesn't appear in Qorus apps) | N/A | No app/action registration, request/response field declaration, factory or JNI changes. This is an AbstractDataProviderType validation adapter; existing field metadata is delegated and tested through ordinary/native, soft/mandatory, saved, sample and SoapDataProvider consumers. |
| 50 | **`getRecordTypeImpl()` signature**: must be `private *hash<string, AbstractDataField> getRecordTypeImpl(*hash<auto> search_options)` — NOT returning `*AbstractDataProviderType` | N/A | No app/action registration, request/response field declaration, factory or JNI changes. This is an AbstractDataProviderType validation adapter; existing field metadata is delegated and tested through ordinary/native, soft/mandatory, saved, sample and SoapDataProvider consumers. |
| 51 | **Dependency JARs committed** (for JNI modules): JAR files in `qlib/*/jar/` may be gitignored — use `git add -f` to ensure they're tracked, otherwise CI compilation fails | N/A | No app/action registration, request/response field declaration, factory or JNI changes. This is an AbstractDataProviderType validation adapter; existing field metadata is delegated and tested through ordinary/native, soft/mandatory, saved, sample and SoapDataProvider consumers. |
| 52 | JAR install rules in CMakeLists.txt for all dependency JARs | N/A | No app/action registration, request/response field declaration, factory or JNI changes. This is an AbstractDataProviderType validation adapter; existing field metadata is delegated and tested through ordinary/native, soft/mandatory, saved, sample and SoapDataProvider consumers. |
| 53 | **No workarounds**: No TODOs, FIXMEs, stubs, or partially-implemented features | Pass | Receiving-element equality uses computed XSD identities and the actual selected union member. No fixture-specific rule or relaxed validator verdict. The documented Serializable hook initializes runtime state; old metadata resolves after reconstruction. Empty-element/default/identity work remains explicitly open. |
| 54 | **Exception safety**: C++ uses `ReferenceHolder` for Qore allocations, `std::unique_ptr` for C++ allocations, `*xsink` checked after every fallible operation | Pass | All state uses managed Qore values. Scoped capture and output contexts restore on exceptions; AutoLock releases on conversion cancellation before retry. Invalid provider reconstruction and subsequent reuse are tested. Final Valgrind reports zero errors/lost blocks. |
| 55 | **Thread safety**: All mutable shared state protected by `std::lock_guard<std::mutex>` or documented as immutable-after-construction | Pass | Per-document captures are thread-local and scoped. Declaration identity reads and migration writes use a per-declaration Mutex; successful state is cached. Two barrier-started workers share an old provider while checking valid/invalid inputs; no source namespace mutation. |
| 56 | **Type safety**: Strongly-typed `code<return(args)>` instead of untyped `code`; `static_cast` instead of C casts; typed hashdecls for results; enums where appropriate | Pass | Typed XsdUnionValueIdentity, XsdDocumentValueResult, XsdElementValueConstraintInfo and provider members preserve type checks; polymorphic native/XML payloads use the established auto API. The HTTP callback has code<auto(hash<auto>, auto)> type. |
| 57 | **Performance**: No O(n²) where O(n) is possible; no unnecessary copies; coordinate descent uses incremental residuals not full matrix multiply | Pass | Fixed text is joined once; lists compare ordered computed identities. Conversion captures the original union/list selection and does not call custom converters again. Old declaration identity computes once, demonstrated by counters; provider metadata uses existing copy-on-write variants. |
| 58 | **Error handling**: All inputs validated (dimensions, empty data, unfitted models); C++ I/O handles EAGAIN/EINTR if applicable | Pass | Tests reject wrong primitive spaces, wrong values, reordered lists, QName bindings, mixed child content, missing required attributes and malformed saved provider members with intended error categories. Custom/cancellation errors propagate. Bad HTTP requests receive version-specific faults before callbacks, followed by valid reuse. |
| 59 | **Documentation**: Doxygen `@param`, `@return`, `@throw` on all public methods; `@par Example` with realistic business scenarios; `@note` for important caveats | Pass | Public hash fields and adapter methods document parameters, returns, errors and native/optional behavior. The design covers equality, saved-provider migration, custom conversions, metadata and an invoice example. Final qmod, Doxygen and executed example pass without warnings. |
| 60 | **QPP flags**: `[flags=CONSTANT]` on methods that never throw; `[flags=RET_VALUE_ONLY]` on methods that throw but have no side effects | N/A | No QPP code or flags changed. |
| 61 | **Security**: No user-controlled format strings; no buffer overflows; bounds checking on array indices; no credentials in code | Pass | No credentials, raw memory operations or user-controlled format strings. Fixture and peer inputs are bounded/offline; HTTP tests use loopback. Saved-data tests preserve strict member types and reject missing constraints. |
| 62 | **Correctness**: Algorithms verified against reference implementations; edge cases tested (empty data, single sample, all-zero features) | Pass | The audit found and fixed primitive base-category loss, XSD list classification and premature foreign-member inspection during provider restore. Final 146-suite gate, AST/IR/JIT/AOT and 53-schema/172-document independent matrix pass. Every corpus row is unchanged with no missing/added cases; remaining P5-P9 failures stay visible. |

Result: 19 Pass, 43 N/A, 0 Fail.
