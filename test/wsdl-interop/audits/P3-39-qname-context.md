# P3-39 audit: QName instance and detached-provider integration

Copyright (C) 2026 Qore Technologies, s.r.o.

The full audit-changes skill was applied to all production Qore changes, tests,
coverage tooling, strict selection, documentation and evidence. Its referenced
Qore module-structure, sandboxing, cooperative-cancellation, DataProvider checklist
and development guides were reviewed. No new module registration or C++ change
is required. All final checks below are resolved: **20 Pass / 42 N/A / 0 Fail**.
No C++ change requires a new Valgrind run.

[Evidence](../qname-context-evidence.md) describes the independent requirements,
root causes, validator diagnostic and unchanged later-phase failures.

| Check | Status | Evidence |
| --- | --- | --- |
| 1. Entry exists in `doxygen/lang/120_modules.dox.tmpl` (for modules in the Qore repo; N/A for external module repos) | N/A | No new module, separated module file or QPP class. |
| 2. Entry exists in `doxygen/lang/900_release_notes.dox.tmpl` (for modules in the Qore repo; external modules have release notes in their .qm) | Pass | WSDL v0.5.8 release notes describe scoped QName values, provider facets, fields and standalone output. |
| 3. `qore_user_module()` or `qore_external_user_module()` call in `CMakeLists.txt` | Pass | Existing WSDL registration is unchanged; docs-module successfully rebuilds the changed module. |
| 4. Module added to QMOD list in `CMakeLists.txt` | N/A | No new module, separated module file or QPP class. |
| 5. `.qm` file has `@section <lowercasemodname>intro` as first doc section — **must be all lowercase** (e.g., `avrodataproviderintro`, not `AvroDataProviderintro`) | N/A | No new module, separated module file or QPP class. |
| 6. `%modern` in `.qm` file — no redundant `%new-style`, `%require-types`, `%strict-args`, `%enable-all-warnings` | Pass | WSDL and all new Qore tests use %modern; no redundant parse directives introduced. |
| 7. No parse directives (`%requires`, `%modern`, `%new-style`) in separated `.qc` files (check OUTSIDE of `@code` blocks only) | N/A | No new module, separated module file or QPP class. |
| 8. No `%include` usage (deprecated for modules) | Pass | No deprecated %include introduced. |
| 9. Copyright 2026 on all new files | Pass | All new authored Qore/Python tests, evidence and this audit carry 2026 copyright notices. |
| 10. Directory layout: `.qm` inside `qlib/<ModuleName>/` directory (not at `qlib/<ModuleName>.qm` for multi-file modules) | N/A | No new module, separated module file or QPP class. |
| 11. No second `.qm` for the same module at `qlib/<ModuleName>.qm` | N/A | No new module, separated module file or QPP class. |
| 12. `ns=Qore::XX` matches the QoreNamespace constructor path | N/A | No new module, separated module file or QPP class. |
| 13. `%modern` directive present | Pass | All changed/new qtests and the worker explicitly use %modern. |
| 14. Executable permission set (`chmod +x`) | Pass | All new qtests, the worker and Python matrix have executable permissions. |
| 15. Uses %prepend-module-path  before %requires for in-repo modules (Qore and Qore modules only; not Qorus) | Pass | Qore tests prepend local qlib before relative %requires; worker and environment load the local XML module. |
| 16. External module dependencies use `%try-module` — except modules delivered with the project itself (Qore ex: DataProvider, ConnectionProvider, QUnit, etc.) which use hard `%requires` | Pass | Project xml and core QUnit use hard requirements; external json uses the %try-module pattern and fails with MISSING-MODULE if absent. |
| 17. No filesystem operations (fopen, open, creat, unlink, remove, rename, mkdir, rmdir, stat, chmod) without sandbox checks | N/A | No C++/QPP changes or native I/O/cancellation/flag changes in this increment. |
| 18. No network operations (connect, bind, socket, getaddrinfo, gethostbyname) without sandbox checks | N/A | No C++/QPP changes or native I/O/cancellation/flag changes in this increment. |
| 19. If filesystem/network ops exist, verify `QoreSandboxManagerHelper` usage | N/A | No C++/QPP changes or native I/O/cancellation/flag changes in this increment. |
| 20. No `File::`, `Dir::`, `Socket::`, `HTTPClient::` usage without justification | Pass | Production changes introduce no I/O. The fixture worker reads its explicit manifest, and HTTP tests use bounded local connections with deterministic teardown. |
| 21. All `for`/`while` loops that could iterate >100 times have `qore_check_cancel()` checks | N/A | No C++/QPP changes or native I/O/cancellation/flag changes in this increment. |
| 22. Uses `qore_check_cancel()` (NOT deprecated `qore_check_io_interrupt()`) | N/A | No C++/QPP changes or native I/O/cancellation/flag changes in this increment. |
| 23. Check frequency: every 100 iterations for tight loops, every 10 for expensive iterations | N/A | No C++/QPP changes or native I/O/cancellation/flag changes in this increment. |
| 24. No blocking operations without cancellation support | N/A | No C++/QPP changes or native I/O/cancellation/flag changes in this increment. |
| 25. Every action has `display_name`, `short_desc` (plain text, <80 chars), `desc` (markdown) | N/A | No action/app/factory/JNI registration or request/response record class added. This increment changes primitive QName providers and finite-choice fields; inherited business metadata is unchanged. |
| 26. Every action has `options` populated via `getActionOptionFromFields()` — without this, the action shows an empty, unusable form | N/A | No action/app/factory/JNI registration or request/response record class added. This increment changes primitive QName providers and finite-choice fields; inherited business metadata is unchanged. |
| 27. Every action has `output_type` set to a typed data type constant (e.g., `MyResponseDataType`) — not omitted | N/A | No action/app/factory/JNI registration or request/response record class added. This increment changes primitive QName providers and finite-choice fields; inherited business metadata is unchanged. |
| 28. DPAT_API actions: provider has `"supports_request": True` and implements `doRequestImpl()` | N/A | No action/app/factory/JNI registration or request/response record class added. This increment changes primitive QName providers and finite-choice fields; inherited business metadata is unchanged. |
| 29. DPAT_FIND actions: every option exists in `SearchOptions`, `getRecordTypeImpl()` returns `*hash<string, AbstractDataField>` | N/A | No action/app/factory/JNI registration or request/response record class added. This increment changes primitive QName providers and finite-choice fields; inherited business metadata is unchanged. |
| 30. Scheme-based apps (with `"scheme"` in registerApp): actions use `"path"` and do NOT use `"cls"` — having both `scheme` and `cls` causes a runtime error | N/A | No action/app/factory/JNI registration or request/response record class added. This increment changes primitive QName providers and finite-choice fields; inherited business metadata is unchanged. |
| 31. Single-key hash slices use trailing comma: `Fields{"key",}` (without trailing comma, `Fields{"key"}` returns the value, not a hash) | N/A | No action/app/factory/JNI registration or request/response record class added. This increment changes primitive QName providers and finite-choice fields; inherited business metadata is unchanged. |
| 32. **Typed data type classes exist** for request and response types — inherit `HashDataType`, have `const Fields` hash, call `addQoreFields(Fields)` in constructor, export public constant at bottom (e.g., `public const MyDataType = new MyDataType();`) | N/A | No action/app/factory/JNI registration or request/response record class added. This increment changes primitive QName providers and finite-choice fields; inherited business metadata is unchanged. |
| 33. Request/input types use `public` Fields (enables `ClassName::Fields` in action registration) | N/A | No action/app/factory/JNI registration or request/response record class added. This increment changes primitive QName providers and finite-choice fields; inherited business metadata is unchanged. |
| 34. Response/output types use `private` Fields | N/A | No action/app/factory/JNI registration or request/response record class added. This increment changes primitive QName providers and finite-choice fields; inherited business metadata is unchanged. |
| 35. Each field in data types has `display_name`, `type`, and `desc` (markdown-formatted) | N/A | No action/app/factory/JNI registration or request/response record class added. This increment changes primitive QName providers and finite-choice fields; inherited business metadata is unchanged. |
| 36. Input fields have `example_value` where useful (string fields, endpoint URIs, SQL queries, etc.) | N/A | No action/app/factory/JNI registration or request/response record class added. This increment changes primitive QName providers and finite-choice fields; inherited business metadata is unchanged. |
| 37. Fields with finite allowed values use `allowed_values` with `AllowedValueInfo` containing both `value` and `display_name` (Title Case, human-readable) — never bare values, never described only in text | Pass | XsdQNameDataField exposes AllowedValueInfo entries with value and display_name; URI/local indexes retain display metadata. Alias, repeated-choice and reconstruction tests pass. |
| 38. Password/secret fields have `"sensitive": True` | N/A | No action/app/factory/JNI registration or request/response record class added. This increment changes primitive QName providers and finite-choice fields; inherited business metadata is unchanged. |
| 39. `groups` uses `AppGroup` enum values from `qlib/DataProvider/AppGroup.qc` | N/A | No action/app/factory/JNI registration or request/response record class added. This increment changes primitive QName providers and finite-choice fields; inherited business metadata is unchanged. |
| 40. App `logo` stored as separate file, loaded at module level in `Priv` namespace | N/A | No action/app/factory/JNI registration or request/response record class added. This increment changes primitive QName providers and finite-choice fields; inherited business metadata is unchanged. |
| 41. App `desc` uses markdown: bullet list of capabilities, links to project website, business-language explanation of value | N/A | No action/app/factory/JNI registration or request/response record class added. This increment changes primitive QName providers and finite-choice fields; inherited business metadata is unchanged. |
| 42. `display_name` is user-friendly ("Apache Avro" not "avro") | N/A | No action/app/factory/JNI registration or request/response record class added. This increment changes primitive QName providers and finite-choice fields; inherited business metadata is unchanged. |
| 43. `short_desc` is plain text, under 80 chars, single sentence — no markdown | N/A | No action/app/factory/JNI registration or request/response record class added. This increment changes primitive QName providers and finite-choice fields; inherited business metadata is unchanged. |
| 44. `desc` uses markdown: backticks for code/field refs (`` `field_name` ``, `` `True` ``, `` `pdf` ``), `\n\n` for paragraphs, `- ` bullet lists for enumerations, `**bold**` for caveats | N/A | No action/app/factory/JNI registration or request/response record class added. This increment changes primitive QName providers and finite-choice fields; inherited business metadata is unchanged. |
| 45. Descriptions use plain business language relating to common challenges — not just technical "what" but "why" and "when to use" | N/A | No action/app/factory/JNI registration or request/response record class added. This increment changes primitive QName providers and finite-choice fields; inherited business metadata is unchanged. |
| 46. No bare `True`/`False`/`NOTHING` — must be backtick-wrapped in `desc` | N/A | No action/app/factory/JNI registration or request/response record class added. This increment changes primitive QName providers and finite-choice fields; inherited business metadata is unchanged. |
| 47. No bare field/option names in prose — must use backticks | N/A | No action/app/factory/JNI registration or request/response record class added. This increment changes primitive QName providers and finite-choice fields; inherited business metadata is unchanged. |
| 48. Long descriptions (>500 chars) use bold section headers and bullet lists | N/A | No action/app/factory/JNI registration or request/response record class added. This increment changes primitive QName providers and finite-choice fields; inherited business metadata is unchanged. |
| 49. **Factory registration in Qore repo**: every factory name registered in `qlib/DataProvider/DataProvider.qc` → `FactoryMap` (without this, module loads but doesn't appear in Qorus apps) | N/A | No action/app/factory/JNI registration or request/response record class added. This increment changes primitive QName providers and finite-choice fields; inherited business metadata is unchanged. |
| 50. **`getRecordTypeImpl()` signature**: must be `private *hash<string, AbstractDataField> getRecordTypeImpl(*hash<auto> search_options)` — NOT returning `*AbstractDataProviderType` | N/A | No action/app/factory/JNI registration or request/response record class added. This increment changes primitive QName providers and finite-choice fields; inherited business metadata is unchanged. |
| 51. **Dependency JARs committed** (for JNI modules): JAR files in `qlib/*/jar/` may be gitignored — use `git add -f` to ensure they're tracked, otherwise CI compilation fails | N/A | No action/app/factory/JNI registration or request/response record class added. This increment changes primitive QName providers and finite-choice fields; inherited business metadata is unchanged. |
| 52. JAR install rules in CMakeLists.txt for all dependency JARs | N/A | No action/app/factory/JNI registration or request/response record class added. This increment changes primitive QName providers and finite-choice fields; inherited business metadata is unchanged. |
| 53. **No workarounds**: No TODOs, FIXMEs, stubs, or partially-implemented features | Pass | Root causes are fixed without fixture-specific production branches, ignored facets or skipped verdicts. One exact Xerces warning is independently adjudicated. Conflicting output bindings fail explicitly; P5 remapping remains tracked. |
| 54. **Exception safety**: C++ uses `ReferenceHolder` for Qore allocations, `std::unique_ptr` for C++ allocations, `*xsink` checked after every fallible operation | Pass | Scope constructors/destructors and on_exit paths restore context on errors and cancellation. Fields validate new choices before replacing state, reconstructed indexes rebuild, and complete XML output uses a private registry copy. Failure/recovery and actual thread cancellation tests pass. |
| 55. **Thread safety**: All mutable shared state protected by `std::lock_guard<std::mutex>` or documented as immutable-after-construction | Pass | Namespace/trial state is thread_local, retained values are immutable, and output state belongs to each conversion. Provider metadata is configured before shared use as documented. Barrier-based concurrent scope isolation and cancellation tests pass. |
| 56. **Type safety**: Strongly-typed `code<return(args)>` instead of untyped `code`; `static_cast` instead of C casts; typed hashdecls for results; enums where appropriate | Pass | QName facet and traversal records are hashdecls; maps/lists/providers use explicit types, with auto only for the public heterogeneous XML value domain. Runtime categories and malformed reconstructed state are tested. |
| 57. **Performance**: No O(n²) where O(n) is possible; no unnecessary copies; coordinate descent uses incremental residuals not full matrix multiply | Pass | QName restrictions traverse built-in chains iteratively with indexed enumeration keys. Schema/provider reachability tracks visited nodes. Output maps restore node-local declarations instead of copying inherited maps; prefix filtering visits local declarations. Serialization reads the resolved item schema kind without reconstructing provider graphs. Tests cover 600-level providers and 128-level sibling trees; identical 296-row before/after results verify the allocation change. |
| 58. **Error handling**: All inputs validated (dimensions, empty data, unfitted models); C++ I/O handles EAGAIN/EINTR if applicable | Pass | QName/scoped text and complete namespace maps validate construction/reconstruction. Tests cover empty/unbound/malformed text, namespace aliases, wrong identities, conflicting lists, invalid attributes and specific serialization/deserialization/provider errors. |
| 59. **Documentation**: Doxygen `@param`, `@return`, `@throw` on all public methods; `@par Example` with realistic business scenarios; `@note` for important caveats | Pass | Public value/provider/field methods and serializeXmlValue document arguments/results/errors. QName design, lexical/scalar contracts, examples, release notes, README and evidence are updated; docs-module and executed examples pass. |
| 60. **QPP flags**: `[flags=CONSTANT]` on methods that never throw; `[flags=RET_VALUE_ONLY]` on methods that throw but have no side effects | N/A | No C++/QPP changes or native I/O/cancellation/flag changes in this increment. |
| 61. **Security**: No user-controlled format strings; no buffer overflows; bounds checking on array indices; no credentials in code | Pass | No credentials, user format strings or native buffers added. XML character/binding validation and complete namespace identity checks remain mandatory. XML and local worker inputs keep explicit boundaries and bounded deadlines. |
| 62. **Correctness**: Algorithms verified against reference implementations; edge cases tested (empty data, single sample, all-zero features) | Pass | Exact final source passes 89 Qore suites / 929 cases / 35,144 assertions; 21 focused cases / 1,467 assertions in each execution mode; 4,072 context outcomes and 3,776 documents against Xerces and six native paths. Eight existing independent Python methods and survey/normative/corpus tests pass. Strict coverage adds eight QName value checks with no regression; exactly two known P6 unit assertions remain tracked. |
