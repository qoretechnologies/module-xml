# P5-16e native anyType audit

Copyright (C) 2026 Qore Technologies, s.r.o.

Applied the complete audit-changes skill and all five referenced design guides
on 2026-09-12 to the final native corrections, CMake integration, tests and docs.
Parent: `8619697`. The module-structure guide was reread after its concurrent
update; no main-Qore or production WSDL source change is included.
[Evidence](../native-anytype-evidence.md) and [inventory](../P5-16e-validation.json)
retain commands, hashes and results. This increment is a P5 prerequisite and does
not close WSDL inheritance or the remaining P5-P9 acceptance criteria.

| # | Check | Status | Evidence |
| --- | --- | --- | --- |
| 1 | Entry exists in `doxygen/lang/120_modules.dox.tmpl` (for modules in the Qore repo; N/A for external module repos) | N/A | No new Qore module, entry point or production Qore source. Native build inputs are registered in CMake and Makefile; distribution and installation tests pass. |
| 2 | Entry exists in `doxygen/lang/900_release_notes.dox.tmpl` (for modules in the Qore repo; external modules have release notes in their .qm) | Pass | Native xml 2.3.0 release notes, README and implemented design describe builtin particle safety and effective group content. |
| 3 | `qore_user_module()` or `qore_external_user_module()` call in `CMakeLists.txt` | N/A | No new Qore module, entry point or production Qore source. Native build inputs are registered in CMake and Makefile; distribution and installation tests pass. |
| 4 | Module added to QMOD list in `CMakeLists.txt` | N/A | No new Qore module, entry point or production Qore source. Native build inputs are registered in CMake and Makefile; distribution and installation tests pass. |
| 5 | `.qm` file has `@section <lowercasemodname>intro` as first doc section — **must be all lowercase** (e.g., `avrodataproviderintro`, not `AvroDataProviderintro`) | N/A | No new Qore module, entry point or production Qore source. Native build inputs are registered in CMake and Makefile; distribution and installation tests pass. |
| 6 | `%modern` in `.qm` file — no redundant `%new-style`, `%require-types`, `%strict-args`, `%enable-all-warnings` | N/A | No new Qore module, entry point or production Qore source. Native build inputs are registered in CMake and Makefile; distribution and installation tests pass. |
| 7 | No parse directives (`%requires`, `%modern`, `%new-style`) in separated `.qc` files (check OUTSIDE of `@code` blocks only) | N/A | No new Qore module, entry point or production Qore source. Native build inputs are registered in CMake and Makefile; distribution and installation tests pass. |
| 8 | No `%include` usage (deprecated for modules) | Pass | No Qore %include introduced. The standalone native fault test includes the exact selected C source to exercise private attribution ownership. |
| 9 | Copyright 2026 on all new files | Pass | All authored new files carry copyright 2026; upstream notices remain intact. |
| 10 | Directory layout: `.qm` inside `qlib/<ModuleName>/` directory (not at `qlib/<ModuleName>.qm` for multi-file modules) | N/A | No new Qore module, entry point or production Qore source. Native build inputs are registered in CMake and Makefile; distribution and installation tests pass. |
| 11 | No second `.qm` for the same module at `qlib/<ModuleName>.qm` | N/A | No new Qore module, entry point or production Qore source. Native build inputs are registered in CMake and Makefile; distribution and installation tests pass. |
| 12 | `ns=Qore::XX` matches the QoreNamespace constructor path | N/A | No new QPP class, namespace, public native method or QPP flags. |
| 13 | `%modern` directive present | Pass | Executable modern qtest prepends the local module directory and requires same-project xml and core QUnit. The reused matrix worker guards its external JSON dependency. |
| 14 | Executable permission set (`chmod +x`) | Pass | Executable modern qtest prepends the local module directory and requires same-project xml and core QUnit. The reused matrix worker guards its external JSON dependency. |
| 15 | Uses %prepend-module-path  before %requires for in-repo modules (Qore and Qore modules only; not Qorus) | Pass | Executable modern qtest prepends the local module directory and requires same-project xml and core QUnit. The reused matrix worker guards its external JSON dependency. |
| 16 | External module dependencies use `%try-module` — except modules delivered with the project itself (Qore ex: DataProvider, ConnectionProvider, QUnit, etc.) which use hard `%requires` | Pass | Executable modern qtest prepends the local module directory and requires same-project xml and core QUnit. The reused matrix worker guards its external JSON dependency. |
| 17 | No filesystem operations (fopen, open, creat, unlink, remove, rename, mkdir, rmdir, stat, chmod) without sandbox checks | N/A | No production filesystem or network operation introduced. Allocation hooks are confined to standalone test executables. |
| 18 | No network operations (connect, bind, socket, getaddrinfo, gethostbyname) without sandbox checks | N/A | No production filesystem or network operation introduced. Allocation hooks are confined to standalone test executables. |
| 19 | If filesystem/network ops exist, verify `QoreSandboxManagerHelper` usage | N/A | No production filesystem or network operation introduced. Allocation hooks are confined to standalone test executables. |
| 20 | No `File::`, `Dir::`, `Socket::`, `HTTPClient::` usage without justification | Pass | No production Qore change or new test network I/O. The independent harness uses bounded, temporary offline fixtures. |
| 21 | All `for`/`while` loops that could iterate >100 times have `qore_check_cancel()` checks | N/A | Production changes add constant-time field initialization and guards only; no new loop, recursion or cancellation API. Standalone allocator tests and configure probes do not execute inside Qore. |
| 22 | Uses `qore_check_cancel()` (NOT deprecated `qore_check_io_interrupt()`) | N/A | Production changes add constant-time field initialization and guards only; no new loop, recursion or cancellation API. Standalone allocator tests and configure probes do not execute inside Qore. |
| 23 | Check frequency: every 100 iterations for tight loops, every 10 for expensive iterations | N/A | Production changes add constant-time field initialization and guards only; no new loop, recursion or cancellation API. Standalone allocator tests and configure probes do not execute inside Qore. |
| 24 | No blocking operations without cancellation support | Pass | No blocking production operation added. The qtest explicitly cancels a reader, clears interruption and verifies successful reuse. Concurrent workers complete through bounded queues and counters, without sleeps or polling. |
| 25 | Every action has `display_name`, `short_desc` (plain text, <80 chars), `desc` (markdown) | N/A | No app, action, provider field/type/factory registration or JNI change. Existing provider and SOAP consumers are covered by the full regression gate. |
| 26 | Every action has `options` populated via `getActionOptionFromFields()` — without this, the action shows an empty, unusable form | N/A | No app, action, provider field/type/factory registration or JNI change. Existing provider and SOAP consumers are covered by the full regression gate. |
| 27 | Every action has `output_type` set to a typed data type constant (e.g., `MyResponseDataType`) — not omitted | N/A | No app, action, provider field/type/factory registration or JNI change. Existing provider and SOAP consumers are covered by the full regression gate. |
| 28 | DPAT_API actions: provider has `"supports_request": True` and implements `doRequestImpl()` | N/A | No app, action, provider field/type/factory registration or JNI change. Existing provider and SOAP consumers are covered by the full regression gate. |
| 29 | DPAT_FIND actions: every option exists in `SearchOptions`, `getRecordTypeImpl()` returns `*hash<string, AbstractDataField>` | N/A | No app, action, provider field/type/factory registration or JNI change. Existing provider and SOAP consumers are covered by the full regression gate. |
| 30 | Scheme-based apps (with `"scheme"` in registerApp): actions use `"path"` and do NOT use `"cls"` — having both `scheme` and `cls` causes a runtime error | N/A | No app, action, provider field/type/factory registration or JNI change. Existing provider and SOAP consumers are covered by the full regression gate. |
| 31 | Single-key hash slices use trailing comma: `Fields{"key",}` (without trailing comma, `Fields{"key"}` returns the value, not a hash) | N/A | No app, action, provider field/type/factory registration or JNI change. Existing provider and SOAP consumers are covered by the full regression gate. |
| 32 | **Typed data type classes exist** for request and response types — inherit `HashDataType`, have `const Fields` hash, call `addQoreFields(Fields)` in constructor, export public constant at bottom (e.g., `public const MyDataType = new MyDataType();`) | N/A | No app, action, provider field/type/factory registration or JNI change. Existing provider and SOAP consumers are covered by the full regression gate. |
| 33 | Request/input types use `public` Fields (enables `ClassName::Fields` in action registration) | N/A | No app, action, provider field/type/factory registration or JNI change. Existing provider and SOAP consumers are covered by the full regression gate. |
| 34 | Response/output types use `private` Fields | N/A | No app, action, provider field/type/factory registration or JNI change. Existing provider and SOAP consumers are covered by the full regression gate. |
| 35 | Each field in data types has `display_name`, `type`, and `desc` (markdown-formatted) | N/A | No app, action, provider field/type/factory registration or JNI change. Existing provider and SOAP consumers are covered by the full regression gate. |
| 36 | Input fields have `example_value` where useful (string fields, endpoint URIs, SQL queries, etc.) | N/A | No app, action, provider field/type/factory registration or JNI change. Existing provider and SOAP consumers are covered by the full regression gate. |
| 37 | Fields with finite allowed values use `allowed_values` with `AllowedValueInfo` containing both `value` and `display_name` (Title Case, human-readable) — never bare values, never described only in text | N/A | No app, action, provider field/type/factory registration or JNI change. Existing provider and SOAP consumers are covered by the full regression gate. |
| 38 | Password/secret fields have `"sensitive": True` | N/A | No app, action, provider field/type/factory registration or JNI change. Existing provider and SOAP consumers are covered by the full regression gate. |
| 39 | `groups` uses `AppGroup` enum values from `qlib/DataProvider/AppGroup.qc` | N/A | No app, action, provider field/type/factory registration or JNI change. Existing provider and SOAP consumers are covered by the full regression gate. |
| 40 | App `logo` stored as separate file, loaded at module level in `Priv` namespace | N/A | No app, action, provider field/type/factory registration or JNI change. Existing provider and SOAP consumers are covered by the full regression gate. |
| 41 | App `desc` uses markdown: bullet list of capabilities, links to project website, business-language explanation of value | N/A | No app, action, provider field/type/factory registration or JNI change. Existing provider and SOAP consumers are covered by the full regression gate. |
| 42 | `display_name` is user-friendly ("Apache Avro" not "avro") | N/A | No app, action, provider field/type/factory registration or JNI change. Existing provider and SOAP consumers are covered by the full regression gate. |
| 43 | `short_desc` is plain text, under 80 chars, single sentence — no markdown | N/A | No app, action, provider field/type/factory registration or JNI change. Existing provider and SOAP consumers are covered by the full regression gate. |
| 44 | `desc` uses markdown: backticks for code/field refs (`` `field_name` ``, `` `True` ``, `` `pdf` ``), `\n\n` for paragraphs, `- ` bullet lists for enumerations, `**bold**` for caveats | N/A | No app, action, provider field/type/factory registration or JNI change. Existing provider and SOAP consumers are covered by the full regression gate. |
| 45 | Descriptions use plain business language relating to common challenges — not just technical "what" but "why" and "when to use" | N/A | No app, action, provider field/type/factory registration or JNI change. Existing provider and SOAP consumers are covered by the full regression gate. |
| 46 | No bare `True`/`False`/`NOTHING` — must be backtick-wrapped in `desc` | N/A | No app, action, provider field/type/factory registration or JNI change. Existing provider and SOAP consumers are covered by the full regression gate. |
| 47 | No bare field/option names in prose — must use backticks | N/A | No app, action, provider field/type/factory registration or JNI change. Existing provider and SOAP consumers are covered by the full regression gate. |
| 48 | Long descriptions (>500 chars) use bold section headers and bullet lists | N/A | No app, action, provider field/type/factory registration or JNI change. Existing provider and SOAP consumers are covered by the full regression gate. |
| 49 | **Factory registration in Qore repo**: every factory name registered in `qlib/DataProvider/DataProvider.qc` → `FactoryMap` (without this, module loads but doesn't appear in Qorus apps) | N/A | No app, action, provider field/type/factory registration or JNI change. Existing provider and SOAP consumers are covered by the full regression gate. |
| 50 | **`getRecordTypeImpl()` signature**: must be `private *hash<string, AbstractDataField> getRecordTypeImpl(*hash<auto> search_options)` — NOT returning `*AbstractDataProviderType` | N/A | No app, action, provider field/type/factory registration or JNI change. Existing provider and SOAP consumers are covered by the full regression gate. |
| 51 | **Dependency JARs committed** (for JNI modules): JAR files in `qlib/*/jar/` may be gitignored — use `git add -f` to ensure they're tracked, otherwise CI compilation fails | N/A | No app, action, provider field/type/factory registration or JNI change. Existing provider and SOAP consumers are covered by the full regression gate. |
| 52 | JAR install rules in CMakeLists.txt for all dependency JARs | N/A | No app, action, provider field/type/factory registration or JNI change. Existing provider and SOAP consumers are covered by the full regression gate. |
| 53 | **No workarounds**: No TODOs, FIXMEs, stubs, or partially-implemented features | Pass | Fixes both allocation-layout inconsistency and group-reference collapse at their roots. No fixture weakening, hidden failure, workaround or unsupported fallback. WSDL inheritance and remaining P5-P9 requirements remain explicitly open. |
| 54 | **Exception safety**: C++ uses `ReferenceHolder` for Qore allocations, `std::unique_ptr` for C++ allocations, `*xsink` checked after every fallible operation | Pass | Both particle constructors allocate the shared layout and initialize every added field. Existing native allocation cleanup is retained; all 284 permanent attribution allocation failures recover with exact live-block accounting. Unit, probe and allocation Valgrind runs report zero errors/lost blocks. |
| 55 | **Thread safety**: All mutable shared state protected by `std::lock_guard<std::mutex>` or documented as immutable-after-construction | Pass | Builtin particles are initialized once and tagged immutable. Schema attribution guards both metadata writes; allocation tests compare the entire builtin storage before/after every attempt. Four concurrent workers each compile/validate via all three APIs. Hooks remain process-local test state. |
| 56 | **Type safety**: Strongly-typed `code<return(args)>` instead of untyped `code`; `static_cast` instead of C casts; typed hashdecls for results; enums where appropriate | Pass | Shared C typedefs eliminate inconsistent casts between layouts. C code retains the upstream enum/pointer types. Qore helpers use declared argument/return types and typed containers. Byte snapshots use memcpy, preserving initialized padding for the immutability test. |
| 57 | **Performance**: No O(n²) where O(n) is possible; no unnecessary copies; coordinate descent uses incremental residuals not full matrix multiply | Pass | One shared struct declaration, constant-time guards and initialization. No extra runtime traversal, numeric occurrence expansion or quadratic algorithm. Schema group context uses the existing source node. |
| 58 | **Error handling**: All inputs validated (dimensions, empty data, unfitted models); C++ I/O handles EAGAIN/EINTR if applicable | Pass | All native APIs assert schema versus document error categories for invalid children, attributes, restriction content, mixed derivation and ambiguous extensions. Fault tests prove cleanup and subsequent success; provider tests reject both a mismatched layout and independently broken group semantics. |
| 59 | **Documentation**: Doxygen `@param`, `@return`, `@throw` on all public methods; `@par Example` with realistic business scenarios; `@note` for important caveats | Pass | No public API change. Release notes and durable design document the ownership and derivation rules, with an executed shipment example. Final Doxygen build is warning-free. |
| 60 | **QPP flags**: `[flags=CONSTANT]` on methods that never throw; `[flags=RET_VALUE_ONLY]` on methods that throw but have no side effects | N/A | No new QPP class, namespace, public native method or QPP flags. |
| 61 | **Security**: No user-controlled format strings; no buffer overflows; bounds checking on array indices; no credentials in code | Pass | No credentials or user-controlled format strings. Probe buffer sizes are checked. Exact source/header/result hashes and private target sources preserve dependency integrity. Both private struct definitions now share their allocation size and offsets. |
| 62 | **Correctness**: Algorithms verified against reference implementations; edge cases tested (empty data, single sample, all-zero features) | Pass | 143 suites/1370 cases/66622 reported assertions, three execution modes, 49 provider tests, and 10 schemas/100 documents agree with pinned Xerces. Existing 95-schema/774-document value matrix also passes. Both complete corpus JSON objects are identical to P5-16d, including remaining failures. |

Result: 18 Pass, 44 N/A, 0 Fail. The native increment may be committed.
