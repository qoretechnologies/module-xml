# P5-18j WSDL empty-element defaults audit

Copyright (C) 2026 Qore Technologies, s.r.o.

Applied every audit-changes checklist item to final changes based on `3767c18`.
All five referenced design guides were read and their hashes verified unchanged.
Scope: WSDL default application, retained values/providers, unit/HTTP/independent
regressions, release notes, design and evidence. No main-Qore mutation, installation
or push. Unrelated `test/cmake/__pycache__/` is excluded. See
[evidence](../wsdl-element-defaults-evidence.md) and [inventory](../P5-18j-validation.json).

| # | Check | Status | Evidence |
| --- | --- | --- | --- |
| 1 | Entry exists in `doxygen/lang/120_modules.dox.tmpl` (for modules in the Qore repo; N/A for external module repos) | N/A | No new module, separated source, QPP class, build registration or layout. Existing WSDL remains modern and registered. |
| 2 | Entry exists in `doxygen/lang/900_release_notes.dox.tmpl` (for modules in the Qore repo; external modules have release notes in their .qm) | N/A | No new module, separated source, QPP class, build registration or layout. Existing WSDL remains modern and registered. |
| 3 | `qore_user_module()` or `qore_external_user_module()` call in `CMakeLists.txt` | N/A | No new module, separated source, QPP class, build registration or layout. Existing WSDL remains modern and registered. |
| 4 | Module added to QMOD list in `CMakeLists.txt` | N/A | No new module, separated source, QPP class, build registration or layout. Existing WSDL remains modern and registered. |
| 5 | `.qm` file has `@section <lowercasemodname>intro` as first doc section — **must be all lowercase** (e.g., `avrodataproviderintro`, not `AvroDataProviderintro`) | N/A | No new module, separated source, QPP class, build registration or layout. Existing WSDL remains modern and registered. |
| 6 | `%modern` in `.qm` file — no redundant `%new-style`, `%require-types`, `%strict-args`, `%enable-all-warnings` | N/A | No new module, separated source, QPP class, build registration or layout. Existing WSDL remains modern and registered. |
| 7 | No parse directives (`%requires`, `%modern`, `%new-style`) in separated `.qc` files (check OUTSIDE of `@code` blocks only) | N/A | No new module, separated source, QPP class, build registration or layout. Existing WSDL remains modern and registered. |
| 8 | No `%include` usage (deprecated for modules) | N/A | No new module, separated source, QPP class, build registration or layout. Existing WSDL remains modern and registered. |
| 9 | Copyright 2026 on all new files | Pass | New Qore/Python/design/evidence files carry 2026 copyright; existing third-party fixtures are unchanged. |
| 10 | Directory layout: `.qm` inside `qlib/<ModuleName>/` directory (not at `qlib/<ModuleName>.qm` for multi-file modules) | N/A | No new module, separated source, QPP class, build registration or layout. Existing WSDL remains modern and registered. |
| 11 | No second `.qm` for the same module at `qlib/<ModuleName>.qm` | N/A | No new module, separated source, QPP class, build registration or layout. Existing WSDL remains modern and registered. |
| 12 | `ns=Qore::XX` matches the QoreNamespace constructor path | N/A | No new module, separated source, QPP class, build registration or layout. Existing WSDL remains modern and registered. |
| 13 | `%modern` directive present | Pass | Both new suites and the binding worker use modern parsing; the updated fixed-value suite remains modern. |
| 14 | Executable permission set (`chmod +x`) | Pass | Both new qtests, the .qr worker and Python unittest are executable. |
| 15 | Uses %prepend-module-path  before %requires for in-repo modules (Qore and Qore modules only; not Qorus) | Pass | Local qlib is prepended before relative WSDL/SoapClient/SoapHandler/SoapDataProvider requirements; local Debug XML is selected through QORE_MODULE_DIR. |
| 16 | External module dependencies use `%try-module` — except modules delivered with the project itself (Qore ex: DataProvider, ConnectionProvider, QUnit, etc.) which use hard `%requires` | Pass | External json uses %try-module with an explicit dependency error. XML is delivered by this project; other hard requirements are core Qore modules. |
| 17 | No filesystem operations (fopen, open, creat, unlink, remove, rename, mkdir, rmdir, stat, chmod) without sandbox checks | N/A | No C++/QPP changes. Qore provides loop cancellation and exception-safe scope cleanup; no new production blocking operation. |
| 18 | No network operations (connect, bind, socket, getaddrinfo, gethostbyname) without sandbox checks | N/A | No C++/QPP changes. Qore provides loop cancellation and exception-safe scope cleanup; no new production blocking operation. |
| 19 | If filesystem/network ops exist, verify `QoreSandboxManagerHelper` usage | N/A | No C++/QPP changes. Qore provides loop cancellation and exception-safe scope cleanup; no new production blocking operation. |
| 20 | No `File::`, `Dir::`, `Socket::`, `HTTPClient::` usage without justification | Pass | No production I/O added. Tests read committed fixtures and use loopback HTTP with bounded calls, queue deadlines and on_exit cleanup. |
| 21 | All `for`/`while` loops that could iterate >100 times have `qore_check_cancel()` checks | N/A | No C++/QPP changes. Qore provides loop cancellation and exception-safe scope cleanup; no new production blocking operation. |
| 22 | Uses `qore_check_cancel()` (NOT deprecated `qore_check_io_interrupt()`) | N/A | No C++/QPP changes. Qore provides loop cancellation and exception-safe scope cleanup; no new production blocking operation. |
| 23 | Check frequency: every 100 iterations for tight loops, every 10 for expensive iterations | N/A | No C++/QPP changes. Qore provides loop cancellation and exception-safe scope cleanup; no new production blocking operation. |
| 24 | No blocking operations without cancellation support | N/A | No C++/QPP changes. Qore provides loop cancellation and exception-safe scope cleanup; no new production blocking operation. |
| 25 | Every action has `display_name`, `short_desc` (plain text, <80 chars), `desc` (markdown) | N/A | This adds a generic schema value provider, not an app/action, request/response DTO, new field, factory, search provider, secret, logo or JNI dependency. Existing API registrations remain unchanged. |
| 26 | Every action has `options` populated via `getActionOptionFromFields()` — without this, the action shows an empty, unusable form | N/A | This adds a generic schema value provider, not an app/action, request/response DTO, new field, factory, search provider, secret, logo or JNI dependency. Existing API registrations remain unchanged. |
| 27 | Every action has `output_type` set to a typed data type constant (e.g., `MyResponseDataType`) — not omitted | N/A | This adds a generic schema value provider, not an app/action, request/response DTO, new field, factory, search provider, secret, logo or JNI dependency. Existing API registrations remain unchanged. |
| 28 | DPAT_API actions: provider has `"supports_request": True` and implements `doRequestImpl()` | N/A | This adds a generic schema value provider, not an app/action, request/response DTO, new field, factory, search provider, secret, logo or JNI dependency. Existing API registrations remain unchanged. |
| 29 | DPAT_FIND actions: every option exists in `SearchOptions`, `getRecordTypeImpl()` returns `*hash<string, AbstractDataField>` | N/A | This adds a generic schema value provider, not an app/action, request/response DTO, new field, factory, search provider, secret, logo or JNI dependency. Existing API registrations remain unchanged. |
| 30 | Scheme-based apps (with `"scheme"` in registerApp): actions use `"path"` and do NOT use `"cls"` — having both `scheme` and `cls` causes a runtime error | N/A | This adds a generic schema value provider, not an app/action, request/response DTO, new field, factory, search provider, secret, logo or JNI dependency. Existing API registrations remain unchanged. |
| 31 | Single-key hash slices use trailing comma: `Fields{"key",}` (without trailing comma, `Fields{"key"}` returns the value, not a hash) | N/A | This adds a generic schema value provider, not an app/action, request/response DTO, new field, factory, search provider, secret, logo or JNI dependency. Existing API registrations remain unchanged. |
| 32 | **Typed data type classes exist** for request and response types — inherit `HashDataType`, have `const Fields` hash, call `addQoreFields(Fields)` in constructor, export public constant at bottom (e.g., `public const MyDataType = new MyDataType();`) | N/A | This adds a generic schema value provider, not an app/action, request/response DTO, new field, factory, search provider, secret, logo or JNI dependency. Existing API registrations remain unchanged. |
| 33 | Request/input types use `public` Fields (enables `ClassName::Fields` in action registration) | N/A | This adds a generic schema value provider, not an app/action, request/response DTO, new field, factory, search provider, secret, logo or JNI dependency. Existing API registrations remain unchanged. |
| 34 | Response/output types use `private` Fields | N/A | This adds a generic schema value provider, not an app/action, request/response DTO, new field, factory, search provider, secret, logo or JNI dependency. Existing API registrations remain unchanged. |
| 35 | Each field in data types has `display_name`, `type`, and `desc` (markdown-formatted) | N/A | This adds a generic schema value provider, not an app/action, request/response DTO, new field, factory, search provider, secret, logo or JNI dependency. Existing API registrations remain unchanged. |
| 36 | Input fields have `example_value` where useful (string fields, endpoint URIs, SQL queries, etc.) | N/A | This adds a generic schema value provider, not an app/action, request/response DTO, new field, factory, search provider, secret, logo or JNI dependency. Existing API registrations remain unchanged. |
| 37 | Fields with finite allowed values use `allowed_values` with `AllowedValueInfo` containing both `value` and `display_name` (Title Case, human-readable) — never bare values, never described only in text | Pass | Existing choice comparators remain in XsdNativeTypeDataField; its unwrapping now reaches default native values. Native/fixed provider regression suites pass without relaxing allowed-value checks. |
| 38 | Password/secret fields have `"sensitive": True` | N/A | This adds a generic schema value provider, not an app/action, request/response DTO, new field, factory, search provider, secret, logo or JNI dependency. Existing API registrations remain unchanged. |
| 39 | `groups` uses `AppGroup` enum values from `qlib/DataProvider/AppGroup.qc` | N/A | This adds a generic schema value provider, not an app/action, request/response DTO, new field, factory, search provider, secret, logo or JNI dependency. Existing API registrations remain unchanged. |
| 40 | App `logo` stored as separate file, loaded at module level in `Priv` namespace | N/A | This adds a generic schema value provider, not an app/action, request/response DTO, new field, factory, search provider, secret, logo or JNI dependency. Existing API registrations remain unchanged. |
| 41 | App `desc` uses markdown: bullet list of capabilities, links to project website, business-language explanation of value | N/A | This adds a generic schema value provider, not an app/action, request/response DTO, new field, factory, search provider, secret, logo or JNI dependency. Existing API registrations remain unchanged. |
| 42 | `display_name` is user-friendly ("Apache Avro" not "avro") | N/A | This adds a generic schema value provider, not an app/action, request/response DTO, new field, factory, search provider, secret, logo or JNI dependency. Existing API registrations remain unchanged. |
| 43 | `short_desc` is plain text, under 80 chars, single sentence — no markdown | N/A | This adds a generic schema value provider, not an app/action, request/response DTO, new field, factory, search provider, secret, logo or JNI dependency. Existing API registrations remain unchanged. |
| 44 | `desc` uses markdown: backticks for code/field refs (`` `field_name` ``, `` `True` ``, `` `pdf` ``), `\n\n` for paragraphs, `- ` bullet lists for enumerations, `**bold**` for caveats | Pass | The new provider description uses backticks for XsdDefaultValue and describes the native/empty alternatives in complete sentences. |
| 45 | Descriptions use plain business language relating to common challenges — not just technical "what" but "why" and "when to use" | Pass | Provider and public carrier documentation explain retaining an empty partner status and avoiding unintended explicit text; design examples include an order quantity. |
| 46 | No bare `True`/`False`/`NOTHING` — must be backtick-wrapped in `desc` | Pass | No bare True/False/NOTHING is added to provider descriptions. |
| 47 | No bare field/option names in prose — must use backticks | Pass | The only new API identifier in the provider description is backtick-wrapped. |
| 48 | Long descriptions (>500 chars) use bold section headers and bullet lists | N/A | This adds a generic schema value provider, not an app/action, request/response DTO, new field, factory, search provider, secret, logo or JNI dependency. Existing API registrations remain unchanged. |
| 49 | **Factory registration in Qore repo**: every factory name registered in `qlib/DataProvider/DataProvider.qc` → `FactoryMap` (without this, module loads but doesn't appear in Qorus apps) | N/A | This adds a generic schema value provider, not an app/action, request/response DTO, new field, factory, search provider, secret, logo or JNI dependency. Existing API registrations remain unchanged. |
| 50 | **`getRecordTypeImpl()` signature**: must be `private *hash<string, AbstractDataField> getRecordTypeImpl(*hash<auto> search_options)` — NOT returning `*AbstractDataProviderType` | N/A | This adds a generic schema value provider, not an app/action, request/response DTO, new field, factory, search provider, secret, logo or JNI dependency. Existing API registrations remain unchanged. |
| 51 | **Dependency JARs committed** (for JNI modules): JAR files in `qlib/*/jar/` may be gitignored — use `git add -f` to ensure they're tracked, otherwise CI compilation fails | N/A | This adds a generic schema value provider, not an app/action, request/response DTO, new field, factory, search provider, secret, logo or JNI dependency. Existing API registrations remain unchanged. |
| 52 | JAR install rules in CMakeLists.txt for all dependency JARs | N/A | This adds a generic schema value provider, not an app/action, request/response DTO, new field, factory, search provider, secret, logo or JNI dependency. Existing API registrations remain unchanged. |
| 53 | **No workarounds**: No TODOs, FIXMEs, stubs, or partially-implemented features | Pass | Canonical default application and checked empty serialization are implemented with the approved interpretation. No fixture-name branch, skip or workaround. The independent NOTATION gap is explicitly routed to required P5 follow-up. |
| 54 | **Exception safety**: C++ uses `ReferenceHolder` for Qore allocations, `std::unique_ptr` for C++ allocations, `*xsink` checked after every fallible operation | Pass | Only local copied values and scoped lexical/identity contexts are changed. Actual serialization validates attributes and bindings before empty emission. Custom errors and program interruptions restore scopes and preserve caller values, with successful same-model recovery in four modes and AOT. |
| 55 | **Thread safety**: All mutable shared state protected by `std::lock_guard<std::mutex>` or documented as immutable-after-construction | Pass | New carriers have no mutators; schema/provider references are finalized before shared HTTP use. Per-call namespace maps and thread-local scoped captures do not mutate declaration metadata. Provider variants copy before modification. |
| 56 | **Type safety**: Strongly-typed `code<return(args)>` instead of untyped `code`; `static_cast` instead of C casts; typed hashdecls for results; enums where appropriate | Pass | Constraints/results use existing typed hashdecls; classes and provider fields have declared types. Polymorphic native values require auto. Callbacks use code signatures; no native pointer casts are introduced. |
| 57 | **Performance**: No O(n²) where O(n) is possible; no unnecessary copies; coordinate descent uses incremental residuals not full matrix multiply | Pass | Empty-content classification and removal scan each value field once. Canonical assessment is limited to defaulted values, and existing identity/copy machinery is reused. No quadratic search or unbounded new traversal. |
| 58 | **Error handling**: All inputs validated (dimensions, empty data, unfitted models); C++ I/O handles EAGAIN/EINTR if applicable | Pass | Tests reject missing/invalid attributes, character/child content, wrong actual-type patterns, invalid fixed values, malformed serialized state and unconstrained receivers. Optional absence/nil remain distinct; cancellation/custom errors retain their categories. |
| 59 | **Documentation**: Doxygen `@param`, `@return`, `@throw` on all public methods; `@par Example` with realistic business scenarios; `@note` for important caveats | Pass | New public methods document parameters, results and relevant exceptions. Module and native release notes, README, examples and durable design describe legacy/opt-in behavior and the SoapDataProvider API boundary. Both affected Doxygen targets build cleanly. |
| 60 | **QPP flags**: `[flags=CONSTANT]` on methods that never throw; `[flags=RET_VALUE_ONLY]` on methods that throw but have no side effects | N/A | No C++/QPP changes. Qore provides loop cancellation and exception-safe scope cleanup; no new production blocking operation. |
| 61 | **Security**: No user-controlled format strings; no buffer overflows; bounds checking on array indices; no credentials in code | Pass | No new production I/O, credentials, uncontrolled format string or buffer operation. Test subprocesses and HTTP calls have bounded deadlines. |
| 62 | **Correctness**: Algorithms verified against reference implementations; edge cases tested (empty data, single sample, all-zero features) | Pass | All 165 Qore suites and 18 supplemental runs pass. New suites have 1077 and 100 assertions; fixed regression has 177. The 3200-record matrix checks 1760 outputs against pinned Xerces with 120 explicit calendar differences. Both corpus modes retain all verdict/count/stage records; all eight changed XML payloads match the intended default behavior. |

Result: 20 Pass, 42 N/A, zero Fail.
