# P5-15 generic XML values audit

Copyright (C) 2026 Qore Technologies, s.r.o.

2026-09-12, parent `3d8e57d`. Applied the complete audit-changes skill and all
five referenced module structure, sandboxing, cancellation and provider guides
to the final generic conversion/provider changes, tests and documentation.
The five guide hashes are unchanged from the preceding full read and are recorded
in the validation inventory. Every checklist item is assessed below.

The 135-suite source gate passes 1,319 cases/65,233 reported assertions. Generic
unit/HTTP source modes and default AOT pass, as do independent value matrices,
documentation and the example. Both generic suites pass Valgrind with zero
errors/lost blocks. The known system SSSD warning retains P9 environment ownership.

An expanded existing QName AOT consumer check exposes excessive native stack use
at 128 levels. The unchanged parent reproduces it, including with full optimization.
It is an independent P9 runtime finding, retained as a failure in the inventory;
no test depth, stack limit or compiler default was changed. Independent
nil/default/fixed findings remain assigned to the next P5-16 increment. Routing
these findings follows the explicit execution-prompt rule. This audit closes the
current generic increment only; P5 and P6-P9 acceptance remain open.

[Evidence](../generic-values-evidence.md) and
[validation inventory](../P5-15-validation.json) record requirements and commands.

| # | Check | Status | Evidence |
| --- | --- | --- | --- |
| 1 | Entry exists in `doxygen/lang/120_modules.dox.tmpl` (for modules in the Qore repo; N/A for external module repos) | N/A | No new module entry point, separated file or directory layout. Existing WSDL CMake/QMOD/install registrations remain applicable. |
| 2 | Entry exists in `doxygen/lang/900_release_notes.dox.tmpl` (for modules in the Qore repo; external modules have release notes in their .qm) | Pass | WSDL 0.5.8 release notes, README and implemented design document describe generic XML preservation, provider validation and exact numeric inference. |
| 3 | `qore_user_module()` or `qore_external_user_module()` call in `CMakeLists.txt` | N/A | No new module entry point, separated file or directory layout. Existing WSDL CMake/QMOD/install registrations remain applicable. |
| 4 | Module added to QMOD list in `CMakeLists.txt` | N/A | No new module entry point, separated file or directory layout. Existing WSDL CMake/QMOD/install registrations remain applicable. |
| 5 | `.qm` file has `@section <lowercasemodname>intro` as first doc section — **must be all lowercase** (e.g., `avrodataproviderintro`, not `AvroDataProviderintro`) | Pass | Existing wsdlintro and %modern remain. No redundant directives or %include introduced. |
| 6 | `%modern` in `.qm` file — no redundant `%new-style`, `%require-types`, `%strict-args`, `%enable-all-warnings` | Pass | Existing wsdlintro and %modern remain. No redundant directives or %include introduced. |
| 7 | No parse directives (`%requires`, `%modern`, `%new-style`) in separated `.qc` files (check OUTSIDE of `@code` blocks only) | N/A | No new module entry point, separated file or directory layout. Existing WSDL CMake/QMOD/install registrations remain applicable. |
| 8 | No `%include` usage (deprecated for modules) | Pass | Existing wsdlintro and %modern remain. No redundant directives or %include introduced. |
| 9 | Copyright 2026 on all new files | Pass | All new authored tests, Python worker/matrix, design, evidence and audit carry 2026 copyright notices; JSON inventory has a copyright field. |
| 10 | Directory layout: `.qm` inside `qlib/<ModuleName>/` directory (not at `qlib/<ModuleName>.qm` for multi-file modules) | N/A | No new module entry point, separated file or directory layout. Existing WSDL CMake/QMOD/install registrations remain applicable. |
| 11 | No second `.qm` for the same module at `qlib/<ModuleName>.qm` | N/A | No new module entry point, separated file or directory layout. Existing WSDL CMake/QMOD/install registrations remain applicable. |
| 12 | `ns=Qore::XX` matches the QoreNamespace constructor path | N/A | No C++/QPP source, native I/O, native loop or QPP flags change. The native XML module hash is unchanged. |
| 13 | `%modern` directive present | Pass | Both executable modern qtests and worker prepend local module paths and use relative WSDL/consumer requires. Same-repository xml and core modules have hard requires; external json is guarded with a clear missing-dependency error. |
| 14 | Executable permission set (`chmod +x`) | Pass | Both executable modern qtests and worker prepend local module paths and use relative WSDL/consumer requires. Same-repository xml and core modules have hard requires; external json is guarded with a clear missing-dependency error. |
| 15 | Uses %prepend-module-path  before %requires for in-repo modules (Qore and Qore modules only; not Qorus) | Pass | Both executable modern qtests and worker prepend local module paths and use relative WSDL/consumer requires. Same-repository xml and core modules have hard requires; external json is guarded with a clear missing-dependency error. |
| 16 | External module dependencies use `%try-module` — except modules delivered with the project itself (Qore ex: DataProvider, ConnectionProvider, QUnit, etc.) which use hard `%requires` | Pass | Both executable modern qtests and worker prepend local module paths and use relative WSDL/consumer requires. Same-repository xml and core modules have hard requires; external json is guarded with a clear missing-dependency error. |
| 17 | No filesystem operations (fopen, open, creat, unlink, remove, rename, mkdir, rmdir, stat, chmod) without sandbox checks | N/A | No C++/QPP source, native I/O, native loop or QPP flags change. The native XML module hash is unchanged. |
| 18 | No network operations (connect, bind, socket, getaddrinfo, gethostbyname) without sandbox checks | N/A | No C++/QPP source, native I/O, native loop or QPP flags change. The native XML module hash is unchanged. |
| 19 | If filesystem/network ops exist, verify `QoreSandboxManagerHelper` usage | N/A | No C++/QPP source, native I/O, native loop or QPP flags change. The native XML module hash is unchanged. |
| 20 | No `File::`, `Dir::`, `Socket::`, `HTTPClient::` usage without justification | Pass | Production conversion adds no I/O. The offline worker reads explicit manifests; HTTP tests use existing core sandboxed APIs and loopback listeners with deterministic cleanup. |
| 21 | All `for`/`while` loops that could iterate >100 times have `qore_check_cancel()` checks | N/A | No C++/QPP source, native I/O, native loop or QPP flags change. The native XML module hash is unchanged. |
| 22 | Uses `qore_check_cancel()` (NOT deprecated `qore_check_io_interrupt()`) | N/A | No C++/QPP source, native I/O, native loop or QPP flags change. The native XML module hash is unchanged. |
| 23 | Check frequency: every 100 iterations for tight loops, every 10 for expensive iterations | N/A | No C++/QPP source, native I/O, native loop or QPP flags change. The native XML module hash is unchanged. |
| 24 | No blocking operations without cancellation support | Pass | No new blocking production operation. Qore loops retain runtime cancellation; scoped cancellation, namespace restoration and subsequent SOAP serialization pass. |
| 25 | Every action has `display_name`, `short_desc` (plain text, <80 chars), `desc` (markdown) | N/A | No action/app registration, fixed request/response schema, secrets, FactoryMap/getRecordTypeImpl changes, long action description or JNI dependency. This increment adds a schema-component datatype. |
| 26 | Every action has `options` populated via `getActionOptionFromFields()` — without this, the action shows an empty, unusable form | N/A | No action/app registration, fixed request/response schema, secrets, FactoryMap/getRecordTypeImpl changes, long action description or JNI dependency. This increment adds a schema-component datatype. |
| 27 | Every action has `output_type` set to a typed data type constant (e.g., `MyResponseDataType`) — not omitted | N/A | No action/app registration, fixed request/response schema, secrets, FactoryMap/getRecordTypeImpl changes, long action description or JNI dependency. This increment adds a schema-component datatype. |
| 28 | DPAT_API actions: provider has `"supports_request": True` and implements `doRequestImpl()` | N/A | No action/app registration, fixed request/response schema, secrets, FactoryMap/getRecordTypeImpl changes, long action description or JNI dependency. This increment adds a schema-component datatype. |
| 29 | DPAT_FIND actions: every option exists in `SearchOptions`, `getRecordTypeImpl()` returns `*hash<string, AbstractDataField>` | N/A | No action/app registration, fixed request/response schema, secrets, FactoryMap/getRecordTypeImpl changes, long action description or JNI dependency. This increment adds a schema-component datatype. |
| 30 | Scheme-based apps (with `"scheme"` in registerApp): actions use `"path"` and do NOT use `"cls"` — having both `scheme` and `cls` causes a runtime error | N/A | No action/app registration, fixed request/response schema, secrets, FactoryMap/getRecordTypeImpl changes, long action description or JNI dependency. This increment adds a schema-component datatype. |
| 31 | Single-key hash slices use trailing comma: `Fields{"key",}` (without trailing comma, `Fields{"key"}` returns the value, not a hash) | N/A | No action/app registration, fixed request/response schema, secrets, FactoryMap/getRecordTypeImpl changes, long action description or JNI dependency. This increment adds a schema-component datatype. |
| 32 | **Typed data type classes exist** for request and response types — inherit `HashDataType`, have `const Fields` hash, call `addQoreFields(Fields)` in constructor, export public constant at bottom (e.g., `public const MyDataType = new MyDataType();`) | Pass | XsdGenericDataType is a typed AbstractDataProviderType with scalar/XML-data categories. A fixed HashDataType Fields table cannot model generic XML; enclosing native/list/field/message providers use the shared schema-generated field path. |
| 33 | Request/input types use `public` Fields (enables `ClassName::Fields` in action registration) | N/A | No action/app registration, fixed request/response schema, secrets, FactoryMap/getRecordTypeImpl changes, long action description or JNI dependency. This increment adds a schema-component datatype. |
| 34 | Response/output types use `private` Fields | N/A | No action/app registration, fixed request/response schema, secrets, FactoryMap/getRecordTypeImpl changes, long action description or JNI dependency. This increment adds a schema-component datatype. |
| 35 | Each field in data types has `display_name`, `type`, and `desc` (markdown-formatted) | Pass | Existing generated field metadata remains intact. The new datatype prevents direct-type shortcuts from bypassing assessment; typed enclosing fields, native and list providers are tested. |
| 36 | Input fields have `example_value` where useful (string fields, endpoint URIs, SQL queries, etc.) | Pass | Mandatory generic providers return accepted sample text; optional variants return omission. Unit tests validate generated examples and the shipment documentation example executes successfully. |
| 37 | Fields with finite allowed values use `allowed_values` with `AllowedValueInfo` containing both `value` and `display_name` (Title Case, human-readable) — never bare values, never described only in text | N/A | Generic types have no finite allowed-value set; no allowed_values metadata is introduced or modified. |
| 38 | Password/secret fields have `"sensitive": True` | N/A | No action/app registration, fixed request/response schema, secrets, FactoryMap/getRecordTypeImpl changes, long action description or JNI dependency. This increment adds a schema-component datatype. |
| 39 | `groups` uses `AppGroup` enum values from `qlib/DataProvider/AppGroup.qc` | N/A | No action/app registration, fixed request/response schema, secrets, FactoryMap/getRecordTypeImpl changes, long action description or JNI dependency. This increment adds a schema-component datatype. |
| 40 | App `logo` stored as separate file, loaded at module level in `Priv` namespace | N/A | No action/app registration, fixed request/response schema, secrets, FactoryMap/getRecordTypeImpl changes, long action description or JNI dependency. This increment adds a schema-component datatype. |
| 41 | App `desc` uses markdown: bullet list of capabilities, links to project website, business-language explanation of value | N/A | No action/app registration, fixed request/response schema, secrets, FactoryMap/getRecordTypeImpl changes, long action description or JNI dependency. This increment adds a schema-component datatype. |
| 42 | `display_name` is user-friendly ("Apache Avro" not "avro") | N/A | No action/app registration, fixed request/response schema, secrets, FactoryMap/getRecordTypeImpl changes, long action description or JNI dependency. This increment adds a schema-component datatype. |
| 43 | `short_desc` is plain text, under 80 chars, single sentence — no markdown | N/A | No action/app registration, fixed request/response schema, secrets, FactoryMap/getRecordTypeImpl changes, long action description or JNI dependency. This increment adds a schema-component datatype. |
| 44 | `desc` uses markdown: backticks for code/field refs (`` `field_name` ``, `` `True` ``, `` `pdf` ``), `\n\n` for paragraphs, `- ` bullet lists for enumerations, `**bold**` for caveats | Pass | Descriptions explain XML content and complete-carrier usage in plain language, format XsdXmlValue as code and introduce no bare boolean/NOTHING or field-name prose. |
| 45 | Descriptions use plain business language relating to common challenges — not just technical "what" but "why" and "when to use" | Pass | Descriptions explain XML content and complete-carrier usage in plain language, format XsdXmlValue as code and introduce no bare boolean/NOTHING or field-name prose. |
| 46 | No bare `True`/`False`/`NOTHING` — must be backtick-wrapped in `desc` | Pass | Descriptions explain XML content and complete-carrier usage in plain language, format XsdXmlValue as code and introduce no bare boolean/NOTHING or field-name prose. |
| 47 | No bare field/option names in prose — must use backticks | Pass | Descriptions explain XML content and complete-carrier usage in plain language, format XsdXmlValue as code and introduce no bare boolean/NOTHING or field-name prose. |
| 48 | Long descriptions (>500 chars) use bold section headers and bullet lists | N/A | No action/app registration, fixed request/response schema, secrets, FactoryMap/getRecordTypeImpl changes, long action description or JNI dependency. This increment adds a schema-component datatype. |
| 49 | **Factory registration in Qore repo**: every factory name registered in `qlib/DataProvider/DataProvider.qc` → `FactoryMap` (without this, module loads but doesn't appear in Qorus apps) | N/A | No action/app registration, fixed request/response schema, secrets, FactoryMap/getRecordTypeImpl changes, long action description or JNI dependency. This increment adds a schema-component datatype. |
| 50 | **`getRecordTypeImpl()` signature**: must be `private *hash<string, AbstractDataField> getRecordTypeImpl(*hash<auto> search_options)` — NOT returning `*AbstractDataProviderType` | N/A | No action/app registration, fixed request/response schema, secrets, FactoryMap/getRecordTypeImpl changes, long action description or JNI dependency. This increment adds a schema-component datatype. |
| 51 | **Dependency JARs committed** (for JNI modules): JAR files in `qlib/*/jar/` may be gitignored — use `git add -f` to ensure they're tracked, otherwise CI compilation fails | N/A | No action/app registration, fixed request/response schema, secrets, FactoryMap/getRecordTypeImpl changes, long action description or JNI dependency. This increment adds a schema-component datatype. |
| 52 | JAR install rules in CMakeLists.txt for all dependency JARs | N/A | No action/app registration, fixed request/response schema, secrets, FactoryMap/getRecordTypeImpl changes, long action description or JNI dependency. This increment adds a schema-component datatype. |
| 53 | **No workarounds**: No TODOs, FIXMEs, stubs, or partially-implemented features | Pass | No fixture special case, TODO, stub, suppression, input rewriting or ownership workaround. Independent nil/default/fixed findings are assigned to P5-16 and the baseline-reproduced AOT stack finding to P9, retaining their failures per the execution prompt. |
| 54 | **Exception safety**: C++ uses `ReferenceHolder` for Qore allocations, `std::unique_ptr` for C++ allocations, `*xsink` checked after every fallible operation | Pass | Qore owns per-call temporaries; namespace/native-type scopes restore on exceptions. Invalid reconstruction, caller preservation, detached provider lifetime, cancellation/reuse and both new complete suites under Valgrind pass with zero errors/lost blocks on deployed Qore 783ecefc0. |
| 55 | **Thread safety**: All mutable shared state protected by `std::lock_guard<std::mutex>` or documented as immutable-after-construction | Pass | Completed schema registries are read-only during conversion. Output namespaces and lexical/type state are local/scoped; saved consumers and synchronized four-worker tests preserve independent partner bindings and unchanged shared maps. |
| 56 | **Type safety**: Strongly-typed `code<return(args)>` instead of untyped `code`; `static_cast` instead of C casts; typed hashdecls for results; enums where appropriate | Pass | Typed component/provider classes, namespace maps, typed callbacks and explicit heterogeneous list construction are used. XML-data hashes are widened locally before inserting metadata, with inferred-hash/caller preservation regressions. |
| 57 | **Performance**: No O(n²) where O(n) is possible; no unnecessary copies; coordinate descent uses incremental residuals not full matrix multiply | Pass | Known declarations use registry lookup. Declared anyType owns descendant assessment, removing repeated recursive traversal; a 32-level valid/invalid tree is tested. The independent pre-existing 128-level AOT native-frame limit is measured and remains a P9 runtime failure. |
| 58 | **Error handling**: All inputs validated (dimensions, empty data, unfitted models); C++ I/O handles EAGAIN/EINTR if applicable | Pass | Tests reject malformed XML metadata, invalid attribute/text/list shapes, unbound names, invalid known values and selected types, abstract types, missing required attributes and absent mandatory provider input with specific categories; recovery is covered. |
| 59 | **Documentation**: Doxygen `@param`, `@return`, `@throw` on all public methods; `@par Example` with realistic business scenarios; `@note` for important caveats | Pass | New public provider methods document parameters, results and errors as applicable. Realistic shipment example, representation caveats, implemented design and release notes are provided; qdx/Doxygen and the executable example pass. |
| 60 | **QPP flags**: `[flags=CONSTANT]` on methods that never throw; `[flags=RET_VALUE_ONLY]` on methods that throw but have no side effects | N/A | No C++/QPP source, native I/O, native loop or QPP flags change. The native XML module hash is unchanged. |
| 61 | **Security**: No user-controlled format strings; no buffer overflows; bounds checking on array indices; no credentials in code | Pass | No credentials, user-controlled format strings or new resource access. XML names, metadata and shapes are checked before output; same-process consumer state is scoped. No new suppression; the traced system SSSD warning retains P9 ownership. |
| 62 | **Correctness**: Algorithms verified against reference implementations; edge cases tested (empty data, single sample, all-zero features) | Pass | All 135 source suites pass, both new features pass source modes and default AOT, and the 112-document independent matrix compares validity plus typed/XML values. Both-version corpus improves twelve strict directions without regression. The unrelated QName default-AOT failure reproduces on the parent and is explicitly tracked outside this increment; no full P5/P9 acceptance claim. |

Result: 27 Pass, 35 N/A, 0 Fail. No failure in the P5-15 changes; the independent P9 AOT result remains open.
