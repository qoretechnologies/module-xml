# P1-01 audit: pinned corpus and offline catalog

Copyright (C) 2026 Qore Technologies, s.r.o.

Applied `/home/david/.codex/skills/audit-changes/SKILL.md` to the staged changes
on `develop`, parent `c81b2db`. Every checkbox from steps 2–6 is recorded below.
No implementation failures remain in this commit. This is a P1 component commit;
it does not close P1, adjudicate every source, or claim SOAP conformance.

## Step 1: changed-file scope and referenced design

Python test infrastructure, unmodified third-party corpus bytes, generated
inventory/current report, README/execution record, and module release notes.
No C++, Qore module, QPP, .qtest, DataProvider, or build/install code changes.

Consulted the skill's referenced documents in `/home/david/src/qore/git/qore/design/`:
`qore-module-structure.md` (layout, dependencies, documentation and test checklist),
`module-sandboxing-audit-guide.md` (filesystem/network/resource/cancellation audit
checklists), `cooperative-cancellation.md` (primary cancellation API and cleanup),
`data-provider-checklist.md` and `data-provider-development-guide.md` (registration
and applicability). Language-specific and provider checks are N/A for this diff,
not omitted. Python security, exception handling, resource bounds and cancellation
are reviewed explicitly below.

## Tests and evidence

- `python3 test/wsdl-interop/test_corpus.py -v`: 14 pass.
- `python3 test/wsdl-interop/test_survey.py -v`: 9 pass.
- `qore --enable-debug test/wsdl-interop.qtest`: 10 cases, 230 assertions pass.
- All seven SOAP/XSD/client/handler suites: 199 cases pass without warnings/errors.
  `soap.qtest` has three comparator assertions deliberately caught by negative tests;
  they are not failed test cases (lines 1448, 1461, 1466).
- Both SOAP versions, with and without catalog: complete diagnostic reports;
  no formerly successful worker stage lost. Full catalog result in `current-report.json`.
- Replayed `test_schema_failure_isolation` against parent survey source: assertion
  fails because the unrelated undefined-type error is replaced by the earlier
  missing-resource error. It passes with isolated parsers.
- `corpus.py --verify /tmp/module-xml-wsdl-survey`: all 4,191 originals verified.
- `git diff --cached --check` for authored files: clean. Upstream XSD whitespace is
  preserved byte-for-byte and excluded from authored-style checks.

## Full skill checklist

| Check | Status | Evidence |
| --- | --- | --- |
| Entry exists in `doxygen/lang/120_modules.dox.tmpl` (for modules in the Qore repo; N/A for external module repos) | N/A | No new or modified Qore module, QPP class, or .qtest file. Existing worker still uses %modern, local %prepend-module-path/%requires, and %try-module json; all Qore runs enable debugging. |
| Entry exists in `doxygen/lang/900_release_notes.dox.tmpl` (for modules in the Qore repo; external modules have release notes in their .qm) | N/A | No new or modified Qore module, QPP class, or .qtest file. Existing worker still uses %modern, local %prepend-module-path/%requires, and %try-module json; all Qore runs enable debugging. |
| `qore_user_module()` or `qore_external_user_module()` call in `CMakeLists.txt` | N/A | No new or modified Qore module, QPP class, or .qtest file. Existing worker still uses %modern, local %prepend-module-path/%requires, and %try-module json; all Qore runs enable debugging. |
| Module added to QMOD list in `CMakeLists.txt` | N/A | No new or modified Qore module, QPP class, or .qtest file. Existing worker still uses %modern, local %prepend-module-path/%requires, and %try-module json; all Qore runs enable debugging. |
| `.qm` file has `@section <lowercasemodname>intro` as first doc section — **must be all lowercase** (e.g., `avrodataproviderintro`, not `AvroDataProviderintro`) | N/A | No new or modified Qore module, QPP class, or .qtest file. Existing worker still uses %modern, local %prepend-module-path/%requires, and %try-module json; all Qore runs enable debugging. |
| `%modern` in `.qm` file — no redundant `%new-style`, `%require-types`, `%strict-args`, `%enable-all-warnings` | N/A | No new or modified Qore module, QPP class, or .qtest file. Existing worker still uses %modern, local %prepend-module-path/%requires, and %try-module json; all Qore runs enable debugging. |
| No parse directives (`%requires`, `%modern`, `%new-style`) in separated `.qc` files (check OUTSIDE of `@code` blocks only) | N/A | No new or modified Qore module, QPP class, or .qtest file. Existing worker still uses %modern, local %prepend-module-path/%requires, and %try-module json; all Qore runs enable debugging. |
| No `%include` usage (deprecated for modules) | N/A | No new or modified Qore module, QPP class, or .qtest file. Existing worker still uses %modern, local %prepend-module-path/%requires, and %try-module json; all Qore runs enable debugging. |
| Copyright 2026 on all new files | Pass | Authored Python/Markdown carries 2026 notices; vendored upstream bytes retain original notices; generated JSON is provenance/test data. |
| Directory layout: `.qm` inside `qlib/<ModuleName>/` directory (not at `qlib/<ModuleName>.qm` for multi-file modules) | N/A | No new or modified Qore module, QPP class, or .qtest file. Existing worker still uses %modern, local %prepend-module-path/%requires, and %try-module json; all Qore runs enable debugging. |
| No second `.qm` for the same module at `qlib/<ModuleName>.qm` | N/A | No new or modified Qore module, QPP class, or .qtest file. Existing worker still uses %modern, local %prepend-module-path/%requires, and %try-module json; all Qore runs enable debugging. |
| `ns=Qore::XX` matches the QoreNamespace constructor path | N/A | No new or modified Qore module, QPP class, or .qtest file. Existing worker still uses %modern, local %prepend-module-path/%requires, and %try-module json; all Qore runs enable debugging. |
| `%modern` directive present | N/A | No new or modified Qore module, QPP class, or .qtest file. Existing worker still uses %modern, local %prepend-module-path/%requires, and %try-module json; all Qore runs enable debugging. |
| Executable permission set (`chmod +x`) | N/A | No new or modified Qore module, QPP class, or .qtest file. Existing worker still uses %modern, local %prepend-module-path/%requires, and %try-module json; all Qore runs enable debugging. |
| Uses %prepend-module-path  before %requires for in-repo modules (Qore and Qore modules only; not Qorus) | N/A | No new or modified Qore module, QPP class, or .qtest file. Existing worker still uses %modern, local %prepend-module-path/%requires, and %try-module json; all Qore runs enable debugging. |
| External module dependencies use `%try-module` — except modules delivered with the project itself (Qore ex: DataProvider, ConnectionProvider, QUnit, etc.) which use hard `%requires` | N/A | No new or modified Qore module, QPP class, or .qtest file. Existing worker still uses %modern, local %prepend-module-path/%requires, and %try-module json; all Qore runs enable debugging. |
| No filesystem operations (fopen, open, creat, unlink, remove, rename, mkdir, rmdir, stat, chmod) without sandbox checks | N/A | No C++ or Qore implementation changes. Python offline resolver path confinement and network refusal reviewed under Security. |
| No network operations (connect, bind, socket, getaddrinfo, gethostbyname) without sandbox checks | N/A | No C++ or Qore implementation changes. Python offline resolver path confinement and network refusal reviewed under Security. |
| If filesystem/network ops exist, verify `QoreSandboxManagerHelper` usage | N/A | No C++ or Qore implementation changes. Python offline resolver path confinement and network refusal reviewed under Security. |
| No `File::`, `Dir::`, `Socket::`, `HTTPClient::` usage without justification | N/A | No C++ or Qore implementation changes. Python offline resolver path confinement and network refusal reviewed under Security. |
| All `for`/`while` loops that could iterate >100 times have `qore_check_cancel()` checks | N/A | No C++ loops or blocking operations changed. Python cancellation cleanup tested deterministically; no sleeps added. |
| Uses `qore_check_cancel()` (NOT deprecated `qore_check_io_interrupt()`) | N/A | No C++ loops or blocking operations changed. Python cancellation cleanup tested deterministically; no sleeps added. |
| Check frequency: every 100 iterations for tight loops, every 10 for expensive iterations | N/A | No C++ loops or blocking operations changed. Python cancellation cleanup tested deterministically; no sleeps added. |
| No blocking operations without cancellation support | N/A | No C++ loops or blocking operations changed. Python cancellation cleanup tested deterministically; no sleeps added. |
| Every action has `display_name`, `short_desc` (plain text, <80 chars), `desc` (markdown) | N/A | No DataProvider actions, types, app registrations, FactoryMap entries, JNI dependencies or install rules changed. |
| Every action has `options` populated via `getActionOptionFromFields()` — without this, the action shows an empty, unusable form | N/A | No DataProvider actions, types, app registrations, FactoryMap entries, JNI dependencies or install rules changed. |
| Every action has `output_type` set to a typed data type constant (e.g., `MyResponseDataType`) — not omitted | N/A | No DataProvider actions, types, app registrations, FactoryMap entries, JNI dependencies or install rules changed. |
| DPAT_API actions: provider has `"supports_request": True` and implements `doRequestImpl()` | N/A | No DataProvider actions, types, app registrations, FactoryMap entries, JNI dependencies or install rules changed. |
| DPAT_FIND actions: every option exists in `SearchOptions`, `getRecordTypeImpl()` returns `*hash<string, AbstractDataField>` | N/A | No DataProvider actions, types, app registrations, FactoryMap entries, JNI dependencies or install rules changed. |
| Scheme-based apps (with `"scheme"` in registerApp): actions use `"path"` and do NOT use `"cls"` — having both `scheme` and `cls` causes a runtime error | N/A | No DataProvider actions, types, app registrations, FactoryMap entries, JNI dependencies or install rules changed. |
| Single-key hash slices use trailing comma: `Fields{"key",}` (without trailing comma, `Fields{"key"}` returns the value, not a hash) | N/A | No DataProvider actions, types, app registrations, FactoryMap entries, JNI dependencies or install rules changed. |
| **Typed data type classes exist** for request and response types — inherit `HashDataType`, have `const Fields` hash, call `addQoreFields(Fields)` in constructor, export public constant at bottom (e.g., `public const MyDataType = new MyDataType();`) | N/A | No DataProvider actions, types, app registrations, FactoryMap entries, JNI dependencies or install rules changed. |
| Request/input types use `public` Fields (enables `ClassName::Fields` in action registration) | N/A | No DataProvider actions, types, app registrations, FactoryMap entries, JNI dependencies or install rules changed. |
| Response/output types use `private` Fields | N/A | No DataProvider actions, types, app registrations, FactoryMap entries, JNI dependencies or install rules changed. |
| Each field in data types has `display_name`, `type`, and `desc` (markdown-formatted) | N/A | No DataProvider actions, types, app registrations, FactoryMap entries, JNI dependencies or install rules changed. |
| Input fields have `example_value` where useful (string fields, endpoint URIs, SQL queries, etc.) | N/A | No DataProvider actions, types, app registrations, FactoryMap entries, JNI dependencies or install rules changed. |
| Fields with finite allowed values use `allowed_values` with `AllowedValueInfo` containing both `value` and `display_name` (Title Case, human-readable) — never bare values, never described only in text | N/A | No DataProvider actions, types, app registrations, FactoryMap entries, JNI dependencies or install rules changed. |
| Password/secret fields have `"sensitive": True` | N/A | No DataProvider actions, types, app registrations, FactoryMap entries, JNI dependencies or install rules changed. |
| `groups` uses `AppGroup` enum values from `qlib/DataProvider/AppGroup.qc` | N/A | No DataProvider actions, types, app registrations, FactoryMap entries, JNI dependencies or install rules changed. |
| App `logo` stored as separate file, loaded at module level in `Priv` namespace | N/A | No DataProvider actions, types, app registrations, FactoryMap entries, JNI dependencies or install rules changed. |
| App `desc` uses markdown: bullet list of capabilities, links to project website, business-language explanation of value | N/A | No DataProvider actions, types, app registrations, FactoryMap entries, JNI dependencies or install rules changed. |
| `display_name` is user-friendly ("Apache Avro" not "avro") | N/A | No DataProvider actions, types, app registrations, FactoryMap entries, JNI dependencies or install rules changed. |
| `short_desc` is plain text, under 80 chars, single sentence — no markdown | N/A | No DataProvider actions, types, app registrations, FactoryMap entries, JNI dependencies or install rules changed. |
| `desc` uses markdown: backticks for code/field refs (`` `field_name` ``, `` `True` ``, `` `pdf` ``), `\n\n` for paragraphs, `- ` bullet lists for enumerations, `**bold**` for caveats | N/A | No DataProvider actions, types, app registrations, FactoryMap entries, JNI dependencies or install rules changed. |
| Descriptions use plain business language relating to common challenges — not just technical "what" but "why" and "when to use" | N/A | No DataProvider actions, types, app registrations, FactoryMap entries, JNI dependencies or install rules changed. |
| No bare `True`/`False`/`NOTHING` — must be backtick-wrapped in `desc` | N/A | No DataProvider actions, types, app registrations, FactoryMap entries, JNI dependencies or install rules changed. |
| No bare field/option names in prose — must use backticks | N/A | No DataProvider actions, types, app registrations, FactoryMap entries, JNI dependencies or install rules changed. |
| Long descriptions (>500 chars) use bold section headers and bullet lists | N/A | No DataProvider actions, types, app registrations, FactoryMap entries, JNI dependencies or install rules changed. |
| **Factory registration in Qore repo**: every factory name registered in `qlib/DataProvider/DataProvider.qc` → `FactoryMap` (without this, module loads but doesn't appear in Qorus apps) | N/A | No DataProvider actions, types, app registrations, FactoryMap entries, JNI dependencies or install rules changed. |
| **`getRecordTypeImpl()` signature**: must be `private *hash<string, AbstractDataField> getRecordTypeImpl(*hash<auto> search_options)` — NOT returning `*AbstractDataProviderType` | N/A | No DataProvider actions, types, app registrations, FactoryMap entries, JNI dependencies or install rules changed. |
| **Dependency JARs committed** (for JNI modules): JAR files in `qlib/*/jar/` may be gitignored — use `git add -f` to ensure they're tracked, otherwise CI compilation fails | N/A | No DataProvider actions, types, app registrations, FactoryMap entries, JNI dependencies or install rules changed. |
| JAR install rules in CMakeLists.txt for all dependency JARs | N/A | No DataProvider actions, types, app registrations, FactoryMap entries, JNI dependencies or install rules changed. |
| **No workarounds**: No TODOs, FIXMEs, stubs, or partially-implemented features | Pass | No fixture substitutions, relaxation, skips, TODOs or stubs in authored code. Empty authoritative resource remains empty and fails parsing. P1 remains open. |
| **Exception safety**: C++ uses `ReferenceHolder` for Qore allocations, `std::unique_ptr` for C++ allocations, `*xsink` checked after every fallible operation | Pass | No C++ changes. Context-managed ZIPs and staging directories; injected OSError and KeyboardInterrupt tests verify cleanup without a published destination. |
| **Thread safety**: All mutable shared state protected by `std::lock_guard<std::mutex>` or documented as immutable-after-construction | Pass | Single-threaded utility; immutable catalog mappings and per-compilation/per-payload parsers. Destination ownership and no-concurrent-creator precondition documented. |
| **Type safety**: Strongly-typed `code<return(args)>` instead of untyped `code`; `static_cast` instead of C casts; typed hashdecls for results; enums where appropriate | Pass | Typed Python corpus API; manifest shapes, exact integer types, digests, URI and path constraints validated before use. No Qore code pointers or C casts changed. |
| **Performance**: No O(n²) where O(n) is possible; no unnecessary copies; coordinate descent uses incremental residuals not full matrix multiply | Pass | Linear member/hash verification; 10,000-file, 64 MiB archive/resource and 128 MiB aggregate bounds; shared URI cache. Entire extraction plus real-Qore integration completes within 60-second test deadline. |
| **Error handling**: All inputs validated (dimensions, empty data, unfitted models); C++ I/O handles EAGAIN/EINTR if applicable | Pass | Negative tests cover duplicate JSON/URI/ZIP entries, malformed JSON/manifest, missing files, hashes/sizes, path traversal/symlink escape, bad XML, ZIP member types and resource bounds. |
| **Documentation**: Doxygen `@param`, `@return`, `@throw` on all public methods; `@par Example` with realistic business scenarios; `@note` for important caveats | Pass | README documents commands, format/provenance, empty source, resource behavior and extraction ownership. Module release notes and execution record updated. No new installed public API requiring Doxygen method documentation. |
| **QPP flags**: `[flags=CONSTANT]` on methods that never throw; `[flags=RET_VALUE_ONLY]` on methods that throw but have no side effects | N/A | No QPP methods changed. |
| **Security**: No user-controlled format strings; no buffer overflows; bounds checking on array indices; no credentials in code | Pass | Verified files confined to roots; no extraction through symlinks, network fallback, shell interpolation, secrets or XML entity substitution. Every catalog byte checked before use. |
| **Correctness**: Algorithms verified against reference implementations; edge cases tested (empty data, single sample, all-zero features) | Pass | 14 corpus + 9 survey tests, 10 Qore interoperability cases and all 199 cases in seven affected suites pass. Both-version surveys retain every previously successful worker stage. Regression fails on old shared-parser code. |

## Reviewed file hashes

The audit and final execution-record bookkeeping are excluded from their own hash list.

- `docs/mainpage.doxygen.tmpl`: `80bedc94882bdf18fd4bcd6a820d5cc35bf6401f93ff78ee366298f5f2445d7f`
- `test/wsdl-interop/README.md`: `2b2e8edcd31648ea9304be1855b653c07bf5f72282a148c5211a501ecea7f456`
- `test/wsdl-interop/corpus.py`: `4f2ec803ed702ccdabbea7501ea98f1ffb4e814b58dda109278e7256344d689e`
- `test/wsdl-interop/corpus/catalog.json`: `d3aa2747adff53490f2128adb51e9f461a5538fbd58b4dee9c6452f1722c65dd`
- `test/wsdl-interop/corpus/imports/ChameleonIncluded.xsd`: `08458070791a8a279deab06342ef3f610e3ca3d9c5c7edcc5efad0dd3008835c`
- `test/wsdl-interop/corpus/imports/Imported.xsd`: `e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855`
- `test/wsdl-interop/corpus/imports/Included.xsd`: `d73b09b9a951753cfb015b0a3e4084bd496c9ed5f482690ee415ebe9d0e6b7a0`
- `test/wsdl-interop/corpus/imports/RelativeIncluded.xsd`: `13f4111f29444a9b1dd4fe3a80206cb1d7f8e0bf3b44e7c8a4760a0be561d547`
- `test/wsdl-interop/corpus/imports/soap11-encoding.xsd`: `b77d77ae19836e21914c676730ede7269bc4907496470c692dfef85eee0e34cc`
- `test/wsdl-interop/corpus/inventory.json`: `2f0cea2bffa83a46024c75bc830989afb876c7b61010e9076a49ae46dd497951`
- `test/wsdl-interop/corpus/w3c-databinding.zip`: `510b1528e5bdaee527c416524e6462c73f5e82b5237af4a4f7fef65904b90aca`
- `test/wsdl-interop/current-report.json`: `4d46d89b42fd135ac7cc7080c540311e3b597cfcb587341b9d13c246d09890f7`
- `test/wsdl-interop/survey.py`: `b887f4dee9b0619783963f3ff73086f5543c546f3875fea0d68f4a82283fdd49`
- `test/wsdl-interop/test_corpus.py`: `df2497cfc07e678443f2a8f2aab204e9ab4cc39e5024ba1ec1d42086cbca5d14`
