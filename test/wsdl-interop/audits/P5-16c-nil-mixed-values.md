# P5-16c nil and ordered mixed value audit

Copyright (C) 2026 Qore Technologies, s.r.o.

2026-09-12, parent `c587d09`. Applied all 62 checks from audit-changes and its five
referenced design guides to the final code, tests and documentation. Guide hashes
are recorded in the [inventory](../P5-16c-validation.json).

All 141 source suites pass: 1,362 cases and
66,046 reported assertions. Of 52 supplements, 51 pass, including every new test in four source modes and
default AOT, and both independent matrices. The unchanged 128-level QName AOT
case reproduces its documented baseline stack failure and remains assigned to P9. Both complete
new unit suites have zero Valgrind errors and zero lost blocks. Documentation and
two examples pass without warnings. The corrected SOAP test compares complete
namespace-normalized values; its fewer individual assertions reflect that change,
and deliberate assertion-failure negatives remain caught by assertThrows.

Initial and intermediate failing evidence is retained under
`/tmp/wsdl-p5-16-values-validation/`. The final source remained unchanged throughout
acceptance. A temporary Python runner named `corpus.py` initially shadowed the
repository import, so two AOT matrices did not execute. The runner was renamed,
nonempty test summaries were required, and both matrices then passed against
unchanged source/artifacts. Original unexecuted results remain in the inventory.
All discovered regressions in this increment were fixed. The independent
baseline QName AOT stack finding remains a P9 runtime failure. Instance default/fixed,
identity constraints and P6–P9 acceptance remain open; no waiver or new scope limit.

| # | Check | Status | Evidence |
| --- | --- | --- | --- |
| 1 | Entry exists in `doxygen/lang/120_modules.dox.tmpl` (for modules in the Qore repo; N/A for external module repos) | N/A | Existing WSDL entry point and build registration; no new module, separated file, duplicate entry point or directory layout. |
| 2 | Entry exists in `doxygen/lang/900_release_notes.dox.tmpl` (for modules in the Qore repo; external modules have release notes in their .qm) | Pass | WSDL 0.5.8 release notes and README describe nil carriers, mixed native values and schema/provider/HTTP consumers. |
| 3 | `qore_user_module()` or `qore_external_user_module()` call in `CMakeLists.txt` | N/A | Existing WSDL entry point and build registration; no new module, separated file, duplicate entry point or directory layout. |
| 4 | Module added to QMOD list in `CMakeLists.txt` | N/A | Existing WSDL entry point and build registration; no new module, separated file, duplicate entry point or directory layout. |
| 5 | `.qm` file has `@section <lowercasemodname>intro` as first doc section — **must be all lowercase** (e.g., `avrodataproviderintro`, not `AvroDataProviderintro`) | Pass | Existing lowercase wsdlintro and %modern retained; no redundant parse directives or deprecated includes added. |
| 6 | `%modern` in `.qm` file — no redundant `%new-style`, `%require-types`, `%strict-args`, `%enable-all-warnings` | Pass | Existing lowercase wsdlintro and %modern retained; no redundant parse directives or deprecated includes added. |
| 7 | No parse directives (`%requires`, `%modern`, `%new-style`) in separated `.qc` files (check OUTSIDE of `@code` blocks only) | N/A | Existing WSDL entry point and build registration; no new module, separated file, duplicate entry point or directory layout. |
| 8 | No `%include` usage (deprecated for modules) | Pass | Existing lowercase wsdlintro and %modern retained; no redundant parse directives or deprecated includes added. |
| 9 | Copyright 2026 on all new files | Pass | All new Qore/Python tests, design, evidence and audit files carry copyright 2026; JSON inventory includes the notice. |
| 10 | Directory layout: `.qm` inside `qlib/<ModuleName>/` directory (not at `qlib/<ModuleName>.qm` for multi-file modules) | N/A | Existing WSDL entry point and build registration; no new module, separated file, duplicate entry point or directory layout. |
| 11 | No second `.qm` for the same module at `qlib/<ModuleName>.qm` | N/A | Existing WSDL entry point and build registration; no new module, separated file, duplicate entry point or directory layout. |
| 12 | `ns=Qore::XX` matches the QoreNamespace constructor path | N/A | No C++/QPP source, native I/O, native loop or method flags changed; native XML and deployed Qore artifacts remain unchanged. |
| 13 | `%modern` directive present | Pass | Executable modern Qore tests prepend local qlib and import local modules by relative path. xml is delivered by this repository. Workers guard external json with %try-module and explicit missing-module failure. |
| 14 | Executable permission set (`chmod +x`) | Pass | Executable modern Qore tests prepend local qlib and import local modules by relative path. xml is delivered by this repository. Workers guard external json with %try-module and explicit missing-module failure. |
| 15 | Uses %prepend-module-path  before %requires for in-repo modules (Qore and Qore modules only; not Qorus) | Pass | Executable modern Qore tests prepend local qlib and import local modules by relative path. xml is delivered by this repository. Workers guard external json with %try-module and explicit missing-module failure. |
| 16 | External module dependencies use `%try-module` — except modules delivered with the project itself (Qore ex: DataProvider, ConnectionProvider, QUnit, etc.) which use hard `%requires` | Pass | Executable modern Qore tests prepend local qlib and import local modules by relative path. xml is delivered by this repository. Workers guard external json with %try-module and explicit missing-module failure. |
| 17 | No filesystem operations (fopen, open, creat, unlink, remove, rename, mkdir, rmdir, stat, chmod) without sandbox checks | N/A | No C++/QPP source, native I/O, native loop or method flags changed; native XML and deployed Qore artifacts remain unchanged. |
| 18 | No network operations (connect, bind, socket, getaddrinfo, gethostbyname) without sandbox checks | N/A | No C++/QPP source, native I/O, native loop or method flags changed; native XML and deployed Qore artifacts remain unchanged. |
| 19 | If filesystem/network ops exist, verify `QoreSandboxManagerHelper` usage | N/A | No C++/QPP source, native I/O, native loop or method flags changed; native XML and deployed Qore artifacts remain unchanged. |
| 20 | No `File::`, `Dir::`, `Socket::`, `HTTPClient::` usage without justification | Pass | Production conversion adds no I/O. Test HTTP listeners bind loopback on assigned ports; servers stop on exit. Offline workers read bounded temporary fixtures and use the pinned independent oracle. |
| 21 | All `for`/`while` loops that could iterate >100 times have `qore_check_cancel()` checks | N/A | No C++/QPP source, native I/O, native loop or method flags changed; native XML and deployed Qore artifacts remain unchanged. |
| 22 | Uses `qore_check_cancel()` (NOT deprecated `qore_check_io_interrupt()`) | N/A | No C++/QPP source, native I/O, native loop or method flags changed; native XML and deployed Qore artifacts remain unchanged. |
| 23 | Check frequency: every 100 iterations for tight loops, every 10 for expensive iterations | N/A | No C++/QPP source, native I/O, native loop or method flags changed; native XML and deployed Qore artifacts remain unchanged. |
| 24 | No blocking operations without cancellation support | Pass | Real SandboxManager interruption propagates unchanged. Deterministic Queue/Counter barriers, request completion and bounded deadlines cover concurrent consumers; no sleeps or polling. |
| 25 | Every action has `display_name`, `short_desc` (plain text, <80 chars), `desc` (markdown) | N/A | No action/app/factory registration, static request/response Fields API, secret, JNI/JAR dependency or long description. Existing provider integration and registration remain. |
| 26 | Every action has `options` populated via `getActionOptionFromFields()` — without this, the action shows an empty, unusable form | N/A | No action/app/factory registration, static request/response Fields API, secret, JNI/JAR dependency or long description. Existing provider integration and registration remain. |
| 27 | Every action has `output_type` set to a typed data type constant (e.g., `MyResponseDataType`) — not omitted | N/A | No action/app/factory registration, static request/response Fields API, secret, JNI/JAR dependency or long description. Existing provider integration and registration remain. |
| 28 | DPAT_API actions: provider has `"supports_request": True` and implements `doRequestImpl()` | N/A | No action/app/factory registration, static request/response Fields API, secret, JNI/JAR dependency or long description. Existing provider integration and registration remain. |
| 29 | DPAT_FIND actions: every option exists in `SearchOptions`, `getRecordTypeImpl()` returns `*hash<string, AbstractDataField>` | N/A | No action/app/factory registration, static request/response Fields API, secret, JNI/JAR dependency or long description. Existing provider integration and registration remain. |
| 30 | Scheme-based apps (with `"scheme"` in registerApp): actions use `"path"` and do NOT use `"cls"` — having both `scheme` and `cls` causes a runtime error | N/A | No action/app/factory registration, static request/response Fields API, secret, JNI/JAR dependency or long description. Existing provider integration and registration remain. |
| 31 | Single-key hash slices use trailing comma: `Fields{"key",}` (without trailing comma, `Fields{"key"}` returns the value, not a hash) | N/A | No action/app/factory registration, static request/response Fields API, secret, JNI/JAR dependency or long description. Existing provider integration and registration remain. |
| 32 | **Typed data type classes exist** for request and response types — inherit `HashDataType`, have `const Fields` hash, call `addQoreFields(Fields)` in constructor, export public constant at bottom (e.g., `public const MyDataType = new MyDataType();`) | Pass | Public XsdMixedContentDataType derives from HashDataType and creates typed fields from resolved schema declarations. XsdNillableDataType delegates the native provider and adds validated nil semantics; runtime schema shapes cannot use a constant Fields table. |
| 33 | Request/input types use `public` Fields (enables `ClassName::Fields` in action registration) | N/A | No action/app/factory registration, static request/response Fields API, secret, JNI/JAR dependency or long description. Existing provider integration and registration remain. |
| 34 | Response/output types use `private` Fields | N/A | No action/app/factory registration, static request/response Fields API, secret, JNI/JAR dependency or long description. Existing provider integration and registration remain. |
| 35 | Each field in data types has `display_name`, `type`, and `desc` (markdown-formatted) | Pass | New mixed child fields have display_name, type, short_desc and Markdown desc. Shared attribute fields retain their explicit metadata; nil union alternatives reflect effective mandatory/optional copies. |
| 36 | Input fields have `example_value` where useful (string fields, endpoint URIs, SQL queries, etc.) | Pass | Bounded mixed/native examples retain selected particle schedules, repeated groups/list-valued children and required native values; executable shipment/product examples pass. |
| 37 | Fields with finite allowed values use `allowed_values` with `AllowedValueInfo` containing both `value` and `display_name` (Title Case, human-readable) — never bare values, never described only in text | Pass | Existing typed field helpers retain AllowedValueInfo choices and names, including element choices for repeats. Explicit nil avoids scalar enumeration comparison while still checking its own declaration/attributes. |
| 38 | Password/secret fields have `"sensitive": True` | N/A | No action/app/factory registration, static request/response Fields API, secret, JNI/JAR dependency or long description. Existing provider integration and registration remain. |
| 39 | `groups` uses `AppGroup` enum values from `qlib/DataProvider/AppGroup.qc` | N/A | No action/app/factory registration, static request/response Fields API, secret, JNI/JAR dependency or long description. Existing provider integration and registration remain. |
| 40 | App `logo` stored as separate file, loaded at module level in `Priv` namespace | N/A | No action/app/factory registration, static request/response Fields API, secret, JNI/JAR dependency or long description. Existing provider integration and registration remain. |
| 41 | App `desc` uses markdown: bullet list of capabilities, links to project website, business-language explanation of value | N/A | No action/app/factory registration, static request/response Fields API, secret, JNI/JAR dependency or long description. Existing provider integration and registration remain. |
| 42 | `display_name` is user-friendly ("Apache Avro" not "avro") | N/A | No action/app/factory registration, static request/response Fields API, secret, JNI/JAR dependency or long description. Existing provider integration and registration remain. |
| 43 | `short_desc` is plain text, under 80 chars, single sentence — no markdown | Pass | Short descriptions are plain text under 80 characters. New descriptions explain ordered product/shipment content and preserve code/field/value names in backticks. |
| 44 | `desc` uses markdown: backticks for code/field refs (`` `field_name` ``, `` `True` ``, `` `pdf` ``), `\n\n` for paragraphs, `- ` bullet lists for enumerations, `**bold**` for caveats | Pass | Short descriptions are plain text under 80 characters. New descriptions explain ordered product/shipment content and preserve code/field/value names in backticks. |
| 45 | Descriptions use plain business language relating to common challenges — not just technical "what" but "why" and "when to use" | Pass | Short descriptions are plain text under 80 characters. New descriptions explain ordered product/shipment content and preserve code/field/value names in backticks. |
| 46 | No bare `True`/`False`/`NOTHING` — must be backtick-wrapped in `desc` | Pass | Short descriptions are plain text under 80 characters. New descriptions explain ordered product/shipment content and preserve code/field/value names in backticks. |
| 47 | No bare field/option names in prose — must use backticks | Pass | Short descriptions are plain text under 80 characters. New descriptions explain ordered product/shipment content and preserve code/field/value names in backticks. |
| 48 | Long descriptions (>500 chars) use bold section headers and bullet lists | N/A | No action/app/factory registration, static request/response Fields API, secret, JNI/JAR dependency or long description. Existing provider integration and registration remain. |
| 49 | **Factory registration in Qore repo**: every factory name registered in `qlib/DataProvider/DataProvider.qc` → `FactoryMap` (without this, module loads but doesn't appear in Qorus apps) | N/A | No action/app/factory registration, static request/response Fields API, secret, JNI/JAR dependency or long description. Existing provider integration and registration remain. |
| 50 | **`getRecordTypeImpl()` signature**: must be `private *hash<string, AbstractDataField> getRecordTypeImpl(*hash<auto> search_options)` — NOT returning `*AbstractDataProviderType` | N/A | No action/app/factory registration, static request/response Fields API, secret, JNI/JAR dependency or long description. Existing provider integration and registration remain. |
| 51 | **Dependency JARs committed** (for JNI modules): JAR files in `qlib/*/jar/` may be gitignored — use `git add -f` to ensure they're tracked, otherwise CI compilation fails | N/A | No action/app/factory registration, static request/response Fields API, secret, JNI/JAR dependency or long description. Existing provider integration and registration remain. |
| 52 | JAR install rules in CMakeLists.txt for all dependency JARs | N/A | No action/app/factory registration, static request/response Fields API, secret, JNI/JAR dependency or long description. Existing provider integration and registration remain. |
| 53 | **No workarounds**: No TODOs, FIXMEs, stubs, or partially-implemented features | Pass | Root causes fixed at receiving-element assessment, ordered mixed conversion and provider boundaries. No skip, fixture special case or coercion through XML serialization/parsing. Later default/fixed and identity work remains explicitly required by P5. |
| 54 | **Exception safety**: C++ uses `ReferenceHolder` for Qore allocations, `std::unique_ptr` for C++ allocations, `*xsink` checked after every fallible operation | Pass | Nil attributes and mixed results publish only after successful conversion. Copy-on-write carrier data and namespace scopes preserve callers across malformed graphs, conversion failure, cancellation and reconstructed providers. Both new unit suites have zero Valgrind errors/lost blocks. |
| 55 | **Thread safety**: All mutable shared state protected by `std::lock_guard<std::mutex>` or documented as immutable-after-construction | Pass | Completed schema/provider graphs are read-only during conversion. Namespace maps are per call. Barrier-controlled simultaneous messages retain distinct namespaces without changing their shared service. |
| 56 | **Type safety**: Strongly-typed `code<return(args)>` instead of untyped `code`; `static_cast` instead of C casts; typed hashdecls for results; enums where appropriate | Pass | Typed XsdMixedContentItem records, typed component/provider/container references and code<auto(hash<auto>, auto)> callbacks. Deliberately heterogeneous XML values use hash<auto> without inferred narrowing. |
| 57 | **Performance**: No O(n²) where O(n) is possible; no unnecessary copies; coordinate descent uses incremental residuals not full matrix multiply | Pass | Linear fragment/occurrence storage reuses the existing bounded particle matcher. QName emission suppresses generated redundant declarations while retaining explicit bindings. No schema reparsing or unbounded permutation search added. |
| 58 | **Error handling**: All inputs validated (dimensions, empty data, unfitted models); C++ I/O handles EAGAIN/EINTR if applicable | Pass | Exact categories cover nil permission/lexical forms/content/fixed exclusion, required attributes, complete occurrences, mixed metadata/ordering/list boundaries, provider choices/optionality and malformed reconstruction. Cancellation and unrelated exceptions retain their categories. |
| 59 | **Documentation**: Doxygen `@param`, `@return`, `@throw` on all public methods; `@par Example` with realistic business scenarios; `@note` for important caveats | Pass | New public methods document inputs, returns, exceptions and compatibility where applicable. Two implemented design guides include executable examples. qdx/Doxygen and both examples complete without warnings. |
| 60 | **QPP flags**: `[flags=CONSTANT]` on methods that never throw; `[flags=RET_VALUE_ONLY]` on methods that throw but have no side effects | N/A | No C++/QPP source, native I/O, native loop or method flags changed; native XML and deployed Qore artifacts remain unchanged. |
| 61 | **Security**: No user-controlled format strings; no buffer overflows; bounds checking on array indices; no credentials in code | Pass | Constant diagnostic formats, no credentials, no unchecked user-controlled indexes or new production raw I/O; QName and XML metadata validation occurs before output. |
| 62 | **Correctness**: Algorithms verified against reference implementations; edge cases tested (empty data, single sample, all-zero features) | Pass | All 141 source suites (1362 cases/66046 reported assertions), 51 supplements, pinned nil/mixed matrices and HTTP consumers pass. The separately baseline-proven P9 AOT stack result remains failed. Corpus preserves prior successes and closes 20 direction failures. SOAP comparator tests both local namespace equivalence and wrong bindings/attributes/values. |

Result: 29 Pass, 33 N/A, zero Fail.
