# P5-19g URI/language audit

Copyright (C) 2026 Qore Technologies, s.r.o.

Full audit-changes checklist applied to changes based on `0d3e378`. All five
referenced design guides were previously read and their hashes verified unchanged.
Scope: WSDL lexical conversion and provider metadata, tests, design, release notes
and acceptance evidence. No C++ changes, main-Qore mutations, installation or push.

| # | Check | Status | Evidence |
| --- | --- | --- | --- |
| 1 | Entry exists in `doxygen/lang/120_modules.dox.tmpl` (for modules in the Qore repo; N/A for external module repos) | N/A | No new module, separated source, QPP class, registration or layout change. |
| 2 | Entry exists in `doxygen/lang/900_release_notes.dox.tmpl` (for modules in the Qore repo; external modules have release notes in their .qm) | N/A | No new module, separated source, QPP class, registration or layout change. |
| 3 | `qore_user_module()` or `qore_external_user_module()` call in `CMakeLists.txt` | N/A | No new module, separated source, QPP class, registration or layout change. |
| 4 | Module added to QMOD list in `CMakeLists.txt` | N/A | No new module, separated source, QPP class, registration or layout change. |
| 5 | `.qm` file has `@section <lowercasemodname>intro` as first doc section — **must be all lowercase** (e.g., `avrodataproviderintro`, not `AvroDataProviderintro`) | N/A | No new module, separated source, QPP class, registration or layout change. |
| 6 | `%modern` in `.qm` file — no redundant `%new-style`, `%require-types`, `%strict-args`, `%enable-all-warnings` | N/A | No new module, separated source, QPP class, registration or layout change. |
| 7 | No parse directives (`%requires`, `%modern`, `%new-style`) in separated `.qc` files (check OUTSIDE of `@code` blocks only) | N/A | No new module, separated source, QPP class, registration or layout change. |
| 8 | No `%include` usage (deprecated for modules) | N/A | No new module, separated source, QPP class, registration or layout change. |
| 9 | Copyright 2026 on all new files | Pass | Authored Qore/Python tests, design and evidence carry 2026 copyright; README records authored JSON copyright. |
| 10 | Directory layout: `.qm` inside `qlib/<ModuleName>/` directory (not at `qlib/<ModuleName>.qm` for multi-file modules) | N/A | No new module, separated source, QPP class, registration or layout change. |
| 11 | No second `.qm` for the same module at `qlib/<ModuleName>.qm` | N/A | No new module, separated source, QPP class, registration or layout change. |
| 12 | `ns=Qore::XX` matches the QoreNamespace constructor path | N/A | No new module, separated source, QPP class, registration or layout change. |
| 13 | `%modern` directive present | Pass | All three new .qtest files use %modern. |
| 14 | Executable permission set (`chmod +x`) | Pass | New .qtest files and the Python checker are executable (755). |
| 15 | Uses %prepend-module-path  before %requires for in-repo modules (Qore and Qore modules only; not Qorus) | Pass | Tests prepend local qlib, require relative local WSDL/SoapClient/SoapHandler modules and select Debug XML with QORE_MODULE_DIR. |
| 16 | External module dependencies use `%try-module` — except modules delivered with the project itself (Qore ex: DataProvider, ConnectionProvider, QUnit, etc.) which use hard `%requires` | Pass | Project XML and core QUnit/HttpServer use hard requirements; external json uses %try-module with a missing-dependency error. |
| 17 | No filesystem operations (fopen, open, creat, unlink, remove, rename, mkdir, rmdir, stat, chmod) without sandbox checks | N/A | No C++ changes; the existing native URI API and its cancellation/ownership boundaries are unchanged. |
| 18 | No network operations (connect, bind, socket, getaddrinfo, gethostbyname) without sandbox checks | N/A | No C++ changes; the existing native URI API and its cancellation/ownership boundaries are unchanged. |
| 19 | If filesystem/network ops exist, verify `QoreSandboxManagerHelper` usage | N/A | No C++ changes; the existing native URI API and its cancellation/ownership boundaries are unchanged. |
| 20 | No `File::`, `Dir::`, `Socket::`, `HTTPClient::` usage without justification | Pass | Only test fixture reads and bounded local SOAP HTTP exchanges perform I/O; URI validation itself does not dereference references. |
| 21 | All `for`/`while` loops that could iterate >100 times have `qore_check_cancel()` checks | N/A | No C++ changes; the existing native URI API and its cancellation/ownership boundaries are unchanged. |
| 22 | Uses `qore_check_cancel()` (NOT deprecated `qore_check_io_interrupt()`) | N/A | No C++ changes; the existing native URI API and its cancellation/ownership boundaries are unchanged. |
| 23 | Check frequency: every 100 iterations for tight loops, every 10 for expensive iterations | N/A | No C++ changes; the existing native URI API and its cancellation/ownership boundaries are unchanged. |
| 24 | No blocking operations without cancellation support | N/A | No C++ changes; the existing native URI API and its cancellation/ownership boundaries are unchanged. |
| 25 | Every action has `display_name`, `short_desc` (plain text, <80 chars), `desc` (markdown) | N/A | No provider action, field definition, app, factory, JNI dependency or installation change; only existing scalar-provider validation metadata changes. |
| 26 | Every action has `options` populated via `getActionOptionFromFields()` — without this, the action shows an empty, unusable form | N/A | No provider action, field definition, app, factory, JNI dependency or installation change; only existing scalar-provider validation metadata changes. |
| 27 | Every action has `output_type` set to a typed data type constant (e.g., `MyResponseDataType`) — not omitted | N/A | No provider action, field definition, app, factory, JNI dependency or installation change; only existing scalar-provider validation metadata changes. |
| 28 | DPAT_API actions: provider has `"supports_request": True` and implements `doRequestImpl()` | N/A | No provider action, field definition, app, factory, JNI dependency or installation change; only existing scalar-provider validation metadata changes. |
| 29 | DPAT_FIND actions: every option exists in `SearchOptions`, `getRecordTypeImpl()` returns `*hash<string, AbstractDataField>` | N/A | No provider action, field definition, app, factory, JNI dependency or installation change; only existing scalar-provider validation metadata changes. |
| 30 | Scheme-based apps (with `"scheme"` in registerApp): actions use `"path"` and do NOT use `"cls"` — having both `scheme` and `cls` causes a runtime error | N/A | No provider action, field definition, app, factory, JNI dependency or installation change; only existing scalar-provider validation metadata changes. |
| 31 | Single-key hash slices use trailing comma: `Fields{"key",}` (without trailing comma, `Fields{"key"}` returns the value, not a hash) | N/A | No provider action, field definition, app, factory, JNI dependency or installation change; only existing scalar-provider validation metadata changes. |
| 32 | **Typed data type classes exist** for request and response types — inherit `HashDataType`, have `const Fields` hash, call `addQoreFields(Fields)` in constructor, export public constant at bottom (e.g., `public const MyDataType = new MyDataType();`) | N/A | No provider action, field definition, app, factory, JNI dependency or installation change; only existing scalar-provider validation metadata changes. |
| 33 | Request/input types use `public` Fields (enables `ClassName::Fields` in action registration) | N/A | No provider action, field definition, app, factory, JNI dependency or installation change; only existing scalar-provider validation metadata changes. |
| 34 | Response/output types use `private` Fields | N/A | No provider action, field definition, app, factory, JNI dependency or installation change; only existing scalar-provider validation metadata changes. |
| 35 | Each field in data types has `display_name`, `type`, and `desc` (markdown-formatted) | N/A | No provider action, field definition, app, factory, JNI dependency or installation change; only existing scalar-provider validation metadata changes. |
| 36 | Input fields have `example_value` where useful (string fields, endpoint URIs, SQL queries, etc.) | N/A | No provider action, field definition, app, factory, JNI dependency or installation change; only existing scalar-provider validation metadata changes. |
| 37 | Fields with finite allowed values use `allowed_values` with `AllowedValueInfo` containing both `value` and `display_name` (Title Case, human-readable) — never bare values, never described only in text | N/A | No provider action, field definition, app, factory, JNI dependency or installation change; only existing scalar-provider validation metadata changes. |
| 38 | Password/secret fields have `"sensitive": True` | N/A | No provider action, field definition, app, factory, JNI dependency or installation change; only existing scalar-provider validation metadata changes. |
| 39 | `groups` uses `AppGroup` enum values from `qlib/DataProvider/AppGroup.qc` | N/A | No provider action, field definition, app, factory, JNI dependency or installation change; only existing scalar-provider validation metadata changes. |
| 40 | App `logo` stored as separate file, loaded at module level in `Priv` namespace | N/A | No provider action, field definition, app, factory, JNI dependency or installation change; only existing scalar-provider validation metadata changes. |
| 41 | App `desc` uses markdown: bullet list of capabilities, links to project website, business-language explanation of value | N/A | No provider action, field definition, app, factory, JNI dependency or installation change; only existing scalar-provider validation metadata changes. |
| 42 | `display_name` is user-friendly ("Apache Avro" not "avro") | N/A | No provider action, field definition, app, factory, JNI dependency or installation change; only existing scalar-provider validation metadata changes. |
| 43 | `short_desc` is plain text, under 80 chars, single sentence — no markdown | N/A | No provider action, field definition, app, factory, JNI dependency or installation change; only existing scalar-provider validation metadata changes. |
| 44 | `desc` uses markdown: backticks for code/field refs (`` `field_name` ``, `` `True` ``, `` `pdf` ``), `\n\n` for paragraphs, `- ` bullet lists for enumerations, `**bold**` for caveats | N/A | No provider action, field definition, app, factory, JNI dependency or installation change; only existing scalar-provider validation metadata changes. |
| 45 | Descriptions use plain business language relating to common challenges — not just technical "what" but "why" and "when to use" | N/A | No provider action, field definition, app, factory, JNI dependency or installation change; only existing scalar-provider validation metadata changes. |
| 46 | No bare `True`/`False`/`NOTHING` — must be backtick-wrapped in `desc` | N/A | No provider action, field definition, app, factory, JNI dependency or installation change; only existing scalar-provider validation metadata changes. |
| 47 | No bare field/option names in prose — must use backticks | N/A | No provider action, field definition, app, factory, JNI dependency or installation change; only existing scalar-provider validation metadata changes. |
| 48 | Long descriptions (>500 chars) use bold section headers and bullet lists | N/A | No provider action, field definition, app, factory, JNI dependency or installation change; only existing scalar-provider validation metadata changes. |
| 49 | **Factory registration in Qore repo**: every factory name registered in `qlib/DataProvider/DataProvider.qc` → `FactoryMap` (without this, module loads but doesn't appear in Qorus apps) | N/A | No provider action, field definition, app, factory, JNI dependency or installation change; only existing scalar-provider validation metadata changes. |
| 50 | **`getRecordTypeImpl()` signature**: must be `private *hash<string, AbstractDataField> getRecordTypeImpl(*hash<auto> search_options)` — NOT returning `*AbstractDataProviderType` | N/A | No provider action, field definition, app, factory, JNI dependency or installation change; only existing scalar-provider validation metadata changes. |
| 51 | **Dependency JARs committed** (for JNI modules): JAR files in `qlib/*/jar/` may be gitignored — use `git add -f` to ensure they're tracked, otherwise CI compilation fails | N/A | No provider action, field definition, app, factory, JNI dependency or installation change; only existing scalar-provider validation metadata changes. |
| 52 | JAR install rules in CMakeLists.txt for all dependency JARs | N/A | No provider action, field definition, app, factory, JNI dependency or installation change; only existing scalar-provider validation metadata changes. |
| 53 | **No workarounds**: No TODOs, FIXMEs, stubs, or partially-implemented features | Pass | Builtin generic-string branches now validate the correct lexical spaces. Explicit provider metadata preserves the rules; no name guessing or fixture-specific behavior. Remaining identity constraints are tracked as P5 work. |
| 54 | **Exception safety**: C++ uses `ReferenceHolder` for Qore allocations, `std::unique_ptr` for C++ allocations, `*xsink` checked after every fallible operation | Pass | The helper translates only native URI lexical rejection; cancellation, allocation and encoding errors propagate. No new native ownership. Existing scopes restore context; interruption/retry and malformed reconstruction tests pass. |
| 55 | **Thread safety**: All mutable shared state protected by `std::lock_guard<std::mutex>` or documented as immutable-after-construction | Pass | New conversion state is local; provider metadata is initialized before publication and copied by existing optional/reconstruction paths. No new shared mutable state. |
| 56 | **Type safety**: Strongly-typed `code<return(args)>` instead of untyped `code`; `static_cast` instead of C casts; typed hashdecls for results; enums where appropriate | Pass | The optional text_type member belongs to the typed XsdSizedFacetInfo hashdecl and validates kind, whitespace and allowed names. HTTP callbacks are explicitly typed. |
| 57 | **Performance**: No O(n²) where O(n) is possible; no unnecessary copies; coordinate descent uses incremental residuals not full matrix multiply | Pass | One bounded-pattern or URI lexical check per supplied value, without additional schema traversal or reference resolution. Derived providers reuse the established base chain. |
| 58 | **Error handling**: All inputs validated (dimensions, empty data, unfitted models); C++ I/O handles EAGAIN/EINTR if applicable | Pass | Positive/negative and boundary fixtures cover malformed URI/language, whitespace, escaping, Unicode, list/union selection, enumeration, fixed/default, attributes, simple content, saved metadata and recovery. |
| 59 | **Documentation**: Doxygen `@param`, `@return`, `@throw` on all public methods; `@par Example` with realistic business scenarios; `@note` for important caveats | Pass | Public metadata member has documentation; module/native release notes, implemented design/example and acceptance records are updated. Documentation and metadata targets are checked. |
| 60 | **QPP flags**: `[flags=CONSTANT]` on methods that never throw; `[flags=RET_VALUE_ONLY]` on methods that throw but have no side effects | N/A | No C++ changes; the existing native URI API and its cancellation/ownership boundaries are unchanged. |
| 61 | **Security**: No user-controlled format strings; no buffer overflows; bounds checking on array indices; no credentials in code | Pass | Constant error formats, no reference retrieval or credentials, and existing checked native URI/XML character validation. |
| 62 | **Correctness**: Algorithms verified against reference implementations; edge cases tested (empty data, single sample, all-zero features) | Pass | Pinned Xerces and native validation assess all 73 fixture documents. WSDL checks returned values and explicit union primitive keys, both decoding modes, saved schemas/providers, generated samples, simple content and actual HTTP directions. Full gate and corpus evidence are recorded in the inventory. |

Result: 15 Pass, 47 N/A, zero Fail.
