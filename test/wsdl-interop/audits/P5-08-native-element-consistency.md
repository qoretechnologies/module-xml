# P5-08 native element declaration consistency audit

Copyright (C) 2026 Qore Technologies, s.r.o.

Audited 2026-09-11 against parent `cc16263` using the complete
`/home/david/.codex/skills/audit-changes/SKILL.md` and all five referenced guides.
All 62 individual checks are recorded: **19 Pass, 43 N/A, 0 Fail**.

The native checker enforces EDC after substitution resolution, with independent
model scopes and local allocation ownership. The 34-schema configure probe tests
behavior, including acceptance of a corrected system library with different
error codes and wording. Both original native failure overrides are removed;
the historical diagnostics and supporting-validator omissions remain recorded.

The final gate passes 121 suites/1237 cases/60736 assertions. Native tests pass
8 cases/129 assertions across AST/IR/JIT/tiered. Each child-substitution matrix
passes 53 schemas/310 documents/2586 rows with 1050 independently valid SOAP
payloads/samples; AOT consumers pass too. The new 43-schema EDC matrix passes
four source modes and AOT, with 25 independently valid documents and 50 retained
original/reconstructed values. The unchanged earlier 30-schema/60-binding EDC
suite passes both methods. All 38 distinct provider tests pass: the final
37-test gate and the newly added independent-diagnostic test. All 69 injected
allocation failures clean up; native unit, allocation test and full probe
Valgrind runs report zero errors and no lost blocks. Native docs and the
executed order example pass.

The both-version corpus is unchanged: 2457 survey rows, 143 selected WSDLs/1384
directions with no selected failures, and 64 broader failures still visible.
There are no missing/skip/value failures. No upstream corpus or native source
was changed. The six WSDL/consumer qmods and isolated libqore are unchanged.
Main Qore's unrelated work was committed separately as `35dc29f31`; it has no
incoming remote commits. This XML work made no main-Qore edits or pushes.
P5 message-root identity and all remaining P5–P9 requirements remain open.
See the [inventory](../P5-08-validation.json) for artifacts, commands and hashes.

| # | Check | Status | Evidence |
| --- | --- | --- | --- |
| 1 | Entry exists in `doxygen/lang/120_modules.dox.tmpl` (for modules in the Qore repo; N/A for external module repos) | N/A | No new or modified .qm module or separated .qc implementation; existing module registration/layout remains unchanged. |
| 2 | Entry exists in `doxygen/lang/900_release_notes.dox.tmpl` (for modules in the Qore repo; external modules have release notes in their .qm) | Pass | Native xml 2.3.0 release notes, README, implemented design and execution/evidence describe EDC behavior and provider selection. |
| 3 | `qore_user_module()` or `qore_external_user_module()` call in `CMakeLists.txt` | N/A | No new or modified .qm module or separated .qc implementation; existing module registration/layout remains unchanged. |
| 4 | Module added to QMOD list in `CMakeLists.txt` | N/A | No new or modified .qm module or separated .qc implementation; existing module registration/layout remains unchanged. |
| 5 | `.qm` file has `@section <lowercasemodname>intro` as first doc section — **must be all lowercase** (e.g., `avrodataproviderintro`, not `AvroDataProviderintro`) | N/A | No new or modified .qm module or separated .qc implementation; existing module registration/layout remains unchanged. |
| 6 | `%modern` in `.qm` file — no redundant `%new-style`, `%require-types`, `%strict-args`, `%enable-all-warnings` | N/A | No new or modified .qm module or separated .qc implementation; existing module registration/layout remains unchanged. |
| 7 | No parse directives (`%requires`, `%modern`, `%new-style`) in separated `.qc` files (check OUTSIDE of `@code` blocks only) | N/A | No new or modified .qm module or separated .qc implementation; existing module registration/layout remains unchanged. |
| 8 | No `%include` usage (deprecated for modules) | N/A | No new or modified .qm module or separated .qc implementation; existing module registration/layout remains unchanged. |
| 9 | Copyright 2026 on all new files | Pass | New C/CMake/Qore/Python/design/audit/inventory files have 2026 copyright notices; upstream sources and notices remain unchanged. |
| 10 | Directory layout: `.qm` inside `qlib/<ModuleName>/` directory (not at `qlib/<ModuleName>.qm` for multi-file modules) | N/A | No new or modified .qm module or separated .qc implementation; existing module registration/layout remains unchanged. |
| 11 | No second `.qm` for the same module at `qlib/<ModuleName>.qm` | N/A | No new or modified .qm module or separated .qc implementation; existing module registration/layout remains unchanged. |
| 12 | `ns=Qore::XX` matches the QoreNamespace constructor path | N/A | No QPP class, method or registration changes. |
| 13 | `%modern` directive present | Pass | New native qtest and integration worker explicitly use %modern; embedded Program source does too. |
| 14 | Executable permission set (`chmod +x`) | Pass | New qtest, integration worker and Python matrix are executable. Existing test permissions are unchanged. |
| 15 | Uses %prepend-module-path  before %requires for in-repo modules (Qore and Qore modules only; not Qorus) | Pass | Local qlib is prepended before relative WSDL requires. Final AST/IR/JIT/tiered and staged AOT runs verify the local source and compiled module. |
| 16 | External module dependencies use `%try-module` — except modules delivered with the project itself (Qore ex: DataProvider, ConnectionProvider, QUnit, etc.) which use hard `%requires` | Pass | Project xml/QUnit use hard requires. External json uses %try-module and an explicit missing dependency error; no skipped dependency assertions. |
| 17 | No filesystem operations (fopen, open, creat, unlink, remove, rename, mkdir, rmdir, stat, chmod) without sandbox checks | Pass | The native checker adds no filesystem calls. CMake writes checked build copies and tests read their explicit fixtures through existing APIs. |
| 18 | No network operations (connect, bind, socket, getaddrinfo, gethostbyname) without sandbox checks | Pass | The native checker adds no network calls. Imported schema regressions serve the requested members.xsd through an in-memory callback and leave unrelated resource handling to the established loader. |
| 19 | If filesystem/network ops exist, verify `QoreSandboxManagerHelper` usage | N/A | No new native filesystem/network operations require a sandbox manager helper. |
| 20 | No `File::`, `Dir::`, `Socket::`, `HTTPClient::` usage without justification | Pass | Production Qore is unchanged. The new worker reads its explicit generated JSON fixture; imports and concurrent tests use existing reader/callback APIs with bounded deadlines. |
| 21 | All `for`/`while` loops that could iterate >100 times have `qore_check_cancel()` checks | N/A | No C++ loops changed. The checker is private libxml2 C with no Qore runtime dependency; existing Qore entry/resource cancellation boundaries remain and are tested. No claim of a new per-iteration C cancellation hook. |
| 22 | Uses `qore_check_cancel()` (NOT deprecated `qore_check_io_interrupt()`) | N/A | No C++ loops changed. The checker is private libxml2 C with no Qore runtime dependency; existing Qore entry/resource cancellation boundaries remain and are tested. No claim of a new per-iteration C cancellation hook. |
| 23 | Check frequency: every 100 iterations for tight loops, every 10 for expensive iterations | N/A | No C++ loops changed. The checker is private libxml2 C with no Qore runtime dependency; existing Qore entry/resource cancellation boundaries remain and are tested. No claim of a new per-iteration C cancellation hook. |
| 24 | No blocking operations without cancellation support | Pass | No blocking native operations added. Existing Qore schema-entry and resource-callback cancellation checks remain; tests verify interruption, clear/reuse and two concurrent readers. C graph traversal has no I/O. |
| 25 | Every action has `display_name`, `short_desc` (plain text, <80 chars), `desc` (markdown) | N/A | No static DataProvider action/app/datatype/factory registration or JNI dependencies changed; retained/native providers and compiled HTTP/Cargo/CDA consumers are tested. |
| 26 | Every action has `options` populated via `getActionOptionFromFields()` — without this, the action shows an empty, unusable form | N/A | No static DataProvider action/app/datatype/factory registration or JNI dependencies changed; retained/native providers and compiled HTTP/Cargo/CDA consumers are tested. |
| 27 | Every action has `output_type` set to a typed data type constant (e.g., `MyResponseDataType`) — not omitted | N/A | No static DataProvider action/app/datatype/factory registration or JNI dependencies changed; retained/native providers and compiled HTTP/Cargo/CDA consumers are tested. |
| 28 | DPAT_API actions: provider has `"supports_request": True` and implements `doRequestImpl()` | N/A | No static DataProvider action/app/datatype/factory registration or JNI dependencies changed; retained/native providers and compiled HTTP/Cargo/CDA consumers are tested. |
| 29 | DPAT_FIND actions: every option exists in `SearchOptions`, `getRecordTypeImpl()` returns `*hash<string, AbstractDataField>` | N/A | No static DataProvider action/app/datatype/factory registration or JNI dependencies changed; retained/native providers and compiled HTTP/Cargo/CDA consumers are tested. |
| 30 | Scheme-based apps (with `"scheme"` in registerApp): actions use `"path"` and do NOT use `"cls"` — having both `scheme` and `cls` causes a runtime error | N/A | No static DataProvider action/app/datatype/factory registration or JNI dependencies changed; retained/native providers and compiled HTTP/Cargo/CDA consumers are tested. |
| 31 | Single-key hash slices use trailing comma: `Fields{"key",}` (without trailing comma, `Fields{"key"}` returns the value, not a hash) | N/A | No static DataProvider action/app/datatype/factory registration or JNI dependencies changed; retained/native providers and compiled HTTP/Cargo/CDA consumers are tested. |
| 32 | **Typed data type classes exist** for request and response types — inherit `HashDataType`, have `const Fields` hash, call `addQoreFields(Fields)` in constructor, export public constant at bottom (e.g., `public const MyDataType = new MyDataType();`) | N/A | No static DataProvider action/app/datatype/factory registration or JNI dependencies changed; retained/native providers and compiled HTTP/Cargo/CDA consumers are tested. |
| 33 | Request/input types use `public` Fields (enables `ClassName::Fields` in action registration) | N/A | No static DataProvider action/app/datatype/factory registration or JNI dependencies changed; retained/native providers and compiled HTTP/Cargo/CDA consumers are tested. |
| 34 | Response/output types use `private` Fields | N/A | No static DataProvider action/app/datatype/factory registration or JNI dependencies changed; retained/native providers and compiled HTTP/Cargo/CDA consumers are tested. |
| 35 | Each field in data types has `display_name`, `type`, and `desc` (markdown-formatted) | N/A | No static DataProvider action/app/datatype/factory registration or JNI dependencies changed; retained/native providers and compiled HTTP/Cargo/CDA consumers are tested. |
| 36 | Input fields have `example_value` where useful (string fields, endpoint URIs, SQL queries, etc.) | N/A | No static DataProvider action/app/datatype/factory registration or JNI dependencies changed; retained/native providers and compiled HTTP/Cargo/CDA consumers are tested. |
| 37 | Fields with finite allowed values use `allowed_values` with `AllowedValueInfo` containing both `value` and `display_name` (Title Case, human-readable) — never bare values, never described only in text | N/A | No static DataProvider action/app/datatype/factory registration or JNI dependencies changed; retained/native providers and compiled HTTP/Cargo/CDA consumers are tested. |
| 38 | Password/secret fields have `"sensitive": True` | N/A | No static DataProvider action/app/datatype/factory registration or JNI dependencies changed; retained/native providers and compiled HTTP/Cargo/CDA consumers are tested. |
| 39 | `groups` uses `AppGroup` enum values from `qlib/DataProvider/AppGroup.qc` | N/A | No static DataProvider action/app/datatype/factory registration or JNI dependencies changed; retained/native providers and compiled HTTP/Cargo/CDA consumers are tested. |
| 40 | App `logo` stored as separate file, loaded at module level in `Priv` namespace | N/A | No static DataProvider action/app/datatype/factory registration or JNI dependencies changed; retained/native providers and compiled HTTP/Cargo/CDA consumers are tested. |
| 41 | App `desc` uses markdown: bullet list of capabilities, links to project website, business-language explanation of value | N/A | No static DataProvider action/app/datatype/factory registration or JNI dependencies changed; retained/native providers and compiled HTTP/Cargo/CDA consumers are tested. |
| 42 | `display_name` is user-friendly ("Apache Avro" not "avro") | N/A | No static DataProvider action/app/datatype/factory registration or JNI dependencies changed; retained/native providers and compiled HTTP/Cargo/CDA consumers are tested. |
| 43 | `short_desc` is plain text, under 80 chars, single sentence — no markdown | N/A | No static DataProvider action/app/datatype/factory registration or JNI dependencies changed; retained/native providers and compiled HTTP/Cargo/CDA consumers are tested. |
| 44 | `desc` uses markdown: backticks for code/field refs (`` `field_name` ``, `` `True` ``, `` `pdf` ``), `\n\n` for paragraphs, `- ` bullet lists for enumerations, `**bold**` for caveats | N/A | No static DataProvider action/app/datatype/factory registration or JNI dependencies changed; retained/native providers and compiled HTTP/Cargo/CDA consumers are tested. |
| 45 | Descriptions use plain business language relating to common challenges — not just technical "what" but "why" and "when to use" | N/A | No static DataProvider action/app/datatype/factory registration or JNI dependencies changed; retained/native providers and compiled HTTP/Cargo/CDA consumers are tested. |
| 46 | No bare `True`/`False`/`NOTHING` — must be backtick-wrapped in `desc` | N/A | No static DataProvider action/app/datatype/factory registration or JNI dependencies changed; retained/native providers and compiled HTTP/Cargo/CDA consumers are tested. |
| 47 | No bare field/option names in prose — must use backticks | N/A | No static DataProvider action/app/datatype/factory registration or JNI dependencies changed; retained/native providers and compiled HTTP/Cargo/CDA consumers are tested. |
| 48 | Long descriptions (>500 chars) use bold section headers and bullet lists | N/A | No static DataProvider action/app/datatype/factory registration or JNI dependencies changed; retained/native providers and compiled HTTP/Cargo/CDA consumers are tested. |
| 49 | **Factory registration in Qore repo**: every factory name registered in `qlib/DataProvider/DataProvider.qc` → `FactoryMap` (without this, module loads but doesn't appear in Qorus apps) | N/A | No static DataProvider action/app/datatype/factory registration or JNI dependencies changed; retained/native providers and compiled HTTP/Cargo/CDA consumers are tested. |
| 50 | **`getRecordTypeImpl()` signature**: must be `private *hash<string, AbstractDataField> getRecordTypeImpl(*hash<auto> search_options)` — NOT returning `*AbstractDataProviderType` | N/A | No static DataProvider action/app/datatype/factory registration or JNI dependencies changed; retained/native providers and compiled HTTP/Cargo/CDA consumers are tested. |
| 51 | **Dependency JARs committed** (for JNI modules): JAR files in `qlib/*/jar/` may be gitignored — use `git add -f` to ensure they're tracked, otherwise CI compilation fails | N/A | No static DataProvider action/app/datatype/factory registration or JNI dependencies changed; retained/native providers and compiled HTTP/Cargo/CDA consumers are tested. |
| 52 | JAR install rules in CMakeLists.txt for all dependency JARs | N/A | No static DataProvider action/app/datatype/factory registration or JNI dependencies changed; retained/native providers and compiled HTTP/Cargo/CDA consumers are tested. |
| 53 | **No workarounds**: No TODOs, FIXMEs, stubs, or partially-implemented features | Pass | A complete native EDC pass checks explicit and admitted implicit declarations; no diagnostic exceptions remain for the fixed native outcomes. Configure probes test behavior independently of correction-specific wording/codes. No upstream fixture or supported scope was changed. |
| 54 | **Exception safety**: C++ uses `ReferenceHolder` for Qore allocations, `std::unique_ptr` for C++ allocations, `*xsink` checked after every fallible operation | Pass | Every checker exit frees local hash tables and arena blocks without modifying schema components. All 69 injected allocation failures on successful and conflicting paths reject construction and return to the allocation baseline; native unit, allocation and complete-probe Valgrind report no errors or lost blocks. |
| 55 | **Thread safety**: All mutable shared state protected by `std::lock_guard<std::mutex>` or documented as immutable-after-construction | Pass | All checker state is local per invocation; resolved components are read only. No mutable global checker state. Two native readers execute concurrently; invalid schema replacement preserves the earlier reader schema. |
| 56 | **Type safety**: Strongly-typed `code<return(args)>` instead of untyped `code`; `static_cast` instead of C casts; typed hashdecls for results; enums where appropriate | Pass | C component tags guard typed pointer conversions; declaration/type maps store typed native pointers. Qore helpers have typed parameters/returns and explicit heterogeneous result hashes. No new C++ casts or public hashdecls are needed. |
| 57 | **Performance**: No O(n²) where O(n) is possible; no unnecessary copies; coordinate descent uses incremental residuals not full matrix multiply | Pass | Per-model visited sets process each shared component once, hashed expanded names avoid pairwise declaration comparisons, and occurrence counts are never expanded. The native tests cover a 24-level shared group DAG and large exact counts. No Release benchmark is claimed. |
| 58 | **Error handling**: All inputs validated (dimensions, empty data, unfitted models); C++ I/O handles EAGAIN/EINTR if applicable | Pass | Same-name conflicts, imported and unused groups, abstract heads, blocked/abstract members, anonymous types, nested scopes, inherited particles and zero maxima are tested with required XSD-SYNTAX-ERROR or WSDL-ERROR categories. Probe failures include memory/error-domain checks, positive cases and alternative system diagnostics. |
| 59 | **Documentation**: Doxygen `@param`, `@return`, `@throw` on all public methods; `@par Example` with realistic business scenarios; `@note` for important caveats | Pass | No public API was added. The durable design documents scope, ownership, error mapping, cancellation boundary, dependency selection and an executed order/quantity example. Native documentation builds without warnings. |
| 60 | **QPP flags**: `[flags=CONSTANT]` on methods that never throw; `[flags=RET_VALUE_ONLY]` on methods that throw but have no side effects | N/A | No QPP class, method or registration changes. |
| 61 | **Security**: No user-controlled format strings; no buffer overflows; bounds checking on array indices; no credentials in code | Pass | Fixed diagnostic/format strings, checked snprintf lengths and existing hash/arena overflow handling prevent input-controlled formatting or allocation overflow. No credentials or new I/O authorization bypasses. Unknown dependency sources remain rejected by hashes. |
| 62 | **Correctness**: Algorithms verified against reference implementations; edge cases tested (empty data, single sample, all-zero features) | Pass | 121 suites/1237 cases/60736 assertions pass. Native 8-case/129-assertion tests and 53-schema child matrices pass four source modes; child/AOT consumers and the new 43-schema EDC matrix pass, with 25 independent documents and 50 retained values in five modes. All 38 distinct provider tests pass (37-test gate plus the new alternative-diagnostic test). The prior 30-schema/60-binding EDC suite passes both methods. Full corpus results are unchanged with zero selected failures. |
