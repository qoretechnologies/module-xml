# P2-05 audit: file-relative schema dependencies

Copyright (C) 2026 Qore Technologies, s.r.o.

Applied the complete `/home/david/.codex/skills/audit-changes/SKILL.md` to this
increment on develop, parent `505a38b`. Read the skill before this commit; the
five referenced Qore design documents remain applicable and were consulted in
this execution. P2 acceptance remains incomplete; P3-P9 have not started.

## Scope, findings and verification

- WSDL.qm scopes the base directory for each file addition. A successful first
  addition retains the established default; later additions restore the previous
  default. File/read/schema failures and failed directory additions restore it.
  Existing File/Dir ownership and per-file registry rollback remain in place.
- Root cause: addSchemaFile previously assigned dirname(path) only when no default
  was present. A second root importing dependency.xsd loaded the first root's file,
  reproducing a namespace mismatch instead of resolving the correct dependency.
  Failed first additions also left their directory as the new default.
- All three new Qore scenarios failed before the fix. The complete composition
  suite now passes 14 cases / 66 assertions, including distinct files/directories,
  typed 42/False values, reconstruction, missing-file FILE-OPEN2-ERROR, invalid-root
  XSD-ERROR and subsequent successful reuse.
- The authored source-bases fixtures have 2026 notices and SHA-256 records in their
  manifest. libxml2 and pinned Xerces validate both import graphs and assess four
  positive/negative payloads; Qore resolves both graphs and the Qore integration
  tests retain their exact distinct values. Existing P3 boolean-lexical rejection
  coverage is not replaced by these schema-construction tests.
- All 310 Qore cases in the 18 required/affected suites pass with debugging enabled,
  without warnings or unhandled errors. Logs: /tmp/wsdl-p2-05-<suite>.log.
- Python runs 68 tests: 67 pass; the existing dual-binding test still reports the
  two P6-selected-binding-version subtest failures. They remain real failures,
  with all P2 assertions executed, and do not represent a green full suite.
  Log: /tmp/wsdl-p2-05-python.log. No binding-version implementation changed.
- Both-version survey and strict coverage are identical to 505a38b except the
  recorded WSDL.qm digest. Strict gate: 38 descriptions / 140 directions, zero
  selected failures. All 384 diagnostic failures and complete stage counts remain
  visible; zero newly failing requirements, missing or skipped stages.
- Full diff reviewed; git diff --check passes. Release notes and implemented design
  explain file bases, first-default compatibility and per-file failure behavior.

Final production module SHA-256: `f074a055ec54971b9f3ce1969c46ab219ab3b63702b09d8198ddd69311d0a879`.

## Complete checklist

| Check | Status | Evidence |
| --- | --- | --- |
| 1. Entry exists in `doxygen/lang/120_modules.dox.tmpl` (for modules in the Qore repo; N/A for external module repos) | N/A | Existing external module; no new module, separated source, QPP class or build registration. |
| 2. Entry exists in `doxygen/lang/900_release_notes.dox.tmpl` (for modules in the Qore repo; external modules have release notes in their .qm) | Pass | WSDL 0.5.8 release notes describe file-relative imports and base restoration. |
| 3. `qore_user_module()` or `qore_external_user_module()` call in `CMakeLists.txt` | N/A | Existing external module; no new module, separated source, QPP class or build registration. |
| 4. Module added to QMOD list in `CMakeLists.txt` | N/A | Existing external module; no new module, separated source, QPP class or build registration. |
| 5. `.qm` file has `@section <lowercasemodname>intro` as first doc section — **must be all lowercase** (e.g., `avrodataproviderintro`, not `AvroDataProviderintro`) | Pass | Existing wsdlintro lowercase section retained. |
| 6. `%modern` in `.qm` file — no redundant `%new-style`, `%require-types`, `%strict-args`, `%enable-all-warnings` | Pass | WSDL.qm uses %modern; no parse directive changes. |
| 7. No parse directives (`%requires`, `%modern`, `%new-style`) in separated `.qc` files (check OUTSIDE of `@code` blocks only) | N/A | Existing external module; no new module, separated source, QPP class or build registration. |
| 8. No `%include` usage (deprecated for modules) | Pass | No %include introduced. |
| 9. Copyright 2026 on all new files | Pass | Five authored fixtures and their manifest carry 2026 notices; module/test/design/audit notices remain 2026. No upstream fixture modified. |
| 10. Directory layout: `.qm` inside `qlib/<ModuleName>/` directory (not at `qlib/<ModuleName>.qm` for multi-file modules) | N/A | Existing external module; no new module, separated source, QPP class or build registration. |
| 11. No second `.qm` for the same module at `qlib/<ModuleName>.qm` | N/A | Existing external module; no new module, separated source, QPP class or build registration. |
| 12. `ns=Qore::XX` matches the QoreNamespace constructor path | N/A | Existing external module; no new module, separated source, QPP class or build registration. |
| 13. `%modern` directive present | Pass | Changed schema-composition qtest retains %modern. |
| 14. Executable permission set (`chmod +x`) | Pass | Changed qtest remains executable (100755). |
| 15. Uses %prepend-module-path  before %requires for in-repo modules (Qore and Qore modules only; not Qorus) | Pass | Local qlib path is prepended before relative ../qlib/WSDL.qm requires. |
| 16. External module dependencies use `%try-module` — except modules delivered with the project itself (Qore ex: DataProvider, ConnectionProvider, QUnit, etc.) which use hard `%requires` | Pass | QUnit is core and xml belongs to this repository; no external binary dependencies added. |
| 17. No filesystem operations (fopen, open, creat, unlink, remove, rename, mkdir, rmdir, stat, chmod) without sandbox checks | N/A | No C++/QPP change; Qore owns allocations and cancellation. No native build or valgrind is required. |
| 18. No network operations (connect, bind, socket, getaddrinfo, gethostbyname) without sandbox checks | N/A | No C++/QPP change; Qore owns allocations and cancellation. No native build or valgrind is required. |
| 19. If filesystem/network ops exist, verify `QoreSandboxManagerHelper` usage | N/A | No C++/QPP change; Qore owns allocations and cancellation. No native build or valgrind is required. |
| 20. No `File::`, `Dir::`, `Socket::`, `HTTPClient::` usage without justification | Pass | Existing File and Dir calls read requested XSDs and directory entries. The change scopes their resolution base; tests read only authored local fixtures and offline cached resources. |
| 21. All `for`/`while` loops that could iterate >100 times have `qore_check_cancel()` checks | N/A | No C++/QPP change; Qore owns allocations and cancellation. No native build or valgrind is required. |
| 22. Uses `qore_check_cancel()` (NOT deprecated `qore_check_io_interrupt()`) | N/A | No C++/QPP change; Qore owns allocations and cancellation. No native build or valgrind is required. |
| 23. Check frequency: every 100 iterations for tight loops, every 10 for expensive iterations | N/A | No C++/QPP change; Qore owns allocations and cancellation. No native build or valgrind is required. |
| 24. No blocking operations without cancellation support | N/A | No C++/QPP change; Qore owns allocations and cancellation. No native build or valgrind is required. |
| 25. Every action has `display_name`, `short_desc` (plain text, <80 chars), `desc` (markdown) | N/A | No provider action/app, field class, FactoryMap, JNI or build registration change. |
| 26. Every action has `options` populated via `getActionOptionFromFields()` — without this, the action shows an empty, unusable form | N/A | No provider action/app, field class, FactoryMap, JNI or build registration change. |
| 27. Every action has `output_type` set to a typed data type constant (e.g., `MyResponseDataType`) — not omitted | N/A | No provider action/app, field class, FactoryMap, JNI or build registration change. |
| 28. DPAT_API actions: provider has `"supports_request": True` and implements `doRequestImpl()` | N/A | No provider action/app, field class, FactoryMap, JNI or build registration change. |
| 29. DPAT_FIND actions: every option exists in `SearchOptions`, `getRecordTypeImpl()` returns `*hash<string, AbstractDataField>` | N/A | No provider action/app, field class, FactoryMap, JNI or build registration change. |
| 30. Scheme-based apps (with `"scheme"` in registerApp): actions use `"path"` and do NOT use `"cls"` — having both `scheme` and `cls` causes a runtime error | N/A | No provider action/app, field class, FactoryMap, JNI or build registration change. |
| 31. Single-key hash slices use trailing comma: `Fields{"key",}` (without trailing comma, `Fields{"key"}` returns the value, not a hash) | N/A | No provider action/app, field class, FactoryMap, JNI or build registration change. |
| 32. **Typed data type classes exist** for request and response types — inherit `HashDataType`, have `const Fields` hash, call `addQoreFields(Fields)` in constructor, export public constant at bottom (e.g., `public const MyDataType = new MyDataType();`) | N/A | No provider action/app, field class, FactoryMap, JNI or build registration change. |
| 33. Request/input types use `public` Fields (enables `ClassName::Fields` in action registration) | N/A | No provider action/app, field class, FactoryMap, JNI or build registration change. |
| 34. Response/output types use `private` Fields | N/A | No provider action/app, field class, FactoryMap, JNI or build registration change. |
| 35. Each field in data types has `display_name`, `type`, and `desc` (markdown-formatted) | N/A | No provider action/app, field class, FactoryMap, JNI or build registration change. |
| 36. Input fields have `example_value` where useful (string fields, endpoint URIs, SQL queries, etc.) | N/A | No provider action/app, field class, FactoryMap, JNI or build registration change. |
| 37. Fields with finite allowed values use `allowed_values` with `AllowedValueInfo` containing both `value` and `display_name` (Title Case, human-readable) — never bare values, never described only in text | N/A | No provider action/app, field class, FactoryMap, JNI or build registration change. |
| 38. Password/secret fields have `"sensitive": True` | N/A | No provider action/app, field class, FactoryMap, JNI or build registration change. |
| 39. `groups` uses `AppGroup` enum values from `qlib/DataProvider/AppGroup.qc` | N/A | No provider action/app, field class, FactoryMap, JNI or build registration change. |
| 40. App `logo` stored as separate file, loaded at module level in `Priv` namespace | N/A | No provider action/app, field class, FactoryMap, JNI or build registration change. |
| 41. App `desc` uses markdown: bullet list of capabilities, links to project website, business-language explanation of value | N/A | No provider action/app, field class, FactoryMap, JNI or build registration change. |
| 42. `display_name` is user-friendly ("Apache Avro" not "avro") | N/A | No provider action/app, field class, FactoryMap, JNI or build registration change. |
| 43. `short_desc` is plain text, under 80 chars, single sentence — no markdown | N/A | No provider action/app, field class, FactoryMap, JNI or build registration change. |
| 44. `desc` uses markdown: backticks for code/field refs (`` `field_name` ``, `` `True` ``, `` `pdf` ``), `\n\n` for paragraphs, `- ` bullet lists for enumerations, `**bold**` for caveats | N/A | No provider action/app, field class, FactoryMap, JNI or build registration change. |
| 45. Descriptions use plain business language relating to common challenges — not just technical "what" but "why" and "when to use" | N/A | No provider action/app, field class, FactoryMap, JNI or build registration change. |
| 46. No bare `True`/`False`/`NOTHING` — must be backtick-wrapped in `desc` | N/A | No provider action/app, field class, FactoryMap, JNI or build registration change. |
| 47. No bare field/option names in prose — must use backticks | N/A | No provider action/app, field class, FactoryMap, JNI or build registration change. |
| 48. Long descriptions (>500 chars) use bold section headers and bullet lists | N/A | No provider action/app, field class, FactoryMap, JNI or build registration change. |
| 49. **Factory registration in Qore repo**: every factory name registered in `qlib/DataProvider/DataProvider.qc` → `FactoryMap` (without this, module loads but doesn't appear in Qorus apps) | N/A | No provider action/app, field class, FactoryMap, JNI or build registration change. |
| 50. **`getRecordTypeImpl()` signature**: must be `private *hash<string, AbstractDataField> getRecordTypeImpl(*hash<auto> search_options)` — NOT returning `*AbstractDataProviderType` | N/A | No provider action/app, field class, FactoryMap, JNI or build registration change. |
| 51. **Dependency JARs committed** (for JNI modules): JAR files in `qlib/*/jar/` may be gitignored — use `git add -f` to ensure they're tracked, otherwise CI compilation fails | N/A | No provider action/app, field class, FactoryMap, JNI or build registration change. |
| 52. JAR install rules in CMakeLists.txt for all dependency JARs | N/A | No provider action/app, field class, FactoryMap, JNI or build registration change. |
| 53. **No workarounds**: No TODOs, FIXMEs, stubs, or partially-implemented features | Pass | The root file context is corrected directly; no special-case filename, remapping workaround, skip, TODO or stub added. |
| 54. **Exception safety**: C++ uses `ReferenceHolder` for Qore allocations, `std::unique_ptr` for C++ allocations, `*xsink` checked after every fallible operation | Pass | on_error restores prior bases on file-open and schema errors; on_success restores configured defaults. File ownership and schema map rollback remain intact; failure/retry and reconstruction tests pass. |
| 55. **Thread safety**: All mutable shared state protected by `std::lock_guard<std::mutex>` or documented as immutable-after-construction | Pass | Changes affect schema construction only, already documented as requiring completion before sharing. No shared global state added. |
| 56. **Type safety**: Strongly-typed `code<return(args)>` instead of untyped `code`; `static_cast` instead of C casts; typed hashdecls for results; enums where appropriate | Pass | Previous bases use *string; method signatures and typed component maps are unchanged. Typed scalar assertions distinguish the two dependencies. |
| 57. **Performance**: No O(n²) where O(n) is possible; no unnecessary copies; coordinate descent uses incremental residuals not full matrix multiply | Pass | Constant-time base save/restore per file; no additional scans, copies of schema graphs or blocking operations. |
| 58. **Error handling**: All inputs validated (dimensions, empty data, unfitted models); C++ I/O handles EAGAIN/EINTR if applicable | Pass | Tests assert FILE-OPEN2-ERROR and XSD-ERROR, absent/existing default restoration, file/directory processing and successful reuse after failures. |
| 59. **Documentation**: Doxygen `@param`, `@return`, `@throw` on all public methods; `@par Example` with realistic business scenarios; `@note` for important caveats | Pass | Public file/directory methods document parameters, errors and default/rollback behavior. Existing multi-source example and durable source-base design explain consumer use; release notes updated. |
| 60. **QPP flags**: `[flags=CONSTANT]` on methods that never throw; `[flags=RET_VALUE_ONLY]` on methods that throw but have no side effects | N/A | No C++/QPP change; Qore owns allocations and cancellation. No native build or valgrind is required. |
| 61. **Security**: No user-controlled format strings; no buffer overflows; bounds checking on array indices; no credentials in code | Pass | No credentials, user-controlled format strings or new native buffers. File paths remain handled by Qore File/Dir and existing URL normalization. |
| 62. **Correctness**: Algorithms verified against reference implementations; edge cases tested (empty data, single sample, all-zero features) | Pass | 310 Qore cases pass, independent import graphs and exact distinct values pass, and corpus behavior is unchanged. The full Python suite retains only the separately routed P6 version failure; no phase boundary or full conformance claimed. |

All applicable checks for this bounded P2 change pass. Known P6 subtests remain failing and phase acceptance stays open.
