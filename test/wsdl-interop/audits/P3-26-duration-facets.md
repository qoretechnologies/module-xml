# P3-26 audit: exact duration facets and value identity

Copyright (C) 2026 Qore Technologies, s.r.o.

Final audit of the exact tested duration increment. All current-change verification gates are complete.

Scope: exact Gregorian duration arithmetic and restrictions; finite choices/fixed attributes/list/union identity; provider reconstruction and examples; Qore/Python/HTTP tests; independent reference and pinned validator defects; strict corpus promotion, current reports, design and release notes. No Qore-core changes are included.

The complete audit-changes skill and referenced module-structure, sandboxing, cooperative-cancellation, DataProvider checklist and development guides apply. No application actions or timestamp record catalog are introduced. Review reductions caught optional regex captures and Qore empty-string ordering in the new arithmetic path; both were corrected before the successful final matrix. Validator precision/Gregorian disagreements are separately pinned and explained, not treated as XML successes.

Checklist: **21 Pass, 41 N/A, 0 Fail**.

| Check | Status | Evidence |
| --- | --- | --- |
| 1. Entry exists in `doxygen/lang/120_modules.dox.tmpl` (for modules in the Qore repo; N/A for external module repos) | N/A | Existing external single-file WSDL module; no new module, separated source, duplicate entry point or module-list registration. |
| 2. Entry exists in `doxygen/lang/900_release_notes.dox.tmpl` (for modules in the Qore repo; external modules have release notes in their .qm) | Pass | WSDL 0.5.8 release notes document exact duration order/identity, restriction providers, finite choices and checked examples. |
| 3. `qore_user_module()` or `qore_external_user_module()` call in `CMakeLists.txt` | Pass | Existing WSDL/SoapDataProvider/SoapClient/SoapHandler qmod targets and WSDL documentation rebuild with zero warnings/errors; local compiled tests and design example pass. |
| 4. Module added to QMOD list in `CMakeLists.txt` | N/A | Existing external single-file WSDL module; no new module, separated source, duplicate entry point or module-list registration. |
| 5. `.qm` file has `@section <lowercasemodname>intro` as first doc section — **must be all lowercase** (e.g., `avrodataproviderintro`, not `AvroDataProviderintro`) | Pass | The existing module retains lowercase wsdlintro as its first documentation section. |
| 6. `%modern` in `.qm` file — no redundant `%new-style`, `%require-types`, `%strict-args`, `%enable-all-warnings` | Pass | WSDL and Qore tests/workers use %modern; no redundant legacy directives are introduced. |
| 7. No parse directives (`%requires`, `%modern`, `%new-style`) in separated `.qc` files (check OUTSIDE of `@code` blocks only) | N/A | Existing external single-file WSDL module; no new module, separated source, duplicate entry point or module-list registration. |
| 8. No `%include` usage (deprecated for modules) | Pass | No deprecated %include directive is introduced. |
| 9. Copyright 2026 on all new files | Pass | New Qore/Python tests, worker, validator JSON, evidence and audit carry copyright 2026; existing authored headers already use 2026. |
| 10. Directory layout: `.qm` inside `qlib/<ModuleName>/` directory (not at `qlib/<ModuleName>.qm` for multi-file modules) | N/A | Existing external single-file WSDL module; no new module, separated source, duplicate entry point or module-list registration. |
| 11. No second `.qm` for the same module at `qlib/<ModuleName>.qm` | N/A | Existing external single-file WSDL module; no new module, separated source, duplicate entry point or module-list registration. |
| 12. `ns=Qore::XX` matches the QoreNamespace constructor path | N/A | No C++/QPP change or new native I/O/loops. Qore owns memory and supplies cancellation; Valgrind is not required for this Qore-only increment. |
| 13. `%modern` directive present | Pass | New duration facet qtest and boundary worker use %modern; the expanded HTTP consumer retains it. |
| 14. Executable permission set (`chmod +x`) | Pass | New qtest, boundary worker and Python test entry point have executable permission; imported reference utilities remain nonexecutables. |
| 15. Uses %prepend-module-path  before %requires for in-repo modules (Qore and Qore modules only; not Qorus) | Pass | Qore tests/workers prepend local qlib before relative WSDL/SoapClient/SoapHandler requirements; AOT variants explicitly load the local built modules. |
| 16. External module dependencies use `%try-module` — except modules delivered with the project itself (Qore ex: DataProvider, ConnectionProvider, QUnit, etc.) which use hard `%requires` | Pass | Repository modules and core QUnit/HttpServer use hard requirements. The boundary worker guards external json with %try-module and fails with a clear missing-module category. |
| 17. No filesystem operations (fopen, open, creat, unlink, remove, rename, mkdir, rmdir, stat, chmod) without sandbox checks | N/A | No C++/QPP change or new native I/O/loops. Qore owns memory and supplies cancellation; Valgrind is not required for this Qore-only increment. |
| 18. No network operations (connect, bind, socket, getaddrinfo, gethostbyname) without sandbox checks | N/A | No C++/QPP change or new native I/O/loops. Qore owns memory and supplies cancellation; Valgrind is not required for this Qore-only increment. |
| 19. If filesystem/network ops exist, verify `QoreSandboxManagerHelper` usage | N/A | No C++/QPP change or new native I/O/loops. Qore owns memory and supplies cancellation; Valgrind is not required for this Qore-only increment. |
| 20. No `File::`, `Dir::`, `Socket::`, `HTTPClient::` usage without justification | Pass | No production I/O added. Boundary diagnostics use sandbox-aware ReadOnlyFile; HTTP tests use local listeners, bounded queue/network deadlines and on_exit server cleanup; Python subprocesses have deadlines. |
| 21. All `for`/`while` loops that could iterate >100 times have `qore_check_cancel()` checks | N/A | No C++/QPP change or new native I/O/loops. Qore owns memory and supplies cancellation; Valgrind is not required for this Qore-only increment. |
| 22. Uses `qore_check_cancel()` (NOT deprecated `qore_check_io_interrupt()`) | N/A | No C++/QPP change or new native I/O/loops. Qore owns memory and supplies cancellation; Valgrind is not required for this Qore-only increment. |
| 23. Check frequency: every 100 iterations for tight loops, every 10 for expensive iterations | N/A | No C++/QPP change or new native I/O/loops. Qore owns memory and supplies cancellation; Valgrind is not required for this Qore-only increment. |
| 24. No blocking operations without cancellation support | N/A | No C++/QPP change or new native I/O/loops. Qore owns memory and supplies cancellation; Valgrind is not required for this Qore-only increment. |
| 25. Every action has `display_name`, `short_desc` (plain text, <80 chars), `desc` (markdown) | N/A | No app/action/connection registration, static request/response field catalog, factory, logo, secret or JAR change. These are schema scalar providers and dynamic finite-choice fields. |
| 26. Every action has `options` populated via `getActionOptionFromFields()` — without this, the action shows an empty, unusable form | N/A | No app/action/connection registration, static request/response field catalog, factory, logo, secret or JAR change. These are schema scalar providers and dynamic finite-choice fields. |
| 27. Every action has `output_type` set to a typed data type constant (e.g., `MyResponseDataType`) — not omitted | N/A | No app/action/connection registration, static request/response field catalog, factory, logo, secret or JAR change. These are schema scalar providers and dynamic finite-choice fields. |
| 28. DPAT_API actions: provider has `"supports_request": True` and implements `doRequestImpl()` | N/A | No app/action/connection registration, static request/response field catalog, factory, logo, secret or JAR change. These are schema scalar providers and dynamic finite-choice fields. |
| 29. DPAT_FIND actions: every option exists in `SearchOptions`, `getRecordTypeImpl()` returns `*hash<string, AbstractDataField>` | N/A | No app/action/connection registration, static request/response field catalog, factory, logo, secret or JAR change. These are schema scalar providers and dynamic finite-choice fields. |
| 30. Scheme-based apps (with `"scheme"` in registerApp): actions use `"path"` and do NOT use `"cls"` — having both `scheme` and `cls` causes a runtime error | N/A | No app/action/connection registration, static request/response field catalog, factory, logo, secret or JAR change. These are schema scalar providers and dynamic finite-choice fields. |
| 31. Single-key hash slices use trailing comma: `Fields{"key",}` (without trailing comma, `Fields{"key"}` returns the value, not a hash) | N/A | No app/action/connection registration, static request/response field catalog, factory, logo, secret or JAR change. These are schema scalar providers and dynamic finite-choice fields. |
| 32. **Typed data type classes exist** for request and response types — inherit `HashDataType`, have `const Fields` hash, call `addQoreFields(Fields)` in constructor, export public constant at bottom (e.g., `public const MyDataType = new MyDataType();`) | N/A | No app/action/connection registration, static request/response field catalog, factory, logo, secret or JAR change. These are schema scalar providers and dynamic finite-choice fields. |
| 33. Request/input types use `public` Fields (enables `ClassName::Fields` in action registration) | N/A | No app/action/connection registration, static request/response field catalog, factory, logo, secret or JAR change. These are schema scalar providers and dynamic finite-choice fields. |
| 34. Response/output types use `private` Fields | N/A | No app/action/connection registration, static request/response field catalog, factory, logo, secret or JAR change. These are schema scalar providers and dynamic finite-choice fields. |
| 35. Each field in data types has `display_name`, `type`, and `desc` (markdown-formatted) | N/A | No app/action/connection registration, static request/response field catalog, factory, logo, secret or JAR change. These are schema scalar providers and dynamic finite-choice fields. |
| 36. Input fields have `example_value` where useful (string fields, endpoint URIs, SQL queries, etc.) | N/A | No app/action/connection registration, static request/response field catalog, factory, logo, secret or JAR change. These are schema scalar providers and dynamic finite-choice fields. |
| 37. Fields with finite allowed values use `allowed_values` with `AllowedValueInfo` containing both `value` and `display_name` (Title Case, human-readable) — never bare values, never described only in text | Pass | XsdDurationDataField converts choices to AllowedValueInfo with value/display_name, compares exact duration keys, and validates replacements before assignment. Original/reconstructed and repeated choices, failed setters and creatable behavior pass. |
| 38. Password/secret fields have `"sensitive": True` | N/A | No app/action/connection registration, static request/response field catalog, factory, logo, secret or JAR change. These are schema scalar providers and dynamic finite-choice fields. |
| 39. `groups` uses `AppGroup` enum values from `qlib/DataProvider/AppGroup.qc` | N/A | No app/action/connection registration, static request/response field catalog, factory, logo, secret or JAR change. These are schema scalar providers and dynamic finite-choice fields. |
| 40. App `logo` stored as separate file, loaded at module level in `Priv` namespace | N/A | No app/action/connection registration, static request/response field catalog, factory, logo, secret or JAR change. These are schema scalar providers and dynamic finite-choice fields. |
| 41. App `desc` uses markdown: bullet list of capabilities, links to project website, business-language explanation of value | N/A | No app/action/connection registration, static request/response field catalog, factory, logo, secret or JAR change. These are schema scalar providers and dynamic finite-choice fields. |
| 42. `display_name` is user-friendly ("Apache Avro" not "avro") | N/A | No app/action/connection registration, static request/response field catalog, factory, logo, secret or JAR change. These are schema scalar providers and dynamic finite-choice fields. |
| 43. `short_desc` is plain text, under 80 chars, single sentence — no markdown | N/A | No app/action/connection registration, static request/response field catalog, factory, logo, secret or JAR change. These are schema scalar providers and dynamic finite-choice fields. |
| 44. `desc` uses markdown: backticks for code/field refs (`` `field_name` ``, `` `True` ``, `` `pdf` ``), `\n\n` for paragraphs, `- ` bullet lists for enumerations, `**bold**` for caveats | N/A | No app/action/connection registration, static request/response field catalog, factory, logo, secret or JAR change. These are schema scalar providers and dynamic finite-choice fields. |
| 45. Descriptions use plain business language relating to common challenges — not just technical "what" but "why" and "when to use" | N/A | No app/action/connection registration, static request/response field catalog, factory, logo, secret or JAR change. These are schema scalar providers and dynamic finite-choice fields. |
| 46. No bare `True`/`False`/`NOTHING` — must be backtick-wrapped in `desc` | N/A | No app/action/connection registration, static request/response field catalog, factory, logo, secret or JAR change. These are schema scalar providers and dynamic finite-choice fields. |
| 47. No bare field/option names in prose — must use backticks | N/A | No app/action/connection registration, static request/response field catalog, factory, logo, secret or JAR change. These are schema scalar providers and dynamic finite-choice fields. |
| 48. Long descriptions (>500 chars) use bold section headers and bullet lists | N/A | No app/action/connection registration, static request/response field catalog, factory, logo, secret or JAR change. These are schema scalar providers and dynamic finite-choice fields. |
| 49. **Factory registration in Qore repo**: every factory name registered in `qlib/DataProvider/DataProvider.qc` → `FactoryMap` (without this, module loads but doesn't appear in Qorus apps) | N/A | No app/action/connection registration, static request/response field catalog, factory, logo, secret or JAR change. These are schema scalar providers and dynamic finite-choice fields. |
| 50. **`getRecordTypeImpl()` signature**: must be `private *hash<string, AbstractDataField> getRecordTypeImpl(*hash<auto> search_options)` — NOT returning `*AbstractDataProviderType` | N/A | No app/action/connection registration, static request/response field catalog, factory, logo, secret or JAR change. These are schema scalar providers and dynamic finite-choice fields. |
| 51. **Dependency JARs committed** (for JNI modules): JAR files in `qlib/*/jar/` may be gitignored — use `git add -f` to ensure they're tracked, otherwise CI compilation fails | N/A | No app/action/connection registration, static request/response field catalog, factory, logo, secret or JAR change. These are schema scalar providers and dynamic finite-choice fields. |
| 52. JAR install rules in CMakeLists.txt for all dependency JARs | N/A | No app/action/connection registration, static request/response field catalog, factory, logo, secret or JAR change. These are schema scalar providers and dynamic finite-choice fields. |
| 53. **No workarounds**: No TODOs, FIXMEs, stubs, or partially-implemented features | Pass | Exact four-reference-date arithmetic replaces coercion and raw-text identity. No production fixture names, numeric tolerance, rounding heuristic, magnitude cap, stub or workaround is added. Remaining dateTime/time and later-phase work stay explicit. |
| 54. **Exception safety**: C++ uses `ReferenceHolder` for Qore allocations, `std::unique_ptr` for C++ allocations, `*xsink` checked after every fallible operation | Pass | All arithmetic and validation state is local and managed by Qore. Reconstruction validates before assigning members; finite-choice setters build complete replacements before mutation. Cancellation propagates and valid conversion recovers. |
| 55. **Thread safety**: All mutable shared state protected by `std::lock_guard<std::mutex>` or documented as immutable-after-construction | Pass | Helpers have immutable constants and local calculation state. Restriction metadata is immutable after construction; optional/mandatory providers retain the established copy/wrapper contract. Mutable fields retain the framework ownership contract; HTTP exchanges use synchronized queues. |
| 56. **Type safety**: Strongly-typed `code<return(args)>` instead of untyped `code`; `static_cast` instead of C casts; typed hashdecls for results; enums where appropriate | Pass | Typed hash declarations represent division results, components, instants and facets. Signed integers operate on bounded limbs; *int explicitly represents incomparable values. Known incompatible provider identities reject, including duration restriction providers passed as numeric bases. |
| 57. **Performance**: No O(n²) where O(n) is possible; no unnecessary copies; coordinate descent uses incremental residuals not full matrix multiply | Pass | ASCII limb extraction, arithmetic and rendering are linear in lexical length. Division reduces arbitrary month counts into 400-year cycles; no loop scales with elapsed years/days. Candidate generation is bounded per endpoint and validates before return. |
| 58. **Error handling**: All inputs validated (dimensions, empty data, unfitted models); C++ I/O handles EAGAIN/EINTR if applicable | Pass | Positive, negative, empty/malformed, inherited/fixed/repeated bounds, 1,000-digit components/fractions, metadata corruption, cancellation/recovery and exact error categories are tested. Incomparable duration bounds reject. |
| 59. **Documentation**: Doxygen `@param`, `@return`, `@throw` on all public methods; `@par Example` with realistic business scenarios; `@note` for important caveats | Pass | Public provider/field methods document parameters, output, relevant exceptions and examples. Durable design describes exact arithmetic, lexical/native representations, single-sign sample constraints and the bounded generation error; its example executes with local qmods. |
| 60. **QPP flags**: `[flags=CONSTANT]` on methods that never throw; `[flags=RET_VALUE_ONLY]` on methods that throw but have no side effects | N/A | No C++/QPP change or new native I/O/loops. Qore owns memory and supplies cancellation; Valgrind is not required for this Qore-only increment. |
| 61. **Security**: No user-controlled format strings; no buffer overflows; bounds checking on array indices; no credentials in code | Pass | Strict ASCII duration validation precedes arithmetic. Base-10^9 limbs and factors/divisors below 10^9 keep intermediates below 10^18; signed division handles negative cycles. No native buffers, arbitrary format strings, credentials or production I/O are added. |
| 62. **Correctness**: Algorithms verified against reference implementations; edge cases tested (empty data, single sample, all-zero features) | Pass | 72 Qore suites / 800 cases / 29678 assertions pass. Duration suites pass all eight mode/zone combinations (160 cases / 26152 assertions) and AOT (20 / 3269) plus the design example. Independent 1376-row arithmetic matrix and both-binding consumers pass. The 34-method affected Python run retains only two known P6 signatures; broad corpus failures remain exactly 168, with zero selected failures across 116 WSDLs / 1120 directions. |

Independent evidence: [duration-facets-evidence.md](../duration-facets-evidence.md). Exact verification logs and source/artifact hashes are recorded in [EXECUTION.md](../EXECUTION.md).
