# P1-04 audit: whole archive and P1 acceptance

Copyright (C) 2026 Qore Technologies, s.r.o.

Applied `/home/david/.codex/skills/audit-changes/SKILL.md` to final changes on
`develop`, parent `150dc80`. All 62 checklist items are recorded individually below.
P1 corpus/adjudication acceptance is complete; P2–P9 implementation remains required.

## Scope and referenced design

New Python archive-role/source-adjudication tool and tests, HTTP source-defect metadata,
full archive report, token-content oracle adjudication, Qore parse-only/schema-only test
worker support, coverage report refresh, README/release notes and execution/plan status.
No production module, C++, DataProvider, build or installation changes.

Referenced documents under `/home/david/src/qore/git/qore/design/`:
`qore-module-structure.md`, `module-sandboxing-audit-guide.md`,
`cooperative-cancellation.md`, `data-provider-checklist.md`, and
`data-provider-development-guide.md`. Qore test conventions and justified test I/O
apply; C++/provider-only items are individually marked N/A.

## Verification

- Full Python discovery: 58 tests pass. Three archive tests rerun after final
  completeness guards; all pass. No warnings/errors.
- Qore interoperability: 10 cases / 230 assertions pass. All seven affected suites:
  199 additional cases pass, debugging enabled. Logs `/tmp/wsdl-p1-tests/*-p1-04.log`.
  soap.qtest comparator negative assertions are deliberately caught, as in earlier audits.
- Both SOAP versions, original/catalog broad surveys: exactly unchanged from P1-03.
- Strict Qore selection passes 23 descriptions and 68 message/direction combinations.
  All 449 broad echo failures remain assigned to P2–P5, never passing conformance results.
- All 4,191 archive roles and 48 import edges accounted for; 18 supplemental contracts
  independently schema-assessed and Qore-parsed. Nine valid historical failures have a
  concrete P2 namespace reproducer and source/implementation evidence.
- The four invalid supplemental schemas are independently justified by normative
  grammar tests. Production import-order validation is explicitly not credited when
  an unrelated namespace or empty-resource error rejects the source first.
- All 32 current output-oracle disagreements adjudicated, with exact numbers or
  unchanged expanded token content. Historical findings and all upstream bytes preserved.
- Authored whitespace checked. No C++ changes: valgrind not required.

## Full checklist

| Check | Status | Evidence |
| --- | --- | --- |
| 1. Entry exists in `doxygen/lang/120_modules.dox.tmpl` (for modules in the Qore repo; N/A for external module repos) | N/A | No new Qore module, QPP class, build registration or installation changes. |
| 2. Entry exists in `doxygen/lang/900_release_notes.dox.tmpl` (for modules in the Qore repo; external modules have release notes in their .qm) | N/A | No new Qore module, QPP class, build registration or installation changes. |
| 3. `qore_user_module()` or `qore_external_user_module()` call in `CMakeLists.txt` | N/A | No new Qore module, QPP class, build registration or installation changes. |
| 4. Module added to QMOD list in `CMakeLists.txt` | N/A | No new Qore module, QPP class, build registration or installation changes. |
| 5. `.qm` file has `@section <lowercasemodname>intro` as first doc section — **must be all lowercase** (e.g., `avrodataproviderintro`, not `AvroDataProviderintro`) | N/A | No new Qore module, QPP class, build registration or installation changes. |
| 6. `%modern` in `.qm` file — no redundant `%new-style`, `%require-types`, `%strict-args`, `%enable-all-warnings` | N/A | No new Qore module, QPP class, build registration or installation changes. |
| 7. No parse directives (`%requires`, `%modern`, `%new-style`) in separated `.qc` files (check OUTSIDE of `@code` blocks only) | N/A | No new Qore module, QPP class, build registration or installation changes. |
| 8. No `%include` usage (deprecated for modules) | N/A | No new Qore module, QPP class, build registration or installation changes. |
| 9. Copyright 2026 on all new files | Pass | All authored Python/Qore/Markdown has 2026 copyright; original archive bytes and notices are unchanged. JSON records generated evidence/provenance. |
| 10. Directory layout: `.qm` inside `qlib/<ModuleName>/` directory (not at `qlib/<ModuleName>.qm` for multi-file modules) | N/A | No new Qore module, QPP class, build registration or installation changes. |
| 11. No second `.qm` for the same module at `qlib/<ModuleName>.qm` | N/A | No new Qore module, QPP class, build registration or installation changes. |
| 12. `ns=Qore::XX` matches the QoreNamespace constructor path | N/A | No new Qore module, QPP class, build registration or installation changes. |
| 13. `%modern` directive present | Pass | probe.qr retains %modern; every Qore invocation enables debugging. |
| 14. Executable permission set (`chmod +x`) | Pass | probe.qr and new archive CLI/test scripts are executable. No .qtest changed. |
| 15. Uses %prepend-module-path  before %requires for in-repo modules (Qore and Qore modules only; not Qorus) | Pass | probe.qr prepends ../../qlib and requires ../../qlib/WSDL.qm; local development code is used. |
| 16. External module dependencies use `%try-module` — except modules delivered with the project itself (Qore ex: DataProvider, ConnectionProvider, QUnit, etc.) which use hard `%requires` | Pass | External json is guarded with %try-module; same-project xml and WSDL use hard %requires. |
| 17. No filesystem operations (fopen, open, creat, unlink, remove, rename, mkdir, rmdir, stat, chmod) without sandbox checks | N/A | No C++ filesystem/network changes; Python/Qore test I/O reviewed explicitly below. |
| 18. No network operations (connect, bind, socket, getaddrinfo, gethostbyname) without sandbox checks | N/A | No C++ filesystem/network changes; Python/Qore test I/O reviewed explicitly below. |
| 19. If filesystem/network ops exist, verify `QoreSandboxManagerHelper` usage | N/A | No C++ filesystem/network changes; Python/Qore test I/O reviewed explicitly below. |
| 20. No `File::`, `Dir::`, `Socket::`, `HTTPClient::` usage without justification | Pass | Only local verified archive reads. WebService/XsdSchema use async_only and the supplied cache; historical toolkit programs are never executed. |
| 21. All `for`/`while` loops that could iterate >100 times have `qore_check_cancel()` checks | N/A | No C++ loops or blocking operations changed. Existing deterministic readiness/cancellation/reaping and bounded process tests pass. |
| 22. Uses `qore_check_cancel()` (NOT deprecated `qore_check_io_interrupt()`) | N/A | No C++ loops or blocking operations changed. Existing deterministic readiness/cancellation/reaping and bounded process tests pass. |
| 23. Check frequency: every 100 iterations for tight loops, every 10 for expensive iterations | N/A | No C++ loops or blocking operations changed. Existing deterministic readiness/cancellation/reaping and bounded process tests pass. |
| 24. No blocking operations without cancellation support | N/A | No C++ loops or blocking operations changed. Existing deterministic readiness/cancellation/reaping and bounded process tests pass. |
| 25. Every action has `display_name`, `short_desc` (plain text, <80 chars), `desc` (markdown) | N/A | No DataProvider action/type/app/FactoryMap changes, JNI dependencies or installation changes. |
| 26. Every action has `options` populated via `getActionOptionFromFields()` — without this, the action shows an empty, unusable form | N/A | No DataProvider action/type/app/FactoryMap changes, JNI dependencies or installation changes. |
| 27. Every action has `output_type` set to a typed data type constant (e.g., `MyResponseDataType`) — not omitted | N/A | No DataProvider action/type/app/FactoryMap changes, JNI dependencies or installation changes. |
| 28. DPAT_API actions: provider has `"supports_request": True` and implements `doRequestImpl()` | N/A | No DataProvider action/type/app/FactoryMap changes, JNI dependencies or installation changes. |
| 29. DPAT_FIND actions: every option exists in `SearchOptions`, `getRecordTypeImpl()` returns `*hash<string, AbstractDataField>` | N/A | No DataProvider action/type/app/FactoryMap changes, JNI dependencies or installation changes. |
| 30. Scheme-based apps (with `"scheme"` in registerApp): actions use `"path"` and do NOT use `"cls"` — having both `scheme` and `cls` causes a runtime error | N/A | No DataProvider action/type/app/FactoryMap changes, JNI dependencies or installation changes. |
| 31. Single-key hash slices use trailing comma: `Fields{"key",}` (without trailing comma, `Fields{"key"}` returns the value, not a hash) | N/A | No DataProvider action/type/app/FactoryMap changes, JNI dependencies or installation changes. |
| 32. **Typed data type classes exist** for request and response types — inherit `HashDataType`, have `const Fields` hash, call `addQoreFields(Fields)` in constructor, export public constant at bottom (e.g., `public const MyDataType = new MyDataType();`) | N/A | No DataProvider action/type/app/FactoryMap changes, JNI dependencies or installation changes. |
| 33. Request/input types use `public` Fields (enables `ClassName::Fields` in action registration) | N/A | No DataProvider action/type/app/FactoryMap changes, JNI dependencies or installation changes. |
| 34. Response/output types use `private` Fields | N/A | No DataProvider action/type/app/FactoryMap changes, JNI dependencies or installation changes. |
| 35. Each field in data types has `display_name`, `type`, and `desc` (markdown-formatted) | N/A | No DataProvider action/type/app/FactoryMap changes, JNI dependencies or installation changes. |
| 36. Input fields have `example_value` where useful (string fields, endpoint URIs, SQL queries, etc.) | N/A | No DataProvider action/type/app/FactoryMap changes, JNI dependencies or installation changes. |
| 37. Fields with finite allowed values use `allowed_values` with `AllowedValueInfo` containing both `value` and `display_name` (Title Case, human-readable) — never bare values, never described only in text | N/A | No DataProvider action/type/app/FactoryMap changes, JNI dependencies or installation changes. |
| 38. Password/secret fields have `"sensitive": True` | N/A | No DataProvider action/type/app/FactoryMap changes, JNI dependencies or installation changes. |
| 39. `groups` uses `AppGroup` enum values from `qlib/DataProvider/AppGroup.qc` | N/A | No DataProvider action/type/app/FactoryMap changes, JNI dependencies or installation changes. |
| 40. App `logo` stored as separate file, loaded at module level in `Priv` namespace | N/A | No DataProvider action/type/app/FactoryMap changes, JNI dependencies or installation changes. |
| 41. App `desc` uses markdown: bullet list of capabilities, links to project website, business-language explanation of value | N/A | No DataProvider action/type/app/FactoryMap changes, JNI dependencies or installation changes. |
| 42. `display_name` is user-friendly ("Apache Avro" not "avro") | N/A | No DataProvider action/type/app/FactoryMap changes, JNI dependencies or installation changes. |
| 43. `short_desc` is plain text, under 80 chars, single sentence — no markdown | N/A | No DataProvider action/type/app/FactoryMap changes, JNI dependencies or installation changes. |
| 44. `desc` uses markdown: backticks for code/field refs (`` `field_name` ``, `` `True` ``, `` `pdf` ``), `\n\n` for paragraphs, `- ` bullet lists for enumerations, `**bold**` for caveats | N/A | No DataProvider action/type/app/FactoryMap changes, JNI dependencies or installation changes. |
| 45. Descriptions use plain business language relating to common challenges — not just technical "what" but "why" and "when to use" | N/A | No DataProvider action/type/app/FactoryMap changes, JNI dependencies or installation changes. |
| 46. No bare `True`/`False`/`NOTHING` — must be backtick-wrapped in `desc` | N/A | No DataProvider action/type/app/FactoryMap changes, JNI dependencies or installation changes. |
| 47. No bare field/option names in prose — must use backticks | N/A | No DataProvider action/type/app/FactoryMap changes, JNI dependencies or installation changes. |
| 48. Long descriptions (>500 chars) use bold section headers and bullet lists | N/A | No DataProvider action/type/app/FactoryMap changes, JNI dependencies or installation changes. |
| 49. **Factory registration in Qore repo**: every factory name registered in `qlib/DataProvider/DataProvider.qc` → `FactoryMap` (without this, module loads but doesn't appear in Qorus apps) | N/A | No DataProvider action/type/app/FactoryMap changes, JNI dependencies or installation changes. |
| 50. **`getRecordTypeImpl()` signature**: must be `private *hash<string, AbstractDataField> getRecordTypeImpl(*hash<auto> search_options)` — NOT returning `*AbstractDataProviderType` | N/A | No DataProvider action/type/app/FactoryMap changes, JNI dependencies or installation changes. |
| 51. **Dependency JARs committed** (for JNI modules): JAR files in `qlib/*/jar/` may be gitignored — use `git add -f` to ensure they're tracked, otherwise CI compilation fails | N/A | No DataProvider action/type/app/FactoryMap changes, JNI dependencies or installation changes. |
| 52. JAR install rules in CMakeLists.txt for all dependency JARs | N/A | No DataProvider action/type/app/FactoryMap changes, JNI dependencies or installation changes. |
| 53. **No workarounds**: No TODOs, FIXMEs, stubs, or partially-implemented features | Pass | Source defects classified from schema grammar/expanded names and preserved HTTP evidence, with no substitutions or runtime relaxation. Nine valid historical namespace failures are routed to P2; production grammar checks remain explicitly unassessed in P6. |
| 54. **Exception safety**: C++ uses `ReferenceHolder` for Qore allocations, `std::unique_ptr` for C++ allocations, `*xsink` checked after every fallible operation | Pass | Qore per-stage exception handling and automatic object cleanup; Python file/parser/process ownership and context managers. Last-row token comparisons initialize their DOM references to prevent reuse after an exception. |
| 55. **Thread safety**: All mutable shared state protected by `std::lock_guard<std::mutex>` or documented as immutable-after-construction | Pass | Single-threaded tools with per-run dictionaries; immutable pinned catalog. No new shared mutable state. |
| 56. **Type safety**: Strongly-typed `code<return(args)>` instead of untyped `code`; `static_cast` instead of C casts; typed hashdecls for results; enums where appropriate | Pass | Typed Python interfaces and Qore XsdSchema/WebService objects. Parse-only flags require booleans and empty message jobs; all fixture identities and checksums verified before processing. |
| 57. **Performance**: No O(n²) where O(n) is possible; no unnecessary copies; coordinate descent uses incremental residuals not full matrix multiply | Pass | One indexed inventory pass plus bounded oracle/Qore batches. Duplicate schema copies reuse adjudicated evidence after byte comparison; unused DOM cache removed. No quadratic searches in per-file classification. |
| 58. **Error handling**: All inputs validated (dimensions, empty data, unfitted models); C++ I/O handles EAGAIN/EINTR if applicable | Pass | Extra/missing source files, duplicate case/message identities, changed copies, unclassified imports and unused source-defect metadata are fatal. Failed production behavior is retained with expected categories and phase ownership. |
| 59. **Documentation**: Doxygen `@param`, `@return`, `@throw` on all public methods; `@par Example` with realistic business scenarios; `@note` for important caveats | Pass | README adds reproducible archive command and exact source/implementation distinctions; execution record has every P1 acceptance criterion and evidence; release notes and plan status updated. No public runtime API added. |
| 60. **QPP flags**: `[flags=CONSTANT]` on methods that never throw; `[flags=RET_VALUE_ONLY]` on methods that throw but have no side effects | N/A | No QPP methods changed. |
| 61. **Security**: No user-controlled format strings; no buffer overflows; bounds checking on array indices; no credentials in code | Pass | Pinned inventory/hash verification, no network fallback, restricted external-resource catalog, bounded subprocesses. Original historical scripts are treated solely as data. No credentials or shell evaluation of manifests. |
| 62. **Correctness**: Algorithms verified against reference implementations; edge cases tested (empty data, single sample, all-zero features) | Pass | 58 Python tests and 209 Qore cases pass. All 4,191 roles, 48 import edges, 18 supplemental artifacts and 1,136 aggregate entries accounted for. Both legacy surveys unchanged. Expanded QName/token comparison adjudicates the 16 unchanged invalid IDREF outputs without hiding P5 failures. |

All applicable checks pass; no audit failures remain.
