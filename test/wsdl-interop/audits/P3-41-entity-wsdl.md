# P3-41 audit: selected ENTITY instance requirements

Copyright (C) 2026 Qore Technologies, s.r.o.

The complete audit-changes skill was applied to the final Qore implementation,
new Qore/Python tests, strict selection and generated reports, documentation and
evidence. The referenced module-structure, sandboxing, cancellation and DataProvider
guides were considered for their applicable requirements. Every checklist item is
resolved below: **19 Pass / 43 N/A / 0 Fail**.

Review found and corrected default QName reinterpretation and strengthened the
matrix to require every consumer/sample row and repeated value. The new namespace,
callback/cancellation and completeness checks pass after those corrections.
See [the evidence](../entity-wsdl-evidence.md) for exact gate scope and the explicit
later-phase P6/P7 failures; the phase is not declared complete.

| Check | Status | Evidence |
| --- | --- | --- |
| 1. Entry exists in `doxygen/lang/120_modules.dox.tmpl` (for modules in the Qore repo; N/A for external module repos) | N/A | No new module, separated implementation file, QPP class or registration is introduced. |
| 2. Entry exists in `doxygen/lang/900_release_notes.dox.tmpl` (for modules in the Qore repo; external modules have release notes in their .qm) | Pass | WSDL 0.5.8 release notes describe selected ENTITY instance checks, declaration defaults and nil namespace registration. |
| 3. `qore_user_module()` or `qore_external_user_module()` call in `CMakeLists.txt` | Pass | The existing WSDL external-module registration remains correct; the docs-module build passes. |
| 4. Module added to QMOD list in `CMakeLists.txt` | N/A | No new module, separated implementation file, QPP class or registration is introduced. |
| 5. `.qm` file has `@section <lowercasemodname>intro` as first doc section — **must be all lowercase** (e.g., `avrodataproviderintro`, not `AvroDataProviderintro`) | N/A | No new module, separated implementation file, QPP class or registration is introduced. |
| 6. `%modern` in `.qm` file — no redundant `%new-style`, `%require-types`, `%strict-args`, `%enable-all-warnings` | Pass | The existing WSDL module and both new qtests use %modern; no redundant parse directives were introduced. |
| 7. No parse directives (`%requires`, `%modern`, `%new-style`) in separated `.qc` files (check OUTSIDE of `@code` blocks only) | N/A | No new module, separated implementation file, QPP class or registration is introduced. |
| 8. No `%include` usage (deprecated for modules) | Pass | No deprecated %include was introduced. |
| 9. Copyright 2026 on all new files | Pass | New authored Qore/Python tests, design, evidence and audit have 2026 notices; existing WSDL notice includes 2026. |
| 10. Directory layout: `.qm` inside `qlib/<ModuleName>/` directory (not at `qlib/<ModuleName>.qm` for multi-file modules) | N/A | No new module, separated implementation file, QPP class or registration is introduced. |
| 11. No second `.qm` for the same module at `qlib/<ModuleName>.qm` | N/A | No new module, separated implementation file, QPP class or registration is introduced. |
| 12. `ns=Qore::XX` matches the QoreNamespace constructor path | N/A | No new module, separated implementation file, QPP class or registration is introduced. |
| 13. `%modern` directive present | Pass | Both new qtests explicitly use %modern; reused .qr workers already use it. |
| 14. Executable permission set (`chmod +x`) | Pass | Both qtests and the new Python test are executable. |
| 15. Uses %prepend-module-path  before %requires for in-repo modules (Qore and Qore modules only; not Qorus) | Pass | Qtests prepend local qlib before relative %requires; workers use the same repository modules and the frozen local core environment. |
| 16. External module dependencies use `%try-module` — except modules delivered with the project itself (Qore ex: DataProvider, ConnectionProvider, QUnit, etc.) which use hard `%requires` | Pass | Project xml and core QUnit/HttpServer use hard requirements. Reused workers guard external json and fail explicitly if it is absent. |
| 17. No filesystem operations (fopen, open, creat, unlink, remove, rename, mkdir, rmdir, stat, chmod) without sandbox checks | N/A | No C++/QPP, native I/O, native loop, cancellation or method-flag change. |
| 18. No network operations (connect, bind, socket, getaddrinfo, gethostbyname) without sandbox checks | N/A | No C++/QPP, native I/O, native loop, cancellation or method-flag change. |
| 19. If filesystem/network ops exist, verify `QoreSandboxManagerHelper` usage | N/A | No C++/QPP, native I/O, native loop, cancellation or method-flag change. |
| 20. No `File::`, `Dir::`, `Socket::`, `HTTPClient::` usage without justification | Pass | Production adds no I/O. Tests use explicit temporary fixture files and local HTTP with bounded queues/timeouts and exception-safe server teardown. |
| 21. All `for`/`while` loops that could iterate >100 times have `qore_check_cancel()` checks | N/A | No C++/QPP, native I/O, native loop, cancellation or method-flag change. |
| 22. Uses `qore_check_cancel()` (NOT deprecated `qore_check_io_interrupt()`) | N/A | No C++/QPP, native I/O, native loop, cancellation or method-flag change. |
| 23. Check frequency: every 100 iterations for tight loops, every 10 for expensive iterations | N/A | No C++/QPP, native I/O, native loop, cancellation or method-flag change. |
| 24. No blocking operations without cancellation support | N/A | No C++/QPP, native I/O, native loop, cancellation or method-flag change. |
| 25. Every action has `display_name`, `short_desc` (plain text, <80 chars), `desc` (markdown) | N/A | No action/app/factory, request/response record class, business field metadata or JNI dependency was added or changed. |
| 26. Every action has `options` populated via `getActionOptionFromFields()` — without this, the action shows an empty, unusable form | N/A | No action/app/factory, request/response record class, business field metadata or JNI dependency was added or changed. |
| 27. Every action has `output_type` set to a typed data type constant (e.g., `MyResponseDataType`) — not omitted | N/A | No action/app/factory, request/response record class, business field metadata or JNI dependency was added or changed. |
| 28. DPAT_API actions: provider has `"supports_request": True` and implements `doRequestImpl()` | N/A | No action/app/factory, request/response record class, business field metadata or JNI dependency was added or changed. |
| 29. DPAT_FIND actions: every option exists in `SearchOptions`, `getRecordTypeImpl()` returns `*hash<string, AbstractDataField>` | N/A | No action/app/factory, request/response record class, business field metadata or JNI dependency was added or changed. |
| 30. Scheme-based apps (with `"scheme"` in registerApp): actions use `"path"` and do NOT use `"cls"` — having both `scheme` and `cls` causes a runtime error | N/A | No action/app/factory, request/response record class, business field metadata or JNI dependency was added or changed. |
| 31. Single-key hash slices use trailing comma: `Fields{"key",}` (without trailing comma, `Fields{"key"}` returns the value, not a hash) | N/A | No action/app/factory, request/response record class, business field metadata or JNI dependency was added or changed. |
| 32. **Typed data type classes exist** for request and response types — inherit `HashDataType`, have `const Fields` hash, call `addQoreFields(Fields)` in constructor, export public constant at bottom (e.g., `public const MyDataType = new MyDataType();`) | N/A | No action/app/factory, request/response record class, business field metadata or JNI dependency was added or changed. |
| 33. Request/input types use `public` Fields (enables `ClassName::Fields` in action registration) | N/A | No action/app/factory, request/response record class, business field metadata or JNI dependency was added or changed. |
| 34. Response/output types use `private` Fields | N/A | No action/app/factory, request/response record class, business field metadata or JNI dependency was added or changed. |
| 35. Each field in data types has `display_name`, `type`, and `desc` (markdown-formatted) | N/A | No action/app/factory, request/response record class, business field metadata or JNI dependency was added or changed. |
| 36. Input fields have `example_value` where useful (string fields, endpoint URIs, SQL queries, etc.) | N/A | No action/app/factory, request/response record class, business field metadata or JNI dependency was added or changed. |
| 37. Fields with finite allowed values use `allowed_values` with `AllowedValueInfo` containing both `value` and `display_name` (Title Case, human-readable) — never bare values, never described only in text | N/A | No action/app/factory, request/response record class, business field metadata or JNI dependency was added or changed. |
| 38. Password/secret fields have `"sensitive": True` | N/A | No action/app/factory, request/response record class, business field metadata or JNI dependency was added or changed. |
| 39. `groups` uses `AppGroup` enum values from `qlib/DataProvider/AppGroup.qc` | N/A | No action/app/factory, request/response record class, business field metadata or JNI dependency was added or changed. |
| 40. App `logo` stored as separate file, loaded at module level in `Priv` namespace | N/A | No action/app/factory, request/response record class, business field metadata or JNI dependency was added or changed. |
| 41. App `desc` uses markdown: bullet list of capabilities, links to project website, business-language explanation of value | N/A | No action/app/factory, request/response record class, business field metadata or JNI dependency was added or changed. |
| 42. `display_name` is user-friendly ("Apache Avro" not "avro") | N/A | No action/app/factory, request/response record class, business field metadata or JNI dependency was added or changed. |
| 43. `short_desc` is plain text, under 80 chars, single sentence — no markdown | N/A | No action/app/factory, request/response record class, business field metadata or JNI dependency was added or changed. |
| 44. `desc` uses markdown: backticks for code/field refs (`` `field_name` ``, `` `True` ``, `` `pdf` ``), `\n\n` for paragraphs, `- ` bullet lists for enumerations, `**bold**` for caveats | N/A | No action/app/factory, request/response record class, business field metadata or JNI dependency was added or changed. |
| 45. Descriptions use plain business language relating to common challenges — not just technical "what" but "why" and "when to use" | N/A | No action/app/factory, request/response record class, business field metadata or JNI dependency was added or changed. |
| 46. No bare `True`/`False`/`NOTHING` — must be backtick-wrapped in `desc` | N/A | No action/app/factory, request/response record class, business field metadata or JNI dependency was added or changed. |
| 47. No bare field/option names in prose — must use backticks | N/A | No action/app/factory, request/response record class, business field metadata or JNI dependency was added or changed. |
| 48. Long descriptions (>500 chars) use bold section headers and bullet lists | N/A | No action/app/factory, request/response record class, business field metadata or JNI dependency was added or changed. |
| 49. **Factory registration in Qore repo**: every factory name registered in `qlib/DataProvider/DataProvider.qc` → `FactoryMap` (without this, module loads but doesn't appear in Qorus apps) | N/A | No action/app/factory, request/response record class, business field metadata or JNI dependency was added or changed. |
| 50. **`getRecordTypeImpl()` signature**: must be `private *hash<string, AbstractDataField> getRecordTypeImpl(*hash<auto> search_options)` — NOT returning `*AbstractDataProviderType` | N/A | No action/app/factory, request/response record class, business field metadata or JNI dependency was added or changed. |
| 51. **Dependency JARs committed** (for JNI modules): JAR files in `qlib/*/jar/` may be gitignored — use `git add -f` to ensure they're tracked, otherwise CI compilation fails | N/A | No action/app/factory, request/response record class, business field metadata or JNI dependency was added or changed. |
| 52. JAR install rules in CMakeLists.txt for all dependency JARs | N/A | No action/app/factory, request/response record class, business field metadata or JNI dependency was added or changed. |
| 53. **No workarounds**: No TODOs, FIXMEs, stubs, or partially-implemented features | Pass | The fix retains the selected document requirement instead of altering lexical acceptance or retrying a later union member. No fixture names, stub, TODO or workaround added. Remaining provider/sample and P6/P7 work is explicit phase ownership, not claimed implemented. |
| 54. **Exception safety**: C++ uses `ReferenceHolder` for Qore allocations, `std::unique_ptr` for C++ allocations, `*xsink` checked after every fallible operation | Pass | RAII capture lifetimes restore both thread-local slots on success, nested rejection and cancellation. Default checking uses a local namespace registry and preserves the compiled value. Callback, sandbox interruption and recovery tests pass. |
| 55. **Thread safety**: All mutable shared state protected by `std::lock_guard<std::mutex>` or documented as immutable-after-construction | Pass | Mutable capture state is thread-local and scoped to each operation. Four synchronized workers exercise reconstruction, rejection and reuse without sharing state. |
| 56. **Type safety**: Strongly-typed `code<return(args)>` instead of untyped `code`; `static_cast` instead of C casts; typed hashdecls for results; enums where appropriate | Pass | Selected requirements use list<string> in the typed identity hashdecl. Capture targets and caller state have concrete class types; test closures are typed, and no native casts were added. |
| 57. **Performance**: No O(n²) where O(n) is possible; no unnecessary copies; coordinate descent uses incremental residuals not full matrix multiply | Pass | Entity requirements append to existing ordered identity traversal, with linear storage. Cached selected identities are reused; callback-count tests show no extra member conversion at document boundaries. Existing shared-graph suites pass. |
| 58. **Error handling**: All inputs validated (dimensions, empty data, unfitted models); C++ I/O handles EAGAIN/EINTR if applicable | Pass | Expected serialization/deserialization categories are asserted for missing declarations, facets, lists, defaults and retained DTDs. Empty custom lists and absent/nilled values remain valid. Unexpected cancellation/control errors propagate. |
| 59. **Documentation**: Doxygen `@param`, `@return`, `@throw` on all public methods; `@par Example` with realistic business scenarios; `@note` for important caveats | Pass | getInstanceDefaultValue documents result, failure, namespace caveat and an example. The implemented design, release notes, README, evidence and execution record are updated. The example and Doxygen build pass. |
| 60. **QPP flags**: `[flags=CONSTANT]` on methods that never throw; `[flags=RET_VALUE_ONLY]` on methods that throw but have no side effects | N/A | No C++/QPP, native I/O, native loop, cancellation or method-flag change. |
| 61. **Security**: No user-controlled format strings; no buffer overflows; bounds checking on array indices; no credentials in code | Pass | No credentials, raw buffers, format-string input, network access or new parser policy added. First-name indexing is guarded by a nonempty list; SOAP DTD rejection remains intact. |
| 62. **Correctness**: Algorithms verified against reference implementations; edge cases tested (empty data, single sample, all-zero features) | Pass | 92 Qore suites pass 948 cases / 36,146 reported assertions; the new unit suite passes all four execution modes. Independent tests check 12,096 outcomes and 8,280 document verdicts with exact values/completeness. Strict coverage adds 16 expected rejections and no non-ENTITY regression. The two existing P6 assertions and reduced P7 fault-origin issue remain explicitly tracked. |
