# P3-24 audit: exact calendar values

Copyright (C) 2026 Qore Technologies, s.r.o.

Final audit of the tested calendar increment; all current-change verification gates are complete.

Scope: the six WSDL calendar primitives, exact facets/choices/list/union identity, provider metadata and samples; Qore/Python/HTTP regressions; independent ordinal reference and validator evidence; strict corpus promotion, current reports, public design and release notes. Core prerequisites are already committed separately as `db964e98f` (formerly `516e6f434`).

The complete audit-changes checklist and applicable module-structure, sandboxing, cooperative-cancellation and DataProvider guides were reviewed. Hand-written application timestamp-field guidance does not define XSD partial-date or absent-timezone value spaces; the schema provider reports the documented lossless alternatives. No app/action registration or C++ code changes are included.

Checklist: **21 Pass, 41 N/A, 0 Fail**.

| Check | Status | Evidence |
| --- | --- | --- |
| 1. Entry exists in `doxygen/lang/120_modules.dox.tmpl` (for modules in the Qore repo; N/A for external module repos) | N/A | Existing external single-file module; no new module, separated source, duplicate entry point or core module-list entry. |
| 2. Entry exists in `doxygen/lang/900_release_notes.dox.tmpl` (for modules in the Qore repo; external modules have release notes in their .qm) | Pass | WSDL 0.5.8 release notes document calendar grammar, timezone preservation, string alternatives, facets/providers and examples. |
| 3. `qore_user_module()` or `qore_external_user_module()` call in `CMakeLists.txt` | Pass | Existing WSDL/SoapDataProvider/SoapHandler CMake targets compile the local AOT modules; Qdx and Doxygen pass without diagnostics. |
| 4. Module added to QMOD list in `CMakeLists.txt` | N/A | Existing external single-file module; no new module, separated source, duplicate entry point or core module-list entry. |
| 5. `.qm` file has `@section <lowercasemodname>intro` as first doc section — **must be all lowercase** (e.g., `avrodataproviderintro`, not `AvroDataProviderintro`) | Pass | The existing single-file module keeps lowercase wsdlintro as its first documentation section. |
| 6. `%modern` in `.qm` file — no redundant `%new-style`, `%require-types`, `%strict-args`, `%enable-all-warnings` | Pass | WSDL and all new Qore regression/worker files use %modern; no redundant legacy directives are added. |
| 7. No parse directives (`%requires`, `%modern`, `%new-style`) in separated `.qc` files (check OUTSIDE of `@code` blocks only) | N/A | Existing external single-file module; no new module, separated source, duplicate entry point or core module-list entry. |
| 8. No `%include` usage (deprecated for modules) | Pass | No deprecated %include directive is introduced. |
| 9. Copyright 2026 on all new files | Pass | Every new authored source, test, JSON fixture, design and evidence file carries copyright 2026. |
| 10. Directory layout: `.qm` inside `qlib/<ModuleName>/` directory (not at `qlib/<ModuleName>.qm` for multi-file modules) | N/A | Existing external single-file module; no new module, separated source, duplicate entry point or core module-list entry. |
| 11. No second `.qm` for the same module at `qlib/<ModuleName>.qm` | N/A | Existing external single-file module; no new module, separated source, duplicate entry point or core module-list entry. |
| 12. `ns=Qore::XX` matches the QoreNamespace constructor path | N/A | No C++/QPP changes or new native I/O. Existing native cancellation and sandbox-aware APIs remain unchanged; a new Valgrind run is not required for Qore-only changes. |
| 13. `%modern` directive present | Pass | New qtest and .qr workers use %modern; existing updated tests retain their modern directive. |
| 14. Executable permission set (`chmod +x`) | Pass | All three new qtests, both .qr workers and the Python test entry point are executable; the Python reference is an imported utility. |
| 15. Uses %prepend-module-path  before %requires for in-repo modules (Qore and Qore modules only; not Qorus) | Pass | New qtests and the value worker prepend local qlib and require relative WSDL source. The native-only diagnostic needs no user module from this repository. Compiled-module tests explicitly load the local qmods. |
| 16. External module dependencies use `%try-module` — except modules delivered with the project itself (Qore ex: DataProvider, ConnectionProvider, QUnit, etc.) which use hard `%requires` | Pass | Repository xml/WSDL/SoapClient/SoapHandler and Qore QUnit/HttpServer remain hard requirements. External json uses %try-module with an explicit missing-dependency error. |
| 17. No filesystem operations (fopen, open, creat, unlink, remove, rename, mkdir, rmdir, stat, chmod) without sandbox checks | N/A | No C++/QPP changes or new native I/O. Existing native cancellation and sandbox-aware APIs remain unchanged; a new Valgrind run is not required for Qore-only changes. |
| 18. No network operations (connect, bind, socket, getaddrinfo, gethostbyname) without sandbox checks | N/A | No C++/QPP changes or new native I/O. Existing native cancellation and sandbox-aware APIs remain unchanged; a new Valgrind run is not required for Qore-only changes. |
| 19. If filesystem/network ops exist, verify `QoreSandboxManagerHelper` usage | N/A | No C++/QPP changes or new native I/O. Existing native cancellation and sandbox-aware APIs remain unchanged; a new Valgrind run is not required for Qore-only changes. |
| 20. No `File::`, `Dir::`, `Socket::`, `HTTPClient::` usage without justification | Pass | Production calendar conversion adds no filesystem/network access. Workers read only supplied manifests. HTTP tests use local listeners, bounded queue/I/O deadlines and on_exit cleanup. |
| 21. All `for`/`while` loops that could iterate >100 times have `qore_check_cancel()` checks | N/A | No C++ loops are changed. Qore loops retain runtime cancellation checks; deterministic provider cancellation and recovery are tested. |
| 22. Uses `qore_check_cancel()` (NOT deprecated `qore_check_io_interrupt()`) | N/A | No C++/QPP changes or new native I/O. Existing native cancellation and sandbox-aware APIs remain unchanged; a new Valgrind run is not required for Qore-only changes. |
| 23. Check frequency: every 100 iterations for tight loops, every 10 for expensive iterations | N/A | No C++/QPP changes or new native I/O. Existing native cancellation and sandbox-aware APIs remain unchanged; a new Valgrind run is not required for Qore-only changes. |
| 24. No blocking operations without cancellation support | N/A | No C++/QPP changes or new native I/O. Existing native cancellation and sandbox-aware APIs remain unchanged; a new Valgrind run is not required for Qore-only changes. |
| 25. Every action has `display_name`, `short_desc` (plain text, <80 chars), `desc` (markdown) | N/A | No app/action registration is introduced; the increment implements schema-generated scalar providers and fields. |
| 26. Every action has `options` populated via `getActionOptionFromFields()` — without this, the action shows an empty, unusable form | N/A | No app/action registration, request/response record catalog, secret, connection scheme, factory, logo, presentation catalog or JAR is introduced. This increment implements schema-generated scalar providers and fields. |
| 27. Every action has `output_type` set to a typed data type constant (e.g., `MyResponseDataType`) — not omitted | N/A | No app/action registration, request/response record catalog, secret, connection scheme, factory, logo, presentation catalog or JAR is introduced. This increment implements schema-generated scalar providers and fields. |
| 28. DPAT_API actions: provider has `"supports_request": True` and implements `doRequestImpl()` | N/A | No app/action registration, request/response record catalog, secret, connection scheme, factory, logo, presentation catalog or JAR is introduced. This increment implements schema-generated scalar providers and fields. |
| 29. DPAT_FIND actions: every option exists in `SearchOptions`, `getRecordTypeImpl()` returns `*hash<string, AbstractDataField>` | N/A | No app/action registration, request/response record catalog, secret, connection scheme, factory, logo, presentation catalog or JAR is introduced. This increment implements schema-generated scalar providers and fields. |
| 30. Scheme-based apps (with `"scheme"` in registerApp): actions use `"path"` and do NOT use `"cls"` — having both `scheme` and `cls` causes a runtime error | N/A | No app/action registration, request/response record catalog, secret, connection scheme, factory, logo, presentation catalog or JAR is introduced. This increment implements schema-generated scalar providers and fields. |
| 31. Single-key hash slices use trailing comma: `Fields{"key",}` (without trailing comma, `Fields{"key"}` returns the value, not a hash) | N/A | No app/action registration, request/response record catalog, secret, connection scheme, factory, logo, presentation catalog or JAR is introduced. This increment implements schema-generated scalar providers and fields. |
| 32. **Typed data type classes exist** for request and response types — inherit `HashDataType`, have `const Fields` hash, call `addQoreFields(Fields)` in constructor, export public constant at bottom (e.g., `public const MyDataType = new MyDataType();`) | N/A | No app/action registration, request/response record catalog, secret, connection scheme, factory, logo, presentation catalog or JAR is introduced. This increment implements schema-generated scalar providers and fields. |
| 33. Request/input types use `public` Fields (enables `ClassName::Fields` in action registration) | N/A | No app/action registration, request/response record catalog, secret, connection scheme, factory, logo, presentation catalog or JAR is introduced. This increment implements schema-generated scalar providers and fields. |
| 34. Response/output types use `private` Fields | N/A | No app/action registration, request/response record catalog, secret, connection scheme, factory, logo, presentation catalog or JAR is introduced. This increment implements schema-generated scalar providers and fields. |
| 35. Each field in data types has `display_name`, `type`, and `desc` (markdown-formatted) | N/A | No app/action registration, request/response record catalog, secret, connection scheme, factory, logo, presentation catalog or JAR is introduced. This increment implements schema-generated scalar providers and fields. |
| 36. Input fields have `example_value` where useful (string fields, endpoint URIs, SQL queries, etc.) | N/A | No app/action registration, request/response record catalog, secret, connection scheme, factory, logo, presentation catalog or JAR is introduced. This increment implements schema-generated scalar providers and fields. |
| 37. Fields with finite allowed values use `allowed_values` with `AllowedValueInfo` containing both `value` and `display_name` (Title Case, human-readable) — never bare values, never described only in text | Pass | XsdCalendarDataField builds AllowedValueInfo entries with value/display_name, compares exact calendar keys and validates replacement choices before assignment; original/reconstructed and repeated choices are tested. |
| 38. Password/secret fields have `"sensitive": True` | N/A | No app/action registration, request/response record catalog, secret, connection scheme, factory, logo, presentation catalog or JAR is introduced. This increment implements schema-generated scalar providers and fields. |
| 39. `groups` uses `AppGroup` enum values from `qlib/DataProvider/AppGroup.qc` | N/A | No app/action registration, request/response record catalog, secret, connection scheme, factory, logo, presentation catalog or JAR is introduced. This increment implements schema-generated scalar providers and fields. |
| 40. App `logo` stored as separate file, loaded at module level in `Priv` namespace | N/A | No app/action registration, request/response record catalog, secret, connection scheme, factory, logo, presentation catalog or JAR is introduced. This increment implements schema-generated scalar providers and fields. |
| 41. App `desc` uses markdown: bullet list of capabilities, links to project website, business-language explanation of value | N/A | No app/action registration, request/response record catalog, secret, connection scheme, factory, logo, presentation catalog or JAR is introduced. This increment implements schema-generated scalar providers and fields. |
| 42. `display_name` is user-friendly ("Apache Avro" not "avro") | N/A | No app/action registration, request/response record catalog, secret, connection scheme, factory, logo, presentation catalog or JAR is introduced. This increment implements schema-generated scalar providers and fields. |
| 43. `short_desc` is plain text, under 80 chars, single sentence — no markdown | N/A | No app/action registration, request/response record catalog, secret, connection scheme, factory, logo, presentation catalog or JAR is introduced. This increment implements schema-generated scalar providers and fields. |
| 44. `desc` uses markdown: backticks for code/field refs (`` `field_name` ``, `` `True` ``, `` `pdf` ``), `\n\n` for paragraphs, `- ` bullet lists for enumerations, `**bold**` for caveats | N/A | No app/action registration, request/response record catalog, secret, connection scheme, factory, logo, presentation catalog or JAR is introduced. This increment implements schema-generated scalar providers and fields. |
| 45. Descriptions use plain business language relating to common challenges — not just technical "what" but "why" and "when to use" | N/A | No app/action registration, request/response record catalog, secret, connection scheme, factory, logo, presentation catalog or JAR is introduced. This increment implements schema-generated scalar providers and fields. |
| 46. No bare `True`/`False`/`NOTHING` — must be backtick-wrapped in `desc` | N/A | No app/action registration, request/response record catalog, secret, connection scheme, factory, logo, presentation catalog or JAR is introduced. This increment implements schema-generated scalar providers and fields. |
| 47. No bare field/option names in prose — must use backticks | N/A | No app/action registration, request/response record catalog, secret, connection scheme, factory, logo, presentation catalog or JAR is introduced. This increment implements schema-generated scalar providers and fields. |
| 48. Long descriptions (>500 chars) use bold section headers and bullet lists | N/A | No app/action registration, request/response record catalog, secret, connection scheme, factory, logo, presentation catalog or JAR is introduced. This increment implements schema-generated scalar providers and fields. |
| 49. **Factory registration in Qore repo**: every factory name registered in `qlib/DataProvider/DataProvider.qc` → `FactoryMap` (without this, module loads but doesn't appear in Qorus apps) | N/A | No app/action registration, request/response record catalog, secret, connection scheme, factory, logo, presentation catalog or JAR is introduced. This increment implements schema-generated scalar providers and fields. |
| 50. **`getRecordTypeImpl()` signature**: must be `private *hash<string, AbstractDataField> getRecordTypeImpl(*hash<auto> search_options)` — NOT returning `*AbstractDataProviderType` | N/A | No app/action registration, request/response record catalog, secret, connection scheme, factory, logo, presentation catalog or JAR is introduced. This increment implements schema-generated scalar providers and fields. |
| 51. **Dependency JARs committed** (for JNI modules): JAR files in `qlib/*/jar/` may be gitignored — use `git add -f` to ensure they're tracked, otherwise CI compilation fails | N/A | No app/action registration, request/response record catalog, secret, connection scheme, factory, logo, presentation catalog or JAR is introduced. This increment implements schema-generated scalar providers and fields. |
| 52. JAR install rules in CMakeLists.txt for all dependency JARs | N/A | No app/action registration, request/response record catalog, secret, connection scheme, factory, logo, presentation catalog or JAR is introduced. This increment implements schema-generated scalar providers and fields. |
| 53. **No workarounds**: No TODOs, FIXMEs, stubs, or partially-implemented features | Pass | Calendar syntax and value semantics are implemented independently of native limits. Exact validator defects remain visible with source evidence; no production fixture names, tolerance, lexical cap or fallback retry is added. |
| 54. **Exception safety**: C++ uses `ReferenceHolder` for Qore allocations, `std::unique_ptr` for C++ allocations, `*xsink` checked after every fallible operation | Pass | Qore owns all allocations. Provider metadata validates before assignment; field setters build complete replacement maps first. Cancellation keeps its category and interrupted provider conversion recovers. |
| 55. **Thread safety**: All mutable shared state protected by `std::lock_guard<std::mutex>` or documented as immutable-after-construction | Pass | No global mutable state is introduced. Builtin/facet metadata is immutable after construction; optionality makes copies. Existing field setters retain caller synchronization, documented in the design. HTTP callback exchange uses queues. |
| 56. **Type safety**: Strongly-typed `code<return(args)>` instead of untyped `code`; `static_cast` instead of C casts; typed hashdecls for results; enums where appropriate | Pass | Calendar lexical fields, normalized instants and facets have typed hash declarations; optional timezone and partial comparison use *int. Known conflicting numeric/calendar provider identities reject. Heterogeneous values stay at existing scalar boundaries. |
| 57. **Performance**: No O(n²) where O(n) is possible; no unnecessary copies; coordinate descent uses incremental residuals not full matrix multiply | Pass | Calendar comparison is linear in lexical length and facet/choice count. Arbitrary year carry scans one suffix, timezone normalization shifts at most one day, and sample generation tries a fixed set per endpoint instead of enumerating calendar values. |
| 58. **Error handling**: All inputs validated (dimensions, empty data, unfitted models); C++ I/O handles EAGAIN/EINTR if applicable | Pass | Positive/negative grammar, timezone/year/leap limits, unknown categories, bounds/indeterminate intervals, inherited/fixed constraints, optionality, malformed reconstruction and failed choice updates have intended error-category assertions. |
| 59. **Documentation**: Doxygen `@param`, `@return`, `@throw` on all public methods; `@par Example` with realistic business scenarios; `@note` for important caveats | Pass | All new public provider/field methods document parameters, results and relevant errors. The design states native/string alternatives and the legacy closest-type distinction; its example executes without output. Qdx/Doxygen are clean. |
| 60. **QPP flags**: `[flags=CONSTANT]` on methods that never throw; `[flags=RET_VALUE_ONLY]` on methods that throw but have no side effects | N/A | No C++/QPP changes or new native I/O. Existing native cancellation and sandbox-aware APIs remain unchanged; a new Valgrind run is not required for Qore-only changes. |
| 61. **Security**: No user-controlled format strings; no buffer overflows; bounds checking on array indices; no credentials in code | Pass | Calendar grammar is fully anchored ASCII with explicit range checks. No unmanaged buffers, arbitrary format strings, secrets or new production I/O are introduced. XML escaping and sandbox-aware test APIs remain in use. |
| 62. **Correctness**: Algorithms verified against reference implementations; edge cases tested (empty data, single sample, all-zero features) | Pass | Full Python discovery ran 179 methods in 1118.493 seconds: exactly the prior 33 later-phase failure signatures, no new signatures and no errors. Completed gates: 69 Qore suites / 780 cases / 26409 assertions; new 19 cases / 5451 assertions across eight mode/zone combinations and compiled modules; all nine new Python methods, exact 1838-row boundaries, strict 114 WSDLs / 1100 directions with zero selected failures, 12 resolved broad failures and zero added signatures. |

Independent evidence is in [calendar-values-evidence.md](../calendar-values-evidence.md). Final source/runtime hashes and test results are recorded in [EXECUTION.md](../EXECUTION.md) for this commit.
