# P5-20i ordinary recursive provider audit

Copyright (C) 2026 Qore Technologies, s.r.o.

The audit-changes skill and all five referenced guides were consulted. Their
hashes match the preceding accepted guide snapshot. This review covers the
ordinary recursive-provider implementation, validating-record optionality,
three new qtests, design/example, release notes and acceptance records.
No C++ changes or application/action registration changes are included.

Review and testing corrected acceptance-vs-return metadata and mandatory NULL
handling in the prototype. The Qore shared-soft-graph dependency is verified
fixed in installed commit `16ae86ca7`. Failed construction, failed soft
completion and actual program interruption recover; no workaround is included.
The separate identity-tuple prototype remains outside this commit.

| # | Check | Status | Evidence |
| --- | --- | --- | --- |
| 1 | Entry exists in `doxygen/lang/120_modules.dox.tmpl` (for modules in the Qore repo; N/A for external module repos) | N/A | No new module, split-module layout or Qore module-index entry. |
| 2 | Entry exists in `doxygen/lang/900_release_notes.dox.tmpl` (for modules in the Qore repo; external modules have release notes in their .qm) | Pass | WSDL 0.5.8 release notes describe complete recursive fields and validating-record optionality. |
| 3 | `qore_user_module()` or `qore_external_user_module()` call in `CMakeLists.txt` | Pass | Existing qore_external_user_module registration builds WSDL documentation; no module added. |
| 4 | Module added to QMOD list in `CMakeLists.txt` | N/A | No new module, split-module layout or Qore module-index entry. |
| 5 | `.qm` file has `@section <lowercasemodname>intro` as first doc section — **must be all lowercase** (e.g., `avrodataproviderintro`, not `AvroDataProviderintro`) | Pass | Existing first section is wsdlintro. |
| 6 | `%modern` in `.qm` file — no redundant `%new-style`, `%require-types`, `%strict-args`, `%enable-all-warnings` | Pass | Existing WSDL entry declares %modern; no redundant directives added. |
| 7 | No parse directives (`%requires`, `%modern`, `%new-style`) in separated `.qc` files (check OUTSIDE of `@code` blocks only) | N/A | No new module, split-module layout or Qore module-index entry. |
| 8 | No `%include` usage (deprecated for modules) | Pass | No %include introduced. |
| 9 | Copyright 2026 on all new files | Pass | All authored source, test and documentation files carry 2026 copyright. |
| 10 | Directory layout: `.qm` inside `qlib/<ModuleName>/` directory (not at `qlib/<ModuleName>.qm` for multi-file modules) | N/A | No new module, split-module layout or Qore module-index entry. |
| 11 | No second `.qm` for the same module at `qlib/<ModuleName>.qm` | Pass | WSDL remains one flat module; no duplicate module entry. |
| 12 | `ns=Qore::XX` matches the QoreNamespace constructor path | N/A | No C++ or QPP changes; no added external resource operation. |
| 13 | `%modern` directive present | Pass | All three new qtests declare %modern; parsed interruption program does too. |
| 14 | Executable permission set (`chmod +x`) | Pass | All three qtests are executable (0755). |
| 15 | Uses %prepend-module-path  before %requires for in-repo modules (Qore and Qore modules only; not Qorus) | Pass | Tests prepend ../qlib before relative WSDL requirements; commands select local Debug XML and frozen core sources. |
| 16 | External module dependencies use `%try-module` — except modules delivered with the project itself (Qore ex: DataProvider, ConnectionProvider, QUnit, etc.) which use hard `%requires` | Pass | WSDL/XML belong to this repository and are hard requirements; QUnit is supplied by Qore. No optional external binary dependency added. |
| 17 | No filesystem operations (fopen, open, creat, unlink, remove, rename, mkdir, rmdir, stat, chmod) without sandbox checks | N/A | No C++ or QPP changes; no added external resource operation. |
| 18 | No network operations (connect, bind, socket, getaddrinfo, gethostbyname) without sandbox checks | N/A | No C++ or QPP changes; no added external resource operation. |
| 19 | If filesystem/network ops exist, verify `QoreSandboxManagerHelper` usage | N/A | No C++ or QPP changes; no added external resource operation. |
| 20 | No `File::`, `Dir::`, `Socket::`, `HTTPClient::` usage without justification | Pass | No production filesystem/network access added; all graph state is in memory. |
| 21 | All `for`/`while` loops that could iterate >100 times have `qore_check_cancel()` checks | N/A | No C++ or QPP changes; no added external resource operation. |
| 22 | Uses `qore_check_cancel()` (NOT deprecated `qore_check_io_interrupt()`) | N/A | No C++ or QPP changes; no added external resource operation. |
| 23 | Check frequency: every 100 iterations for tight loops, every 10 for expensive iterations | N/A | No C++ or QPP changes; no added external resource operation. |
| 24 | No blocking operations without cancellation support | Pass | No blocking production operations added. Test queues have bounded deadlines and deterministic completion; interruption and retry pass. |
| 25 | Every action has `display_name`, `short_desc` (plain text, <80 chars), `desc` (markdown) | N/A | No app/action registration, endpoint request/response type, factory, JNI dependency or presentation field is added. These are schema-generated provider graph helpers. |
| 26 | Every action has `options` populated via `getActionOptionFromFields()` — without this, the action shows an empty, unusable form | N/A | No app/action registration, endpoint request/response type, factory, JNI dependency or presentation field is added. These are schema-generated provider graph helpers. |
| 27 | Every action has `output_type` set to a typed data type constant (e.g., `MyResponseDataType`) — not omitted | N/A | No app/action registration, endpoint request/response type, factory, JNI dependency or presentation field is added. These are schema-generated provider graph helpers. |
| 28 | DPAT_API actions: provider has `"supports_request": True` and implements `doRequestImpl()` | N/A | No app/action registration, endpoint request/response type, factory, JNI dependency or presentation field is added. These are schema-generated provider graph helpers. |
| 29 | DPAT_FIND actions: every option exists in `SearchOptions`, `getRecordTypeImpl()` returns `*hash<string, AbstractDataField>` | N/A | No app/action registration, endpoint request/response type, factory, JNI dependency or presentation field is added. These are schema-generated provider graph helpers. |
| 30 | Scheme-based apps (with `"scheme"` in registerApp): actions use `"path"` and do NOT use `"cls"` — having both `scheme` and `cls` causes a runtime error | N/A | No app/action registration, endpoint request/response type, factory, JNI dependency or presentation field is added. These are schema-generated provider graph helpers. |
| 31 | Single-key hash slices use trailing comma: `Fields{"key",}` (without trailing comma, `Fields{"key"}` returns the value, not a hash) | N/A | No app/action registration, endpoint request/response type, factory, JNI dependency or presentation field is added. These are schema-generated provider graph helpers. |
| 32 | **Typed data type classes exist** for request and response types — inherit `HashDataType`, have `const Fields` hash, call `addQoreFields(Fields)` in constructor, export public constant at bottom (e.g., `public const MyDataType = new MyDataType();`) | N/A | No app/action registration, endpoint request/response type, factory, JNI dependency or presentation field is added. These are schema-generated provider graph helpers. |
| 33 | Request/input types use `public` Fields (enables `ClassName::Fields` in action registration) | N/A | No app/action registration, endpoint request/response type, factory, JNI dependency or presentation field is added. These are schema-generated provider graph helpers. |
| 34 | Response/output types use `private` Fields | N/A | No app/action registration, endpoint request/response type, factory, JNI dependency or presentation field is added. These are schema-generated provider graph helpers. |
| 35 | Each field in data types has `display_name`, `type`, and `desc` (markdown-formatted) | N/A | No app/action registration, endpoint request/response type, factory, JNI dependency or presentation field is added. These are schema-generated provider graph helpers. |
| 36 | Input fields have `example_value` where useful (string fields, endpoint URIs, SQL queries, etc.) | N/A | No app/action registration, endpoint request/response type, factory, JNI dependency or presentation field is added. These are schema-generated provider graph helpers. |
| 37 | Fields with finite allowed values use `allowed_values` with `AllowedValueInfo` containing both `value` and `display_name` (Title Case, human-readable) — never bare values, never described only in text | N/A | No app/action registration, endpoint request/response type, factory, JNI dependency or presentation field is added. These are schema-generated provider graph helpers. |
| 38 | Password/secret fields have `"sensitive": True` | N/A | No app/action registration, endpoint request/response type, factory, JNI dependency or presentation field is added. These are schema-generated provider graph helpers. |
| 39 | `groups` uses `AppGroup` enum values from `qlib/DataProvider/AppGroup.qc` | N/A | No app/action registration, endpoint request/response type, factory, JNI dependency or presentation field is added. These are schema-generated provider graph helpers. |
| 40 | App `logo` stored as separate file, loaded at module level in `Priv` namespace | N/A | No app/action registration, endpoint request/response type, factory, JNI dependency or presentation field is added. These are schema-generated provider graph helpers. |
| 41 | App `desc` uses markdown: bullet list of capabilities, links to project website, business-language explanation of value | N/A | No app/action registration, endpoint request/response type, factory, JNI dependency or presentation field is added. These are schema-generated provider graph helpers. |
| 42 | `display_name` is user-friendly ("Apache Avro" not "avro") | N/A | No app/action registration, endpoint request/response type, factory, JNI dependency or presentation field is added. These are schema-generated provider graph helpers. |
| 43 | `short_desc` is plain text, under 80 chars, single sentence — no markdown | N/A | No app/action registration, endpoint request/response type, factory, JNI dependency or presentation field is added. These are schema-generated provider graph helpers. |
| 44 | `desc` uses markdown: backticks for code/field refs (`` `field_name` ``, `` `True` ``, `` `pdf` ``), `\n\n` for paragraphs, `- ` bullet lists for enumerations, `**bold**` for caveats | N/A | No app/action registration, endpoint request/response type, factory, JNI dependency or presentation field is added. These are schema-generated provider graph helpers. |
| 45 | Descriptions use plain business language relating to common challenges — not just technical "what" but "why" and "when to use" | N/A | No app/action registration, endpoint request/response type, factory, JNI dependency or presentation field is added. These are schema-generated provider graph helpers. |
| 46 | No bare `True`/`False`/`NOTHING` — must be backtick-wrapped in `desc` | N/A | No app/action registration, endpoint request/response type, factory, JNI dependency or presentation field is added. These are schema-generated provider graph helpers. |
| 47 | No bare field/option names in prose — must use backticks | N/A | No app/action registration, endpoint request/response type, factory, JNI dependency or presentation field is added. These are schema-generated provider graph helpers. |
| 48 | Long descriptions (>500 chars) use bold section headers and bullet lists | N/A | No app/action registration, endpoint request/response type, factory, JNI dependency or presentation field is added. These are schema-generated provider graph helpers. |
| 49 | **Factory registration in Qore repo**: every factory name registered in `qlib/DataProvider/DataProvider.qc` → `FactoryMap` (without this, module loads but doesn't appear in Qorus apps) | N/A | No app/action registration, endpoint request/response type, factory, JNI dependency or presentation field is added. These are schema-generated provider graph helpers. |
| 50 | **`getRecordTypeImpl()` signature**: must be `private *hash<string, AbstractDataField> getRecordTypeImpl(*hash<auto> search_options)` — NOT returning `*AbstractDataProviderType` | N/A | No app/action registration, endpoint request/response type, factory, JNI dependency or presentation field is added. These are schema-generated provider graph helpers. |
| 51 | **Dependency JARs committed** (for JNI modules): JAR files in `qlib/*/jar/` may be gitignored — use `git add -f` to ensure they're tracked, otherwise CI compilation fails | N/A | No app/action registration, endpoint request/response type, factory, JNI dependency or presentation field is added. These are schema-generated provider graph helpers. |
| 52 | JAR install rules in CMakeLists.txt for all dependency JARs | N/A | No app/action registration, endpoint request/response type, factory, JNI dependency or presentation field is added. These are schema-generated provider graph helpers. |
| 53 | **No workarounds**: No TODOs, FIXMEs, stubs, or partially-implemented features | Pass | Recursive edges retain completed definitions instead of incomplete copies. No skipped validation, depth cap, fixture-specific branch or workaround. Full tuple/P5 work remains separate. |
| 54 | **Exception safety**: C++ uses `ReferenceHolder` for Qore allocations, `std::unique_ptr` for C++ allocations, `*xsink` checked after every fallible operation | Pass | Construction context clears through on_exit; definitions are published only after completion. Failed construction, soft conversion, malformed saved graphs and actual interruption recover. Valgrind reports zero errors and zero lost bytes. |
| 55 | **Thread safety**: All mutable shared state protected by `std::lock_guard<std::mutex>` or documented as immutable-after-construction | Pass | Construction context is thread-local; completed definitions are immutable by contract. Four concurrent consumers validate independent values and metadata against a saved shared graph. |
| 56 | **Type safety**: Strongly-typed `code<return(args)>` instead of untyped `code`; `static_cast` instead of C casts; typed hashdecls for results; enums where appropriate | Pass | Typed provider/reference fields, typed definition maps and code<AbstractDataProviderType()> builder. Typed Qore lists/maps and explicit casts; no C++ changes. |
| 57 | **Performance**: No O(n²) where O(n) is possible; no unnecessary copies; coordinate descent uses incremental residuals not full matrix multiply | Pass | Complex source types are memoized during construction; only recursive definitions acquire soft targets. Qore 16ae86ca7 bounds shared soft conversion. Depth 4/8/12 ordinary/wildcard regressions pass constant leaf-work bounds; core reproducer counts one leaf at each depth. |
| 58 | **Error handling**: All inputs validated (dimensions, empty data, unfitted models); C++ I/O handles EAGAIN/EINTR if applicable | Pass | Missing mandatory and NULL inputs retain distinct error categories; malformed saved definitions/flags are rejected. Nested missing/invalid values and recovery pass. |
| 59 | **Documentation**: Doxygen `@param`, `@return`, `@throw` on all public methods; `@par Example` with realistic business scenarios; `@note` for important caveats | Pass | New public helpers/methods have documentation for parameters, returns and thrown validation errors as applicable. Design documents ownership, occurrence metadata, Qore dependency and a tested shipment example. WSDL docs build without warnings. |
| 60 | **QPP flags**: `[flags=CONSTANT]` on methods that never throw; `[flags=RET_VALUE_ONLY]` on methods that throw but have no side effects | N/A | No C++ or QPP changes; no added external resource operation. |
| 61 | **Security**: No user-controlled format strings; no buffer overflows; bounds checking on array indices; no credentials in code | Pass | No external access, credentials, format-string input or unchecked indexing added. Internal field selections follow checked lists and maps. |
| 62 | **Correctness**: Algorithms verified against reference implementations; edge cases tested (empty data, single sample, all-zero features) | Pass | 186 affected suites pass 91,480 assertions without warnings/errors with final inputs covered: 37 suites were rerun after the documentation target relinked XML; the other 149 already ran after the relink. All Qore sources/tests stayed unchanged. Core graph, XML graph, optionality, recursive metadata/recovery/actual interruption/concurrency, saved graphs, example and survey tests pass. Four corpus reports have no behavioral changes. Valgrind is clean. |

Result: 22 Pass, 40 N/A, zero Fail. See [inventory](../P5-20i-validation.json) and [evidence](../recursive-providers-evidence.md). No install, push or main-Qore mutation.
