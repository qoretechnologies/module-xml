# P3-44 audit: exact dateTime and time values

Copyright (C) 2026 Qore Technologies, s.r.o.

The full audit-changes skill and its referenced module-structure, sandboxing,
cancellation and DataProvider guides were applied to the exact temporal changes.
All 62 checks are resolved: **19 Pass / 43 N/A / 0 Fail**.

Review fixed union/provider metadata consistency, zero-fraction ordering and
clock pattern example construction. Temporal metadata, fixed/default values,
error recovery and all four execution modes pass. No C++ changed in this XML
increment. The two existing P6 binding-version failures remain diagnostic failures;
no skip or expected-pass conversion was added. See [evidence](../temporal-values-evidence.md)
and `/tmp/wsdl-p3-44-temporal-final-manifest.json` for source and runtime hashes.

| Check | Status | Evidence |
| --- | --- | --- |
| 1. Entry exists in `doxygen/lang/120_modules.dox.tmpl` (for modules in the Qore repo; N/A for external module repos) | N/A | No new module, QPP class, separated source, layout or registration is introduced. |
| 2. Entry exists in `doxygen/lang/900_release_notes.dox.tmpl` (for modules in the Qore repo; external modules have release notes in their .qm) | Pass | WSDL 0.5.8 release notes describe strict temporal validation, lossless return categories, exact facets and metadata checks. |
| 3. `qore_user_module()` or `qore_external_user_module()` call in `CMakeLists.txt` | Pass | Existing qore_external_user_module(WSDL) registration is retained. The final docs-module build succeeds without warnings or errors. |
| 4. Module added to QMOD list in `CMakeLists.txt` | N/A | No new module, QPP class, separated source, layout or registration is introduced. |
| 5. `.qm` file has `@section <lowercasemodname>intro` as first doc section — **must be all lowercase** (e.g., `avrodataproviderintro`, not `AvroDataProviderintro`) | N/A | No new module, QPP class, separated source, layout or registration is introduced. |
| 6. `%modern` in `.qm` file — no redundant `%new-style`, `%require-types`, `%strict-args`, `%enable-all-warnings` | Pass | WSDL.qm and all new Qore tests use %modern; no redundant directives added. |
| 7. No parse directives (`%requires`, `%modern`, `%new-style`) in separated `.qc` files (check OUTSIDE of `@code` blocks only) | N/A | No new module, QPP class, separated source, layout or registration is introduced. |
| 8. No `%include` usage (deprecated for modules) | Pass | No %include directive introduced. |
| 9. Copyright 2026 on all new files | Pass | Every new Qore/Python/Java source, design, evidence, fixture and audit carries copyright 2026. |
| 10. Directory layout: `.qm` inside `qlib/<ModuleName>/` directory (not at `qlib/<ModuleName>.qm` for multi-file modules) | N/A | No new module, QPP class, separated source, layout or registration is introduced. |
| 11. No second `.qm` for the same module at `qlib/<ModuleName>.qm` | N/A | No new module, QPP class, separated source, layout or registration is introduced. |
| 12. `ns=Qore::XX` matches the QoreNamespace constructor path | N/A | No new module, QPP class, separated source, layout or registration is introduced. |
| 13. `%modern` directive present | Pass | Both new qtests and the updated union/list qtest use %modern; the reused boundary workers are modern scripts. |
| 14. Executable permission set (`chmod +x`) | Pass | Both new qtests and the updated union/list qtest have mode 0755. |
| 15. Uses %prepend-module-path  before %requires for in-repo modules (Qore and Qore modules only; not Qorus) | Pass | Qtests prepend ../qlib and require local WSDL/SoapClient/SoapHandler paths. Runtime paths select local XML and the isolated audited core modules. |
| 16. External module dependencies use `%try-module` — except modules delivered with the project itself (Qore ex: DataProvider, ConnectionProvider, QUnit, etc.) which use hard `%requires` | Pass | Project XML and core QUnit/HttpServer use hard requirements. Reused workers guard the external json module with %try-module. |
| 17. No filesystem operations (fopen, open, creat, unlink, remove, rename, mkdir, rmdir, stat, chmod) without sandbox checks | N/A | No C++/QPP changes in this XML increment; native XML bytes are unchanged. Core prerequisites were separately tested and checked with Valgrind. |
| 18. No network operations (connect, bind, socket, getaddrinfo, gethostbyname) without sandbox checks | N/A | No C++/QPP changes in this XML increment; native XML bytes are unchanged. Core prerequisites were separately tested and checked with Valgrind. |
| 19. If filesystem/network ops exist, verify `QoreSandboxManagerHelper` usage | N/A | No C++/QPP changes in this XML increment; native XML bytes are unchanged. Core prerequisites were separately tested and checked with Valgrind. |
| 20. No `File::`, `Dir::`, `Socket::`, `HTTPClient::` usage without justification | Pass | Production helpers add no I/O. Real HTTP tests use ephemeral local listeners, 30-second client/Queue deadlines and on_exit server.stop(); authored test manifests and pinned oracles are read offline. |
| 21. All `for`/`while` loops that could iterate >100 times have `qore_check_cancel()` checks | N/A | No C++/QPP changes in this XML increment; native XML bytes are unchanged. Core prerequisites were separately tested and checked with Valgrind. |
| 22. Uses `qore_check_cancel()` (NOT deprecated `qore_check_io_interrupt()`) | N/A | No C++/QPP changes in this XML increment; native XML bytes are unchanged. Core prerequisites were separately tested and checked with Valgrind. |
| 23. Check frequency: every 100 iterations for tight loops, every 10 for expensive iterations | N/A | No C++/QPP changes in this XML increment; native XML bytes are unchanged. Core prerequisites were separately tested and checked with Valgrind. |
| 24. No blocking operations without cancellation support | N/A | No C++/QPP changes in this XML increment; native XML bytes are unchanged. Core prerequisites were separately tested and checked with Valgrind. |
| 25. Every action has `display_name`, `short_desc` (plain text, <80 chars), `desc` (markdown) | N/A | Only existing scalar provider conversion/return categories and reconstruction validation change; no action, request/response record, app, field description, factory or JNI registration is added or modified. |
| 26. Every action has `options` populated via `getActionOptionFromFields()` — without this, the action shows an empty, unusable form | N/A | Only existing scalar provider conversion/return categories and reconstruction validation change; no action, request/response record, app, field description, factory or JNI registration is added or modified. |
| 27. Every action has `output_type` set to a typed data type constant (e.g., `MyResponseDataType`) — not omitted | N/A | Only existing scalar provider conversion/return categories and reconstruction validation change; no action, request/response record, app, field description, factory or JNI registration is added or modified. |
| 28. DPAT_API actions: provider has `"supports_request": True` and implements `doRequestImpl()` | N/A | Only existing scalar provider conversion/return categories and reconstruction validation change; no action, request/response record, app, field description, factory or JNI registration is added or modified. |
| 29. DPAT_FIND actions: every option exists in `SearchOptions`, `getRecordTypeImpl()` returns `*hash<string, AbstractDataField>` | N/A | Only existing scalar provider conversion/return categories and reconstruction validation change; no action, request/response record, app, field description, factory or JNI registration is added or modified. |
| 30. Scheme-based apps (with `"scheme"` in registerApp): actions use `"path"` and do NOT use `"cls"` — having both `scheme` and `cls` causes a runtime error | N/A | Only existing scalar provider conversion/return categories and reconstruction validation change; no action, request/response record, app, field description, factory or JNI registration is added or modified. |
| 31. Single-key hash slices use trailing comma: `Fields{"key",}` (without trailing comma, `Fields{"key"}` returns the value, not a hash) | N/A | Only existing scalar provider conversion/return categories and reconstruction validation change; no action, request/response record, app, field description, factory or JNI registration is added or modified. |
| 32. **Typed data type classes exist** for request and response types — inherit `HashDataType`, have `const Fields` hash, call `addQoreFields(Fields)` in constructor, export public constant at bottom (e.g., `public const MyDataType = new MyDataType();`) | N/A | Only existing scalar provider conversion/return categories and reconstruction validation change; no action, request/response record, app, field description, factory or JNI registration is added or modified. |
| 33. Request/input types use `public` Fields (enables `ClassName::Fields` in action registration) | N/A | Only existing scalar provider conversion/return categories and reconstruction validation change; no action, request/response record, app, field description, factory or JNI registration is added or modified. |
| 34. Response/output types use `private` Fields | N/A | Only existing scalar provider conversion/return categories and reconstruction validation change; no action, request/response record, app, field description, factory or JNI registration is added or modified. |
| 35. Each field in data types has `display_name`, `type`, and `desc` (markdown-formatted) | N/A | Only existing scalar provider conversion/return categories and reconstruction validation change; no action, request/response record, app, field description, factory or JNI registration is added or modified. |
| 36. Input fields have `example_value` where useful (string fields, endpoint URIs, SQL queries, etc.) | N/A | Only existing scalar provider conversion/return categories and reconstruction validation change; no action, request/response record, app, field description, factory or JNI registration is added or modified. |
| 37. Fields with finite allowed values use `allowed_values` with `AllowedValueInfo` containing both `value` and `display_name` (Title Case, human-readable) — never bare values, never described only in text | N/A | Only existing scalar provider conversion/return categories and reconstruction validation change; no action, request/response record, app, field description, factory or JNI registration is added or modified. |
| 38. Password/secret fields have `"sensitive": True` | N/A | Only existing scalar provider conversion/return categories and reconstruction validation change; no action, request/response record, app, field description, factory or JNI registration is added or modified. |
| 39. `groups` uses `AppGroup` enum values from `qlib/DataProvider/AppGroup.qc` | N/A | Only existing scalar provider conversion/return categories and reconstruction validation change; no action, request/response record, app, field description, factory or JNI registration is added or modified. |
| 40. App `logo` stored as separate file, loaded at module level in `Priv` namespace | N/A | Only existing scalar provider conversion/return categories and reconstruction validation change; no action, request/response record, app, field description, factory or JNI registration is added or modified. |
| 41. App `desc` uses markdown: bullet list of capabilities, links to project website, business-language explanation of value | N/A | Only existing scalar provider conversion/return categories and reconstruction validation change; no action, request/response record, app, field description, factory or JNI registration is added or modified. |
| 42. `display_name` is user-friendly ("Apache Avro" not "avro") | N/A | Only existing scalar provider conversion/return categories and reconstruction validation change; no action, request/response record, app, field description, factory or JNI registration is added or modified. |
| 43. `short_desc` is plain text, under 80 chars, single sentence — no markdown | N/A | Only existing scalar provider conversion/return categories and reconstruction validation change; no action, request/response record, app, field description, factory or JNI registration is added or modified. |
| 44. `desc` uses markdown: backticks for code/field refs (`` `field_name` ``, `` `True` ``, `` `pdf` ``), `\n\n` for paragraphs, `- ` bullet lists for enumerations, `**bold**` for caveats | N/A | Only existing scalar provider conversion/return categories and reconstruction validation change; no action, request/response record, app, field description, factory or JNI registration is added or modified. |
| 45. Descriptions use plain business language relating to common challenges — not just technical "what" but "why" and "when to use" | N/A | Only existing scalar provider conversion/return categories and reconstruction validation change; no action, request/response record, app, field description, factory or JNI registration is added or modified. |
| 46. No bare `True`/`False`/`NOTHING` — must be backtick-wrapped in `desc` | N/A | Only existing scalar provider conversion/return categories and reconstruction validation change; no action, request/response record, app, field description, factory or JNI registration is added or modified. |
| 47. No bare field/option names in prose — must use backticks | N/A | Only existing scalar provider conversion/return categories and reconstruction validation change; no action, request/response record, app, field description, factory or JNI registration is added or modified. |
| 48. Long descriptions (>500 chars) use bold section headers and bullet lists | N/A | Only existing scalar provider conversion/return categories and reconstruction validation change; no action, request/response record, app, field description, factory or JNI registration is added or modified. |
| 49. **Factory registration in Qore repo**: every factory name registered in `qlib/DataProvider/DataProvider.qc` → `FactoryMap` (without this, module loads but doesn't appear in Qorus apps) | N/A | Only existing scalar provider conversion/return categories and reconstruction validation change; no action, request/response record, app, field description, factory or JNI registration is added or modified. |
| 50. **`getRecordTypeImpl()` signature**: must be `private *hash<string, AbstractDataField> getRecordTypeImpl(*hash<auto> search_options)` — NOT returning `*AbstractDataProviderType` | N/A | Only existing scalar provider conversion/return categories and reconstruction validation change; no action, request/response record, app, field description, factory or JNI registration is added or modified. |
| 51. **Dependency JARs committed** (for JNI modules): JAR files in `qlib/*/jar/` may be gitignored — use `git add -f` to ensure they're tracked, otherwise CI compilation fails | N/A | Only existing scalar provider conversion/return categories and reconstruction validation change; no action, request/response record, app, field description, factory or JNI registration is added or modified. |
| 52. JAR install rules in CMakeLists.txt for all dependency JARs | N/A | Only existing scalar provider conversion/return categories and reconstruction validation change; no action, request/response record, app, field description, factory or JNI registration is added or modified. |
| 53. **No workarounds**: No TODOs, FIXMEs, stubs, or partially-implemented features | Pass | Lexical/value separation fixes native conversion loss. Exact fractions handle zero explicitly; provider consistency is enforced. No fixture branches or validation bypasses. Approved leap identity is documented; bounded sample-generation errors are allowed by P3. |
| 54. **Exception safety**: C++ uses `ReferenceHolder` for Qore allocations, `std::unique_ptr` for C++ allocations, `*xsink` checked after every fallible operation | Pass | Parsed calendar hashes are private values; shifts mutate only local copies. Validation precedes native conversion and construction. Callback interruption and recovery pass; HTTP teardown uses on_exit and Java/Python workers have deterministic resource cleanup. |
| 55. **Thread safety**: All mutable shared state protected by `std::lock_guard<std::mutex>` or documented as immutable-after-construction | Pass | New helper constants are immutable; parsing, normalization and sample candidates use local state. Existing schemas/providers are not mutated by conversion; real HTTP callbacks communicate through Queue. |
| 56. **Type safety**: Strongly-typed `code<return(args)>` instead of untyped `code`; `static_cast` instead of C casts; typed hashdecls for results; enums where appropriate | Pass | Calendar field/instant hashdecls type every new field; helper arguments and returns are concrete, including reference<hash<XsdCalendarValueInfo>>. Public auto conversion retains date/string alternatives and validates categories. |
| 57. **Performance**: No O(n²) where O(n) is possible; no unnecessary copies; coordinate descent uses incremental residuals not full matrix multiply | Pass | Year normalization uses existing bounded carry scans. Fractions trim and compare without arbitrary-precision floating conversion; no per-digit indexed traversal. Prefix synthesis tries at most 20 fixed prefixes. The 718-case generated boundary test including 1,000-digit inputs completes within its deadline. |
| 58. **Error handling**: All inputs validated (dimensions, empty data, unfitted models); C++ I/O handles EAGAIN/EINTR if applicable | Pass | Strict ASCII grammar, Gregorian boundaries, hour 24, second 60, zone limits and native relative values are validated. Tests assert SOAP/provider/metadata/cancellation categories and subsequent recovery; partial bounds reject indeterminate relations. |
| 59. **Documentation**: Doxygen `@param`, `@return`, `@throw` on all public methods; `@par Example` with realistic business scenarios; `@note` for important caveats | Pass | Public provider docs describe parameters, return alternatives, exceptions, an example and the historical time anchor. README, release notes and implemented design are updated; Doxygen and the extracted example pass. |
| 60. **QPP flags**: `[flags=CONSTANT]` on methods that never throw; `[flags=RET_VALUE_ONLY]` on methods that throw but have no side effects | N/A | No C++/QPP changes in this XML increment; native XML bytes are unchanged. Core prerequisites were separately tested and checked with Valgrind. |
| 61. **Security**: No user-controlled format strings; no buffer overflows; bounds checking on array indices; no credentials in code | Pass | No credentials, raw buffer access, user-controlled printf format or new production external resource resolution. Oracle schemas/documents are authored or pinned and processed with existing offline bounds. |
| 62. **Correctness**: Algorithms verified against reference implementations; edge cases tested (empty data, single sample, all-zero features) | Pass | 96 suites / 980 successful cases / 38,536 reported assertions; temporal, HTTP and metadata tests pass in all four modes. Independent matrices check 6,880 documents, 718 generated boundaries and exact Java/native reductions. Strict selection adds all 88 temporal directions without new diagnostic failures. |
