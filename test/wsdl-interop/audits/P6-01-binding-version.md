# P6-01 binding-version audit

Copyright (C) 2026 Qore Technologies, s.r.o.

Applied `/home/david/.codex/skills/audit-changes/SKILL.md` to the final binding-version increment.
Consulted all five Qore guides: `qore-module-structure.md`, `module-sandboxing-audit-guide.md`,
`cooperative-cancellation.md`, `data-provider-checklist.md`, and `data-provider-development-guide.md`.
All 62 checklist items are classified individually. No C++ changes; Valgrind is not required.
Main Qore was read only. No install, push, or sub-agent delegation.

| # | Check | Status | Evidence |
| --- | --- | --- | --- |
| 1 | Entry exists in `doxygen/lang/120_modules.dox.tmpl` (for modules in the Qore repo; N/A for external module repos) | N/A | No new user module, QPP class, namespace, build registration or module directory. |
| 2 | Entry exists in `doxygen/lang/900_release_notes.dox.tmpl` (for modules in the Qore repo; external modules have release notes in their .qm) | N/A | No new user module, QPP class, namespace, build registration or module directory. |
| 3 | `qore_user_module()` or `qore_external_user_module()` call in `CMakeLists.txt` | N/A | No new user module, QPP class, namespace, build registration or module directory. |
| 4 | Module added to QMOD list in `CMakeLists.txt` | N/A | No new user module, QPP class, namespace, build registration or module directory. |
| 5 | `.qm` file has `@section <lowercasemodname>intro` as first doc section — **must be all lowercase** (e.g., `avrodataproviderintro`, not `AvroDataProviderintro`) | Pass | Existing WSDL mainpage begins with wsdlintro; no module layout or registration changes. |
| 6 | `%modern` in `.qm` file — no redundant `%new-style`, `%require-types`, `%strict-args`, `%enable-all-warnings` | Pass | WSDL.qm retains %modern; no redundant directives introduced. |
| 7 | No parse directives (`%requires`, `%modern`, `%new-style`) in separated `.qc` files (check OUTSIDE of `@code` blocks only) | N/A | No new user module, QPP class, namespace, build registration or module directory. |
| 8 | No `%include` usage (deprecated for modules) | Pass | No %include introduced; existing single-file WSDL module remains registered. |
| 9 | Copyright 2026 on all new files | Pass | All authored new test, design, evidence, audit and validation files have 2026 copyright. |
| 10 | Directory layout: `.qm` inside `qlib/<ModuleName>/` directory (not at `qlib/<ModuleName>.qm` for multi-file modules) | N/A | No new user module, QPP class, namespace, build registration or module directory. |
| 11 | No second `.qm` for the same module at `qlib/<ModuleName>.qm` | N/A | No new user module, QPP class, namespace, build registration or module directory. |
| 12 | `ns=Qore::XX` matches the QoreNamespace constructor path | N/A | No new user module, QPP class, namespace, build registration or module directory. |
| 13 | `%modern` directive present | Pass | Both changed Qore tests use %modern. |
| 14 | Executable permission set (`chmod +x`) | Pass | Both Qore tests are executable (0755). |
| 15 | Uses %prepend-module-path  before %requires for in-repo modules (Qore and Qore modules only; not Qorus) | Pass | Both tests prepend local qlib before module imports and require the repository WSDL by relative path. |
| 16 | External module dependencies use `%try-module` — except modules delivered with the project itself (Qore ex: DataProvider, ConnectionProvider, QUnit, etc.) which use hard `%requires` | Pass | QUnit and HttpServer are core standard modules; WSDL/SoapClient/SoapHandler/xml are delivered by this repo; no new external binary dependency. |
| 17 | No filesystem operations (fopen, open, creat, unlink, remove, rename, mkdir, rmdir, stat, chmod) without sandbox checks | N/A | No C++ change or native I/O loop. Qore loops use core cancellation points; lexical scopes and HTTP cleanup retain exception-safe lifetime handling. |
| 18 | No network operations (connect, bind, socket, getaddrinfo, gethostbyname) without sandbox checks | N/A | No C++ change or native I/O loop. Qore loops use core cancellation points; lexical scopes and HTTP cleanup retain exception-safe lifetime handling. |
| 19 | If filesystem/network ops exist, verify `QoreSandboxManagerHelper` usage | N/A | No C++ change or native I/O loop. Qore loops use core cancellation points; lexical scopes and HTTP cleanup retain exception-safe lifetime handling. |
| 20 | No `File::`, `Dir::`, `Socket::`, `HTTPClient::` usage without justification | Pass | Production changes perform no I/O. Tests use bounded loopback HttpServer/SoapClient with on_exit server.stop(); no direct unchecked network or filesystem primitives added. |
| 21 | All `for`/`while` loops that could iterate >100 times have `qore_check_cancel()` checks | N/A | No C++ change or native I/O loop. Qore loops use core cancellation points; lexical scopes and HTTP cleanup retain exception-safe lifetime handling. |
| 22 | Uses `qore_check_cancel()` (NOT deprecated `qore_check_io_interrupt()`) | N/A | No C++ change or native I/O loop. Qore loops use core cancellation points; lexical scopes and HTTP cleanup retain exception-safe lifetime handling. |
| 23 | Check frequency: every 100 iterations for tight loops, every 10 for expensive iterations | N/A | No C++ change or native I/O loop. Qore loops use core cancellation points; lexical scopes and HTTP cleanup retain exception-safe lifetime handling. |
| 24 | No blocking operations without cancellation support | N/A | No C++ change or native I/O loop. Qore loops use core cancellation points; lexical scopes and HTTP cleanup retain exception-safe lifetime handling. |
| 25 | Every action has `display_name`, `short_desc` (plain text, <80 chars), `desc` (markdown) | N/A | No provider action, app, type field, factory, REST/JNI dependency or registration change. Existing provider representations remain unchanged. |
| 26 | Every action has `options` populated via `getActionOptionFromFields()` — without this, the action shows an empty, unusable form | N/A | No provider action, app, type field, factory, REST/JNI dependency or registration change. Existing provider representations remain unchanged. |
| 27 | Every action has `output_type` set to a typed data type constant (e.g., `MyResponseDataType`) — not omitted | N/A | No provider action, app, type field, factory, REST/JNI dependency or registration change. Existing provider representations remain unchanged. |
| 28 | DPAT_API actions: provider has `"supports_request": True` and implements `doRequestImpl()` | N/A | No provider action, app, type field, factory, REST/JNI dependency or registration change. Existing provider representations remain unchanged. |
| 29 | DPAT_FIND actions: every option exists in `SearchOptions`, `getRecordTypeImpl()` returns `*hash<string, AbstractDataField>` | N/A | No provider action, app, type field, factory, REST/JNI dependency or registration change. Existing provider representations remain unchanged. |
| 30 | Scheme-based apps (with `"scheme"` in registerApp): actions use `"path"` and do NOT use `"cls"` — having both `scheme` and `cls` causes a runtime error | N/A | No provider action, app, type field, factory, REST/JNI dependency or registration change. Existing provider representations remain unchanged. |
| 31 | Single-key hash slices use trailing comma: `Fields{"key",}` (without trailing comma, `Fields{"key"}` returns the value, not a hash) | N/A | No provider action, app, type field, factory, REST/JNI dependency or registration change. Existing provider representations remain unchanged. |
| 32 | **Typed data type classes exist** for request and response types — inherit `HashDataType`, have `const Fields` hash, call `addQoreFields(Fields)` in constructor, export public constant at bottom (e.g., `public const MyDataType = new MyDataType();`) | N/A | No provider action, app, type field, factory, REST/JNI dependency or registration change. Existing provider representations remain unchanged. |
| 33 | Request/input types use `public` Fields (enables `ClassName::Fields` in action registration) | N/A | No provider action, app, type field, factory, REST/JNI dependency or registration change. Existing provider representations remain unchanged. |
| 34 | Response/output types use `private` Fields | N/A | No provider action, app, type field, factory, REST/JNI dependency or registration change. Existing provider representations remain unchanged. |
| 35 | Each field in data types has `display_name`, `type`, and `desc` (markdown-formatted) | N/A | No provider action, app, type field, factory, REST/JNI dependency or registration change. Existing provider representations remain unchanged. |
| 36 | Input fields have `example_value` where useful (string fields, endpoint URIs, SQL queries, etc.) | N/A | No provider action, app, type field, factory, REST/JNI dependency or registration change. Existing provider representations remain unchanged. |
| 37 | Fields with finite allowed values use `allowed_values` with `AllowedValueInfo` containing both `value` and `display_name` (Title Case, human-readable) — never bare values, never described only in text | N/A | No provider action, app, type field, factory, REST/JNI dependency or registration change. Existing provider representations remain unchanged. |
| 38 | Password/secret fields have `"sensitive": True` | N/A | No provider action, app, type field, factory, REST/JNI dependency or registration change. Existing provider representations remain unchanged. |
| 39 | `groups` uses `AppGroup` enum values from `qlib/DataProvider/AppGroup.qc` | N/A | No provider action, app, type field, factory, REST/JNI dependency or registration change. Existing provider representations remain unchanged. |
| 40 | App `logo` stored as separate file, loaded at module level in `Priv` namespace | N/A | No provider action, app, type field, factory, REST/JNI dependency or registration change. Existing provider representations remain unchanged. |
| 41 | App `desc` uses markdown: bullet list of capabilities, links to project website, business-language explanation of value | N/A | No provider action, app, type field, factory, REST/JNI dependency or registration change. Existing provider representations remain unchanged. |
| 42 | `display_name` is user-friendly ("Apache Avro" not "avro") | N/A | No provider action, app, type field, factory, REST/JNI dependency or registration change. Existing provider representations remain unchanged. |
| 43 | `short_desc` is plain text, under 80 chars, single sentence — no markdown | N/A | No provider action, app, type field, factory, REST/JNI dependency or registration change. Existing provider representations remain unchanged. |
| 44 | `desc` uses markdown: backticks for code/field refs (`` `field_name` ``, `` `True` ``, `` `pdf` ``), `\n\n` for paragraphs, `- ` bullet lists for enumerations, `**bold**` for caveats | N/A | No provider action, app, type field, factory, REST/JNI dependency or registration change. Existing provider representations remain unchanged. |
| 45 | Descriptions use plain business language relating to common challenges — not just technical "what" but "why" and "when to use" | N/A | No provider action, app, type field, factory, REST/JNI dependency or registration change. Existing provider representations remain unchanged. |
| 46 | No bare `True`/`False`/`NOTHING` — must be backtick-wrapped in `desc` | N/A | No provider action, app, type field, factory, REST/JNI dependency or registration change. Existing provider representations remain unchanged. |
| 47 | No bare field/option names in prose — must use backticks | N/A | No provider action, app, type field, factory, REST/JNI dependency or registration change. Existing provider representations remain unchanged. |
| 48 | Long descriptions (>500 chars) use bold section headers and bullet lists | N/A | No provider action, app, type field, factory, REST/JNI dependency or registration change. Existing provider representations remain unchanged. |
| 49 | **Factory registration in Qore repo**: every factory name registered in `qlib/DataProvider/DataProvider.qc` → `FactoryMap` (without this, module loads but doesn't appear in Qorus apps) | N/A | No provider action, app, type field, factory, REST/JNI dependency or registration change. Existing provider representations remain unchanged. |
| 50 | **`getRecordTypeImpl()` signature**: must be `private *hash<string, AbstractDataField> getRecordTypeImpl(*hash<auto> search_options)` — NOT returning `*AbstractDataProviderType` | N/A | No provider action, app, type field, factory, REST/JNI dependency or registration change. Existing provider representations remain unchanged. |
| 51 | **Dependency JARs committed** (for JNI modules): JAR files in `qlib/*/jar/` may be gitignored — use `git add -f` to ensure they're tracked, otherwise CI compilation fails | N/A | No provider action, app, type field, factory, REST/JNI dependency or registration change. Existing provider representations remain unchanged. |
| 52 | JAR install rules in CMakeLists.txt for all dependency JARs | N/A | No provider action, app, type field, factory, REST/JNI dependency or registration change. Existing provider representations remain unchanged. |
| 53 | **No workarounds**: No TODOs, FIXMEs, stubs, or partially-implemented features | Pass | Protocol identity is stored from the expanded extension name, not inferred from prefixes or unrelated declarations. Explicit response overrides retain their established behavior; strict incoming-version enforcement is tracked in P7. No new stub or workaround. Existing multiTest comments are pre-existing, while complete outgoing golden payload checks now run for both bindings. |
| 54 | **Exception safety**: C++ uses `ReferenceHolder` for Qore allocations, `std::unique_ptr` for C++ allocations, `*xsink` checked after every fallible operation | Pass | Lexical NamespacePrefixHelper scopes restore state on normal exit and exception. Input hashes are local copies; no shared registry mutation during serialization. HTTP cleanup is registered before the listener starts. |
| 55 | **Thread safety**: All mutable shared state protected by `std::lock_guard<std::mutex>` or documented as immutable-after-construction | Pass | Private SOAP-version metadata is immutable after construction/reconstruction. Namespace mutations occur only during unpublished service construction and restore on scope exit; calls read metadata and retain existing per-call output context. |
| 56 | **Type safety**: Strongly-typed `code<return(args)>` instead of untyped `code`; `static_cast` instead of C casts; typed hashdecls for results; enums where appropriate | Pass | Version metadata uses *bool to distinguish unknown legacy data from SOAP 1.1. Binding/OperationalBinding references and HTTP callback signatures are typed. Parsed extension shape is checked before access. |
| 57 | **Performance**: No O(n²) where O(n) is possible; no unnecessary copies; coordinate descent uses incremental residuals not full matrix multiply | Pass | Version selection is constant time per operation. Service capability query visits each binding once; scoped declaration processing is linear in namespace declarations. |
| 58 | **Error handling**: All inputs validated (dimensions, empty data, unfitted models); C++ I/O handles EAGAIN/EINTR if applicable | Pass | Missing, duplicate, unknown, empty-namespace and invalid protocol extensions reject with WSDL-ERROR. Old standalone metadata absence rejects with WSDL-BINDING-ERROR. Negative explicit legacy-version availability and immediate retry are covered. |
| 59 | **Documentation**: Doxygen `@param`, `@return`, `@throw` on all public methods; `@par Example` with realistic business scenarios; `@note` for important caveats | Pass | Public query/constructor parameters, returns and relevant errors documented; durable design explains endpoint example, saved-object migration and explicit override compatibility. Both WSDL and native docs build without diagnostics; release notes updated. |
| 60 | **QPP flags**: `[flags=CONSTANT]` on methods that never throw; `[flags=RET_VALUE_ONLY]` on methods that throw but have no side effects | N/A | No QPP methods or flags changed. |
| 61 | **Security**: No user-controlled format strings; no buffer overflows; bounds checking on array indices; no credentials in code | Pass | Only fixed format strings; no credentials, process interpolation, unchecked memory access or XML resource-policy changes. HTTP tests bind ephemeral local ports and use bounded call/queue deadlines. |
| 62 | **Correctness**: Algorithms verified against reference implementations; edge cases tested (empty data, single sample, all-zero features) | Pass | Binding identity follows WSDL 1.1 and its SOAP 1.2 extension. Tests cover real HTTP both directions, saved services/bindings, lexical scopes, defaults, mixed protocols and received fault versions. Independent dual-binding coverage now passes; final results and corpus comparisons are recorded in P6-01-validation.json. |

All 62 checks: **18 Pass / 44 N/A / zero Fail**.
See [validation](../P6-01-validation.json) and [evidence](../binding-version-evidence.md).
