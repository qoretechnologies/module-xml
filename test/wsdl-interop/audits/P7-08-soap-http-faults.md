# P7-08 SOAP HTTP fault audit

Copyright (C) 2026 Qore Technologies, s.r.o.

Applied `/home/david/.codex/skills/audit-changes/SKILL.md` to the main-develop changes based on `0ff6026`. All 62 items were checked against the final changes. Scope: existing SoapClient and SoapHandler private processing, one executable qtest, Qore/Python peers, release notes, evidence and durable design. No C++, new module, DataProvider registration or DGC change. Unrelated `test/cmake/__pycache__/` is excluded.

| # | Check | Status | Evidence |
| --- | --- | --- | --- |
| 1 | Entry exists in `doxygen/lang/120_modules.dox.tmpl` (for modules in the Qore repo; N/A for external module repos) | N/A | No new module, layout or installation entry; existing external module release notes are updated. |
| 2 | Entry exists in `doxygen/lang/900_release_notes.dox.tmpl` (for modules in the Qore repo; external modules have release notes in their .qm) | N/A | No new module, layout or installation entry; existing external module release notes are updated. |
| 3 | `qore_user_module()` or `qore_external_user_module()` call in `CMakeLists.txt` | N/A | No new module, layout or installation entry; existing external module release notes are updated. |
| 4 | Module added to QMOD list in `CMakeLists.txt` | N/A | No new module, layout or installation entry; existing external module release notes are updated. |
| 5 | `.qm` file has `@section <lowercasemodname>intro` as first doc section — **must be all lowercase** (e.g., `avrodataproviderintro`, not `AvroDataProviderintro`) | Pass | Existing lowercase soapclientintro and soaphandlerintro remain their modules first sections. |
| 6 | `%modern` in `.qm` file — no redundant `%new-style`, `%require-types`, `%strict-args`, `%enable-all-warnings` | Pass | The changed module uses %modern without redundant directives. |
| 7 | No parse directives (`%requires`, `%modern`, `%new-style`) in separated `.qc` files (check OUTSIDE of `@code` blocks only) | N/A | No separated .qc file changed. |
| 8 | No `%include` usage (deprecated for modules) | Pass | No %include introduced. |
| 9 | Copyright 2026 on all new files | Pass | New qtest, Qore/Python peers and evidence carry 2026 copyright notices; changed modules already use 2026. |
| 10 | Directory layout: `.qm` inside `qlib/<ModuleName>/` directory (not at `qlib/<ModuleName>.qm` for multi-file modules) | N/A | No new module, layout or installation entry; existing external module release notes are updated. |
| 11 | No second `.qm` for the same module at `qlib/<ModuleName>.qm` | N/A | No new module, layout or installation entry; existing external module release notes are updated. |
| 12 | `ns=Qore::XX` matches the QoreNamespace constructor path | N/A | No C++ or QPP changes; native ownership, sandbox guards, cancellation and flags are unchanged. |
| 13 | `%modern` directive present | Pass | The qtest and Qore peer use %modern. |
| 14 | Executable permission set (`chmod +x`) | Pass | The qtest and both peers have executable permissions. |
| 15 | Uses %prepend-module-path  before %requires for in-repo modules (Qore and Qore modules only; not Qorus) | Pass | Qore tests and the peer prepend local qlib before relative in-repo requirements; compiled derivatives load compiled modules. |
| 16 | External module dependencies use `%try-module` — except modules delivered with the project itself (Qore ex: DataProvider, ConnectionProvider, QUnit, etc.) which use hard `%requires` | Pass | The external json peer dependency uses %try-module; project XML and core Qore dependencies use hard requirements. |
| 17 | No filesystem operations (fopen, open, creat, unlink, remove, rename, mkdir, rmdir, stat, chmod) without sandbox checks | N/A | No C++ or QPP changes; native ownership, sandbox guards, cancellation and flags are unchanged. |
| 18 | No network operations (connect, bind, socket, getaddrinfo, gethostbyname) without sandbox checks | N/A | No C++ or QPP changes; native ownership, sandbox guards, cancellation and flags are unchanged. |
| 19 | If filesystem/network ops exist, verify `QoreSandboxManagerHelper` usage | N/A | No C++ or QPP changes; native ownership, sandbox guards, cancellation and flags are unchanged. |
| 20 | No `File::`, `Dir::`, `Socket::`, `HTTPClient::` usage without justification | Pass | Production uses existing transport and XML APIs. Tests use pinned local files and bounded loopback HTTP with explicit shutdown and queue-count assertions. |
| 21 | All `for`/`while` loops that could iterate >100 times have `qore_check_cancel()` checks | N/A | No C++ or QPP changes; native ownership, sandbox guards, cancellation and flags are unchanged. |
| 22 | Uses `qore_check_cancel()` (NOT deprecated `qore_check_io_interrupt()`) | N/A | No C++ or QPP changes; native ownership, sandbox guards, cancellation and flags are unchanged. |
| 23 | Check frequency: every 100 iterations for tight loops, every 10 for expensive iterations | N/A | No C++ or QPP changes; native ownership, sandbox guards, cancellation and flags are unchanged. |
| 24 | No blocking operations without cancellation support | N/A | No C++ or QPP changes; native ownership, sandbox guards, cancellation and flags are unchanged. |
| 25 | Every action has `display_name`, `short_desc` (plain text, <80 chars), `desc` (markdown) | N/A | No DataProvider production or registration changes; provider behavior is covered through the existing SoapClient integration. |
| 26 | Every action has `options` populated via `getActionOptionFromFields()` — without this, the action shows an empty, unusable form | N/A | No DataProvider production or registration changes; provider behavior is covered through the existing SoapClient integration. |
| 27 | Every action has `output_type` set to a typed data type constant (e.g., `MyResponseDataType`) — not omitted | N/A | No DataProvider production or registration changes; provider behavior is covered through the existing SoapClient integration. |
| 28 | DPAT_API actions: provider has `"supports_request": True` and implements `doRequestImpl()` | N/A | No DataProvider production or registration changes; provider behavior is covered through the existing SoapClient integration. |
| 29 | DPAT_FIND actions: every option exists in `SearchOptions`, `getRecordTypeImpl()` returns `*hash<string, AbstractDataField>` | N/A | No DataProvider production or registration changes; provider behavior is covered through the existing SoapClient integration. |
| 30 | Scheme-based apps (with `"scheme"` in registerApp): actions use `"path"` and do NOT use `"cls"` — having both `scheme` and `cls` causes a runtime error | N/A | No DataProvider production or registration changes; provider behavior is covered through the existing SoapClient integration. |
| 31 | Single-key hash slices use trailing comma: `Fields{"key",}` (without trailing comma, `Fields{"key"}` returns the value, not a hash) | N/A | No DataProvider production or registration changes; provider behavior is covered through the existing SoapClient integration. |
| 32 | **Typed data type classes exist** for request and response types — inherit `HashDataType`, have `const Fields` hash, call `addQoreFields(Fields)` in constructor, export public constant at bottom (e.g., `public const MyDataType = new MyDataType();`) | N/A | No DataProvider production or registration changes; provider behavior is covered through the existing SoapClient integration. |
| 33 | Request/input types use `public` Fields (enables `ClassName::Fields` in action registration) | N/A | No DataProvider production or registration changes; provider behavior is covered through the existing SoapClient integration. |
| 34 | Response/output types use `private` Fields | N/A | No DataProvider production or registration changes; provider behavior is covered through the existing SoapClient integration. |
| 35 | Each field in data types has `display_name`, `type`, and `desc` (markdown-formatted) | N/A | No DataProvider production or registration changes; provider behavior is covered through the existing SoapClient integration. |
| 36 | Input fields have `example_value` where useful (string fields, endpoint URIs, SQL queries, etc.) | N/A | No DataProvider production or registration changes; provider behavior is covered through the existing SoapClient integration. |
| 37 | Fields with finite allowed values use `allowed_values` with `AllowedValueInfo` containing both `value` and `display_name` (Title Case, human-readable) — never bare values, never described only in text | N/A | No DataProvider production or registration changes; provider behavior is covered through the existing SoapClient integration. |
| 38 | Password/secret fields have `"sensitive": True` | N/A | No DataProvider production or registration changes; provider behavior is covered through the existing SoapClient integration. |
| 39 | `groups` uses `AppGroup` enum values from `qlib/DataProvider/AppGroup.qc` | N/A | No DataProvider production or registration changes; provider behavior is covered through the existing SoapClient integration. |
| 40 | App `logo` stored as separate file, loaded at module level in `Priv` namespace | N/A | No DataProvider production or registration changes; provider behavior is covered through the existing SoapClient integration. |
| 41 | App `desc` uses markdown: bullet list of capabilities, links to project website, business-language explanation of value | N/A | No DataProvider production or registration changes; provider behavior is covered through the existing SoapClient integration. |
| 42 | `display_name` is user-friendly ("Apache Avro" not "avro") | N/A | No DataProvider production or registration changes; provider behavior is covered through the existing SoapClient integration. |
| 43 | `short_desc` is plain text, under 80 chars, single sentence — no markdown | N/A | No DataProvider production or registration changes; provider behavior is covered through the existing SoapClient integration. |
| 44 | `desc` uses markdown: backticks for code/field refs (`` `field_name` ``, `` `True` ``, `` `pdf` ``), `\n\n` for paragraphs, `- ` bullet lists for enumerations, `**bold**` for caveats | N/A | No DataProvider production or registration changes; provider behavior is covered through the existing SoapClient integration. |
| 45 | Descriptions use plain business language relating to common challenges — not just technical "what" but "why" and "when to use" | N/A | No DataProvider production or registration changes; provider behavior is covered through the existing SoapClient integration. |
| 46 | No bare `True`/`False`/`NOTHING` — must be backtick-wrapped in `desc` | N/A | No DataProvider production or registration changes; provider behavior is covered through the existing SoapClient integration. |
| 47 | No bare field/option names in prose — must use backticks | N/A | No DataProvider production or registration changes; provider behavior is covered through the existing SoapClient integration. |
| 48 | Long descriptions (>500 chars) use bold section headers and bullet lists | N/A | No DataProvider production or registration changes; provider behavior is covered through the existing SoapClient integration. |
| 49 | **Factory registration in Qore repo**: every factory name registered in `qlib/DataProvider/DataProvider.qc` → `FactoryMap` (without this, module loads but doesn't appear in Qorus apps) | N/A | No DataProvider production or registration changes; provider behavior is covered through the existing SoapClient integration. |
| 50 | **`getRecordTypeImpl()` signature**: must be `private *hash<string, AbstractDataField> getRecordTypeImpl(*hash<auto> search_options)` — NOT returning `*AbstractDataProviderType` | N/A | No DataProvider production or registration changes; provider behavior is covered through the existing SoapClient integration. |
| 51 | **Dependency JARs committed** (for JNI modules): JAR files in `qlib/*/jar/` may be gitignored — use `git add -f` to ensure they're tracked, otherwise CI compilation fails | N/A | No DataProvider production or registration changes; provider behavior is covered through the existing SoapClient integration. |
| 52 | JAR install rules in CMakeLists.txt for all dependency JARs | N/A | No DataProvider production or registration changes; provider behavior is covered through the existing SoapClient integration. |
| 53 | **No workarounds**: No TODOs, FIXMEs, stubs, or partially-implemented features | Pass | The regex heuristic is replaced by decoded XML namespace/structure recognition. The charset fix identifies generated UTF-8 data explicitly; no fallback, dropped case or fixture-dependent production branch. |
| 54 | **Exception safety**: C++ uses `ReferenceHolder` for Qore allocations, `std::unique_ptr` for C++ allocations, `*xsink` checked after every fallible operation | Pass | Original HTTP exceptions rethrow with their metadata when no protocol Fault is present. Parsing/protocol errors propagate. Local normalized responses are reused; repeated error/success calls verify recovery and on_exit/context managers provide cleanup. |
| 55 | **Thread safety**: All mutable shared state protected by `std::lock_guard<std::mutex>` or documented as immutable-after-construction | Pass | Expanded-name recognition and reparsing use request-local values. Shared bindings, service graphs, original MIME metadata and node configuration remain unchanged. |
| 56 | **Type safety**: Strongly-typed `code<return(args)>` instead of untyped `code`; `static_cast` instead of C casts; typed hashdecls for results; enums where appropriate | Pass | Parsed response containers retain hash<auto>; the helper explicitly handles scalar/list occurrences and checks hash type before inspecting Fault. Test callbacks are typed; ExceptionInfo and HttpResponseInfo retain typed boundaries. |
| 57 | **Performance**: No O(n²) where O(n) is possible; no unnecessary copies; coordinate descent uses incremental residuals not full matrix multiply | Pass | Namespace expansion is linear in document size; the normalized MIME root and parsed data are reused. Recognition traverses only Envelope/Body containers after expansion; there is no retry, repeated network call or shared graph copy. |
| 58 | **Error handling**: All inputs validated (dimensions, empty data, unfitted models); C++ I/O handles EAGAIN/EINTR if applicable | Pass | Positive/negative tests cover charset decoding, aliases, whitespace, case/namespace collisions, comments/CDATA, Header versus Body, repeated Body, DTDs, malformed XML, non-SOAP entities, original statuses and post-error recovery. |
| 59 | **Documentation**: Doxygen `@param`, `@return`, `@throw` on all public methods; `@par Example` with realistic business scenarios; `@note` for important caveats | Pass | Module release notes and durable design explain recognition, original HTTP errors, parser errors and generated UTF-8 boundaries. No public API signature or DGC behavior changed; existing API examples remain applicable. |
| 60 | **QPP flags**: `[flags=CONSTANT]` on methods that never throw; `[flags=RET_VALUE_ONLY]` on methods that throw but have no side effects | N/A | No C++ or QPP changes; native ownership, sandbox guards, cancellation and flags are unchanged. |
| 61 | **Security**: No user-controlled format strings; no buffer overflows; bounds checking on array indices; no credentials in code | Pass | Recognition uses the existing XML parser with SOAP document restrictions before namespace expansion. No regex sniffing, external resource fetch, credentials or user-controlled format string is introduced. |
| 62 | **Correctness**: Algorithms verified against reference implementations; edge cases tested (empty data, single sample, all-zero features) | Pass | All 30 affected Qore suites pass (391 cases / 22,088 assertions), nine compiled suites pass (32 cases / 11,125 assertions), nine Python gates pass including 1,806 new HTTP exchanges, and all 16 corpus commands retain their outcomes. Docs and four-file AST checks are clean. |

All 62 checks accounted for: **18 Pass / 44 N/A / zero Fail**. No C++ change; Valgrind is not required. No unresolved finding remains in this increment. Remaining HTTP/action/profile acceptance is subsequent plan work.
