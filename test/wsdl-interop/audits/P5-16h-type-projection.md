# P5-16h native type projection audit

Copyright (C) 2026 Qore Technologies, s.r.o.

Applied the full audit-changes skill and all five referenced design guides to
the final changes on 2026-09-13, parent `ac20898`. Guide hashes match the previously
read versions recorded by P5-16g. No production module, C/C++, build or Qore changes.
See [evidence](../type-projection-evidence.md) and [inventory](../P5-16h-validation.json).

| # | Check | Status | Evidence |
| --- | --- | --- | --- |
| 1 | Entry exists in `doxygen/lang/120_modules.dox.tmpl` (for modules in the Qore repo; N/A for external module repos) | N/A | No new or changed production module, module layout, QPP class or build registration. This increment changes the interoperability harness and its documentation. |
| 2 | Entry exists in `doxygen/lang/900_release_notes.dox.tmpl` (for modules in the Qore repo; external modules have release notes in their .qm) | N/A | No new or changed production module, module layout, QPP class or build registration. This increment changes the interoperability harness and its documentation. |
| 3 | `qore_user_module()` or `qore_external_user_module()` call in `CMakeLists.txt` | N/A | No new or changed production module, module layout, QPP class or build registration. This increment changes the interoperability harness and its documentation. |
| 4 | Module added to QMOD list in `CMakeLists.txt` | N/A | No new or changed production module, module layout, QPP class or build registration. This increment changes the interoperability harness and its documentation. |
| 5 | `.qm` file has `@section <lowercasemodname>intro` as first doc section — **must be all lowercase** (e.g., `avrodataproviderintro`, not `AvroDataProviderintro`) | N/A | No new or changed production module, module layout, QPP class or build registration. This increment changes the interoperability harness and its documentation. |
| 6 | `%modern` in `.qm` file — no redundant `%new-style`, `%require-types`, `%strict-args`, `%enable-all-warnings` | N/A | No new or changed production module, module layout, QPP class or build registration. This increment changes the interoperability harness and its documentation. |
| 7 | No parse directives (`%requires`, `%modern`, `%new-style`) in separated `.qc` files (check OUTSIDE of `@code` blocks only) | N/A | No new or changed production module, module layout, QPP class or build registration. This increment changes the interoperability harness and its documentation. |
| 8 | No `%include` usage (deprecated for modules) | N/A | No new or changed production module, module layout, QPP class or build registration. This increment changes the interoperability harness and its documentation. |
| 9 | Copyright 2026 on all new files | Pass | All new and changed authored files retain 2026 copyright; original corpus bytes and notices are untouched. |
| 10 | Directory layout: `.qm` inside `qlib/<ModuleName>/` directory (not at `qlib/<ModuleName>.qm` for multi-file modules) | N/A | No new or changed production module, module layout, QPP class or build registration. This increment changes the interoperability harness and its documentation. |
| 11 | No second `.qm` for the same module at `qlib/<ModuleName>.qm` | N/A | No new or changed production module, module layout, QPP class or build registration. This increment changes the interoperability harness and its documentation. |
| 12 | `ns=Qore::XX` matches the QoreNamespace constructor path | N/A | No new or changed production module, module layout, QPP class or build registration. This increment changes the interoperability harness and its documentation. |
| 13 | `%modern` directive present | Pass | The changed Qore worker explicitly uses %modern; Python tests are executable Python entry points. |
| 14 | Executable permission set (`chmod +x`) | Pass | All six changed/new executable scripts have mode 0755. |
| 15 | Uses %prepend-module-path  before %requires for in-repo modules (Qore and Qore modules only; not Qorus) | Pass | probe.qr prepends repository qlib and requires ../../qlib/WSDL.qm; tests use frozen local runtime and build-debug binary. |
| 16 | External module dependencies use `%try-module` — except modules delivered with the project itself (Qore ex: DataProvider, ConnectionProvider, QUnit, etc.) which use hard `%requires` | Pass | Same-project xml is required; the external json module is guarded with %try-module. |
| 17 | No filesystem operations (fopen, open, creat, unlink, remove, rename, mkdir, rmdir, stat, chmod) without sandbox checks | N/A | No C/C++, native loops or blocking operations changed; existing worker cancellation tests pass. |
| 18 | No network operations (connect, bind, socket, getaddrinfo, gethostbyname) without sandbox checks | N/A | No C/C++, native loops or blocking operations changed; existing worker cancellation tests pass. |
| 19 | If filesystem/network ops exist, verify `QoreSandboxManagerHelper` usage | N/A | No C/C++, native loops or blocking operations changed; existing worker cancellation tests pass. |
| 20 | No `File::`, `Dir::`, `Socket::`, `HTTPClient::` usage without justification | Pass | Only the existing offline manifest/file reader is used. New tests use generated or digest-verified corpus files, bounded subprocesses and temporary-directory cleanup. |
| 21 | All `for`/`while` loops that could iterate >100 times have `qore_check_cancel()` checks | N/A | No C/C++, native loops or blocking operations changed; existing worker cancellation tests pass. |
| 22 | Uses `qore_check_cancel()` (NOT deprecated `qore_check_io_interrupt()`) | N/A | No C/C++, native loops or blocking operations changed; existing worker cancellation tests pass. |
| 23 | Check frequency: every 100 iterations for tight loops, every 10 for expensive iterations | N/A | No C/C++, native loops or blocking operations changed; existing worker cancellation tests pass. |
| 24 | No blocking operations without cancellation support | N/A | No C/C++, native loops or blocking operations changed; existing worker cancellation tests pass. |
| 25 | Every action has `display_name`, `short_desc` (plain text, <80 chars), `desc` (markdown) | N/A | No DataProvider action, field, app, factory, JNI dependency or installation registration changes. |
| 26 | Every action has `options` populated via `getActionOptionFromFields()` — without this, the action shows an empty, unusable form | N/A | No DataProvider action, field, app, factory, JNI dependency or installation registration changes. |
| 27 | Every action has `output_type` set to a typed data type constant (e.g., `MyResponseDataType`) — not omitted | N/A | No DataProvider action, field, app, factory, JNI dependency or installation registration changes. |
| 28 | DPAT_API actions: provider has `"supports_request": True` and implements `doRequestImpl()` | N/A | No DataProvider action, field, app, factory, JNI dependency or installation registration changes. |
| 29 | DPAT_FIND actions: every option exists in `SearchOptions`, `getRecordTypeImpl()` returns `*hash<string, AbstractDataField>` | N/A | No DataProvider action, field, app, factory, JNI dependency or installation registration changes. |
| 30 | Scheme-based apps (with `"scheme"` in registerApp): actions use `"path"` and do NOT use `"cls"` — having both `scheme` and `cls` causes a runtime error | N/A | No DataProvider action, field, app, factory, JNI dependency or installation registration changes. |
| 31 | Single-key hash slices use trailing comma: `Fields{"key",}` (without trailing comma, `Fields{"key"}` returns the value, not a hash) | N/A | No DataProvider action, field, app, factory, JNI dependency or installation registration changes. |
| 32 | **Typed data type classes exist** for request and response types — inherit `HashDataType`, have `const Fields` hash, call `addQoreFields(Fields)` in constructor, export public constant at bottom (e.g., `public const MyDataType = new MyDataType();`) | N/A | No DataProvider action, field, app, factory, JNI dependency or installation registration changes. |
| 33 | Request/input types use `public` Fields (enables `ClassName::Fields` in action registration) | N/A | No DataProvider action, field, app, factory, JNI dependency or installation registration changes. |
| 34 | Response/output types use `private` Fields | N/A | No DataProvider action, field, app, factory, JNI dependency or installation registration changes. |
| 35 | Each field in data types has `display_name`, `type`, and `desc` (markdown-formatted) | N/A | No DataProvider action, field, app, factory, JNI dependency or installation registration changes. |
| 36 | Input fields have `example_value` where useful (string fields, endpoint URIs, SQL queries, etc.) | N/A | No DataProvider action, field, app, factory, JNI dependency or installation registration changes. |
| 37 | Fields with finite allowed values use `allowed_values` with `AllowedValueInfo` containing both `value` and `display_name` (Title Case, human-readable) — never bare values, never described only in text | N/A | No DataProvider action, field, app, factory, JNI dependency or installation registration changes. |
| 38 | Password/secret fields have `"sensitive": True` | N/A | No DataProvider action, field, app, factory, JNI dependency or installation registration changes. |
| 39 | `groups` uses `AppGroup` enum values from `qlib/DataProvider/AppGroup.qc` | N/A | No DataProvider action, field, app, factory, JNI dependency or installation registration changes. |
| 40 | App `logo` stored as separate file, loaded at module level in `Priv` namespace | N/A | No DataProvider action, field, app, factory, JNI dependency or installation registration changes. |
| 41 | App `desc` uses markdown: bullet list of capabilities, links to project website, business-language explanation of value | N/A | No DataProvider action, field, app, factory, JNI dependency or installation registration changes. |
| 42 | `display_name` is user-friendly ("Apache Avro" not "avro") | N/A | No DataProvider action, field, app, factory, JNI dependency or installation registration changes. |
| 43 | `short_desc` is plain text, under 80 chars, single sentence — no markdown | N/A | No DataProvider action, field, app, factory, JNI dependency or installation registration changes. |
| 44 | `desc` uses markdown: backticks for code/field refs (`` `field_name` ``, `` `True` ``, `` `pdf` ``), `\n\n` for paragraphs, `- ` bullet lists for enumerations, `**bold**` for caveats | N/A | No DataProvider action, field, app, factory, JNI dependency or installation registration changes. |
| 45 | Descriptions use plain business language relating to common challenges — not just technical "what" but "why" and "when to use" | N/A | No DataProvider action, field, app, factory, JNI dependency or installation registration changes. |
| 46 | No bare `True`/`False`/`NOTHING` — must be backtick-wrapped in `desc` | N/A | No DataProvider action, field, app, factory, JNI dependency or installation registration changes. |
| 47 | No bare field/option names in prose — must use backticks | N/A | No DataProvider action, field, app, factory, JNI dependency or installation registration changes. |
| 48 | Long descriptions (>500 chars) use bold section headers and bullet lists | N/A | No DataProvider action, field, app, factory, JNI dependency or installation registration changes. |
| 49 | **Factory registration in Qore repo**: every factory name registered in `qlib/DataProvider/DataProvider.qc` → `FactoryMap` (without this, module loads but doesn't appear in Qorus apps) | N/A | No DataProvider action, field, app, factory, JNI dependency or installation registration changes. |
| 50 | **`getRecordTypeImpl()` signature**: must be `private *hash<string, AbstractDataField> getRecordTypeImpl(*hash<auto> search_options)` — NOT returning `*AbstractDataProviderType` | N/A | No DataProvider action, field, app, factory, JNI dependency or installation registration changes. |
| 51 | **Dependency JARs committed** (for JNI modules): JAR files in `qlib/*/jar/` may be gitignored — use `git add -f` to ensure they're tracked, otherwise CI compilation fails | N/A | No DataProvider action, field, app, factory, JNI dependency or installation registration changes. |
| 52 | JAR install rules in CMakeLists.txt for all dependency JARs | N/A | No DataProvider action, field, app, factory, JNI dependency or installation registration changes. |
| 53 | **No workarounds**: No TODOs, FIXMEs, stubs, or partially-implemented features | Pass | The user approved the unchanged compatibility default. The worker forwards the real API option; no fixture-specific production behavior, altered corpus bytes, skips or relaxed acceptance. Known P6 failures stay failing. |
| 54 | **Exception safety**: C++ uses `ReferenceHolder` for Qore allocations, `std::unique_ptr` for C++ allocations, `*xsink` checked after every fallible operation | Pass | Managed Qore values and Python context managers own all state. Existing interruption, child reaping, timeout, error-report and temporary-manifest cleanup regressions pass. |
| 55 | **Thread safety**: All mutable shared state protected by `std::lock_guard<std::mutex>` or documented as immutable-after-construction | Pass | The capture setting is a local boolean in each worker invocation; no shared mutable configuration or schema state is introduced. |
| 56 | **Type safety**: Strongly-typed `code<return(args)>` instead of untyped `code`; `static_cast` instead of C casts; typed hashdecls for results; enums where appropriate | Pass | Python rejects non-boolean capture values; the Qore worker checks the manifest value and assigns a bool before API calls. Existing flexible case/row formats retain their completeness checks. |
| 57 | **Performance**: No O(n²) where O(n) is possible; no unnecessary copies; coordinate descent uses incremental residuals not full matrix multiply | Pass | Option forwarding and scope reporting add constant work per call/manifest. Each explicitly requested mode runs the complete existing manifest once. |
| 58 | **Error handling**: All inputs validated (dimensions, empty data, unfitted models); C++ I/O handles EAGAIN/EINTR if applicable | Pass | Malformed options reject; omitted and false agree. Both directions reject invalid selected types and facets with SOAP-DESERIALIZATION-ERROR. Legacy serialization failures retain their original error category. |
| 59 | **Documentation**: Doxygen `@param`, `@return`, `@throw` on all public methods; `@par Example` with realistic business scenarios; `@note` for important caveats | Pass | Both CLI help texts, report scope, README commands and the durable API contract explain the compatibility default, explicit capture and complete-infoset limitation. |
| 60 | **QPP flags**: `[flags=CONSTANT]` on methods that never throw; `[flags=RET_VALUE_ONLY]` on methods that throw but have no side effects | N/A | No QPP code or flags changed. |
| 61 | **Security**: No user-controlled format strings; no buffer overflows; bounds checking on array indices; no credentials in code | Pass | Argument lists are passed without shell interpolation; format strings are constant. Offline resolution and bounds/manifest validation remain active; no credentials or new network I/O. |
| 62 | **Correctness**: Algorithms verified against reference implementations; edge cases tested (empty data, single sample, all-zero features) | Pass | 17 survey tests, original dynamic-type fixture QName/field assertions, independent native matrices and 11 affected Qore suites pass. Full corpus comparison preserves all legacy rows and tests both modes. Coverage has 15 passing methods and its two previously assigned P6 subtest failures; no new failure. |

Result: 15 Pass, 47 N/A, 0 Fail.
