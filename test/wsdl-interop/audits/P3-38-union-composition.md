# P3-38 audit: nested union composition

Copyright (C) 2026 Qore Technologies, s.r.o.

The full audit-changes checklist and applicable module-structure, sandboxing and
cancellation guides were applied to the exact final Qore change, tests, design,
release note and evidence. DataProvider checklist items were assessed separately.
**19 Pass / 43 N/A / 0 Fail**. No C++ changes require Valgrind.

[Evidence](../union-composition-evidence.md) records every gate and the exact
unchanged later-phase failures. The incomplete QName prototype is excluded.

| Check | Status | Evidence |
| --- | --- | --- |
| 1. Entry exists in `doxygen/lang/120_modules.dox.tmpl` (for modules in the Qore repo; N/A for external module repos) | N/A | No new module, separated module file or QPP class. |
| 2. Entry exists in `doxygen/lang/900_release_notes.dox.tmpl` (for modules in the Qore repo; external modules have release notes in their .qm) | Pass | WSDL v0.5.8 release notes describe member composition and retained restrictions. |
| 3. `qore_user_module()` or `qore_external_user_module()` call in `CMakeLists.txt` | Pass | Existing WSDL registration is unchanged; docs-module successfully rebuilds the changed module. |
| 4. Module added to QMOD list in `CMakeLists.txt` | N/A | No new module, separated module file or QPP class. |
| 5. `.qm` file has `@section <lowercasemodname>intro` as first doc section — **must be all lowercase** (e.g., `avrodataproviderintro`, not `AvroDataProviderintro`) | N/A | No new module, separated module file or QPP class. |
| 6. `%modern` in `.qm` file — no redundant `%new-style`, `%require-types`, `%strict-args`, `%enable-all-warnings` | Pass | Existing WSDL and the new qtest use %modern without redundant directives. |
| 7. No parse directives (`%requires`, `%modern`, `%new-style`) in separated `.qc` files (check OUTSIDE of `@code` blocks only) | N/A | No new module, separated module file or QPP class. |
| 8. No `%include` usage (deprecated for modules) | Pass | No deprecated %include introduced. |
| 9. Copyright 2026 on all new files | Pass | New qtest, Python regression, design, evidence and audit carry 2026 notices. |
| 10. Directory layout: `.qm` inside `qlib/<ModuleName>/` directory (not at `qlib/<ModuleName>.qm` for multi-file modules) | N/A | No new module, separated module file or QPP class. |
| 11. No second `.qm` for the same module at `qlib/<ModuleName>.qm` | N/A | No new module, separated module file or QPP class. |
| 12. `ns=Qore::XX` matches the QoreNamespace constructor path | N/A | No new module, separated module file or QPP class. |
| 13. `%modern` directive present | Pass | The new qtest explicitly uses %modern. |
| 14. Executable permission set (`chmod +x`) | Pass | The qtest and Python entry point are executable (0755). |
| 15. Uses %prepend-module-path  before %requires for in-repo modules (Qore and Qore modules only; not Qorus) | Pass | The qtest prepends local qlib and uses relative %requires ../qlib/WSDL.qm; Python workers use the existing local module path. |
| 16. External module dependencies use `%try-module` — except modules delivered with the project itself (Qore ex: DataProvider, ConnectionProvider, QUnit, etc.) which use hard `%requires` | Pass | The project xml module and core QUnit use hard requirements; existing Python workers retain their external json guards. |
| 17. No filesystem operations (fopen, open, creat, unlink, remove, rename, mkdir, rmdir, stat, chmod) without sandbox checks | N/A | No C++/QPP changes or native I/O/cancellation/flag changes in this increment. |
| 18. No network operations (connect, bind, socket, getaddrinfo, gethostbyname) without sandbox checks | N/A | No C++/QPP changes or native I/O/cancellation/flag changes in this increment. |
| 19. If filesystem/network ops exist, verify `QoreSandboxManagerHelper` usage | N/A | No C++/QPP changes or native I/O/cancellation/flag changes in this increment. |
| 20. No `File::`, `Dir::`, `Socket::`, `HTTPClient::` usage without justification | Pass | No new Qore filesystem, network or process operation. Integration fixtures use existing bounded local workers. |
| 21. All `for`/`while` loops that could iterate >100 times have `qore_check_cancel()` checks | N/A | No C++/QPP changes or native I/O/cancellation/flag changes in this increment. |
| 22. Uses `qore_check_cancel()` (NOT deprecated `qore_check_io_interrupt()`) | N/A | No C++/QPP changes or native I/O/cancellation/flag changes in this increment. |
| 23. Check frequency: every 100 iterations for tight loops, every 10 for expensive iterations | N/A | No C++/QPP changes or native I/O/cancellation/flag changes in this increment. |
| 24. No blocking operations without cancellation support | N/A | No C++/QPP changes or native I/O/cancellation/flag changes in this increment. |
| 25. Every action has `display_name`, `short_desc` (plain text, <80 chars), `desc` (markdown) | N/A | No DataProvider action/app/field/factory/JNI registration or new provider class. Existing schema providers consume the corrected resolved member graph and have independent tests. |
| 26. Every action has `options` populated via `getActionOptionFromFields()` — without this, the action shows an empty, unusable form | N/A | No DataProvider action/app/field/factory/JNI registration or new provider class. Existing schema providers consume the corrected resolved member graph and have independent tests. |
| 27. Every action has `output_type` set to a typed data type constant (e.g., `MyResponseDataType`) — not omitted | N/A | No DataProvider action/app/field/factory/JNI registration or new provider class. Existing schema providers consume the corrected resolved member graph and have independent tests. |
| 28. DPAT_API actions: provider has `"supports_request": True` and implements `doRequestImpl()` | N/A | No DataProvider action/app/field/factory/JNI registration or new provider class. Existing schema providers consume the corrected resolved member graph and have independent tests. |
| 29. DPAT_FIND actions: every option exists in `SearchOptions`, `getRecordTypeImpl()` returns `*hash<string, AbstractDataField>` | N/A | No DataProvider action/app/field/factory/JNI registration or new provider class. Existing schema providers consume the corrected resolved member graph and have independent tests. |
| 30. Scheme-based apps (with `"scheme"` in registerApp): actions use `"path"` and do NOT use `"cls"` — having both `scheme` and `cls` causes a runtime error | N/A | No DataProvider action/app/field/factory/JNI registration or new provider class. Existing schema providers consume the corrected resolved member graph and have independent tests. |
| 31. Single-key hash slices use trailing comma: `Fields{"key",}` (without trailing comma, `Fields{"key"}` returns the value, not a hash) | N/A | No DataProvider action/app/field/factory/JNI registration or new provider class. Existing schema providers consume the corrected resolved member graph and have independent tests. |
| 32. **Typed data type classes exist** for request and response types — inherit `HashDataType`, have `const Fields` hash, call `addQoreFields(Fields)` in constructor, export public constant at bottom (e.g., `public const MyDataType = new MyDataType();`) | N/A | No DataProvider action/app/field/factory/JNI registration or new provider class. Existing schema providers consume the corrected resolved member graph and have independent tests. |
| 33. Request/input types use `public` Fields (enables `ClassName::Fields` in action registration) | N/A | No DataProvider action/app/field/factory/JNI registration or new provider class. Existing schema providers consume the corrected resolved member graph and have independent tests. |
| 34. Response/output types use `private` Fields | N/A | No DataProvider action/app/field/factory/JNI registration or new provider class. Existing schema providers consume the corrected resolved member graph and have independent tests. |
| 35. Each field in data types has `display_name`, `type`, and `desc` (markdown-formatted) | N/A | No DataProvider action/app/field/factory/JNI registration or new provider class. Existing schema providers consume the corrected resolved member graph and have independent tests. |
| 36. Input fields have `example_value` where useful (string fields, endpoint URIs, SQL queries, etc.) | N/A | No DataProvider action/app/field/factory/JNI registration or new provider class. Existing schema providers consume the corrected resolved member graph and have independent tests. |
| 37. Fields with finite allowed values use `allowed_values` with `AllowedValueInfo` containing both `value` and `display_name` (Title Case, human-readable) — never bare values, never described only in text | N/A | No DataProvider action/app/field/factory/JNI registration or new provider class. Existing schema providers consume the corrected resolved member graph and have independent tests. |
| 38. Password/secret fields have `"sensitive": True` | N/A | No DataProvider action/app/field/factory/JNI registration or new provider class. Existing schema providers consume the corrected resolved member graph and have independent tests. |
| 39. `groups` uses `AppGroup` enum values from `qlib/DataProvider/AppGroup.qc` | N/A | No DataProvider action/app/field/factory/JNI registration or new provider class. Existing schema providers consume the corrected resolved member graph and have independent tests. |
| 40. App `logo` stored as separate file, loaded at module level in `Priv` namespace | N/A | No DataProvider action/app/field/factory/JNI registration or new provider class. Existing schema providers consume the corrected resolved member graph and have independent tests. |
| 41. App `desc` uses markdown: bullet list of capabilities, links to project website, business-language explanation of value | N/A | No DataProvider action/app/field/factory/JNI registration or new provider class. Existing schema providers consume the corrected resolved member graph and have independent tests. |
| 42. `display_name` is user-friendly ("Apache Avro" not "avro") | N/A | No DataProvider action/app/field/factory/JNI registration or new provider class. Existing schema providers consume the corrected resolved member graph and have independent tests. |
| 43. `short_desc` is plain text, under 80 chars, single sentence — no markdown | N/A | No DataProvider action/app/field/factory/JNI registration or new provider class. Existing schema providers consume the corrected resolved member graph and have independent tests. |
| 44. `desc` uses markdown: backticks for code/field refs (`` `field_name` ``, `` `True` ``, `` `pdf` ``), `\n\n` for paragraphs, `- ` bullet lists for enumerations, `**bold**` for caveats | N/A | No DataProvider action/app/field/factory/JNI registration or new provider class. Existing schema providers consume the corrected resolved member graph and have independent tests. |
| 45. Descriptions use plain business language relating to common challenges — not just technical "what" but "why" and "when to use" | N/A | No DataProvider action/app/field/factory/JNI registration or new provider class. Existing schema providers consume the corrected resolved member graph and have independent tests. |
| 46. No bare `True`/`False`/`NOTHING` — must be backtick-wrapped in `desc` | N/A | No DataProvider action/app/field/factory/JNI registration or new provider class. Existing schema providers consume the corrected resolved member graph and have independent tests. |
| 47. No bare field/option names in prose — must use backticks | N/A | No DataProvider action/app/field/factory/JNI registration or new provider class. Existing schema providers consume the corrected resolved member graph and have independent tests. |
| 48. Long descriptions (>500 chars) use bold section headers and bullet lists | N/A | No DataProvider action/app/field/factory/JNI registration or new provider class. Existing schema providers consume the corrected resolved member graph and have independent tests. |
| 49. **Factory registration in Qore repo**: every factory name registered in `qlib/DataProvider/DataProvider.qc` → `FactoryMap` (without this, module loads but doesn't appear in Qorus apps) | N/A | No DataProvider action/app/field/factory/JNI registration or new provider class. Existing schema providers consume the corrected resolved member graph and have independent tests. |
| 50. **`getRecordTypeImpl()` signature**: must be `private *hash<string, AbstractDataField> getRecordTypeImpl(*hash<auto> search_options)` — NOT returning `*AbstractDataProviderType` | N/A | No DataProvider action/app/field/factory/JNI registration or new provider class. Existing schema providers consume the corrected resolved member graph and have independent tests. |
| 51. **Dependency JARs committed** (for JNI modules): JAR files in `qlib/*/jar/` may be gitignored — use `git add -f` to ensure they're tracked, otherwise CI compilation fails | N/A | No DataProvider action/app/field/factory/JNI registration or new provider class. Existing schema providers consume the corrected resolved member graph and have independent tests. |
| 52. JAR install rules in CMakeLists.txt for all dependency JARs | N/A | No DataProvider action/app/field/factory/JNI registration or new provider class. Existing schema providers consume the corrected resolved member graph and have independent tests. |
| 53. **No workarounds**: No TODOs, FIXMEs, stubs, or partially-implemented features | Pass | The change implements XSD 1.0 member mapping after complete dependency validation. No ignored facet on a direct restricted use, weakened error, fixture exception or stub. |
| 54. **Exception safety**: C++ uses `ReferenceHolder` for Qore allocations, `std::unique_ptr` for C++ allocations, `*xsink` checked after every fallible operation | Pass | Composition uses a local typed list and commits it after all dependencies resolve. The temporary map is cleared by the existing parseTypes on_exit path on success/failure. Invalid-dependency recovery and reconstruction pass. |
| 55. **Thread safety**: All mutable shared state protected by `std::lock_guard<std::mutex>` or documented as immutable-after-construction | Pass | The new transient map belongs to the individual schema resolution context and is cleared before that context returns. No shared/global mutable state is added. |
| 56. **Type safety**: Strongly-typed `code<return(args)>` instead of untyped `code`; `static_cast` instead of C casts; typed hashdecls for results; enums where appropriate | Pass | The map is hash<string, XsdSimpleType> and composed members are list<XsdAbstractType>. Tests assert primitive typeCode as well as value; no untyped callable added. |
| 57. **Performance**: No O(n²) where O(n) is possible; no unnecessary copies; coordinate descent uses incremental residuals not full matrix multiply | Pass | Each resolved definition computes its composition target once. Shared member references preserve the compact graph and existing ordered cached traversal. A 32-level duplicated graph retains two shared references per level and bounded serialized metadata. |
| 58. **Error handling**: All inputs validated (dimensions, empty data, unfitted models); C++ I/O handles EAGAIN/EINTR if applicable | Pass | Invalid explicit restrictions are checked before composition; dependency cycles retain WSDL-ERROR. Positive/negative scalar, list, inline and outer-facet cases assert specific errors and recovery. |
| 59. **Documentation**: Doxygen `@param`, `@return`, `@throw` on all public methods; `@par Example` with realistic business scenarios; `@note` for important caveats | Pass | Implemented design includes normative reference, concrete schema example, compact-graph rationale and cleanup contract; test README, release notes and evidence are updated. No public method signature added. |
| 60. **QPP flags**: `[flags=CONSTANT]` on methods that never throw; `[flags=RET_VALUE_ONLY]` on methods that throw but have no side effects | N/A | No C++/QPP changes or native I/O/cancellation/flag changes in this increment. |
| 61. **Security**: No user-controlled format strings; no buffer overflows; bounds checking on array indices; no credentials in code | Pass | No native buffer, formatting, external I/O, credentials or new input parsing boundary. Existing complete schema validation remains ahead of composition. |
| 62. **Correctness**: Algorithms verified against reference implementations; edge cases tested (empty data, single sample, all-zero features) | Pass | 108 focused assertions, all modes, named/inline independent schema and wire matrices, exact primitive order, detached/reconstructed consumers, full XML gate and unchanged corpus results substantiate correctness. |
