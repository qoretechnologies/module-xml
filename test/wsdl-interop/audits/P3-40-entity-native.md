# P3-40 audit: native ENTITY context and reader error cleanup

Copyright (C) 2026 Qore Technologies, s.r.o.

The complete [audit-changes skill](/home/david/.codex/skills/audit-changes/SKILL.md)
was applied to the final native C++/dependency, CMake, test and documentation diff.
The referenced module-structure, sandboxing, cooperative-cancellation and provider
guides were reviewed; provider registration requirements are inapplicable here.
All 62 checks are resolved: **21 Pass / 41 N/A / 0 Fail**.

The [evidence record](../entity-native-evidence.md) records exact sources,
independent verdicts, all gates, the fixed leak and the existing core DWARF
warning. P3 remains active: WSDL document-context integration and the outstanding
dateTime/time decision are not acceptance claims of this native increment.

| Check | Status | Evidence |
| --- | --- | --- |
| 1. Entry exists in `doxygen/lang/120_modules.dox.tmpl` (for modules in the Qore repo; N/A for external module repos) | N/A | No new Qore module, separated .qc file or QPP class. Native release notes and build/test registrations are checked under documentation/correctness. |
| 2. Entry exists in `doxygen/lang/900_release_notes.dox.tmpl` (for modules in the Qore repo; external modules have release notes in their .qm) | N/A | No new Qore module, separated .qc file or QPP class. Native release notes and build/test registrations are checked under documentation/correctness. |
| 3. `qore_user_module()` or `qore_external_user_module()` call in `CMakeLists.txt` | N/A | No new Qore module, separated .qc file or QPP class. Native release notes and build/test registrations are checked under documentation/correctness. |
| 4. Module added to QMOD list in `CMakeLists.txt` | N/A | No new Qore module, separated .qc file or QPP class. Native release notes and build/test registrations are checked under documentation/correctness. |
| 5. `.qm` file has `@section <lowercasemodname>intro` as first doc section — **must be all lowercase** (e.g., `avrodataproviderintro`, not `AvroDataProviderintro`) | N/A | No new Qore module, separated .qc file or QPP class. Native release notes and build/test registrations are checked under documentation/correctness. |
| 6. `%modern` in `.qm` file — no redundant `%new-style`, `%require-types`, `%strict-args`, `%enable-all-warnings` | N/A | No new Qore module, separated .qc file or QPP class. Native release notes and build/test registrations are checked under documentation/correctness. |
| 7. No parse directives (`%requires`, `%modern`, `%new-style`) in separated `.qc` files (check OUTSIDE of `@code` blocks only) | N/A | No new Qore module, separated .qc file or QPP class. Native release notes and build/test registrations are checked under documentation/correctness. |
| 8. No `%include` usage (deprecated for modules) | N/A | No new Qore module, separated .qc file or QPP class. Native release notes and build/test registrations are checked under documentation/correctness. |
| 9. Copyright 2026 on all new files | Pass | All newly authored CMake/C/Qore/Python/Java and documentation files carry 2026 notices; copied dependency context retains upstream notices. |
| 10. Directory layout: `.qm` inside `qlib/<ModuleName>/` directory (not at `qlib/<ModuleName>.qm` for multi-file modules) | N/A | No new Qore module, separated .qc file or QPP class. Native release notes and build/test registrations are checked under documentation/correctness. |
| 11. No second `.qm` for the same module at `qlib/<ModuleName>.qm` | N/A | No new Qore module, separated .qc file or QPP class. Native release notes and build/test registrations are checked under documentation/correctness. |
| 12. `ns=Qore::XX` matches the QoreNamespace constructor path | N/A | No new Qore module, separated .qc file or QPP class. Native release notes and build/test registrations are checked under documentation/correctness. |
| 13. `%modern` directive present | Pass | xml-entities.qtest and entity-validator.qr explicitly use %modern. |
| 14. Executable permission set (`chmod +x`) | Pass | The new qtest, Qore worker and Python matrix are mode 755. |
| 15. Uses %prepend-module-path  before %requires for in-repo modules (Qore and Qore modules only; not Qorus) | Pass | No repository .qm dependency is added. The user-required QORE_MODULE_DIR selects the verified local Debug/Release binary, with local qlib and the tested core paths. |
| 16. External module dependencies use `%try-module` — except modules delivered with the project itself (Qore ex: DataProvider, ConnectionProvider, QUnit, etc.) which use hard `%requires` | Pass | Same-repository xml and core QUnit are mandatory; the worker uses %try-module json with explicit MISSING-MODULE failure for the external dependency. |
| 17. No filesystem operations (fopen, open, creat, unlink, remove, rename, mkdir, rmdir, stat, chmod) without sandbox checks | Pass | The C++ diff changes read-result propagation and element-boundary handling only. Native dependency changes allocate values and record declarations; they add no filesystem operation. |
| 18. No network operations (connect, bind, socket, getaddrinfo, gethostbyname) without sandbox checks | Pass | No native network operation is added. An unparsed entity name is checked against declarations without fetching its resource. |
| 19. If filesystem/network ops exist, verify `QoreSandboxManagerHelper` usage | N/A | No filesystem/network operation is added by the C++ or dependency changes. |
| 20. No `File::`, `Dir::`, `Socket::`, `HTTPClient::` usage without justification | Pass | The Qore worker reads an explicitly supplied test manifest and schema files through Qore APIs. No production Qore I/O is added. Oracle policy tests use temporary readable files and verify external content/declarations remain unavailable. |
| 21. All `for`/`while` loops that could iterate >100 times have `qore_check_cancel()` checks | Pass | No C++ loop is added. getXmlData calls read(xsink), whose cancellation check runs on every iteration. Pure libxml2 C datatype/declaration loops have no Qore ABI dependency; they remain within the existing reader callbacks and per-document context. |
| 22. Uses `qore_check_cancel()` (NOT deprecated `qore_check_io_interrupt()`) | Pass | Reader paths retain qore_check_cancel; no deprecated cancellation API is introduced. The deterministic SandboxManager interrupt test passes. |
| 23. Check frequency: every 100 iterations for tight loops, every 10 for expensive iterations | Pass | Existing C++ read checks execute every iteration. No new C++ tight or expensive loop requires a separate check interval. |
| 24. No blocking operations without cancellation support | Pass | No blocking operation is added. Existing native reader resource/transport cancellation paths and seven independent resource tests pass; worker subprocesses have bounded deadlines. |
| 25. Every action has `display_name`, `short_desc` (plain text, <80 chars), `desc` (markdown) | N/A | No DataProvider action, field, app, factory, record type or JNI dependency is changed in this native increment. |
| 26. Every action has `options` populated via `getActionOptionFromFields()` — without this, the action shows an empty, unusable form | N/A | No DataProvider action, field, app, factory, record type or JNI dependency is changed in this native increment. |
| 27. Every action has `output_type` set to a typed data type constant (e.g., `MyResponseDataType`) — not omitted | N/A | No DataProvider action, field, app, factory, record type or JNI dependency is changed in this native increment. |
| 28. DPAT_API actions: provider has `"supports_request": True` and implements `doRequestImpl()` | N/A | No DataProvider action, field, app, factory, record type or JNI dependency is changed in this native increment. |
| 29. DPAT_FIND actions: every option exists in `SearchOptions`, `getRecordTypeImpl()` returns `*hash<string, AbstractDataField>` | N/A | No DataProvider action, field, app, factory, record type or JNI dependency is changed in this native increment. |
| 30. Scheme-based apps (with `"scheme"` in registerApp): actions use `"path"` and do NOT use `"cls"` — having both `scheme` and `cls` causes a runtime error | N/A | No DataProvider action, field, app, factory, record type or JNI dependency is changed in this native increment. |
| 31. Single-key hash slices use trailing comma: `Fields{"key",}` (without trailing comma, `Fields{"key"}` returns the value, not a hash) | N/A | No DataProvider action, field, app, factory, record type or JNI dependency is changed in this native increment. |
| 32. **Typed data type classes exist** for request and response types — inherit `HashDataType`, have `const Fields` hash, call `addQoreFields(Fields)` in constructor, export public constant at bottom (e.g., `public const MyDataType = new MyDataType();`) | N/A | No DataProvider action, field, app, factory, record type or JNI dependency is changed in this native increment. |
| 33. Request/input types use `public` Fields (enables `ClassName::Fields` in action registration) | N/A | No DataProvider action, field, app, factory, record type or JNI dependency is changed in this native increment. |
| 34. Response/output types use `private` Fields | N/A | No DataProvider action, field, app, factory, record type or JNI dependency is changed in this native increment. |
| 35. Each field in data types has `display_name`, `type`, and `desc` (markdown-formatted) | N/A | No DataProvider action, field, app, factory, record type or JNI dependency is changed in this native increment. |
| 36. Input fields have `example_value` where useful (string fields, endpoint URIs, SQL queries, etc.) | N/A | No DataProvider action, field, app, factory, record type or JNI dependency is changed in this native increment. |
| 37. Fields with finite allowed values use `allowed_values` with `AllowedValueInfo` containing both `value` and `display_name` (Title Case, human-readable) — never bare values, never described only in text | N/A | No DataProvider action, field, app, factory, record type or JNI dependency is changed in this native increment. |
| 38. Password/secret fields have `"sensitive": True` | N/A | No DataProvider action, field, app, factory, record type or JNI dependency is changed in this native increment. |
| 39. `groups` uses `AppGroup` enum values from `qlib/DataProvider/AppGroup.qc` | N/A | No DataProvider action, field, app, factory, record type or JNI dependency is changed in this native increment. |
| 40. App `logo` stored as separate file, loaded at module level in `Priv` namespace | N/A | No DataProvider action, field, app, factory, record type or JNI dependency is changed in this native increment. |
| 41. App `desc` uses markdown: bullet list of capabilities, links to project website, business-language explanation of value | N/A | No DataProvider action, field, app, factory, record type or JNI dependency is changed in this native increment. |
| 42. `display_name` is user-friendly ("Apache Avro" not "avro") | N/A | No DataProvider action, field, app, factory, record type or JNI dependency is changed in this native increment. |
| 43. `short_desc` is plain text, under 80 chars, single sentence — no markdown | N/A | No DataProvider action, field, app, factory, record type or JNI dependency is changed in this native increment. |
| 44. `desc` uses markdown: backticks for code/field refs (`` `field_name` ``, `` `True` ``, `` `pdf` ``), `\n\n` for paragraphs, `- ` bullet lists for enumerations, `**bold**` for caveats | N/A | No DataProvider action, field, app, factory, record type or JNI dependency is changed in this native increment. |
| 45. Descriptions use plain business language relating to common challenges — not just technical "what" but "why" and "when to use" | N/A | No DataProvider action, field, app, factory, record type or JNI dependency is changed in this native increment. |
| 46. No bare `True`/`False`/`NOTHING` — must be backtick-wrapped in `desc` | N/A | No DataProvider action, field, app, factory, record type or JNI dependency is changed in this native increment. |
| 47. No bare field/option names in prose — must use backticks | N/A | No DataProvider action, field, app, factory, record type or JNI dependency is changed in this native increment. |
| 48. Long descriptions (>500 chars) use bold section headers and bullet lists | N/A | No DataProvider action, field, app, factory, record type or JNI dependency is changed in this native increment. |
| 49. **Factory registration in Qore repo**: every factory name registered in `qlib/DataProvider/DataProvider.qc` → `FactoryMap` (without this, module loads but doesn't appear in Qorus apps) | N/A | No DataProvider action, field, app, factory, record type or JNI dependency is changed in this native increment. |
| 50. **`getRecordTypeImpl()` signature**: must be `private *hash<string, AbstractDataField> getRecordTypeImpl(*hash<auto> search_options)` — NOT returning `*AbstractDataProviderType` | N/A | No DataProvider action, field, app, factory, record type or JNI dependency is changed in this native increment. |
| 51. **Dependency JARs committed** (for JNI modules): JAR files in `qlib/*/jar/` may be gitignored — use `git add -f` to ensure they're tracked, otherwise CI compilation fails | N/A | No DataProvider action, field, app, factory, record type or JNI dependency is changed in this native increment. |
| 52. JAR install rules in CMakeLists.txt for all dependency JARs | N/A | No DataProvider action, field, app, factory, record type or JNI dependency is changed in this native increment. |
| 53. **No workarounds**: No TODOs, FIXMEs, stubs, or partially-implemented features | Pass | The native fixes restore missing declaration context, ENTITY computed values and list facets, and enforce the effective length-bound constraint. The reader fix propagates actual callback errors. No ignored facet, fixture-specific production branch, stub, new skip or workaround is added; copied upstream TODO context is unchanged. |
| 54. **Exception safety**: C++ uses `ReferenceHolder` for Qore allocations, `std::unique_ptr` for C++ allocations, `*xsink` checked after every fallible operation | Pass | The reader preserves xml_stack ownership when callbacks raise exceptions, including failed reads at element boundaries. Native tables and computed values free on context clear/free, unplug, conversion and allocation failure. All 78 injected sites and the full eight-case Qore Valgrind run report zero errors/lost bytes; the existing core DWARF diagnostic remains explicitly recorded. |
| 55. **Thread safety**: All mutable shared state protected by `std::lock_guard<std::mutex>` or documented as immutable-after-construction | Pass | Declaration maps belong to individual validation contexts. The two static entity-kind markers are identity-only addresses and never written. Built-in facets initialize with the existing libxml2 initialization mechanism; no new mutable shared state is exposed. |
| 56. **Type safety**: Strongly-typed `code<return(args)>` instead of untyped `code`; `static_cast` instead of C casts; typed hashdecls for results; enums where appropriate | Pass | The C++ result remains a typed integer status, Qore values use existing ownership types, and tests explicitly type containers and errors. Native C uses libxml2 pointer/enumeration APIs and checked buffer lengths. The Python worker validates complete ordered result sets. |
| 57. **Performance**: No O(n²) where O(n) is possible; no unnecessary copies; coordinate descent uses incremental residuals not full matrix multiply | Pass | Declaration insertion and lookup use a per-document hash table. Only types that can contain ENTITY request extra computed values; flattened union members and atomic base chains are traversed without expanding a type graph. Effective length bounds compare existing facet values without allocating copies. |
| 58. **Error handling**: All inputs validated (dimensions, empty data, unfitted models); C++ I/O handles EAGAIN/EINTR if applicable | Pass | Tests cover undeclared/parsed/predefined/parameter names, duplicate declaration order, Unicode and whitespace, empty lists, facets, ordered unions, invalid defaults and schema restrictions, nil, cancellation, early close and recovery. Rejections assert XSD-SYNTAX-ERROR, XSD-ERROR or PARSE-XML-EXCEPTION as appropriate. |
| 59. **Documentation**: Doxygen `@param`, `@return`, `@throw` on all public methods; `@par Example` with realistic business scenarios; `@note` for important caveats | Pass | README, interoperability README, native release notes and the implemented ENTITY design describe semantics, provider selection, external-resource policy and a runnable example. The example executes and docs-module builds without warnings/errors. Internal reader changes add no public signature. |
| 60. **QPP flags**: `[flags=CONSTANT]` on methods that never throw; `[flags=RET_VALUE_ONLY]` on methods that throw but have no side effects | N/A | No QPP declaration or public functional-domain flag is changed. |
| 61. **Security**: No user-controlled format strings; no buffer overflows; bounds checking on array indices; no credentials in code | Pass | No credentials or user-controlled format strings are introduced. Probe snprintf results are bounds checked, dependency input/output hashes are verified, offline sources stay immutable, and standalone oracle tests prove external entity/DTD files cannot supply content or declarations. |
| 62. **Correctness**: Algorithms verified against reference implementations; edge cases tested (empty data, single sample, all-zero features) | Pass | The final 90-suite gate passes 937 cases with 35,897 reported assertions; eight ENTITY cases/753 assertions pass in all four modes and both build types. The 5,679-document matrix checks 39,753 native verdicts and exact values; 432 schema constructions include nine explicit Xerces false acceptances with mandatory native rejection. Provider tests (22), survey (15), independent protocol (7), resource/URI tests (18), native probes and allocation tests pass. The both-version corpus report is exactly unchanged. |
