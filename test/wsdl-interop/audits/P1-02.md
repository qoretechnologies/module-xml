# P1-02 audit: independent source adjudication and pinned contracts

Copyright (C) 2026 Qore Technologies, s.r.o.

Applied `/home/david/.codex/skills/audit-changes/SKILL.md` to the exact intended
changes on `develop`, parent `8914353`. All 62 checklist items follow individually.
This commits a complete P1 component; P1 and the later implementation phases remain open.

## Scope and referenced design

New Python/Java test tools and tests, pinned Xerces JAR with embedded notices,
unmodified CXF contracts/schema/notices, adjudication decisions and generated report,
README/execution/plan status, and module release notes. No production runtime,
C++, Qore module, QPP, DataProvider, .qtest or installation changes.

Consulted the skill's references under `/home/david/src/qore/git/qore/design/`:
`qore-module-structure.md`, `module-sandboxing-audit-guide.md`,
`cooperative-cancellation.md`, `data-provider-checklist.md` and
`data-provider-development-guide.md`. Their language/provider applicability is
recorded separately below; generic correctness and safety checks apply to the test tools.

## Verification evidence

- `python3 -m unittest discover -s test/wsdl-interop -p 'test_*.py' -v`: 44 pass;
  includes a complete offline extraction and strict source-adjudication subprocess.
- `qore --enable-debug test/wsdl-interop.qtest`: 10 cases, 230 assertions pass.
- All seven required SOAP/XSD/handler/client suites rerun: 199 cases pass; logs
  `/tmp/wsdl-p1-tests/*-p1-02.log`. The three comparator assertion failures in
  `soap.qtest` are deliberately caught by negative tests, as documented in P1-01.
- Both-version surveys completed with and without catalog:
  `/tmp/wsdl-survey-p1-02-original.json` and `/tmp/wsdl-survey-p1-02-catalog.json`.
  Catalog report is exactly JSON-equal to committed `current-report.json`: no regression.
- Strict source adjudication: 293 WSDLs, 1,136 messages, three historical outputs;
  14 invalid source WSDLs, 88 invalid source messages, zero unclassified source disagreements.
  This is not a Qore conformance count. Java compiles with all lint checks and `-Werror`.
- Authored whitespace checked; upstream files retained byte-for-byte. No C++ means
  valgrind is not required. All new dependency hashes and CXF import closure tested.

## Full checklist

| Check | Status | Evidence |
| --- | --- | --- |
| 1. Entry exists in `doxygen/lang/120_modules.dox.tmpl` (for modules in the Qore repo; N/A for external module repos) | N/A | No Qore module, QPP class or .qtest changed. Existing Qore tests load local code with debugging enabled. |
| 2. Entry exists in `doxygen/lang/900_release_notes.dox.tmpl` (for modules in the Qore repo; external modules have release notes in their .qm) | N/A | No Qore module, QPP class or .qtest changed. Existing Qore tests load local code with debugging enabled. |
| 3. `qore_user_module()` or `qore_external_user_module()` call in `CMakeLists.txt` | N/A | No Qore module, QPP class or .qtest changed. Existing Qore tests load local code with debugging enabled. |
| 4. Module added to QMOD list in `CMakeLists.txt` | N/A | No Qore module, QPP class or .qtest changed. Existing Qore tests load local code with debugging enabled. |
| 5. `.qm` file has `@section <lowercasemodname>intro` as first doc section — **must be all lowercase** (e.g., `avrodataproviderintro`, not `AvroDataProviderintro`) | N/A | No Qore module, QPP class or .qtest changed. Existing Qore tests load local code with debugging enabled. |
| 6. `%modern` in `.qm` file — no redundant `%new-style`, `%require-types`, `%strict-args`, `%enable-all-warnings` | N/A | No Qore module, QPP class or .qtest changed. Existing Qore tests load local code with debugging enabled. |
| 7. No parse directives (`%requires`, `%modern`, `%new-style`) in separated `.qc` files (check OUTSIDE of `@code` blocks only) | N/A | No Qore module, QPP class or .qtest changed. Existing Qore tests load local code with debugging enabled. |
| 8. No `%include` usage (deprecated for modules) | N/A | No Qore module, QPP class or .qtest changed. Existing Qore tests load local code with debugging enabled. |
| 9. Copyright 2026 on all new files | Pass | All authored Python/Java/Markdown files carry 2026 copyright; original CXF and JAR notices are preserved and verified. JSON is generated evidence or provenance data. |
| 10. Directory layout: `.qm` inside `qlib/<ModuleName>/` directory (not at `qlib/<ModuleName>.qm` for multi-file modules) | N/A | No Qore module, QPP class or .qtest changed. Existing Qore tests load local code with debugging enabled. |
| 11. No second `.qm` for the same module at `qlib/<ModuleName>.qm` | N/A | No Qore module, QPP class or .qtest changed. Existing Qore tests load local code with debugging enabled. |
| 12. `ns=Qore::XX` matches the QoreNamespace constructor path | N/A | No Qore module, QPP class or .qtest changed. Existing Qore tests load local code with debugging enabled. |
| 13. `%modern` directive present | N/A | No Qore module, QPP class or .qtest changed. Existing Qore tests load local code with debugging enabled. |
| 14. Executable permission set (`chmod +x`) | N/A | No Qore module, QPP class or .qtest changed. Existing Qore tests load local code with debugging enabled. |
| 15. Uses %prepend-module-path  before %requires for in-repo modules (Qore and Qore modules only; not Qorus) | N/A | No Qore module, QPP class or .qtest changed. Existing Qore tests load local code with debugging enabled. |
| 16. External module dependencies use `%try-module` — except modules delivered with the project itself (Qore ex: DataProvider, ConnectionProvider, QUnit, etc.) which use hard `%requires` | N/A | No Qore module, QPP class or .qtest changed. Existing Qore tests load local code with debugging enabled. |
| 17. No filesystem operations (fopen, open, creat, unlink, remove, rename, mkdir, rmdir, stat, chmod) without sandbox checks | N/A | No C++ or Qore I/O changes. Python/Java offline resolution, path confinement and DTD rejection are tested under Security. |
| 18. No network operations (connect, bind, socket, getaddrinfo, gethostbyname) without sandbox checks | N/A | No C++ or Qore I/O changes. Python/Java offline resolution, path confinement and DTD rejection are tested under Security. |
| 19. If filesystem/network ops exist, verify `QoreSandboxManagerHelper` usage | N/A | No C++ or Qore I/O changes. Python/Java offline resolution, path confinement and DTD rejection are tested under Security. |
| 20. No `File::`, `Dir::`, `Socket::`, `HTTPClient::` usage without justification | N/A | No C++ or Qore I/O changes. Python/Java offline resolution, path confinement and DTD rejection are tested under Security. |
| 21. All `for`/`while` loops that could iterate >100 times have `qore_check_cancel()` checks | N/A | No C++ loops or blocking operations changed. Oracle subprocesses have bounded deadlines; exception/cancellation cleanup is tested without sleeps. |
| 22. Uses `qore_check_cancel()` (NOT deprecated `qore_check_io_interrupt()`) | N/A | No C++ loops or blocking operations changed. Oracle subprocesses have bounded deadlines; exception/cancellation cleanup is tested without sleeps. |
| 23. Check frequency: every 100 iterations for tight loops, every 10 for expensive iterations | N/A | No C++ loops or blocking operations changed. Oracle subprocesses have bounded deadlines; exception/cancellation cleanup is tested without sleeps. |
| 24. No blocking operations without cancellation support | N/A | No C++ loops or blocking operations changed. Oracle subprocesses have bounded deadlines; exception/cancellation cleanup is tested without sleeps. |
| 25. Every action has `display_name`, `short_desc` (plain text, <80 chars), `desc` (markdown) | N/A | No DataProvider action, typed field, app, scheme or FactoryMap registration changed. |
| 26. Every action has `options` populated via `getActionOptionFromFields()` — without this, the action shows an empty, unusable form | N/A | No DataProvider action, typed field, app, scheme or FactoryMap registration changed. |
| 27. Every action has `output_type` set to a typed data type constant (e.g., `MyResponseDataType`) — not omitted | N/A | No DataProvider action, typed field, app, scheme or FactoryMap registration changed. |
| 28. DPAT_API actions: provider has `"supports_request": True` and implements `doRequestImpl()` | N/A | No DataProvider action, typed field, app, scheme or FactoryMap registration changed. |
| 29. DPAT_FIND actions: every option exists in `SearchOptions`, `getRecordTypeImpl()` returns `*hash<string, AbstractDataField>` | N/A | No DataProvider action, typed field, app, scheme or FactoryMap registration changed. |
| 30. Scheme-based apps (with `"scheme"` in registerApp): actions use `"path"` and do NOT use `"cls"` — having both `scheme` and `cls` causes a runtime error | N/A | No DataProvider action, typed field, app, scheme or FactoryMap registration changed. |
| 31. Single-key hash slices use trailing comma: `Fields{"key",}` (without trailing comma, `Fields{"key"}` returns the value, not a hash) | N/A | No DataProvider action, typed field, app, scheme or FactoryMap registration changed. |
| 32. **Typed data type classes exist** for request and response types — inherit `HashDataType`, have `const Fields` hash, call `addQoreFields(Fields)` in constructor, export public constant at bottom (e.g., `public const MyDataType = new MyDataType();`) | N/A | No DataProvider action, typed field, app, scheme or FactoryMap registration changed. |
| 33. Request/input types use `public` Fields (enables `ClassName::Fields` in action registration) | N/A | No DataProvider action, typed field, app, scheme or FactoryMap registration changed. |
| 34. Response/output types use `private` Fields | N/A | No DataProvider action, typed field, app, scheme or FactoryMap registration changed. |
| 35. Each field in data types has `display_name`, `type`, and `desc` (markdown-formatted) | N/A | No DataProvider action, typed field, app, scheme or FactoryMap registration changed. |
| 36. Input fields have `example_value` where useful (string fields, endpoint URIs, SQL queries, etc.) | N/A | No DataProvider action, typed field, app, scheme or FactoryMap registration changed. |
| 37. Fields with finite allowed values use `allowed_values` with `AllowedValueInfo` containing both `value` and `display_name` (Title Case, human-readable) — never bare values, never described only in text | N/A | No DataProvider action, typed field, app, scheme or FactoryMap registration changed. |
| 38. Password/secret fields have `"sensitive": True` | N/A | No DataProvider action, typed field, app, scheme or FactoryMap registration changed. |
| 39. `groups` uses `AppGroup` enum values from `qlib/DataProvider/AppGroup.qc` | N/A | No DataProvider action, typed field, app, scheme or FactoryMap registration changed. |
| 40. App `logo` stored as separate file, loaded at module level in `Priv` namespace | N/A | No DataProvider action, typed field, app, scheme or FactoryMap registration changed. |
| 41. App `desc` uses markdown: bullet list of capabilities, links to project website, business-language explanation of value | N/A | No DataProvider action, typed field, app, scheme or FactoryMap registration changed. |
| 42. `display_name` is user-friendly ("Apache Avro" not "avro") | N/A | No DataProvider action, typed field, app, scheme or FactoryMap registration changed. |
| 43. `short_desc` is plain text, under 80 chars, single sentence — no markdown | N/A | No DataProvider action, typed field, app, scheme or FactoryMap registration changed. |
| 44. `desc` uses markdown: backticks for code/field refs (`` `field_name` ``, `` `True` ``, `` `pdf` ``), `\n\n` for paragraphs, `- ` bullet lists for enumerations, `**bold**` for caveats | N/A | No DataProvider action, typed field, app, scheme or FactoryMap registration changed. |
| 45. Descriptions use plain business language relating to common challenges — not just technical "what" but "why" and "when to use" | N/A | No DataProvider action, typed field, app, scheme or FactoryMap registration changed. |
| 46. No bare `True`/`False`/`NOTHING` — must be backtick-wrapped in `desc` | N/A | No DataProvider action, typed field, app, scheme or FactoryMap registration changed. |
| 47. No bare field/option names in prose — must use backticks | N/A | No DataProvider action, typed field, app, scheme or FactoryMap registration changed. |
| 48. Long descriptions (>500 chars) use bold section headers and bullet lists | N/A | No DataProvider action, typed field, app, scheme or FactoryMap registration changed. |
| 49. **Factory registration in Qore repo**: every factory name registered in `qlib/DataProvider/DataProvider.qc` → `FactoryMap` (without this, module loads but doesn't appear in Qorus apps) | N/A | No DataProvider action, typed field, app, scheme or FactoryMap registration changed. |
| 50. **`getRecordTypeImpl()` signature**: must be `private *hash<string, AbstractDataField> getRecordTypeImpl(*hash<auto> search_options)` — NOT returning `*AbstractDataProviderType` | N/A | No DataProvider action, typed field, app, scheme or FactoryMap registration changed. |
| 51. **Dependency JARs committed** (for JNI modules): JAR files in `qlib/*/jar/` may be gitignored — use `git add -f` to ensure they're tracked, otherwise CI compilation fails | Pass | The checksum-pinned Xerces dependency JAR is included in this commit with its embedded LICENSE/NOTICE. It is test infrastructure, not JNI runtime code. |
| 52. JAR install rules in CMakeLists.txt for all dependency JARs | N/A | No runtime JAR installation: the oracle is an offline repository test tool invoked through its checked-in manifest. |
| 53. **No workarounds**: No TODOs, FIXMEs, stubs, or partially-implemented features | Pass | Complete source-adjudication component; no fixture rewrites, stubs or production workarounds. Aggregate and Qore conformance obligations remain explicitly open in EXECUTION.md, outside this component. |
| 54. **Exception safety**: C++ uses `ReferenceHolder` for Qore allocations, `std::unique_ptr` for C++ allocations, `*xsink` checked after every fallible operation | Pass | No C++ allocations. Python TemporaryDirectory and subprocess.run clean up on failure, timeout and cancellation; Java uses try-with-resources. Deterministic injected failure/cancellation tests pass. |
| 55. **Thread safety**: All mutable shared state protected by `std::lock_guard<std::mutex>` or documented as immutable-after-construction | Pass | Single-threaded oracle; local job/result maps. Catalog immutable after construction. No mutable state shared between workers. |
| 56. **Type safety**: Strongly-typed `code<return(args)>` instead of untyped `code`; `static_cast` instead of C casts; typed hashdecls for results; enums where appropriate | Pass | SchemaJob dataclass and typed Python interfaces; typed Java schema/resource maps. Oracle status, identities and decoded diagnostics are validated before use. |
| 57. **Performance**: No O(n²) where O(n) is possible; no unnecessary copies; coordinate descent uses incremental residuals not full matrix multiply | Pass | One batched oracle process per run; indexed schema/document results and set-based completeness checks. Inputs capped at 10,000 blobs / 64 MiB, Java heap 256 MiB. |
| 58. **Error handling**: All inputs validated (dimensions, empty data, unfitted models); C++ I/O handles EAGAIN/EINTR if applicable | Pass | Tests reject malformed/duplicate/stale identities, incomplete/reordered worker records, invalid base64, missing resources, invalid namespaces, extra corpus files and contradictory normative assertions. Unexpected process failures are fatal. |
| 59. **Documentation**: Doxygen `@param`, `@return`, `@throw` on all public methods; `@par Example` with realistic business scenarios; `@note` for important caveats | Pass | Internal tool docstrings plus README commands, prerequisites, provenance, exact scope and caveats. Release notes updated; no public Qore API added. |
| 60. **QPP flags**: `[flags=CONSTANT]` on methods that never throw; `[flags=RET_VALUE_ONLY]` on methods that throw but have no side effects | N/A | No QPP methods changed. |
| 61. **Security**: No user-controlled format strings; no buffer overflows; bounds checking on array indices; no credentials in code | Pass | No network fallback; verified archive/resource/JAR hashes; DTD/entity rejection; bounded subprocesses and raw input bytes; no shell evaluation of manifests or credentials. Negative security cases pass. |
| 62. **Correctness**: Algorithms verified against reference implementations; edge cases tested (empty data, single sample, all-zero features) | Pass | 44 Python tests plus 209 Qore cases pass. Inline and echo schemas assessed using independent Xerces and libxml2, with specification predicates overriding oracle disagreements. Both-version Qore report is exactly unchanged from P1-01. |

All applicable checks pass; no audit failures remain.
