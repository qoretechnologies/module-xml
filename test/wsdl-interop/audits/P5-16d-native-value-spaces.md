# P5-16d native value-space audit

Copyright (C) 2026 Qore Technologies, s.r.o.

Applied the complete audit-changes skill and all five referenced design guides
on 2026-09-12 to the exact native corrections, CMake integration, tests and docs.
Parent: `7927265`. No main-Qore or production WSDL source change is included.
[Evidence](../native-value-spaces-evidence.md) and
[inventory](../P5-16d-validation.json) retain commands, hashes and results.
The canonical-default/identity interaction remains a separately tracked P5 question;
this increment does not close P5 or P6-P9 acceptance.

| # | Check | Status | Evidence |
| --- | --- | --- | --- |
| 1 | Entry exists in `doxygen/lang/120_modules.dox.tmpl` (for modules in the Qore repo; N/A for external module repos) | N/A | No Qore module entry point or production Qore source changed. CMake corrections and probe inputs are registered and the source-distribution check passes. |
| 2 | Entry exists in `doxygen/lang/900_release_notes.dox.tmpl` (for modules in the Qore repo; external modules have release notes in their .qm) | Pass | Native xml 2.3.0 release notes, README, implemented design and executed shipment-cutoff example describe the corrections. |
| 3 | `qore_user_module()` or `qore_external_user_module()` call in `CMakeLists.txt` | N/A | No Qore module entry point or production Qore source changed. CMake corrections and probe inputs are registered and the source-distribution check passes. |
| 4 | Module added to QMOD list in `CMakeLists.txt` | N/A | No Qore module entry point or production Qore source changed. CMake corrections and probe inputs are registered and the source-distribution check passes. |
| 5 | `.qm` file has `@section <lowercasemodname>intro` as first doc section — **must be all lowercase** (e.g., `avrodataproviderintro`, not `AvroDataProviderintro`) | N/A | No Qore module entry point or production Qore source changed. CMake corrections and probe inputs are registered and the source-distribution check passes. |
| 6 | `%modern` in `.qm` file — no redundant `%new-style`, `%require-types`, `%strict-args`, `%enable-all-warnings` | N/A | No Qore module entry point or production Qore source changed. CMake corrections and probe inputs are registered and the source-distribution check passes. |
| 7 | No parse directives (`%requires`, `%modern`, `%new-style`) in separated `.qc` files (check OUTSIDE of `@code` blocks only) | N/A | No Qore module entry point or production Qore source changed. CMake corrections and probe inputs are registered and the source-distribution check passes. |
| 8 | No `%include` usage (deprecated for modules) | Pass | No Qore %include introduced. The C fault-injection test includes the exact selected native source to reach its list-validation boundary. |
| 9 | Copyright 2026 on all new files | Pass | Every new authored file has a 2026 copyright notice; third-party notices remain intact. |
| 10 | Directory layout: `.qm` inside `qlib/<ModuleName>/` directory (not at `qlib/<ModuleName>.qm` for multi-file modules) | N/A | No Qore module entry point or production Qore source changed. CMake corrections and probe inputs are registered and the source-distribution check passes. |
| 11 | No second `.qm` for the same module at `qlib/<ModuleName>.qm` | N/A | No Qore module entry point or production Qore source changed. CMake corrections and probe inputs are registered and the source-distribution check passes. |
| 12 | `ns=Qore::XX` matches the QoreNamespace constructor path | N/A | No new QPP class, namespace, public native method or method flags. |
| 13 | `%modern` directive present | Pass | Executable modern qtest prepends the local module path and requires same-project xml and core QUnit. The reused XML worker retains its guarded external JSON dependency. |
| 14 | Executable permission set (`chmod +x`) | Pass | Executable modern qtest prepends the local module path and requires same-project xml and core QUnit. The reused XML worker retains its guarded external JSON dependency. |
| 15 | Uses %prepend-module-path  before %requires for in-repo modules (Qore and Qore modules only; not Qorus) | Pass | Executable modern qtest prepends the local module path and requires same-project xml and core QUnit. The reused XML worker retains its guarded external JSON dependency. |
| 16 | External module dependencies use `%try-module` — except modules delivered with the project itself (Qore ex: DataProvider, ConnectionProvider, QUnit, etc.) which use hard `%requires` | Pass | Executable modern qtest prepends the local module path and requires same-project xml and core QUnit. The reused XML worker retains its guarded external JSON dependency. |
| 17 | No filesystem operations (fopen, open, creat, unlink, remove, rename, mkdir, rmdir, stat, chmod) without sandbox checks | N/A | No production filesystem or network operation introduced; allocator hooks are test/probe-only. |
| 18 | No network operations (connect, bind, socket, getaddrinfo, gethostbyname) without sandbox checks | N/A | No production filesystem or network operation introduced; allocator hooks are test/probe-only. |
| 19 | If filesystem/network ops exist, verify `QoreSandboxManagerHelper` usage | N/A | No production filesystem or network operation introduced; allocator hooks are test/probe-only. |
| 20 | No `File::`, `Dir::`, `Socket::`, `HTTPClient::` usage without justification | Pass | No production Qore change. Existing offline fixture loading and real HTTP consumer tests retain their established I/O boundaries. |
| 21 | All `for`/`while` loops that could iterate >100 times have `qore_check_cancel()` checks | N/A | No new production loop, recursion or cancellation API. New clock/unsigned/error guards are constant time; existing typed-list traversal and native validation controls remain in place. |
| 22 | Uses `qore_check_cancel()` (NOT deprecated `qore_check_io_interrupt()`) | N/A | No new production loop, recursion or cancellation API. New clock/unsigned/error guards are constant time; existing typed-list traversal and native validation controls remain in place. |
| 23 | Check frequency: every 100 iterations for tight loops, every 10 for expensive iterations | N/A | No new production loop, recursion or cancellation API. New clock/unsigned/error guards are constant time; existing typed-list traversal and native validation controls remain in place. |
| 24 | No blocking operations without cancellation support | Pass | No blocking operation added. Existing cancellation, streaming and subsequent-reuse suites pass in the full gate. |
| 25 | Every action has `display_name`, `short_desc` (plain text, <80 chars), `desc` (markdown) | N/A | No provider app/action/datatype/field/factory or JNI change. Existing provider and SOAP consumers pass with the rebuilt native artifact. |
| 26 | Every action has `options` populated via `getActionOptionFromFields()` — without this, the action shows an empty, unusable form | N/A | No provider app/action/datatype/field/factory or JNI change. Existing provider and SOAP consumers pass with the rebuilt native artifact. |
| 27 | Every action has `output_type` set to a typed data type constant (e.g., `MyResponseDataType`) — not omitted | N/A | No provider app/action/datatype/field/factory or JNI change. Existing provider and SOAP consumers pass with the rebuilt native artifact. |
| 28 | DPAT_API actions: provider has `"supports_request": True` and implements `doRequestImpl()` | N/A | No provider app/action/datatype/field/factory or JNI change. Existing provider and SOAP consumers pass with the rebuilt native artifact. |
| 29 | DPAT_FIND actions: every option exists in `SearchOptions`, `getRecordTypeImpl()` returns `*hash<string, AbstractDataField>` | N/A | No provider app/action/datatype/field/factory or JNI change. Existing provider and SOAP consumers pass with the rebuilt native artifact. |
| 30 | Scheme-based apps (with `"scheme"` in registerApp): actions use `"path"` and do NOT use `"cls"` — having both `scheme` and `cls` causes a runtime error | N/A | No provider app/action/datatype/field/factory or JNI change. Existing provider and SOAP consumers pass with the rebuilt native artifact. |
| 31 | Single-key hash slices use trailing comma: `Fields{"key",}` (without trailing comma, `Fields{"key"}` returns the value, not a hash) | N/A | No provider app/action/datatype/field/factory or JNI change. Existing provider and SOAP consumers pass with the rebuilt native artifact. |
| 32 | **Typed data type classes exist** for request and response types — inherit `HashDataType`, have `const Fields` hash, call `addQoreFields(Fields)` in constructor, export public constant at bottom (e.g., `public const MyDataType = new MyDataType();`) | N/A | No provider app/action/datatype/field/factory or JNI change. Existing provider and SOAP consumers pass with the rebuilt native artifact. |
| 33 | Request/input types use `public` Fields (enables `ClassName::Fields` in action registration) | N/A | No provider app/action/datatype/field/factory or JNI change. Existing provider and SOAP consumers pass with the rebuilt native artifact. |
| 34 | Response/output types use `private` Fields | N/A | No provider app/action/datatype/field/factory or JNI change. Existing provider and SOAP consumers pass with the rebuilt native artifact. |
| 35 | Each field in data types has `display_name`, `type`, and `desc` (markdown-formatted) | N/A | No provider app/action/datatype/field/factory or JNI change. Existing provider and SOAP consumers pass with the rebuilt native artifact. |
| 36 | Input fields have `example_value` where useful (string fields, endpoint URIs, SQL queries, etc.) | N/A | No provider app/action/datatype/field/factory or JNI change. Existing provider and SOAP consumers pass with the rebuilt native artifact. |
| 37 | Fields with finite allowed values use `allowed_values` with `AllowedValueInfo` containing both `value` and `display_name` (Title Case, human-readable) — never bare values, never described only in text | N/A | No provider app/action/datatype/field/factory or JNI change. Existing provider and SOAP consumers pass with the rebuilt native artifact. |
| 38 | Password/secret fields have `"sensitive": True` | N/A | No provider app/action/datatype/field/factory or JNI change. Existing provider and SOAP consumers pass with the rebuilt native artifact. |
| 39 | `groups` uses `AppGroup` enum values from `qlib/DataProvider/AppGroup.qc` | N/A | No provider app/action/datatype/field/factory or JNI change. Existing provider and SOAP consumers pass with the rebuilt native artifact. |
| 40 | App `logo` stored as separate file, loaded at module level in `Priv` namespace | N/A | No provider app/action/datatype/field/factory or JNI change. Existing provider and SOAP consumers pass with the rebuilt native artifact. |
| 41 | App `desc` uses markdown: bullet list of capabilities, links to project website, business-language explanation of value | N/A | No provider app/action/datatype/field/factory or JNI change. Existing provider and SOAP consumers pass with the rebuilt native artifact. |
| 42 | `display_name` is user-friendly ("Apache Avro" not "avro") | N/A | No provider app/action/datatype/field/factory or JNI change. Existing provider and SOAP consumers pass with the rebuilt native artifact. |
| 43 | `short_desc` is plain text, under 80 chars, single sentence — no markdown | N/A | No provider app/action/datatype/field/factory or JNI change. Existing provider and SOAP consumers pass with the rebuilt native artifact. |
| 44 | `desc` uses markdown: backticks for code/field refs (`` `field_name` ``, `` `True` ``, `` `pdf` ``), `\n\n` for paragraphs, `- ` bullet lists for enumerations, `**bold**` for caveats | N/A | No provider app/action/datatype/field/factory or JNI change. Existing provider and SOAP consumers pass with the rebuilt native artifact. |
| 45 | Descriptions use plain business language relating to common challenges — not just technical "what" but "why" and "when to use" | N/A | No provider app/action/datatype/field/factory or JNI change. Existing provider and SOAP consumers pass with the rebuilt native artifact. |
| 46 | No bare `True`/`False`/`NOTHING` — must be backtick-wrapped in `desc` | N/A | No provider app/action/datatype/field/factory or JNI change. Existing provider and SOAP consumers pass with the rebuilt native artifact. |
| 47 | No bare field/option names in prose — must use backticks | N/A | No provider app/action/datatype/field/factory or JNI change. Existing provider and SOAP consumers pass with the rebuilt native artifact. |
| 48 | Long descriptions (>500 chars) use bold section headers and bullet lists | N/A | No provider app/action/datatype/field/factory or JNI change. Existing provider and SOAP consumers pass with the rebuilt native artifact. |
| 49 | **Factory registration in Qore repo**: every factory name registered in `qlib/DataProvider/DataProvider.qc` → `FactoryMap` (without this, module loads but doesn't appear in Qorus apps) | N/A | No provider app/action/datatype/field/factory or JNI change. Existing provider and SOAP consumers pass with the rebuilt native artifact. |
| 50 | **`getRecordTypeImpl()` signature**: must be `private *hash<string, AbstractDataField> getRecordTypeImpl(*hash<auto> search_options)` — NOT returning `*AbstractDataProviderType` | N/A | No provider app/action/datatype/field/factory or JNI change. Existing provider and SOAP consumers pass with the rebuilt native artifact. |
| 51 | **Dependency JARs committed** (for JNI modules): JAR files in `qlib/*/jar/` may be gitignored — use `git add -f` to ensure they're tracked, otherwise CI compilation fails | N/A | No provider app/action/datatype/field/factory or JNI change. Existing provider and SOAP consumers pass with the rebuilt native artifact. |
| 52 | JAR install rules in CMakeLists.txt for all dependency JARs | N/A | No provider app/action/datatype/field/factory or JNI change. Existing provider and SOAP consumers pass with the rebuilt native artifact. |
| 53 | **No workarounds**: No TODOs, FIXMEs, stubs, or partially-implemented features | Pass | Corrects native string-vs-value comparison, missing-date normalization, unsigned lexical acceptance and allocation-result classification at their roots. Canonical-default semantics remain a separately documented P5 investigation; no fallback, fixture waiver or source workaround is added. |
| 54 | **Exception safety**: C++ uses `ReferenceHolder` for Qore allocations, `std::unique_ptr` for C++ allocations, `*xsink` checked after every fallible operation | Pass | Computed values follow existing context cleanup. Negative comparison/allocation results enter internal cleanup; NULL list tokens are checked before validation; unpublished string values are freed and the URI safe parser preserves allocation errors. All 100 permanent allocation failures recover cleanly; unit/probe/allocation Valgrind runs have zero errors or lost memory. |
| 55 | **Thread safety**: All mutable shared state protected by `std::lock_guard<std::mutex>` or documented as immutable-after-construction | Pass | No mutable production global or cache. Time comparison reads immutable values; each validation owns its computed value. Allocator hooks run only in standalone test processes before initialization. |
| 56 | **Type safety**: Strongly-typed `code<return(args)>` instead of untyped `code`; `static_cast` instead of C casts; typed hashdecls for results; enums where appropriate | Pass | Uses libxml2 value enums and typed schema pointers; bounded signed minute arithmetic avoids unsigned offset subtraction. Qore test helpers and hashes are typed. C code correctly retains C casts rather than C++ syntax. |
| 57 | **Performance**: No O(n²) where O(n) is possible; no unnecessary copies; coordinate descent uses incremental residuals not full matrix multiply | Pass | Clock comparison and lexical/error guards are constant time. Fixed values use one required computed value and existing linear equality; no new recursive traversal or quadratic conversion. |
| 58 | **Error handling**: All inputs validated (dimensions, empty data, unfitted models); C++ I/O handles EAGAIN/EINTR if applicable | Pass | Positive/negative cases assert DOM/reader/XmlDoc error categories, schema-declaration rejection, unequal fixed values, empty lists, unsigned signs, precision, timezone presence and range facets. Native faults distinguish internal errors from invalid input and test reuse. |
| 59 | **Documentation**: Doxygen `@param`, `@return`, `@throw` on all public methods; `@par Example` with realistic business scenarios; `@note` for important caveats | Pass | No new public API. Release notes, implemented design and executed shipment-cutoff example document behavior and ownership. qpp and final Doxygen finish without warnings or errors. |
| 60 | **QPP flags**: `[flags=CONSTANT]` on methods that never throw; `[flags=RET_VALUE_ONLY]` on methods that throw but have no side effects | N/A | No new QPP class, namespace, public native method or method flags. |
| 61 | **Security**: No user-controlled format strings; no buffer overflows; bounds checking on array indices; no credentials in code | Pass | No credentials or user-controlled format strings. Probe buffer lengths are checked; source transformations verify input/output digests, preserve source trees and stay privately linked. |
| 62 | **Correctness**: Algorithms verified against reference implementations; edge cases tested (empty data, single sample, all-zero features) | Pass | 142 source suites/1366 cases/66465 assertions, all source modes, two AOT consumers, 47 provider tests and 95 schemas/774 documents pass. Fourteen exact Xerces midnight defects are recorded without weakening native expectations. Corpus rows/failures are identical to P5-16c; remaining P5/P6-P9 requirements stay open. |

Result: 18 Pass, 44 N/A, 0 Fail. The independently validated native increment may be committed.
