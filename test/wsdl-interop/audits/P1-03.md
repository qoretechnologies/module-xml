# P1-03 audit: strict Qore coverage and independent directions

Copyright (C) 2026 Qore Technologies, s.r.o.

Applied `/home/david/.codex/skills/audit-changes/SKILL.md` to the intended final
changes on `develop`, parent `7a9692e`. All 62 checklist items are recorded below.
This is a P1 component commit; aggregate-source accounting and final P1 acceptance remain open.

## Scope and design references

Python reporting/selection tools and tests, internal Qore probe script, generated
full coverage ledger, README/execution record and module release notes. No runtime
module, C++, DataProvider, build or installation changes.

Referenced design documents under `/home/david/src/qore/git/qore/design/`:
`qore-module-structure.md`, `module-sandboxing-audit-guide.md`,
`cooperative-cancellation.md`, `data-provider-checklist.md` and
`data-provider-development-guide.md`. Qore test/module-loading and I/O applicability
are reviewed explicitly. C++/provider checks are individually N/A.

## Verification

- Full Python discovery: 54 tests pass. Coverage tests rerun after final runtime
  provenance changes; all five pass. Qore runs enable debugging.
- Qore interop: 10 cases / 230 assertions pass. All seven affected SOAP/XSD/client/
  handler suites pass, 199 additional cases; logs `/tmp/wsdl-p1-tests/*-p1-03.log`.
  Comparator negative-test assertions in soap.qtest are intentionally caught.
- Both-version broad surveys with/without catalog are exactly JSON-equal to P1-02:
  `/tmp/wsdl-survey-p1-03-original.json` and `/tmp/wsdl-survey-p1-03-catalog.json`.
- Strict coverage gate passes 23 selected descriptions and 68 message/direction
  combinations. Full diagnostic ledger retains 449 failures, 40 failed value checks
  and 1,860 unassessed value/infoset checks; no conformance claim for these.
- Complete worker stage counts have no missing/skipped operations. Tests reject
  incomplete results and prove real child termination/reaping on cancellation.
- Authored diff whitespace clean; original fixtures unchanged. No C++/valgrind required.

## Full checklist

| Check | Status | Evidence |
| --- | --- | --- |
| 1. Entry exists in `doxygen/lang/120_modules.dox.tmpl` (for modules in the Qore repo; N/A for external module repos) | N/A | No new Qore module or QPP class; no build or installation changes. |
| 2. Entry exists in `doxygen/lang/900_release_notes.dox.tmpl` (for modules in the Qore repo; external modules have release notes in their .qm) | N/A | No new Qore module or QPP class; no build or installation changes. |
| 3. `qore_user_module()` or `qore_external_user_module()` call in `CMakeLists.txt` | N/A | No new Qore module or QPP class; no build or installation changes. |
| 4. Module added to QMOD list in `CMakeLists.txt` | N/A | No new Qore module or QPP class; no build or installation changes. |
| 5. `.qm` file has `@section <lowercasemodname>intro` as first doc section — **must be all lowercase** (e.g., `avrodataproviderintro`, not `AvroDataProviderintro`) | N/A | No new Qore module or QPP class; no build or installation changes. |
| 6. `%modern` in `.qm` file — no redundant `%new-style`, `%require-types`, `%strict-args`, `%enable-all-warnings` | N/A | No new Qore module or QPP class; no build or installation changes. |
| 7. No parse directives (`%requires`, `%modern`, `%new-style`) in separated `.qc` files (check OUTSIDE of `@code` blocks only) | N/A | No new Qore module or QPP class; no build or installation changes. |
| 8. No `%include` usage (deprecated for modules) | N/A | No new Qore module or QPP class; no build or installation changes. |
| 9. Copyright 2026 on all new files | Pass | Authored Python/Qore/Markdown carries 2026 copyright; JSON is generated evidence or reviewed test selection data. |
| 10. Directory layout: `.qm` inside `qlib/<ModuleName>/` directory (not at `qlib/<ModuleName>.qm` for multi-file modules) | N/A | No new Qore module or QPP class; no build or installation changes. |
| 11. No second `.qm` for the same module at `qlib/<ModuleName>.qm` | N/A | No new Qore module or QPP class; no build or installation changes. |
| 12. `ns=Qore::XX` matches the QoreNamespace constructor path | N/A | No new Qore module or QPP class; no build or installation changes. |
| 13. `%modern` directive present | Pass | probe.qr retains explicit %modern. New Python tests use unittest and run the Qore worker with --enable-debug. |
| 14. Executable permission set (`chmod +x`) | Pass | probe.qr and all Python CLI/test scripts are executable; no .qtest file changed. |
| 15. Uses %prepend-module-path  before %requires for in-repo modules (Qore and Qore modules only; not Qorus) | Pass | probe.qr prepends ../../qlib and requires ../../qlib/WSDL.qm, so tests load local development code. |
| 16. External module dependencies use `%try-module` — except modules delivered with the project itself (Qore ex: DataProvider, ConnectionProvider, QUnit, etc.) which use hard `%requires` | Pass | probe.qr guards external json with %try-module and uses hard %requires for same-project xml/WSDL. |
| 17. No filesystem operations (fopen, open, creat, unlink, remove, rename, mkdir, rmdir, stat, chmod) without sandbox checks | N/A | No C++ I/O changes. Offline resource and process safety reviewed under Security. |
| 18. No network operations (connect, bind, socket, getaddrinfo, gethostbyname) without sandbox checks | N/A | No C++ I/O changes. Offline resource and process safety reviewed under Security. |
| 19. If filesystem/network ops exist, verify `QoreSandboxManagerHelper` usage | N/A | No C++ I/O changes. Offline resource and process safety reviewed under Security. |
| 20. No `File::`, `Dir::`, `Socket::`, `HTTPClient::` usage without justification | Pass | ReadOnlyFile reads verified local test manifests and corpus files; WebService uses async_only and a supplied XSD cache. No HTTP/network activity in this worker. |
| 21. All `for`/`while` loops that could iterate >100 times have `qore_check_cancel()` checks | N/A | No C++ loops/blocking I/O changed. Python subprocess cleanup, cancellation, readiness and bounded deadlines are exercised explicitly. |
| 22. Uses `qore_check_cancel()` (NOT deprecated `qore_check_io_interrupt()`) | N/A | No C++ loops/blocking I/O changed. Python subprocess cleanup, cancellation, readiness and bounded deadlines are exercised explicitly. |
| 23. Check frequency: every 100 iterations for tight loops, every 10 for expensive iterations | N/A | No C++ loops/blocking I/O changed. Python subprocess cleanup, cancellation, readiness and bounded deadlines are exercised explicitly. |
| 24. No blocking operations without cancellation support | N/A | No C++ loops/blocking I/O changed. Python subprocess cleanup, cancellation, readiness and bounded deadlines are exercised explicitly. |
| 25. Every action has `display_name`, `short_desc` (plain text, <80 chars), `desc` (markdown) | N/A | No DataProvider registrations, fields, apps, FactoryMap, JNI dependencies or installation rules changed; pinned Xerces remains tracked from P1-02. |
| 26. Every action has `options` populated via `getActionOptionFromFields()` — without this, the action shows an empty, unusable form | N/A | No DataProvider registrations, fields, apps, FactoryMap, JNI dependencies or installation rules changed; pinned Xerces remains tracked from P1-02. |
| 27. Every action has `output_type` set to a typed data type constant (e.g., `MyResponseDataType`) — not omitted | N/A | No DataProvider registrations, fields, apps, FactoryMap, JNI dependencies or installation rules changed; pinned Xerces remains tracked from P1-02. |
| 28. DPAT_API actions: provider has `"supports_request": True` and implements `doRequestImpl()` | N/A | No DataProvider registrations, fields, apps, FactoryMap, JNI dependencies or installation rules changed; pinned Xerces remains tracked from P1-02. |
| 29. DPAT_FIND actions: every option exists in `SearchOptions`, `getRecordTypeImpl()` returns `*hash<string, AbstractDataField>` | N/A | No DataProvider registrations, fields, apps, FactoryMap, JNI dependencies or installation rules changed; pinned Xerces remains tracked from P1-02. |
| 30. Scheme-based apps (with `"scheme"` in registerApp): actions use `"path"` and do NOT use `"cls"` — having both `scheme` and `cls` causes a runtime error | N/A | No DataProvider registrations, fields, apps, FactoryMap, JNI dependencies or installation rules changed; pinned Xerces remains tracked from P1-02. |
| 31. Single-key hash slices use trailing comma: `Fields{"key",}` (without trailing comma, `Fields{"key"}` returns the value, not a hash) | N/A | No DataProvider registrations, fields, apps, FactoryMap, JNI dependencies or installation rules changed; pinned Xerces remains tracked from P1-02. |
| 32. **Typed data type classes exist** for request and response types — inherit `HashDataType`, have `const Fields` hash, call `addQoreFields(Fields)` in constructor, export public constant at bottom (e.g., `public const MyDataType = new MyDataType();`) | N/A | No DataProvider registrations, fields, apps, FactoryMap, JNI dependencies or installation rules changed; pinned Xerces remains tracked from P1-02. |
| 33. Request/input types use `public` Fields (enables `ClassName::Fields` in action registration) | N/A | No DataProvider registrations, fields, apps, FactoryMap, JNI dependencies or installation rules changed; pinned Xerces remains tracked from P1-02. |
| 34. Response/output types use `private` Fields | N/A | No DataProvider registrations, fields, apps, FactoryMap, JNI dependencies or installation rules changed; pinned Xerces remains tracked from P1-02. |
| 35. Each field in data types has `display_name`, `type`, and `desc` (markdown-formatted) | N/A | No DataProvider registrations, fields, apps, FactoryMap, JNI dependencies or installation rules changed; pinned Xerces remains tracked from P1-02. |
| 36. Input fields have `example_value` where useful (string fields, endpoint URIs, SQL queries, etc.) | N/A | No DataProvider registrations, fields, apps, FactoryMap, JNI dependencies or installation rules changed; pinned Xerces remains tracked from P1-02. |
| 37. Fields with finite allowed values use `allowed_values` with `AllowedValueInfo` containing both `value` and `display_name` (Title Case, human-readable) — never bare values, never described only in text | N/A | No DataProvider registrations, fields, apps, FactoryMap, JNI dependencies or installation rules changed; pinned Xerces remains tracked from P1-02. |
| 38. Password/secret fields have `"sensitive": True` | N/A | No DataProvider registrations, fields, apps, FactoryMap, JNI dependencies or installation rules changed; pinned Xerces remains tracked from P1-02. |
| 39. `groups` uses `AppGroup` enum values from `qlib/DataProvider/AppGroup.qc` | N/A | No DataProvider registrations, fields, apps, FactoryMap, JNI dependencies or installation rules changed; pinned Xerces remains tracked from P1-02. |
| 40. App `logo` stored as separate file, loaded at module level in `Priv` namespace | N/A | No DataProvider registrations, fields, apps, FactoryMap, JNI dependencies or installation rules changed; pinned Xerces remains tracked from P1-02. |
| 41. App `desc` uses markdown: bullet list of capabilities, links to project website, business-language explanation of value | N/A | No DataProvider registrations, fields, apps, FactoryMap, JNI dependencies or installation rules changed; pinned Xerces remains tracked from P1-02. |
| 42. `display_name` is user-friendly ("Apache Avro" not "avro") | N/A | No DataProvider registrations, fields, apps, FactoryMap, JNI dependencies or installation rules changed; pinned Xerces remains tracked from P1-02. |
| 43. `short_desc` is plain text, under 80 chars, single sentence — no markdown | N/A | No DataProvider registrations, fields, apps, FactoryMap, JNI dependencies or installation rules changed; pinned Xerces remains tracked from P1-02. |
| 44. `desc` uses markdown: backticks for code/field refs (`` `field_name` ``, `` `True` ``, `` `pdf` ``), `\n\n` for paragraphs, `- ` bullet lists for enumerations, `**bold**` for caveats | N/A | No DataProvider registrations, fields, apps, FactoryMap, JNI dependencies or installation rules changed; pinned Xerces remains tracked from P1-02. |
| 45. Descriptions use plain business language relating to common challenges — not just technical "what" but "why" and "when to use" | N/A | No DataProvider registrations, fields, apps, FactoryMap, JNI dependencies or installation rules changed; pinned Xerces remains tracked from P1-02. |
| 46. No bare `True`/`False`/`NOTHING` — must be backtick-wrapped in `desc` | N/A | No DataProvider registrations, fields, apps, FactoryMap, JNI dependencies or installation rules changed; pinned Xerces remains tracked from P1-02. |
| 47. No bare field/option names in prose — must use backticks | N/A | No DataProvider registrations, fields, apps, FactoryMap, JNI dependencies or installation rules changed; pinned Xerces remains tracked from P1-02. |
| 48. Long descriptions (>500 chars) use bold section headers and bullet lists | N/A | No DataProvider registrations, fields, apps, FactoryMap, JNI dependencies or installation rules changed; pinned Xerces remains tracked from P1-02. |
| 49. **Factory registration in Qore repo**: every factory name registered in `qlib/DataProvider/DataProvider.qc` → `FactoryMap` (without this, module loads but doesn't appear in Qorus apps) | N/A | No DataProvider registrations, fields, apps, FactoryMap, JNI dependencies or installation rules changed; pinned Xerces remains tracked from P1-02. |
| 50. **`getRecordTypeImpl()` signature**: must be `private *hash<string, AbstractDataField> getRecordTypeImpl(*hash<auto> search_options)` — NOT returning `*AbstractDataProviderType` | N/A | No DataProvider registrations, fields, apps, FactoryMap, JNI dependencies or installation rules changed; pinned Xerces remains tracked from P1-02. |
| 51. **Dependency JARs committed** (for JNI modules): JAR files in `qlib/*/jar/` may be gitignored — use `git add -f` to ensure they're tracked, otherwise CI compilation fails | N/A | No DataProvider registrations, fields, apps, FactoryMap, JNI dependencies or installation rules changed; pinned Xerces remains tracked from P1-02. |
| 52. JAR install rules in CMakeLists.txt for all dependency JARs | N/A | No DataProvider registrations, fields, apps, FactoryMap, JNI dependencies or installation rules changed; pinned Xerces remains tracked from P1-02. |
| 53. **No workarounds**: No TODOs, FIXMEs, stubs, or partially-implemented features | Pass | Complete strict-selection/reporting component. No runtime workaround or source rewrite. Broad failures remain failures; unmeasured value/infoset preservation remains counted and documented. |
| 54. **Exception safety**: C++ uses `ReferenceHolder` for Qore allocations, `std::unique_ptr` for C++ allocations, `*xsink` checked after every fallible operation | Pass | Qore automatic resource ownership and per-stage exceptions; Python context managers/subprocess cleanup. Real child cancellation verifies termination, reaping and manifest cleanup; injected failures/timeouts also tested. |
| 55. **Thread safety**: All mutable shared state protected by `std::lock_guard<std::mutex>` or documented as immutable-after-construction | Pass | All worker/result state is per-run and single-threaded. No new shared mutable global state. |
| 56. **Type safety**: Strongly-typed `code<return(args)>` instead of untyped `code`; `static_cast` instead of C casts; typed hashdecls for results; enums where appropriate | Pass | Typed Qore WSOperation/hash variables and typed Python public interfaces. Generic deserialized auto values are required by the existing WSDL API. Manifest identities and status values validated before use. |
| 57. **Performance**: No O(n²) where O(n) is possible; no unnecessary copies; coordinate descent uses incremental residuals not full matrix multiply | Pass | Set/dict-based inventory and result accounting, one Qore batch and one Xerces batch per full run. Separate direction work is intentional. Corpus report preserves bodies required for independent assertions. |
| 58. **Error handling**: All inputs validated (dimensions, empty data, unfitted models); C++ I/O handles EAGAIN/EINTR if applicable | Pass | Tests reject stale hashes, provisional decisions, absent/duplicate/malformed entries, missing/reordered worker stages and wrong error categories. Invalid source acceptance never becomes a conformance pass. |
| 59. **Documentation**: Doxygen `@param`, `@return`, `@throw` on all public methods; `@par Example` with realistic business scenarios; `@note` for important caveats | Pass | Internal function docstrings; README gives strict command, explicit selection/counts, failure and unassessed semantics, precise scope and limitations. Release notes and execution record updated. |
| 60. **QPP flags**: `[flags=CONSTANT]` on methods that never throw; `[flags=RET_VALUE_ONLY]` on methods that throw but have no side effects | N/A | No QPP methods changed. |
| 61. **Security**: No user-controlled format strings; no buffer overflows; bounds checking on array indices; no credentials in code | Pass | Pinned resources, no network fallback or shell interpretation, bounded processes, verified original hashes. Actual child readiness/cancellation regression uses an event and bounded selector, without sleeps or polling. |
| 62. **Correctness**: Algorithms verified against reference implementations; edge cases tested (empty data, single sample, all-zero features) | Pass | 54 Python tests and all 209 Qore cases pass; both original/catalog diagnostic surveys exactly match P1-02. Independent boolean mutation detects schema-valid value loss. CXF SOAP 1.2 request/response roots and text are asserted independently. |

All applicable checks pass; no audit failures remain.
