# P5-16f WSDL anyType inheritance audit

Copyright (C) 2026 Qore Technologies, s.r.o.

Applied the full audit-changes skill and all five referenced design guides on
2026-09-12 to the final WSDL implementation, shared/new tests and documentation.
Parent: `ad3d574`. No native or main-Qore changes are included.
[Evidence](../anytype-inheritance-evidence.md) and [inventory](../P5-16f-validation.json)
record final source hashes, commands, results and the complete corpus comparison.

| # | Check | Status | Evidence |
| --- | --- | --- | --- |
| 1 | Entry exists in `doxygen/lang/120_modules.dox.tmpl` (for modules in the Qore repo; N/A for external module repos) | N/A | No new module, split entry point, QPP class or build registration. Existing WSDL source and compiled qmod targets are retained. |
| 2 | Entry exists in `doxygen/lang/900_release_notes.dox.tmpl` (for modules in the Qore repo; external modules have release notes in their .qm) | Pass | WSDL 0.5.8 release notes describe effective anyType inheritance and wildcard metadata; README and durable design include an executed shipment example. |
| 3 | `qore_user_module()` or `qore_external_user_module()` call in `CMakeLists.txt` | N/A | No new module, split entry point, QPP class or build registration. Existing WSDL source and compiled qmod targets are retained. |
| 4 | Module added to QMOD list in `CMakeLists.txt` | N/A | No new module, split entry point, QPP class or build registration. Existing WSDL source and compiled qmod targets are retained. |
| 5 | `.qm` file has `@section <lowercasemodname>intro` as first doc section — **must be all lowercase** (e.g., `avrodataproviderintro`, not `AvroDataProviderintro`) | Pass | Existing single-file WSDL module retains wsdlintro and %modern, with no separated directives or %include. |
| 6 | `%modern` in `.qm` file — no redundant `%new-style`, `%require-types`, `%strict-args`, `%enable-all-warnings` | Pass | Existing single-file WSDL module retains wsdlintro and %modern, with no separated directives or %include. |
| 7 | No parse directives (`%requires`, `%modern`, `%new-style`) in separated `.qc` files (check OUTSIDE of `@code` blocks only) | N/A | No new module, split entry point, QPP class or build registration. Existing WSDL source and compiled qmod targets are retained. |
| 8 | No `%include` usage (deprecated for modules) | Pass | Existing single-file WSDL module retains wsdlintro and %modern, with no separated directives or %include. |
| 9 | Copyright 2026 on all new files | Pass | All authored new code, tests and documentation carry copyright 2026. |
| 10 | Directory layout: `.qm` inside `qlib/<ModuleName>/` directory (not at `qlib/<ModuleName>.qm` for multi-file modules) | N/A | No new module, split entry point, QPP class or build registration. Existing WSDL source and compiled qmod targets are retained. |
| 11 | No second `.qm` for the same module at `qlib/<ModuleName>.qm` | N/A | No new module, split entry point, QPP class or build registration. Existing WSDL source and compiled qmod targets are retained. |
| 12 | `ns=Qore::XX` matches the QoreNamespace constructor path | N/A | No new module, split entry point, QPP class or build registration. Existing WSDL source and compiled qmod targets are retained. |
| 13 | `%modern` directive present | Pass | All three qtests are executable, use %modern and prepend the local qlib path before relative WSDL/SoapClient/SoapHandler requirements. Same-project xml and core QUnit/HttpServer are hard requirements; the unchanged matrix worker guards its external JSON dependency. |
| 14 | Executable permission set (`chmod +x`) | Pass | All three qtests are executable, use %modern and prepend the local qlib path before relative WSDL/SoapClient/SoapHandler requirements. Same-project xml and core QUnit/HttpServer are hard requirements; the unchanged matrix worker guards its external JSON dependency. |
| 15 | Uses %prepend-module-path  before %requires for in-repo modules (Qore and Qore modules only; not Qorus) | Pass | All three qtests are executable, use %modern and prepend the local qlib path before relative WSDL/SoapClient/SoapHandler requirements. Same-project xml and core QUnit/HttpServer are hard requirements; the unchanged matrix worker guards its external JSON dependency. |
| 16 | External module dependencies use `%try-module` — except modules delivered with the project itself (Qore ex: DataProvider, ConnectionProvider, QUnit, etc.) which use hard `%requires` | Pass | All three qtests are executable, use %modern and prepend the local qlib path before relative WSDL/SoapClient/SoapHandler requirements. Same-project xml and core QUnit/HttpServer are hard requirements; the unchanged matrix worker guards its external JSON dependency. |
| 17 | No filesystem operations (fopen, open, creat, unlink, remove, rename, mkdir, rmdir, stat, chmod) without sandbox checks | N/A | No C/C++ or native blocking operation changes. Qore traversals retain runtime cancellation support; the new graph walk visits each active node once. |
| 18 | No network operations (connect, bind, socket, getaddrinfo, gethostbyname) without sandbox checks | N/A | No C/C++ or native blocking operation changes. Qore traversals retain runtime cancellation support; the new graph walk visits each active node once. |
| 19 | If filesystem/network ops exist, verify `QoreSandboxManagerHelper` usage | N/A | No C/C++ or native blocking operation changes. Qore traversals retain runtime cancellation support; the new graph walk visits each active node once. |
| 20 | No `File::`, `Dir::`, `Socket::`, `HTTPClient::` usage without justification | Pass | No production filesystem/network I/O added. HTTP integration uses ephemeral loopback listeners, 30-second request/queue bounds and on_exit server shutdown, without sleeps or polling. |
| 21 | All `for`/`while` loops that could iterate >100 times have `qore_check_cancel()` checks | N/A | No C/C++ or native blocking operation changes. Qore traversals retain runtime cancellation support; the new graph walk visits each active node once. |
| 22 | Uses `qore_check_cancel()` (NOT deprecated `qore_check_io_interrupt()`) | N/A | No C/C++ or native blocking operation changes. Qore traversals retain runtime cancellation support; the new graph walk visits each active node once. |
| 23 | Check frequency: every 100 iterations for tight loops, every 10 for expensive iterations | N/A | No C/C++ or native blocking operation changes. Qore traversals retain runtime cancellation support; the new graph walk visits each active node once. |
| 24 | No blocking operations without cancellation support | N/A | No C/C++ or native blocking operation changes. Qore traversals retain runtime cancellation support; the new graph walk visits each active node once. |
| 25 | Every action has `display_name`, `short_desc` (plain text, <80 chars), `desc` (markdown) | N/A | No app/action, field declaration, factory, JNI or provider API registration changes. Effective wildcard metadata selects existing validating provider types; native/ordinary, soft/mandatory, Serializable, samples, SoapDataProvider and compiled consumers are tested. |
| 26 | Every action has `options` populated via `getActionOptionFromFields()` — without this, the action shows an empty, unusable form | N/A | No app/action, field declaration, factory, JNI or provider API registration changes. Effective wildcard metadata selects existing validating provider types; native/ordinary, soft/mandatory, Serializable, samples, SoapDataProvider and compiled consumers are tested. |
| 27 | Every action has `output_type` set to a typed data type constant (e.g., `MyResponseDataType`) — not omitted | N/A | No app/action, field declaration, factory, JNI or provider API registration changes. Effective wildcard metadata selects existing validating provider types; native/ordinary, soft/mandatory, Serializable, samples, SoapDataProvider and compiled consumers are tested. |
| 28 | DPAT_API actions: provider has `"supports_request": True` and implements `doRequestImpl()` | N/A | No app/action, field declaration, factory, JNI or provider API registration changes. Effective wildcard metadata selects existing validating provider types; native/ordinary, soft/mandatory, Serializable, samples, SoapDataProvider and compiled consumers are tested. |
| 29 | DPAT_FIND actions: every option exists in `SearchOptions`, `getRecordTypeImpl()` returns `*hash<string, AbstractDataField>` | N/A | No app/action, field declaration, factory, JNI or provider API registration changes. Effective wildcard metadata selects existing validating provider types; native/ordinary, soft/mandatory, Serializable, samples, SoapDataProvider and compiled consumers are tested. |
| 30 | Scheme-based apps (with `"scheme"` in registerApp): actions use `"path"` and do NOT use `"cls"` — having both `scheme` and `cls` causes a runtime error | N/A | No app/action, field declaration, factory, JNI or provider API registration changes. Effective wildcard metadata selects existing validating provider types; native/ordinary, soft/mandatory, Serializable, samples, SoapDataProvider and compiled consumers are tested. |
| 31 | Single-key hash slices use trailing comma: `Fields{"key",}` (without trailing comma, `Fields{"key"}` returns the value, not a hash) | N/A | No app/action, field declaration, factory, JNI or provider API registration changes. Effective wildcard metadata selects existing validating provider types; native/ordinary, soft/mandatory, Serializable, samples, SoapDataProvider and compiled consumers are tested. |
| 32 | **Typed data type classes exist** for request and response types — inherit `HashDataType`, have `const Fields` hash, call `addQoreFields(Fields)` in constructor, export public constant at bottom (e.g., `public const MyDataType = new MyDataType();`) | N/A | No app/action, field declaration, factory, JNI or provider API registration changes. Effective wildcard metadata selects existing validating provider types; native/ordinary, soft/mandatory, Serializable, samples, SoapDataProvider and compiled consumers are tested. |
| 33 | Request/input types use `public` Fields (enables `ClassName::Fields` in action registration) | N/A | No app/action, field declaration, factory, JNI or provider API registration changes. Effective wildcard metadata selects existing validating provider types; native/ordinary, soft/mandatory, Serializable, samples, SoapDataProvider and compiled consumers are tested. |
| 34 | Response/output types use `private` Fields | N/A | No app/action, field declaration, factory, JNI or provider API registration changes. Effective wildcard metadata selects existing validating provider types; native/ordinary, soft/mandatory, Serializable, samples, SoapDataProvider and compiled consumers are tested. |
| 35 | Each field in data types has `display_name`, `type`, and `desc` (markdown-formatted) | N/A | No app/action, field declaration, factory, JNI or provider API registration changes. Effective wildcard metadata selects existing validating provider types; native/ordinary, soft/mandatory, Serializable, samples, SoapDataProvider and compiled consumers are tested. |
| 36 | Input fields have `example_value` where useful (string fields, endpoint URIs, SQL queries, etc.) | N/A | No app/action, field declaration, factory, JNI or provider API registration changes. Effective wildcard metadata selects existing validating provider types; native/ordinary, soft/mandatory, Serializable, samples, SoapDataProvider and compiled consumers are tested. |
| 37 | Fields with finite allowed values use `allowed_values` with `AllowedValueInfo` containing both `value` and `display_name` (Title Case, human-readable) — never bare values, never described only in text | N/A | No app/action, field declaration, factory, JNI or provider API registration changes. Effective wildcard metadata selects existing validating provider types; native/ordinary, soft/mandatory, Serializable, samples, SoapDataProvider and compiled consumers are tested. |
| 38 | Password/secret fields have `"sensitive": True` | N/A | No app/action, field declaration, factory, JNI or provider API registration changes. Effective wildcard metadata selects existing validating provider types; native/ordinary, soft/mandatory, Serializable, samples, SoapDataProvider and compiled consumers are tested. |
| 39 | `groups` uses `AppGroup` enum values from `qlib/DataProvider/AppGroup.qc` | N/A | No app/action, field declaration, factory, JNI or provider API registration changes. Effective wildcard metadata selects existing validating provider types; native/ordinary, soft/mandatory, Serializable, samples, SoapDataProvider and compiled consumers are tested. |
| 40 | App `logo` stored as separate file, loaded at module level in `Priv` namespace | N/A | No app/action, field declaration, factory, JNI or provider API registration changes. Effective wildcard metadata selects existing validating provider types; native/ordinary, soft/mandatory, Serializable, samples, SoapDataProvider and compiled consumers are tested. |
| 41 | App `desc` uses markdown: bullet list of capabilities, links to project website, business-language explanation of value | N/A | No app/action, field declaration, factory, JNI or provider API registration changes. Effective wildcard metadata selects existing validating provider types; native/ordinary, soft/mandatory, Serializable, samples, SoapDataProvider and compiled consumers are tested. |
| 42 | `display_name` is user-friendly ("Apache Avro" not "avro") | N/A | No app/action, field declaration, factory, JNI or provider API registration changes. Effective wildcard metadata selects existing validating provider types; native/ordinary, soft/mandatory, Serializable, samples, SoapDataProvider and compiled consumers are tested. |
| 43 | `short_desc` is plain text, under 80 chars, single sentence — no markdown | N/A | No app/action, field declaration, factory, JNI or provider API registration changes. Effective wildcard metadata selects existing validating provider types; native/ordinary, soft/mandatory, Serializable, samples, SoapDataProvider and compiled consumers are tested. |
| 44 | `desc` uses markdown: backticks for code/field refs (`` `field_name` ``, `` `True` ``, `` `pdf` ``), `\n\n` for paragraphs, `- ` bullet lists for enumerations, `**bold**` for caveats | N/A | No app/action, field declaration, factory, JNI or provider API registration changes. Effective wildcard metadata selects existing validating provider types; native/ordinary, soft/mandatory, Serializable, samples, SoapDataProvider and compiled consumers are tested. |
| 45 | Descriptions use plain business language relating to common challenges — not just technical "what" but "why" and "when to use" | N/A | No app/action, field declaration, factory, JNI or provider API registration changes. Effective wildcard metadata selects existing validating provider types; native/ordinary, soft/mandatory, Serializable, samples, SoapDataProvider and compiled consumers are tested. |
| 46 | No bare `True`/`False`/`NOTHING` — must be backtick-wrapped in `desc` | N/A | No app/action, field declaration, factory, JNI or provider API registration changes. Effective wildcard metadata selects existing validating provider types; native/ordinary, soft/mandatory, Serializable, samples, SoapDataProvider and compiled consumers are tested. |
| 47 | No bare field/option names in prose — must use backticks | N/A | No app/action, field declaration, factory, JNI or provider API registration changes. Effective wildcard metadata selects existing validating provider types; native/ordinary, soft/mandatory, Serializable, samples, SoapDataProvider and compiled consumers are tested. |
| 48 | Long descriptions (>500 chars) use bold section headers and bullet lists | N/A | No app/action, field declaration, factory, JNI or provider API registration changes. Effective wildcard metadata selects existing validating provider types; native/ordinary, soft/mandatory, Serializable, samples, SoapDataProvider and compiled consumers are tested. |
| 49 | **Factory registration in Qore repo**: every factory name registered in `qlib/DataProvider/DataProvider.qc` → `FactoryMap` (without this, module loads but doesn't appear in Qorus apps) | N/A | No app/action, field declaration, factory, JNI or provider API registration changes. Effective wildcard metadata selects existing validating provider types; native/ordinary, soft/mandatory, Serializable, samples, SoapDataProvider and compiled consumers are tested. |
| 50 | **`getRecordTypeImpl()` signature**: must be `private *hash<string, AbstractDataField> getRecordTypeImpl(*hash<auto> search_options)` — NOT returning `*AbstractDataProviderType` | N/A | No app/action, field declaration, factory, JNI or provider API registration changes. Effective wildcard metadata selects existing validating provider types; native/ordinary, soft/mandatory, Serializable, samples, SoapDataProvider and compiled consumers are tested. |
| 51 | **Dependency JARs committed** (for JNI modules): JAR files in `qlib/*/jar/` may be gitignored — use `git add -f` to ensure they're tracked, otherwise CI compilation fails | N/A | No app/action, field declaration, factory, JNI or provider API registration changes. Effective wildcard metadata selects existing validating provider types; native/ordinary, soft/mandatory, Serializable, samples, SoapDataProvider and compiled consumers are tested. |
| 52 | JAR install rules in CMakeLists.txt for all dependency JARs | N/A | No app/action, field declaration, factory, JNI or provider API registration changes. Effective wildcard metadata selects existing validating provider types; native/ordinary, soft/mandatory, Serializable, samples, SoapDataProvider and compiled consumers are tested. |
| 53 | **No workarounds**: No TODOs, FIXMEs, stubs, or partially-implemented features | Pass | Builds the standard inherited anyType particle and computes metadata from the completed graph. Effective-content and derivation rules are fixed at construction; no fixture special case, weakened expectation or workaround. Remaining default/fixed/identity and P6-P9 requirements remain open. |
| 54 | **Exception safety**: C++ uses `ReferenceHolder` for Qore allocations, `std::unique_ptr` for C++ allocations, `*xsink` checked after every fallible operation | Pass | Schema-owned particles and local temporary traversal state use Qore managed references. Existing finalization on_error restoration and finalizing/on_success guards remain intact. Invalid derivations reject and subsequent construction succeeds. Valgrind on the final suite reports no lost blocks or errors. |
| 55 | **Thread safety**: All mutable shared state protected by `std::lock_guard<std::mutex>` or documented as immutable-after-construction | Pass | Builtin inheritance uses fresh schema-owned particles, with no mutable process-global state. Final wildcard metadata is computed after particle resolution and validation; traversal only reads group/base graphs. Existing concurrent mixed HTTP coverage remains passing. |
| 56 | **Type safety**: Strongly-typed `code<return(args)>` instead of untyped `code`; `static_cast` instead of C casts; typed hashdecls for results; enums where appropriate | Pass | New methods have typed arguments/returns; particle traversal uses list<XsdParticle>, hash<string,bool> and XsdParticleKind. HTTP callback is code<auto(hash<auto>, auto)>; resolved groups are asserted. Auto is confined to the established polymorphic XML/native value API. |
| 57 | **Performance**: No O(n²) where O(n) is possible; no unnecessary copies; coordinate descent uses incremental residuals not full matrix multiply | Pass | Iterative wildcard detection prunes zero-occurrence branches and visits shared nodes once, O(nodes + edges); no expansion of occurrence counts, recursion or added quadratic pass. |
| 58 | **Error handling**: All inputs validated (dimensions, empty data, unfitted models); C++ I/O handles EAGAIN/EINTR if applicable | Pass | Construction rejects mixed/element-only mismatch, ambiguous anyType extension and invalid maxOccurs zero with default minOccurs one. Lax wildcards validate declared children/attributes, including nested known children. Direct malformed HTTP requests assert SOAP-DESERIALIZATION-ERROR and version-specific status, followed by successful reuse. |
| 59 | **Documentation**: Doxygen `@param`, `@return`, `@throw` on all public methods; `@par Example` with realistic business scenarios; `@note` for important caveats | Pass | Public hasElementWildcard() documents effective inherited/resolved scope, zero-occurrence exclusion and continuing namespace/process rules. Durable design covers native value shapes, bounded samples and shipment example. WSDL qmod/Doxygen and executed example pass without warnings. |
| 60 | **QPP flags**: `[flags=CONSTANT]` on methods that never throw; `[flags=RET_VALUE_ONLY]` on methods that throw but have no side effects | N/A | No QPP code or flags change. |
| 61 | **Security**: No user-controlled format strings; no buffer overflows; bounds checking on array indices; no credentials in code | Pass | No credentials, raw memory operations or user-controlled format strings. Independent fixtures are bounded/offline; HTTP tests use loopback. Names, lexical child values, namespace bindings, text/order and comments are compared, including invalid declared children. |
| 62 | **Correctness**: Algorithms verified against reference implementations; edge cases tested (empty data, single sample, all-zero features) | Pass | The full gate identified an invalid existing receiver-test schema; its exact source remains a negative assertion and a Xerces-valid unrelated base preserves the prior receiver checks. Targeted AST/IR/JIT/AOT reruns pass on unchanged production source. Final regression gate, AST/IR/JIT, AOT consumers and independent matrix evidence are recorded in P5-16f-validation.json. The new matrix validates 17 schemas/119 documents/476 real SOAP directions/1020 outputs. Complete keyed corpus comparison closes only four intended directions, with no missing rows, new failures, fixture or catalog changes. |

Result: 19 Pass, 43 N/A, 0 Fail.
