# P5-16a native character-content audit

Copyright (C) 2026 Qore Technologies, s.r.o.

2026-09-12, parent `716e5b5`. Applied the complete audit-changes skill and all
five referenced module, sandboxing, cancellation and provider guides to the
native correction, configure/build integration, tests and documentation.
All 62 checks are recorded below. No main-Qore or production WSDL change is
included. This native prerequisite is an independently tested part of P5-16;
WSDL declaration-level nil/default/fixed work follows it. Complete P5/P6-P9 and
the previously recorded P9 QName AOT stack finding remain open.

The native unit, ownership and complete configure-probe Valgrind runs have zero
errors/lost blocks; the two C runs free every allocation. The full source gate,
independent matrix, provider-selection suite, supplements and documentation pass.
[Evidence](../native-character-content-evidence.md) and
[inventory](../P5-16a-validation.json) retain commands, exact hashes and outcomes.

| # | Check | Status | Evidence |
| --- | --- | --- | --- |
| 1 | Entry exists in `doxygen/lang/120_modules.dox.tmpl` (for modules in the Qore repo; N/A for external module repos) | N/A | No Qore module entry point, layout or production Qore source changed. Native dependency inputs are registered in CMake and distributed by Makefile.am; the source-distribution test passes. |
| 2 | Entry exists in `doxygen/lang/900_release_notes.dox.tmpl` (for modules in the Qore repo; external modules have release notes in their .qm) | Pass | Native xml 2.3.0 release notes, README and implemented native-character-content design describe nil/empty/whitespace and default/fixed event behavior. |
| 3 | `qore_user_module()` or `qore_external_user_module()` call in `CMakeLists.txt` | N/A | No Qore module entry point, layout or production Qore source changed. Native dependency inputs are registered in CMake and distributed by Makefile.am; the source-distribution test passes. |
| 4 | Module added to QMOD list in `CMakeLists.txt` | N/A | No Qore module entry point, layout or production Qore source changed. Native dependency inputs are registered in CMake and distributed by Makefile.am; the source-distribution test passes. |
| 5 | `.qm` file has `@section <lowercasemodname>intro` as first doc section — **must be all lowercase** (e.g., `avrodataproviderintro`, not `AvroDataProviderintro`) | N/A | No Qore module entry point, layout or production Qore source changed. Native dependency inputs are registered in CMake and distributed by Makefile.am; the source-distribution test passes. |
| 6 | `%modern` in `.qm` file — no redundant `%new-style`, `%require-types`, `%strict-args`, `%enable-all-warnings` | N/A | No Qore module entry point, layout or production Qore source changed. Native dependency inputs are registered in CMake and distributed by Makefile.am; the source-distribution test passes. |
| 7 | No parse directives (`%requires`, `%modern`, `%new-style`) in separated `.qc` files (check OUTSIDE of `@code` blocks only) | N/A | No Qore module entry point, layout or production Qore source changed. Native dependency inputs are registered in CMake and distributed by Makefile.am; the source-distribution test passes. |
| 8 | No `%include` usage (deprecated for modules) | Pass | No Qore %include introduced; the native ownership test includes the exact selected C source to verify its private event boundary. |
| 9 | Copyright 2026 on all new files | Pass | All new authored C/CMake, Qore/Python tests, design, evidence and audit files have 2026 copyright notices; original third-party notices are unchanged. |
| 10 | Directory layout: `.qm` inside `qlib/<ModuleName>/` directory (not at `qlib/<ModuleName>.qm` for multi-file modules) | N/A | No Qore module entry point, layout or production Qore source changed. Native dependency inputs are registered in CMake and distributed by Makefile.am; the source-distribution test passes. |
| 11 | No second `.qm` for the same module at `qlib/<ModuleName>.qm` | N/A | No Qore module entry point, layout or production Qore source changed. Native dependency inputs are registered in CMake and distributed by Makefile.am; the source-distribution test passes. |
| 12 | `ns=Qore::XX` matches the QoreNamespace constructor path | N/A | No new QPP class, namespace, public native method or method flags. |
| 13 | `%modern` directive present | Pass | New qtest and worker are executable, modern and prepend local module paths. Same-repository xml and core QUnit are required; external json uses %try-module with a clear missing-dependency error. |
| 14 | Executable permission set (`chmod +x`) | Pass | New qtest and worker are executable, modern and prepend local module paths. Same-repository xml and core QUnit are required; external json uses %try-module with a clear missing-dependency error. |
| 15 | Uses %prepend-module-path  before %requires for in-repo modules (Qore and Qore modules only; not Qorus) | Pass | New qtest and worker are executable, modern and prepend local module paths. Same-repository xml and core QUnit are required; external json uses %try-module with a clear missing-dependency error. |
| 16 | External module dependencies use `%try-module` — except modules delivered with the project itself (Qore ex: DataProvider, ConnectionProvider, QUnit, etc.) which use hard `%requires` | Pass | New qtest and worker are executable, modern and prepend local module paths. Same-repository xml and core QUnit are required; external json uses %try-module with a clear missing-dependency error. |
| 17 | No filesystem operations (fopen, open, creat, unlink, remove, rename, mkdir, rmdir, stat, chmod) without sandbox checks | N/A | The native correction performs no filesystem/network operation and adds no resource access requiring a sandbox check. |
| 18 | No network operations (connect, bind, socket, getaddrinfo, gethostbyname) without sandbox checks | N/A | The native correction performs no filesystem/network operation and adds no resource access requiring a sandbox check. |
| 19 | If filesystem/network ops exist, verify `QoreSandboxManagerHelper` usage | N/A | The native correction performs no filesystem/network operation and adds no resource access requiring a sandbox check. |
| 20 | No `File::`, `Dir::`, `Socket::`, `HTTPClient::` usage without justification | Pass | No production Qore change. The offline worker reads only its explicit manifest; existing public validation calls retain core/module I/O protections. |
| 21 | All `for`/`while` loops that could iterate >100 times have `qore_check_cancel()` checks | N/A | No production loop or cancellation implementation added. The native probe is a finite 24-case configure test and the ownership test has three fixed modes; runtime text-event checks are constant time. |
| 22 | Uses `qore_check_cancel()` (NOT deprecated `qore_check_io_interrupt()`) | N/A | No production loop or cancellation implementation added. The native probe is a finite 24-case configure test and the ownership test has three fixed modes; runtime text-event checks are constant time. |
| 23 | Check frequency: every 100 iterations for tight loops, every 10 for expensive iterations | N/A | No production loop or cancellation implementation added. The native probe is a finite 24-case configure test and the ownership test has three fixed modes; runtime text-event checks are constant time. |
| 24 | No blocking operations without cancellation support | Pass | No blocking operation introduced. Existing reader streaming cancellation, concurrent validation and subsequent reuse run in the full affected gate. |
| 25 | Every action has `display_name`, `short_desc` (plain text, <80 chars), `desc` (markdown) | N/A | No provider app/action/datatype/field/factory or JNI dependency changes. Existing generic provider and SOAP consumers are checked against the rebuilt native artifact. |
| 26 | Every action has `options` populated via `getActionOptionFromFields()` — without this, the action shows an empty, unusable form | N/A | No provider app/action/datatype/field/factory or JNI dependency changes. Existing generic provider and SOAP consumers are checked against the rebuilt native artifact. |
| 27 | Every action has `output_type` set to a typed data type constant (e.g., `MyResponseDataType`) — not omitted | N/A | No provider app/action/datatype/field/factory or JNI dependency changes. Existing generic provider and SOAP consumers are checked against the rebuilt native artifact. |
| 28 | DPAT_API actions: provider has `"supports_request": True` and implements `doRequestImpl()` | N/A | No provider app/action/datatype/field/factory or JNI dependency changes. Existing generic provider and SOAP consumers are checked against the rebuilt native artifact. |
| 29 | DPAT_FIND actions: every option exists in `SearchOptions`, `getRecordTypeImpl()` returns `*hash<string, AbstractDataField>` | N/A | No provider app/action/datatype/field/factory or JNI dependency changes. Existing generic provider and SOAP consumers are checked against the rebuilt native artifact. |
| 30 | Scheme-based apps (with `"scheme"` in registerApp): actions use `"path"` and do NOT use `"cls"` — having both `scheme` and `cls` causes a runtime error | N/A | No provider app/action/datatype/field/factory or JNI dependency changes. Existing generic provider and SOAP consumers are checked against the rebuilt native artifact. |
| 31 | Single-key hash slices use trailing comma: `Fields{"key",}` (without trailing comma, `Fields{"key"}` returns the value, not a hash) | N/A | No provider app/action/datatype/field/factory or JNI dependency changes. Existing generic provider and SOAP consumers are checked against the rebuilt native artifact. |
| 32 | **Typed data type classes exist** for request and response types — inherit `HashDataType`, have `const Fields` hash, call `addQoreFields(Fields)` in constructor, export public constant at bottom (e.g., `public const MyDataType = new MyDataType();`) | N/A | No provider app/action/datatype/field/factory or JNI dependency changes. Existing generic provider and SOAP consumers are checked against the rebuilt native artifact. |
| 33 | Request/input types use `public` Fields (enables `ClassName::Fields` in action registration) | N/A | No provider app/action/datatype/field/factory or JNI dependency changes. Existing generic provider and SOAP consumers are checked against the rebuilt native artifact. |
| 34 | Response/output types use `private` Fields | N/A | No provider app/action/datatype/field/factory or JNI dependency changes. Existing generic provider and SOAP consumers are checked against the rebuilt native artifact. |
| 35 | Each field in data types has `display_name`, `type`, and `desc` (markdown-formatted) | N/A | No provider app/action/datatype/field/factory or JNI dependency changes. Existing generic provider and SOAP consumers are checked against the rebuilt native artifact. |
| 36 | Input fields have `example_value` where useful (string fields, endpoint URIs, SQL queries, etc.) | N/A | No provider app/action/datatype/field/factory or JNI dependency changes. Existing generic provider and SOAP consumers are checked against the rebuilt native artifact. |
| 37 | Fields with finite allowed values use `allowed_values` with `AllowedValueInfo` containing both `value` and `display_name` (Title Case, human-readable) — never bare values, never described only in text | N/A | No provider app/action/datatype/field/factory or JNI dependency changes. Existing generic provider and SOAP consumers are checked against the rebuilt native artifact. |
| 38 | Password/secret fields have `"sensitive": True` | N/A | No provider app/action/datatype/field/factory or JNI dependency changes. Existing generic provider and SOAP consumers are checked against the rebuilt native artifact. |
| 39 | `groups` uses `AppGroup` enum values from `qlib/DataProvider/AppGroup.qc` | N/A | No provider app/action/datatype/field/factory or JNI dependency changes. Existing generic provider and SOAP consumers are checked against the rebuilt native artifact. |
| 40 | App `logo` stored as separate file, loaded at module level in `Priv` namespace | N/A | No provider app/action/datatype/field/factory or JNI dependency changes. Existing generic provider and SOAP consumers are checked against the rebuilt native artifact. |
| 41 | App `desc` uses markdown: bullet list of capabilities, links to project website, business-language explanation of value | N/A | No provider app/action/datatype/field/factory or JNI dependency changes. Existing generic provider and SOAP consumers are checked against the rebuilt native artifact. |
| 42 | `display_name` is user-friendly ("Apache Avro" not "avro") | N/A | No provider app/action/datatype/field/factory or JNI dependency changes. Existing generic provider and SOAP consumers are checked against the rebuilt native artifact. |
| 43 | `short_desc` is plain text, under 80 chars, single sentence — no markdown | N/A | No provider app/action/datatype/field/factory or JNI dependency changes. Existing generic provider and SOAP consumers are checked against the rebuilt native artifact. |
| 44 | `desc` uses markdown: backticks for code/field refs (`` `field_name` ``, `` `True` ``, `` `pdf` ``), `\n\n` for paragraphs, `- ` bullet lists for enumerations, `**bold**` for caveats | N/A | No provider app/action/datatype/field/factory or JNI dependency changes. Existing generic provider and SOAP consumers are checked against the rebuilt native artifact. |
| 45 | Descriptions use plain business language relating to common challenges — not just technical "what" but "why" and "when to use" | N/A | No provider app/action/datatype/field/factory or JNI dependency changes. Existing generic provider and SOAP consumers are checked against the rebuilt native artifact. |
| 46 | No bare `True`/`False`/`NOTHING` — must be backtick-wrapped in `desc` | N/A | No provider app/action/datatype/field/factory or JNI dependency changes. Existing generic provider and SOAP consumers are checked against the rebuilt native artifact. |
| 47 | No bare field/option names in prose — must use backticks | N/A | No provider app/action/datatype/field/factory or JNI dependency changes. Existing generic provider and SOAP consumers are checked against the rebuilt native artifact. |
| 48 | Long descriptions (>500 chars) use bold section headers and bullet lists | N/A | No provider app/action/datatype/field/factory or JNI dependency changes. Existing generic provider and SOAP consumers are checked against the rebuilt native artifact. |
| 49 | **Factory registration in Qore repo**: every factory name registered in `qlib/DataProvider/DataProvider.qc` → `FactoryMap` (without this, module loads but doesn't appear in Qorus apps) | N/A | No provider app/action/datatype/field/factory or JNI dependency changes. Existing generic provider and SOAP consumers are checked against the rebuilt native artifact. |
| 50 | **`getRecordTypeImpl()` signature**: must be `private *hash<string, AbstractDataField> getRecordTypeImpl(*hash<auto> search_options)` — NOT returning `*AbstractDataProviderType` | N/A | No provider app/action/datatype/field/factory or JNI dependency changes. Existing generic provider and SOAP consumers are checked against the rebuilt native artifact. |
| 51 | **Dependency JARs committed** (for JNI modules): JAR files in `qlib/*/jar/` may be gitignored — use `git add -f` to ensure they're tracked, otherwise CI compilation fails | N/A | No provider app/action/datatype/field/factory or JNI dependency changes. Existing generic provider and SOAP consumers are checked against the rebuilt native artifact. |
| 52 | JAR install rules in CMakeLists.txt for all dependency JARs | N/A | No provider app/action/datatype/field/factory or JNI dependency changes. Existing generic provider and SOAP consumers are checked against the rebuilt native artifact. |
| 53 | **No workarounds**: No TODOs, FIXMEs, stubs, or partially-implemented features | Pass | Fixes the shared character-event root: zero-length events are ignored before nil/type/ownership checks, and CDATA whitespace uses the same character test as text. No XML rewriting, suppression, stack/depth workaround or relaxed content/attribute constraint. |
| 54 | **Exception safety**: C++ uses `ReferenceHolder` for Qore allocations, `std::unique_ptr` for C++ allocations, `*xsink` checked after every fallible operation | Pass | No production allocation or new ownership transfer. Empty CREATED input remains caller-owned; zero-length nonterminated buffers are not read. Native ownership/probe and new complete Qore suite pass Valgrind with zero errors/lost blocks; negative and reuse paths are covered. |
| 55 | **Thread safety**: All mutable shared state protected by `std::lock_guard<std::mutex>` or documented as immutable-after-construction | Pass | Changes only the active per-validation element context. No shared mutable state or cache added; existing concurrent native validation/consumer tests retain isolated contexts. |
| 56 | **Type safety**: Strongly-typed `code<return(args)>` instead of untyped `code`; `static_cast` instead of C casts; typed hashdecls for results; enums where appropriate | Pass | Native parameters use xmlChar pointers, explicit lengths, libxml2 ownership modes and schema structures. Removed the unused event-kind parameter consistently from all callers. Typed Qore helpers and structured Python fixtures preserve error categories. |
| 57 | **Performance**: No O(n²) where O(n) is possible; no unnecessary copies; coordinate descent uses incremental residuals not full matrix multiply | Pass | One constant-time empty check precedes existing content validation; empty events allocate nothing. No additional traversal or recursion. Nonempty whitespace uses the existing bounded string check. |
| 58 | **Error handling**: All inputs validated (dimensions, empty data, unfitted models); C++ I/O handles EAGAIN/EINTR if applicable | Pass | Positive and negative cases cover nil true/false/1/0/invalid lexical values, actual text/children, required attributes/particles, fixed/default values, empty fragments, XML/non-XML whitespace and context recovery. Configure checks exercise defective/fixed/absent libraries and source hash integrity. |
| 59 | **Documentation**: Doxygen `@param`, `@return`, `@throw` on all public methods; `@par Example` with realistic business scenarios; `@note` for important caveats | Pass | No new public API. Native release notes, implemented design, executed nilled-shipment example and requirement evidence explain behavior and ownership. qpp/Doxygen/qdx complete without warnings. |
| 60 | **QPP flags**: `[flags=CONSTANT]` on methods that never throw; `[flags=RET_VALUE_ONLY]` on methods that throw but have no side effects | N/A | No new QPP class, namespace, public native method or method flags. |
| 61 | **Security**: No user-controlled format strings; no buffer overflows; bounds checking on array indices; no credentials in code | Pass | No credentials or user-controlled format strings. Probe snprintf output lengths are checked, input/output source digests are pinned, and zero-length buffers are never read or consumed. |
| 62 | **Correctness**: Algorithms verified against reference implementations; edge cases tested (empty data, single sample, all-zero features) | Pass | The 16-schema/812-document independent matrix agrees with pinned Xerces and preserves valid XML data. The new suite has 545 assertions; all 45 provider-selection tests, 136 source suites, 11 supplements and three Valgrind runs pass. Both-version corpus outcomes are exactly unchanged. WSDL declaration nil/default/fixed and the independent P9 AOT finding remain open. |

Result: 18 Pass, 44 N/A, 0 Fail. The native increment may be committed.
