# P6-43 HTTP MIME entity tree audit

Copyright (C) 2026 Qore Technologies, s.r.o.

Applied `/home/david/.codex/skills/audit-changes/SKILL.md` to the candidate based on `7ffcabc`, now transferred without code changes to the main `develop` checkout. Reviewed Qore module-structure, DataProvider checklist and development-guide requirements. No C++ or main-Qore changes. Reapplied all 62 checks on the main develop checkout after verifying installed Qore e8768a8a0. The core conversion reproducer and both source/compiled XML literal gates now pass. The final affected and corpus gates pass; this increment is ready to commit. XML development remains incomplete, so no push is made.

Review fixes address ordinary HTTP bindings with no MIME selector, mixed direct/multipart provider dispatch, pre-transport nested choices, explicit identity precedence, application media parameters and indexed candidate matching. The final validation record includes the now-passing prerequisite. Doxygen Qore examples use the Python highlighting tag required by the module guide; documentation and compiled-module checks pass on the final source.

| # | Check | Status | Evidence |
| --- | --- | --- | --- |
| 1 | Entry exists in `doxygen/lang/120_modules.dox.tmpl` (for modules in the Qore repo; N/A for external module repos) | N/A | Existing external module registrations and layouts are unchanged; no new module or installation entry. Module and repository release notes are updated. |
| 2 | Entry exists in `doxygen/lang/900_release_notes.dox.tmpl` (for modules in the Qore repo; external modules have release notes in their .qm) | N/A | Existing external module registrations and layouts are unchanged; no new module or installation entry. Module and repository release notes are updated. |
| 3 | `qore_user_module()` or `qore_external_user_module()` call in `CMakeLists.txt` | N/A | Existing external module registrations and layouts are unchanged; no new module or installation entry. Module and repository release notes are updated. |
| 4 | Module added to QMOD list in `CMakeLists.txt` | N/A | Existing external module registrations and layouts are unchanged; no new module or installation entry. Module and repository release notes are updated. |
| 5 | `.qm` file has `@section <lowercasemodname>intro` as first doc section — **must be all lowercase** (e.g., `avrodataproviderintro`, not `AvroDataProviderintro`) | Pass | Existing first module introduction identifiers remain lowercase. |
| 6 | `%modern` in `.qm` file — no redundant `%new-style`, `%require-types`, `%strict-args`, `%enable-all-warnings` | Pass | All changed .qm entry points retain %modern without redundant parse directives. |
| 7 | No parse directives (`%requires`, `%modern`, `%new-style`) in separated `.qc` files (check OUTSIDE of `@code` blocks only) | Pass | SoapRequestDataProvider.qc has no parse directives. |
| 8 | No `%include` usage (deprecated for modules) | Pass | No %include introduced. |
| 9 | Copyright 2026 on all new files | Pass | Every new Qore, Python, WSDL and Markdown file carries copyright 2026. |
| 10 | Directory layout: `.qm` inside `qlib/<ModuleName>/` directory (not at `qlib/<ModuleName>.qm` for multi-file modules) | N/A | Existing external module registrations and layouts are unchanged; no new module or installation entry. Module and repository release notes are updated. |
| 11 | No second `.qm` for the same module at `qlib/<ModuleName>.qm` | N/A | Existing external module registrations and layouts are unchanged; no new module or installation entry. Module and repository release notes are updated. |
| 12 | `ns=Qore::XX` matches the QoreNamespace constructor path | N/A | No C++ or QPP changes. Native memory ownership, sandbox guards, cancellation checks and QPP flags are unchanged. |
| 13 | `%modern` directive present | Pass | Both new qtests and the Qore peer use %modern. |
| 14 | Executable permission set (`chmod +x`) | Pass | New qtests, Qore peer and Python runner are executable. |
| 15 | Uses %prepend-module-path  before %requires for in-repo modules (Qore and Qore modules only; not Qorus) | Pass | Tests prepend local qlib before relative module requirements. Debug checks use the local native xml build and local qlib; transfer verification reruns these checks in the main checkout. |
| 16 | External module dependencies use `%try-module` — except modules delivered with the project itself (Qore ex: DataProvider, ConnectionProvider, QUnit, etc.) which use hard `%requires` | Pass | The external json dependency uses %try-module. Local WSDL/client/handler and core QUnit/HttpServer use hard requirements. |
| 17 | No filesystem operations (fopen, open, creat, unlink, remove, rename, mkdir, rmdir, stat, chmod) without sandbox checks | N/A | No C++ or QPP changes. Native memory ownership, sandbox guards, cancellation checks and QPP flags are unchanged. |
| 18 | No network operations (connect, bind, socket, getaddrinfo, gethostbyname) without sandbox checks | N/A | No C++ or QPP changes. Native memory ownership, sandbox guards, cancellation checks and QPP flags are unchanged. |
| 19 | If filesystem/network ops exist, verify `QoreSandboxManagerHelper` usage | N/A | No C++ or QPP changes. Native memory ownership, sandbox guards, cancellation checks and QPP flags are unchanged. |
| 20 | No `File::`, `Dir::`, `Socket::`, `HTTPClient::` usage without justification | Pass | No new production I/O primitive. Fixture files and loopback peers are bounded test resources; readiness, queue and STOP events control completion. |
| 21 | All `for`/`while` loops that could iterate >100 times have `qore_check_cancel()` checks | N/A | No C++ or QPP changes. Native memory ownership, sandbox guards, cancellation checks and QPP flags are unchanged. |
| 22 | Uses `qore_check_cancel()` (NOT deprecated `qore_check_io_interrupt()`) | N/A | No C++ or QPP changes. Native memory ownership, sandbox guards, cancellation checks and QPP flags are unchanged. |
| 23 | Check frequency: every 100 iterations for tight loops, every 10 for expensive iterations | N/A | No C++ or QPP changes. Native memory ownership, sandbox guards, cancellation checks and QPP flags are unchanged. |
| 24 | No blocking operations without cancellation support | N/A | No C++ or QPP changes. Native memory ownership, sandbox guards, cancellation checks and QPP flags are unchanged. |
| 25 | Every action has `display_name`, `short_desc` (plain text, <80 chars), `desc` (markdown) | N/A | Existing dynamic SOAP app/action/factory registrations are unchanged; no new app, action, scheme or asset. MIME choices are per-operation request options. |
| 26 | Every action has `options` populated via `getActionOptionFromFields()` — without this, the action shows an empty, unusable form | N/A | Existing dynamic SOAP app/action/factory registrations are unchanged; no new app, action, scheme or asset. MIME choices are per-operation request options. |
| 27 | Every action has `output_type` set to a typed data type constant (e.g., `MyResponseDataType`) — not omitted | N/A | Existing dynamic SOAP app/action/factory registrations are unchanged; no new app, action, scheme or asset. MIME choices are per-operation request options. |
| 28 | DPAT_API actions: provider has `"supports_request": True` and implements `doRequestImpl()` | N/A | Existing dynamic SOAP app/action/factory registrations are unchanged; no new app, action, scheme or asset. MIME choices are per-operation request options. |
| 29 | DPAT_FIND actions: every option exists in `SearchOptions`, `getRecordTypeImpl()` returns `*hash<string, AbstractDataField>` | N/A | Existing dynamic SOAP app/action/factory registrations are unchanged; no new app, action, scheme or asset. MIME choices are per-operation request options. |
| 30 | Scheme-based apps (with `"scheme"` in registerApp): actions use `"path"` and do NOT use `"cls"` — having both `scheme` and `cls` causes a runtime error | N/A | Existing dynamic SOAP app/action/factory registrations are unchanged; no new app, action, scheme or asset. MIME choices are per-operation request options. |
| 31 | Single-key hash slices use trailing comma: `Fields{"key",}` (without trailing comma, `Fields{"key"}` returns the value, not a hash) | N/A | Existing dynamic SOAP app/action/factory registrations are unchanged; no new app, action, scheme or asset. MIME choices are per-operation request options. |
| 32 | **Typed data type classes exist** for request and response types — inherit `HashDataType`, have `const Fields` hash, call `addQoreFields(Fields)` in constructor, export public constant at bottom (e.g., `public const MyDataType = new MyDataType();`) | Pass | WsdlHttpMimeDataType inherits HashDataType, adds typed fields and describes both message directions from runtime binding metadata; static Fields/singleton data would discard operation-specific choices. |
| 33 | Request/input types use `public` Fields (enables `ClassName::Fields` in action registration) | N/A | The provider describes runtime WSDL declarations, not a static action Fields constant. |
| 34 | Response/output types use `private` Fields | N/A | The provider describes runtime WSDL declarations, not a static action Fields constant. |
| 35 | Each field in data types has `display_name`, `type`, and `desc` (markdown-formatted) | Pass | The MIME tree field and new request/response choices supply display_name, type and Markdown desc. Existing abstract fields retain their schema metadata. |
| 36 | Input fields have `example_value` where useful (string fields, endpoint URIs, SQL queries, etc.) | Pass | The provider generates and validates complete trees, including nested and wildcard media; source/saved/soft/optional provider examples are exercised. |
| 37 | Fields with finite allowed values use `allowed_values` with `AllowedValueInfo` containing both `value` and `display_name` (Title Case, human-readable) — never bare values, never described only in text | N/A | The new values are strings or recursive trees with open Content-IDs and nested structure, not finite option enumerations. Per-declaration representation IDs are available through getMimeFormats(). |
| 38 | Password/secret fields have `"sensitive": True` | N/A | No password, token or secret fields introduced. |
| 39 | `groups` uses `AppGroup` enum values from `qlib/DataProvider/AppGroup.qc` | N/A | Existing dynamic SOAP app/action/factory registrations are unchanged; no new app, action, scheme or asset. MIME choices are per-operation request options. |
| 40 | App `logo` stored as separate file, loaded at module level in `Priv` namespace | N/A | Existing dynamic SOAP app/action/factory registrations are unchanged; no new app, action, scheme or asset. MIME choices are per-operation request options. |
| 41 | App `desc` uses markdown: bullet list of capabilities, links to project website, business-language explanation of value | N/A | Existing dynamic SOAP app/action/factory registrations are unchanged; no new app, action, scheme or asset. MIME choices are per-operation request options. |
| 42 | `display_name` is user-friendly ("Apache Avro" not "avro") | N/A | Existing dynamic SOAP app/action/factory registrations are unchanged; no new app, action, scheme or asset. MIME choices are per-operation request options. |
| 43 | `short_desc` is plain text, under 80 chars, single sentence — no markdown | Pass | Both new request-option short descriptions are plain sentences under 80 characters. |
| 44 | `desc` uses markdown: backticks for code/field refs (`` `field_name` ``, `` `True` ``, `` `pdf` ``), `\n\n` for paragraphs, `- ` bullet lists for enumerations, `**bold**` for caveats | Pass | Provider descriptions use Markdown property lists, backticks around field/option names, and structured examples; no description exceeds 500 characters. |
| 45 | Descriptions use plain business language relating to common challenges — not just technical "what" but "why" and "when to use" | Pass | Descriptions explain choosing representations and preserving binary message packages. |
| 46 | No bare `True`/`False`/`NOTHING` — must be backtick-wrapped in `desc` | Pass | New provider descriptions contain no bare True, False or NOTHING literals. |
| 47 | No bare field/option names in prose — must use backticks | Pass | All technical field and option references in new provider descriptions use backticks. |
| 48 | Long descriptions (>500 chars) use bold section headers and bullet lists | N/A | No new provider description exceeds 500 characters. |
| 49 | **Factory registration in Qore repo**: every factory name registered in `qlib/DataProvider/DataProvider.qc` → `FactoryMap` (without this, module loads but doesn't appear in Qorus apps) | N/A | Existing dynamic SOAP app/action/factory registrations are unchanged; no new app, action, scheme or asset. MIME choices are per-operation request options. |
| 50 | **`getRecordTypeImpl()` signature**: must be `private *hash<string, AbstractDataField> getRecordTypeImpl(*hash<auto> search_options)` — NOT returning `*AbstractDataProviderType` | Pass | Existing getRecordTypeImpl retains private *hash<string, AbstractDataField> getRecordTypeImpl(*hash<auto> search_options). |
| 51 | **Dependency JARs committed** (for JNI modules): JAR files in `qlib/*/jar/` may be gitignored — use `git add -f` to ensure they're tracked, otherwise CI compilation fails | N/A | No added native/JNI dependency or JAR; existing independent CXF dependencies remain pinned and unchanged. |
| 52 | JAR install rules in CMakeLists.txt for all dependency JARs | N/A | No added native/JNI dependency or JAR; existing independent CXF dependencies remain pinned and unchanged. |
| 53 | **No workarounds**: No TODOs, FIXMEs, stubs, or partially-implemented features | Pass | No framing or coercion workaround, fixture fallback, TODO or stub. Qore Mime owns framing; the core literal-conversion regression now passes on the installed Qore fix without module conversion code. |
| 54 | **Exception safety**: C++ uses `ReferenceHolder` for Qore allocations, `std::unique_ptr` for C++ allocations, `*xsink` checked after every fallible operation | Pass | MIME values, writer state and candidate graphs are local. Failed encoding/decoding cannot publish partial results. Provider validation preserves values and rethrows cancellation; recovery tests follow each malformed peer message. |
| 55 | **Thread safety**: All mutable shared state protected by `std::lock_guard<std::mutex>` or documented as immutable-after-construction | Pass | Published declaration graphs are immutable during use; call-local selection scope is restored on exit. Four concurrent workers perform 80 round trips with different formats/roots. |
| 56 | **Type safety**: Strongly-typed `code<return(args)>` instead of untyped `code`; `static_cast` instead of C casts; typed hashdecls for results; enums where appropriate | Pass | Public entities and selections use recursive hashdecls and a union alias. MessageInfo, declaration references and callback signatures are typed. Heterogeneous native payloads and graph work records intentionally use auto. |
| 57 | **Performance**: No O(n²) where O(n) is possible; no unnecessary copies; coordinate descent uses incremental residuals not full matrix multiply | Pass | Candidate indexing partitions selected identities/positions, WSDL part identities and media components. Assignment uses a degree-one queue over candidate edges; no format Cartesian product or repeated all-part scan when identities distinguish entities. |
| 58 | **Error handling**: All inputs validated (dimensions, empty data, unfitted models); C++ I/O handles EAGAIN/EINTR if applicable | Pass | Positive/negative tests cover graph cycles, empty/missing/repeated entities, root/index/selector errors, media overlaps and charsets, duplicate IDs, schema failure and pre-transport rejection. Review fixed mixed direct-format provider dispatch and nested selection validation. |
| 59 | **Documentation**: Doxygen `@param`, `@return`, `@throw` on all public methods; `@par Example` with realistic business scenarios; `@note` for important caveats | Pass | Public APIs document parameters, return values, errors and invoice/package examples. Module/repository release notes and durable wsdl-http-multipart.md describe the implementation; docs and astparser checks are recorded. No DGC behavior changed and no design/dgc.md exists. |
| 60 | **QPP flags**: `[flags=CONSTANT]` on methods that never throw; `[flags=RET_VALUE_ONLY]` on methods that throw but have no side effects | N/A | No C++ or QPP changes. Native memory ownership, sandbox guards, cancellation checks and QPP flags are unchanged. |
| 61 | **Security**: No user-controlled format strings; no buffer overflows; bounds checking on array indices; no credentials in code | Pass | Constant format strings; bounds-checked declaration and wire indexes; validated media/ID headers; no credentials. Independent endpoints bind loopback and use bounded deterministic completion. |
| 62 | **Correctness**: Algorithms verified against reference implementations; edge cases tested (empty data, single sample, all-zero features) | Pass | Independent MIME peers, 43 Qore suites, 16 Python gates and 16 corpus commands pass. Both literal cases now pass on installed Qore e8768a8a0; source and compiled tree/literal suites agree. Six corpus reports retain their semantic results. The separately assigned request-local URL prerequisite remains required before P6 completion. |

All 62 checks: **28 Pass / 34 N/A / zero Fail**. No C++ changes; Valgrind is not required. The durable multipart design describes the implemented behavior; there is no DGC design change.
