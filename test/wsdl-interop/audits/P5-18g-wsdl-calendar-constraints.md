# P5-18g WSDL calendar constraints audit

Copyright (C) 2026 Qore Technologies, s.r.o.

Applied every audit-changes checklist item to final changes based on `6bcb9f5`
on 2026-09-13. All five referenced guides were read and their hashes verified.
Scope: WSDL canonical capture, unit/HTTP and independent tests, docs and evidence.
No C++ or main-Qore mutation, installation or push. Unrelated test/cmake/__pycache__
is excluded. See [evidence](../wsdl-calendar-constraints-evidence.md) and
[inventory](../P5-18g-validation.json).

| # | Check | Status | Evidence |
| --- | --- | --- | --- |
| 1 | Entry exists in `doxygen/lang/120_modules.dox.tmpl` (for modules in the Qore repo; N/A for external module repos) | N/A | No new module, separated source, QPP class, registration or layout. Existing WSDL remains %modern and uses no new parse directive. |
| 2 | Entry exists in `doxygen/lang/900_release_notes.dox.tmpl` (for modules in the Qore repo; external modules have release notes in their .qm) | N/A | No new module, separated source, QPP class, registration or layout. Existing WSDL remains %modern and uses no new parse directive. |
| 3 | `qore_user_module()` or `qore_external_user_module()` call in `CMakeLists.txt` | N/A | No new module, separated source, QPP class, registration or layout. Existing WSDL remains %modern and uses no new parse directive. |
| 4 | Module added to QMOD list in `CMakeLists.txt` | N/A | No new module, separated source, QPP class, registration or layout. Existing WSDL remains %modern and uses no new parse directive. |
| 5 | `.qm` file has `@section <lowercasemodname>intro` as first doc section — **must be all lowercase** (e.g., `avrodataproviderintro`, not `AvroDataProviderintro`) | N/A | No new module, separated source, QPP class, registration or layout. Existing WSDL remains %modern and uses no new parse directive. |
| 6 | `%modern` in `.qm` file — no redundant `%new-style`, `%require-types`, `%strict-args`, `%enable-all-warnings` | N/A | No new module, separated source, QPP class, registration or layout. Existing WSDL remains %modern and uses no new parse directive. |
| 7 | No parse directives (`%requires`, `%modern`, `%new-style`) in separated `.qc` files (check OUTSIDE of `@code` blocks only) | N/A | No new module, separated source, QPP class, registration or layout. Existing WSDL remains %modern and uses no new parse directive. |
| 8 | No `%include` usage (deprecated for modules) | N/A | No new module, separated source, QPP class, registration or layout. Existing WSDL remains %modern and uses no new parse directive. |
| 9 | Copyright 2026 on all new files | Pass | Authored Qore/Python/docs carry 2026 copyright; fixture JSON is accompanied by its copyrighted generator and dated acceptance record. |
| 10 | Directory layout: `.qm` inside `qlib/<ModuleName>/` directory (not at `qlib/<ModuleName>.qm` for multi-file modules) | N/A | No new module, separated source, QPP class, registration or layout. Existing WSDL remains %modern and uses no new parse directive. |
| 11 | No second `.qm` for the same module at `qlib/<ModuleName>.qm` | N/A | No new module, separated source, QPP class, registration or layout. Existing WSDL remains %modern and uses no new parse directive. |
| 12 | `ns=Qore::XX` matches the QoreNamespace constructor path | N/A | No new module, separated source, QPP class, registration or layout. Existing WSDL remains %modern and uses no new parse directive. |
| 13 | `%modern` directive present | Pass | Both Qore suites use %modern. The unchanged shared .qr worker inherits modern parsing. |
| 14 | Executable permission set (`chmod +x`) | Pass | Both new qtests and both Python unittest entry points have executable permission. |
| 15 | Uses %prepend-module-path  before %requires for in-repo modules (Qore and Qore modules only; not Qorus) | Pass | Tests prepend local qlib before relative WSDL/SoapClient/SoapHandler/SoapDataProvider requirements. QORE_MODULE_DIR selects local Debug XML. |
| 16 | External module dependencies use `%try-module` — except modules delivered with the project itself (Qore ex: DataProvider, ConnectionProvider, QUnit, etc.) which use hard `%requires` | Pass | Same-project XML and Qore runtime dependencies are required. External json uses %try-module with a descriptive dependency error. |
| 17 | No filesystem operations (fopen, open, creat, unlink, remove, rename, mkdir, rmdir, stat, chmod) without sandbox checks | N/A | No C++/QPP change or new runtime I/O entry point; Qore runtime cancellation and scope cleanup apply. |
| 18 | No network operations (connect, bind, socket, getaddrinfo, gethostbyname) without sandbox checks | N/A | No C++/QPP change or new runtime I/O entry point; Qore runtime cancellation and scope cleanup apply. |
| 19 | If filesystem/network ops exist, verify `QoreSandboxManagerHelper` usage | N/A | No C++/QPP change or new runtime I/O entry point; Qore runtime cancellation and scope cleanup apply. |
| 20 | No `File::`, `Dir::`, `Socket::`, `HTTPClient::` usage without justification | Pass | Production adds no I/O. Tests read committed fixtures and use loopback HTTP with bounded calls, queue deadlines and on_exit server shutdown. |
| 21 | All `for`/`while` loops that could iterate >100 times have `qore_check_cancel()` checks | N/A | No C++/QPP change or new runtime I/O entry point; Qore runtime cancellation and scope cleanup apply. |
| 22 | Uses `qore_check_cancel()` (NOT deprecated `qore_check_io_interrupt()`) | N/A | No C++/QPP change or new runtime I/O entry point; Qore runtime cancellation and scope cleanup apply. |
| 23 | Check frequency: every 100 iterations for tight loops, every 10 for expensive iterations | N/A | No C++/QPP change or new runtime I/O entry point; Qore runtime cancellation and scope cleanup apply. |
| 24 | No blocking operations without cancellation support | N/A | No C++/QPP change or new runtime I/O entry point; Qore runtime cancellation and scope cleanup apply. |
| 25 | Every action has `display_name`, `short_desc` (plain text, <80 chars), `desc` (markdown) | N/A | No app/action, DTO, field, factory, provider implementation or JNI change. Existing saved providers are exercised by regression tests. |
| 26 | Every action has `options` populated via `getActionOptionFromFields()` — without this, the action shows an empty, unusable form | N/A | No app/action, DTO, field, factory, provider implementation or JNI change. Existing saved providers are exercised by regression tests. |
| 27 | Every action has `output_type` set to a typed data type constant (e.g., `MyResponseDataType`) — not omitted | N/A | No app/action, DTO, field, factory, provider implementation or JNI change. Existing saved providers are exercised by regression tests. |
| 28 | DPAT_API actions: provider has `"supports_request": True` and implements `doRequestImpl()` | N/A | No app/action, DTO, field, factory, provider implementation or JNI change. Existing saved providers are exercised by regression tests. |
| 29 | DPAT_FIND actions: every option exists in `SearchOptions`, `getRecordTypeImpl()` returns `*hash<string, AbstractDataField>` | N/A | No app/action, DTO, field, factory, provider implementation or JNI change. Existing saved providers are exercised by regression tests. |
| 30 | Scheme-based apps (with `"scheme"` in registerApp): actions use `"path"` and do NOT use `"cls"` — having both `scheme` and `cls` causes a runtime error | N/A | No app/action, DTO, field, factory, provider implementation or JNI change. Existing saved providers are exercised by regression tests. |
| 31 | Single-key hash slices use trailing comma: `Fields{"key",}` (without trailing comma, `Fields{"key"}` returns the value, not a hash) | N/A | No app/action, DTO, field, factory, provider implementation or JNI change. Existing saved providers are exercised by regression tests. |
| 32 | **Typed data type classes exist** for request and response types — inherit `HashDataType`, have `const Fields` hash, call `addQoreFields(Fields)` in constructor, export public constant at bottom (e.g., `public const MyDataType = new MyDataType();`) | N/A | No app/action, DTO, field, factory, provider implementation or JNI change. Existing saved providers are exercised by regression tests. |
| 33 | Request/input types use `public` Fields (enables `ClassName::Fields` in action registration) | N/A | No app/action, DTO, field, factory, provider implementation or JNI change. Existing saved providers are exercised by regression tests. |
| 34 | Response/output types use `private` Fields | N/A | No app/action, DTO, field, factory, provider implementation or JNI change. Existing saved providers are exercised by regression tests. |
| 35 | Each field in data types has `display_name`, `type`, and `desc` (markdown-formatted) | N/A | No app/action, DTO, field, factory, provider implementation or JNI change. Existing saved providers are exercised by regression tests. |
| 36 | Input fields have `example_value` where useful (string fields, endpoint URIs, SQL queries, etc.) | N/A | No app/action, DTO, field, factory, provider implementation or JNI change. Existing saved providers are exercised by regression tests. |
| 37 | Fields with finite allowed values use `allowed_values` with `AllowedValueInfo` containing both `value` and `display_name` (Title Case, human-readable) — never bare values, never described only in text | N/A | No app/action, DTO, field, factory, provider implementation or JNI change. Existing saved providers are exercised by regression tests. |
| 38 | Password/secret fields have `"sensitive": True` | N/A | No app/action, DTO, field, factory, provider implementation or JNI change. Existing saved providers are exercised by regression tests. |
| 39 | `groups` uses `AppGroup` enum values from `qlib/DataProvider/AppGroup.qc` | N/A | No app/action, DTO, field, factory, provider implementation or JNI change. Existing saved providers are exercised by regression tests. |
| 40 | App `logo` stored as separate file, loaded at module level in `Priv` namespace | N/A | No app/action, DTO, field, factory, provider implementation or JNI change. Existing saved providers are exercised by regression tests. |
| 41 | App `desc` uses markdown: bullet list of capabilities, links to project website, business-language explanation of value | N/A | No app/action, DTO, field, factory, provider implementation or JNI change. Existing saved providers are exercised by regression tests. |
| 42 | `display_name` is user-friendly ("Apache Avro" not "avro") | N/A | No app/action, DTO, field, factory, provider implementation or JNI change. Existing saved providers are exercised by regression tests. |
| 43 | `short_desc` is plain text, under 80 chars, single sentence — no markdown | N/A | No app/action, DTO, field, factory, provider implementation or JNI change. Existing saved providers are exercised by regression tests. |
| 44 | `desc` uses markdown: backticks for code/field refs (`` `field_name` ``, `` `True` ``, `` `pdf` ``), `\n\n` for paragraphs, `- ` bullet lists for enumerations, `**bold**` for caveats | N/A | No app/action, DTO, field, factory, provider implementation or JNI change. Existing saved providers are exercised by regression tests. |
| 45 | Descriptions use plain business language relating to common challenges — not just technical "what" but "why" and "when to use" | N/A | No app/action, DTO, field, factory, provider implementation or JNI change. Existing saved providers are exercised by regression tests. |
| 46 | No bare `True`/`False`/`NOTHING` — must be backtick-wrapped in `desc` | N/A | No app/action, DTO, field, factory, provider implementation or JNI change. Existing saved providers are exercised by regression tests. |
| 47 | No bare field/option names in prose — must use backticks | N/A | No app/action, DTO, field, factory, provider implementation or JNI change. Existing saved providers are exercised by regression tests. |
| 48 | Long descriptions (>500 chars) use bold section headers and bullet lists | N/A | No app/action, DTO, field, factory, provider implementation or JNI change. Existing saved providers are exercised by regression tests. |
| 49 | **Factory registration in Qore repo**: every factory name registered in `qlib/DataProvider/DataProvider.qc` → `FactoryMap` (without this, module loads but doesn't appear in Qorus apps) | N/A | No app/action, DTO, field, factory, provider implementation or JNI change. Existing saved providers are exercised by regression tests. |
| 50 | **`getRecordTypeImpl()` signature**: must be `private *hash<string, AbstractDataField> getRecordTypeImpl(*hash<auto> search_options)` — NOT returning `*AbstractDataProviderType` | N/A | No app/action, DTO, field, factory, provider implementation or JNI change. Existing saved providers are exercised by regression tests. |
| 51 | **Dependency JARs committed** (for JNI modules): JAR files in `qlib/*/jar/` may be gitignored — use `git add -f` to ensure they're tracked, otherwise CI compilation fails | N/A | No app/action, DTO, field, factory, provider implementation or JNI change. Existing saved providers are exercised by regression tests. |
| 52 | JAR install rules in CMakeLists.txt for all dependency JARs | N/A | No app/action, DTO, field, factory, provider implementation or JNI change. Existing saved providers are exercised by regression tests. |
| 53 | **No workarounds**: No TODOs, FIXMEs, stubs, or partially-implemented features | Pass | Missing canonical calendar capture is implemented using existing exact date arithmetic. No rounding heuristic, fixture-specific production branch or suppressed native failure. The independent native defect is explicitly routed to the next required P5 increment. |
| 54 | **Exception safety**: C++ uses `ReferenceHolder` for Qore allocations, `std::unique_ptr` for C++ allocations, `*xsink` checked after every fallible operation | Pass | Only locally owned typed hashes are changed during canonical computation. Existing scoped capture restores its predecessor on success, exception and cancellation, before publishing metadata. Injected interruption/conversion errors and recovery pass in every execution mode; no C++ change. |
| 55 | **Thread safety**: All mutable shared state protected by `std::lock_guard<std::mutex>` or documented as immutable-after-construction | Pass | Canonical fields are local; capture remains thread-local and scoped. Schema construction owns its metadata. HTTP workers use finalized schemas and immutable response peers. |
| 56 | **Type safety**: Strongly-typed `code<return(args)>` instead of untyped `code`; `static_cast` instead of C casts; typed hashdecls for results; enums where appropriate | Pass | Calendar and instant fields use existing typed hashdecls, integer-minute offsets and exact decimal strings. The HTTP callback has a typed code signature; public identity metadata retains its declared type. |
| 57 | **Performance**: No O(n²) where O(n) is possible; no unnecessary copies; coordinate descent uses incremental residuals not full matrix multiply | Pass | Canonical work is confined to declaration capture. Existing year/fraction operations are linear in lexical size; timezone adjustment shifts at most one day. Long fraction and 1000-digit year boundaries pass. |
| 58 | **Error handling**: All inputs validated (dimensions, empty data, unfitted models); C++ I/O handles EAGAIN/EINTR if applicable | Pass | All 144 invalid declarations reject as WSDL-ERROR. Tests cover source/canonical assessment, union reselection, published metadata preservation, categorized interruption/error recovery and invalid HTTP requests/responses. |
| 59 | **Documentation**: Doxygen `@param`, `@return`, `@throw` on all public methods; `@par Example` with realistic business scenarios; `@note` for important caveats | Pass | Identity-field docs, module/native release notes, README and durable design describe exact calendar rules with examples. Evidence distinguishes every Xerces disagreement and the required native follow-up. Affected documentation builds without warnings; no new public method. |
| 60 | **QPP flags**: `[flags=CONSTANT]` on methods that never throw; `[flags=RET_VALUE_ONLY]` on methods that throw but have no side effects | N/A | No C++/QPP change or new runtime I/O entry point; Qore runtime cancellation and scope cleanup apply. |
| 61 | **Security**: No user-controlled format strings; no buffer overflows; bounds checking on array indices; no credentials in code | Pass | No new production I/O, credentials or user-controlled diagnostic format. Existing validated calendar parser bounds fields; tests use bounded subprocesses and HTTP requests. |
| 62 | **Correctness**: Algorithms verified against reference implementations; edge cases tested (empty data, single sample, all-zero features) | Pass | All 161 Qore suites and 20 supplements pass, including AST/IR/JIT/tiered and compiled WSDL. Independent ordinal/Fraction checks verify 18 boundaries and 1296 SOAP values across 2214 records. Both corpus modes equal every parent case/stage/count/scope. Native failures remain explicitly recorded. |

Result: 15 Pass, 47 N/A, zero Fail.
