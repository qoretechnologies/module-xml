# P2-02 audit: complex-content base identity and inheritance

Copyright (C) 2026 Qore Technologies, s.r.o.

Applied `/home/david/.codex/skills/audit-changes/SKILL.md` to the final changes
on `develop`, parent `1a62154`. P2 remains in progress; P3–P9 are not started.
No push performed.

## Scope and references

Production WSDL.qm, derivation qtest, independently validated source derivatives,
archive/coverage tests, strict selection and current reports, release notes,
README, execution record and implemented design. Consulted all five required
Qore design documents under `/home/david/src/qore/git/qore/design/`:
qore-module-structure.md, module-sandboxing-audit-guide.md,
cooperative-cancellation.md, data-provider-checklist.md and
data-provider-development-guide.md. Normative references: XSD 1.0 QName resolution,
complex-type derivation and attribute ownership (linked in design/provenance).

## Review findings and verification

- Fixed declaration-scope/base identity, builtin anyType resolution, lost empty-base
  inheritance, restored removed restriction particles, custom Array misclassification
  and cyclic finalization. Seven reduced cases failed before implementation.
- Empty XML derivations represented by NOTHING bypassed missing-base checks;
  hasKey() now detects them. Corrected an inconsistent legacy array error category.
- Original ComplexTypeAttributeExtension messages are source-invalid. They remain
  rejection failures; separately hashed derivatives move gender to its parent and
  assert Mary/female preservation through four directions with both validators.
- 235 Qore cases pass: seven new cases / 32 assertions and 228 existing cases.
  Logs `/tmp/wsdl-p2-02-final-*.log`; debugging enabled. The comparator failures
  intentionally caught by soap.qtest remain passing negative test cases.
- 59 Python tests pass without warnings/errors; full log
  `/tmp/wsdl-p2-02-python-final2.log`. Includes worker cleanup/cancellation.
- Original/catalog both-version surveys complete. Catalog: parse 279/14,
  decode 996/120, serialize 994/2, output 948/46. All 279 valid echo descriptions
  and all 14 valid supplemental contracts parse; invalid descriptions still reject.
- Strict 26-description / 84-direction selection passes. Full 2,272-direction
  report has 428 failures (P2=36, P3=208, P4=76, P5=108); no missing/skipped stages.
  No previous successful stage regresses; valid successful wire bodies are unchanged.
  Four changed old outputs belong to the invalid attribute-owner source, never a pass.
- Source findings, adjudication and original hashes unchanged. Module SHA-256:
  `34703784ffaa65423c7c15c55f88fea356cd37236ad55e8299030c14556e9981`. Reviewed final diff and whitespace. No C++/valgrind required.

## Complete checklist

| Check | Status | Evidence |
| --- | --- | --- |
| 1. Entry exists in `doxygen/lang/120_modules.dox.tmpl` (for modules in the Qore repo; N/A for external module repos) | N/A | Existing external WSDL module; no new module, separated source, QPP class or build registration. |
| 2. Entry exists in `doxygen/lang/900_release_notes.dox.tmpl` (for modules in the Qore repo; external modules have release notes in their .qm) | Pass | WSDL.qm v0.5.8 release notes describe expanded derivation identity, inherited members, restricted particles and cycle errors. |
| 3. `qore_user_module()` or `qore_external_user_module()` call in `CMakeLists.txt` | N/A | Existing external WSDL module; no new module, separated source, QPP class or build registration. |
| 4. Module added to QMOD list in `CMakeLists.txt` | N/A | Existing external WSDL module; no new module, separated source, QPP class or build registration. |
| 5. `.qm` file has `@section <lowercasemodname>intro` as first doc section — **must be all lowercase** (e.g., `avrodataproviderintro`, not `AvroDataProviderintro`) | Pass | Existing lowercase wsdlintro and %modern retained; no deprecated includes or redundant parse directives. |
| 6. `%modern` in `.qm` file — no redundant `%new-style`, `%require-types`, `%strict-args`, `%enable-all-warnings` | Pass | Existing lowercase wsdlintro and %modern retained; no deprecated includes or redundant parse directives. |
| 7. No parse directives (`%requires`, `%modern`, `%new-style`) in separated `.qc` files (check OUTSIDE of `@code` blocks only) | N/A | Existing external WSDL module; no new module, separated source, QPP class or build registration. |
| 8. No `%include` usage (deprecated for modules) | Pass | Existing lowercase wsdlintro and %modern retained; no deprecated includes or redundant parse directives. |
| 9. Copyright 2026 on all new files | Pass | Authored module/test/Python/design/audit/manifest files carry 2026 copyright. Corrected derivative copies preserve upstream notices and have 2026 provenance metadata. |
| 10. Directory layout: `.qm` inside `qlib/<ModuleName>/` directory (not at `qlib/<ModuleName>.qm` for multi-file modules) | N/A | Existing external WSDL module; no new module, separated source, QPP class or build registration. |
| 11. No second `.qm` for the same module at `qlib/<ModuleName>.qm` | N/A | Existing external WSDL module; no new module, separated source, QPP class or build registration. |
| 12. `ns=Qore::XX` matches the QoreNamespace constructor path | N/A | Existing external WSDL module; no new module, separated source, QPP class or build registration. |
| 13. `%modern` directive present | Pass | New executable qtest uses %modern, local prepend path and relative WSDL require; xml belongs to this repository and QUnit is core. All Qore invocations enable debugging. |
| 14. Executable permission set (`chmod +x`) | Pass | New executable qtest uses %modern, local prepend path and relative WSDL require; xml belongs to this repository and QUnit is core. All Qore invocations enable debugging. |
| 15. Uses %prepend-module-path  before %requires for in-repo modules (Qore and Qore modules only; not Qorus) | Pass | New executable qtest uses %modern, local prepend path and relative WSDL require; xml belongs to this repository and QUnit is core. All Qore invocations enable debugging. |
| 16. External module dependencies use `%try-module` — except modules delivered with the project itself (Qore ex: DataProvider, ConnectionProvider, QUnit, etc.) which use hard `%requires` | Pass | New executable qtest uses %modern, local prepend path and relative WSDL require; xml belongs to this repository and QUnit is core. All Qore invocations enable debugging. |
| 17. No filesystem operations (fopen, open, creat, unlink, remove, rename, mkdir, rmdir, stat, chmod) without sandbox checks | N/A | No C++ or QPP changes. Qore owns objects and cancellation; no native build or valgrind needed. |
| 18. No network operations (connect, bind, socket, getaddrinfo, gethostbyname) without sandbox checks | N/A | No C++ or QPP changes. Qore owns objects and cancellation; no native build or valgrind needed. |
| 19. If filesystem/network ops exist, verify `QoreSandboxManagerHelper` usage | N/A | No C++ or QPP changes. Qore owns objects and cancellation; no native build or valgrind needed. |
| 20. No `File::`, `Dir::`, `Socket::`, `HTTPClient::` usage without justification | Pass | No new production I/O. Test files and independent validator inputs use verified local bytes and existing bounded offline worker APIs. |
| 21. All `for`/`while` loops that could iterate >100 times have `qore_check_cancel()` checks | N/A | No C++ or QPP changes. Qore owns objects and cancellation; no native build or valgrind needed. |
| 22. Uses `qore_check_cancel()` (NOT deprecated `qore_check_io_interrupt()`) | N/A | No C++ or QPP changes. Qore owns objects and cancellation; no native build or valgrind needed. |
| 23. Check frequency: every 100 iterations for tight loops, every 10 for expensive iterations | N/A | No C++ or QPP changes. Qore owns objects and cancellation; no native build or valgrind needed. |
| 24. No blocking operations without cancellation support | N/A | No C++ or QPP changes. Qore owns objects and cancellation; no native build or valgrind needed. |
| 25. Every action has `display_name`, `short_desc` (plain text, <80 chars), `desc` (markdown) | N/A | No action/app/Fields/FactoryMap/JNI/build registrations. Existing provider field reporting is exercised by the inherited-content regression and affected SOAP consumer tests. |
| 26. Every action has `options` populated via `getActionOptionFromFields()` — without this, the action shows an empty, unusable form | N/A | No action/app/Fields/FactoryMap/JNI/build registrations. Existing provider field reporting is exercised by the inherited-content regression and affected SOAP consumer tests. |
| 27. Every action has `output_type` set to a typed data type constant (e.g., `MyResponseDataType`) — not omitted | N/A | No action/app/Fields/FactoryMap/JNI/build registrations. Existing provider field reporting is exercised by the inherited-content regression and affected SOAP consumer tests. |
| 28. DPAT_API actions: provider has `"supports_request": True` and implements `doRequestImpl()` | N/A | No action/app/Fields/FactoryMap/JNI/build registrations. Existing provider field reporting is exercised by the inherited-content regression and affected SOAP consumer tests. |
| 29. DPAT_FIND actions: every option exists in `SearchOptions`, `getRecordTypeImpl()` returns `*hash<string, AbstractDataField>` | N/A | No action/app/Fields/FactoryMap/JNI/build registrations. Existing provider field reporting is exercised by the inherited-content regression and affected SOAP consumer tests. |
| 30. Scheme-based apps (with `"scheme"` in registerApp): actions use `"path"` and do NOT use `"cls"` — having both `scheme` and `cls` causes a runtime error | N/A | No action/app/Fields/FactoryMap/JNI/build registrations. Existing provider field reporting is exercised by the inherited-content regression and affected SOAP consumer tests. |
| 31. Single-key hash slices use trailing comma: `Fields{"key",}` (without trailing comma, `Fields{"key"}` returns the value, not a hash) | N/A | No action/app/Fields/FactoryMap/JNI/build registrations. Existing provider field reporting is exercised by the inherited-content regression and affected SOAP consumer tests. |
| 32. **Typed data type classes exist** for request and response types — inherit `HashDataType`, have `const Fields` hash, call `addQoreFields(Fields)` in constructor, export public constant at bottom (e.g., `public const MyDataType = new MyDataType();`) | N/A | No action/app/Fields/FactoryMap/JNI/build registrations. Existing provider field reporting is exercised by the inherited-content regression and affected SOAP consumer tests. |
| 33. Request/input types use `public` Fields (enables `ClassName::Fields` in action registration) | N/A | No action/app/Fields/FactoryMap/JNI/build registrations. Existing provider field reporting is exercised by the inherited-content regression and affected SOAP consumer tests. |
| 34. Response/output types use `private` Fields | N/A | No action/app/Fields/FactoryMap/JNI/build registrations. Existing provider field reporting is exercised by the inherited-content regression and affected SOAP consumer tests. |
| 35. Each field in data types has `display_name`, `type`, and `desc` (markdown-formatted) | N/A | No action/app/Fields/FactoryMap/JNI/build registrations. Existing provider field reporting is exercised by the inherited-content regression and affected SOAP consumer tests. |
| 36. Input fields have `example_value` where useful (string fields, endpoint URIs, SQL queries, etc.) | N/A | No action/app/Fields/FactoryMap/JNI/build registrations. Existing provider field reporting is exercised by the inherited-content regression and affected SOAP consumer tests. |
| 37. Fields with finite allowed values use `allowed_values` with `AllowedValueInfo` containing both `value` and `display_name` (Title Case, human-readable) — never bare values, never described only in text | N/A | No action/app/Fields/FactoryMap/JNI/build registrations. Existing provider field reporting is exercised by the inherited-content regression and affected SOAP consumer tests. |
| 38. Password/secret fields have `"sensitive": True` | N/A | No action/app/Fields/FactoryMap/JNI/build registrations. Existing provider field reporting is exercised by the inherited-content regression and affected SOAP consumer tests. |
| 39. `groups` uses `AppGroup` enum values from `qlib/DataProvider/AppGroup.qc` | N/A | No action/app/Fields/FactoryMap/JNI/build registrations. Existing provider field reporting is exercised by the inherited-content regression and affected SOAP consumer tests. |
| 40. App `logo` stored as separate file, loaded at module level in `Priv` namespace | N/A | No action/app/Fields/FactoryMap/JNI/build registrations. Existing provider field reporting is exercised by the inherited-content regression and affected SOAP consumer tests. |
| 41. App `desc` uses markdown: bullet list of capabilities, links to project website, business-language explanation of value | N/A | No action/app/Fields/FactoryMap/JNI/build registrations. Existing provider field reporting is exercised by the inherited-content regression and affected SOAP consumer tests. |
| 42. `display_name` is user-friendly ("Apache Avro" not "avro") | N/A | No action/app/Fields/FactoryMap/JNI/build registrations. Existing provider field reporting is exercised by the inherited-content regression and affected SOAP consumer tests. |
| 43. `short_desc` is plain text, under 80 chars, single sentence — no markdown | N/A | No action/app/Fields/FactoryMap/JNI/build registrations. Existing provider field reporting is exercised by the inherited-content regression and affected SOAP consumer tests. |
| 44. `desc` uses markdown: backticks for code/field refs (`` `field_name` ``, `` `True` ``, `` `pdf` ``), `\n\n` for paragraphs, `- ` bullet lists for enumerations, `**bold**` for caveats | N/A | No action/app/Fields/FactoryMap/JNI/build registrations. Existing provider field reporting is exercised by the inherited-content regression and affected SOAP consumer tests. |
| 45. Descriptions use plain business language relating to common challenges — not just technical "what" but "why" and "when to use" | N/A | No action/app/Fields/FactoryMap/JNI/build registrations. Existing provider field reporting is exercised by the inherited-content regression and affected SOAP consumer tests. |
| 46. No bare `True`/`False`/`NOTHING` — must be backtick-wrapped in `desc` | N/A | No action/app/Fields/FactoryMap/JNI/build registrations. Existing provider field reporting is exercised by the inherited-content regression and affected SOAP consumer tests. |
| 47. No bare field/option names in prose — must use backticks | N/A | No action/app/Fields/FactoryMap/JNI/build registrations. Existing provider field reporting is exercised by the inherited-content regression and affected SOAP consumer tests. |
| 48. Long descriptions (>500 chars) use bold section headers and bullet lists | N/A | No action/app/Fields/FactoryMap/JNI/build registrations. Existing provider field reporting is exercised by the inherited-content regression and affected SOAP consumer tests. |
| 49. **Factory registration in Qore repo**: every factory name registered in `qlib/DataProvider/DataProvider.qc` → `FactoryMap` (without this, module loads but doesn't appear in Qorus apps) | N/A | No action/app/Fields/FactoryMap/JNI/build registrations. Existing provider field reporting is exercised by the inherited-content regression and affected SOAP consumer tests. |
| 50. **`getRecordTypeImpl()` signature**: must be `private *hash<string, AbstractDataField> getRecordTypeImpl(*hash<auto> search_options)` — NOT returning `*AbstractDataProviderType` | N/A | No action/app/Fields/FactoryMap/JNI/build registrations. Existing provider field reporting is exercised by the inherited-content regression and affected SOAP consumer tests. |
| 51. **Dependency JARs committed** (for JNI modules): JAR files in `qlib/*/jar/` may be gitignored — use `git add -f` to ensure they're tracked, otherwise CI compilation fails | N/A | No action/app/Fields/FactoryMap/JNI/build registrations. Existing provider field reporting is exercised by the inherited-content regression and affected SOAP consumer tests. |
| 52. JAR install rules in CMakeLists.txt for all dependency JARs | N/A | No action/app/Fields/FactoryMap/JNI/build registrations. Existing provider field reporting is exercised by the inherited-content regression and affected SOAP consumer tests. |
| 53. **No workarounds**: No TODOs, FIXMEs, stubs, or partially-implemented features | Pass | Root fixes use expanded base identities and inherited content models; no fixture branches, skips or input relaxation. Independent P2/P4/P5 requirements remain explicitly tracked under the authorized incremental plan. |
| 54. **Exception safety**: C++ uses `ReferenceHolder` for Qore allocations, `std::unique_ptr` for C++ allocations, `*xsink` checked after every fallible operation | Pass | Active finalization guard resets on every exit and completion is set only on success. Cycles reject before base links publish; failed schema additions restore registries, tested with reuse. Namespace guards restore declaration scopes on throws. |
| 55. **Thread safety**: All mutable shared state protected by `std::lock_guard<std::mutex>` or documented as immutable-after-construction | Pass | Schema construction is documented as mutable before sharing. New state belongs to each schema type and no shared runtime payload state is introduced. |
| 56. **Type safety**: Strongly-typed `code<return(args)>` instead of untyped `code`; `static_cast` instead of C casts; typed hashdecls for results; enums where appropriate | Pass | Resolved base uses XsdAbstractType and checked XsdComplexType casts; canonical URI/local keys prevent ambiguous registry access. Type metadata retains existing hash<auto> interface. |
| 57. **Performance**: No O(n²) where O(n) is possible; no unnecessary copies; coordinate descent uses incremental residuals not full matrix multiply | Pass | Constant-time canonical lookup replaces local-name ambiguity. Each base finalizes once; inherited maps use Qore value semantics. No new runtime search/backtracking or blocking operation. |
| 58. **Error handling**: All inputs validated (dimensions, empty data, unfitted models); C++ I/O handles EAGAIN/EINTR if applicable | Pass | Wrong namespace, invalid/simple/missing bases, custom Array collision, finite SOAP array occurrence and cyclic derivations assert WSDL-ERROR. Empty extension/restriction key-presence defect found during audit was fixed and retested. |
| 59. **Documentation**: Doxygen `@param`, `@return`, `@throw` on all public methods; `@par Example` with realistic business scenarios; `@note` for important caveats | Pass | Added public base metadata has Doxygen descriptions and version tags; no new public methods. Implemented design links normative derivation rules; README, release notes, execution and derivative provenance explain tested behavior and remaining criteria. |
| 60. **QPP flags**: `[flags=CONSTANT]` on methods that never throw; `[flags=RET_VALUE_ONLY]` on methods that throw but have no side effects | N/A | No C++ or QPP changes. Qore owns objects and cancellation; no native build or valgrind needed. |
| 61. **Security**: No user-controlled format strings; no buffer overflows; bounds checking on array indices; no credentials in code | Pass | No credentials or user-controlled format strings. Base QName syntax uses validated getTypeHash; optional regex prefix extraction permits absent prefixes. Immutable source hashes and offline oracle isolation retained. |
| 62. **Correctness**: Algorithms verified against reference implementations; edge cases tested (empty data, single sample, all-zero features) | Pass | 235 Qore cases and 59 Python tests pass. Both validators accept corrected attribute-owner inputs/four outputs with exact values. Strict 26-description/84-direction gate passes; no previously successful stage regresses and every previously valid successful wire output is byte-identical. |

All applicable checks pass; no audit failures remain.
