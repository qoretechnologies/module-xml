# P3-37 audit: schema URI identity

Copyright (C) 2026 Qore Technologies, s.r.o.

The complete audit-changes skill and its applicable module-structure, sandboxing
and cooperative-cancellation guides were applied to the final native/CMake patch,
C++ warning/resource changes, Java oracle, C/Python tests and documentation. Every
DataProvider item was separately assessed as N/A.

Checklist: **20 Pass / 42 N/A / 0 Fail**. The review added braces to the
extracted URI parsing helper and reran the affected build, direct memory checks,
live URI tests and provider matrix. Upstream source bytes remain immutable; the
checksum-guarded build-tree copies are the only compiled replacements.

[Evidence](../schema-uri-evidence.md) records tests, hashes, six explicit external
validator defects and unchanged later-phase failures. This increment does not
change production WSDL code or declare P3/P9 complete.

| Check | Status | Evidence |
| --- | --- | --- |
| 1. Entry exists in `doxygen/lang/120_modules.dox.tmpl` (for modules in the Qore repo; N/A for external module repos) | N/A | No new user module, separated Qore source or QPP class. |
| 2. Entry exists in `doxygen/lang/900_release_notes.dox.tmpl` (for modules in the Qore repo; external modules have release notes in their .qm) | Pass | Native 2.3.0 release notes describe URI identity, runtime hints and optional diagnostic logging. |
| 3. `qore_user_module()` or `qore_external_user_module()` call in `CMakeLists.txt` | Pass | The existing xml target uses the behavioral provider probe; new allocation target is EXCLUDE_FROM_ALL. CMake/Automake distribute all required patch and probe files; Debug, Release and docs build. |
| 4. Module added to QMOD list in `CMakeLists.txt` | N/A | No new user module, separated Qore source or QPP class. |
| 5. `.qm` file has `@section <lowercasemodname>intro` as first doc section — **must be all lowercase** (e.g., `avrodataproviderintro`, not `AvroDataProviderintro`) | N/A | No new user module, separated Qore source or QPP class. |
| 6. `%modern` in `.qm` file — no redundant `%new-style`, `%require-types`, `%strict-args`, `%enable-all-warnings` | N/A | No new user module, separated Qore source or QPP class. |
| 7. No parse directives (`%requires`, `%modern`, `%new-style`) in separated `.qc` files (check OUTSIDE of `@code` blocks only) | N/A | No new user module, separated Qore source or QPP class. |
| 8. No `%include` usage (deprecated for modules) | Pass | No deprecated %include is added. |
| 9. Copyright 2026 on all new files | Pass | All authored patch/probe, Python regression, design, evidence and audit files carry 2026 notices. Upstream notices and source headers remain intact. |
| 10. Directory layout: `.qm` inside `qlib/<ModuleName>/` directory (not at `qlib/<ModuleName>.qm` for multi-file modules) | N/A | No new user module, separated Qore source or QPP class. |
| 11. No second `.qm` for the same module at `qlib/<ModuleName>.qm` | N/A | No new user module, separated Qore source or QPP class. |
| 12. `ns=Qore::XX` matches the QoreNamespace constructor path | N/A | No new user module, separated Qore source or QPP class. |
| 13. `%modern` directive present | N/A | No new or modified Qore test source; existing native driver uses %modern. |
| 14. Executable permission set (`chmod +x`) | Pass | Both new Python regression entry points are executable (0755). No qtest mode is changed. |
| 15. Uses %prepend-module-path  before %requires for in-repo modules (Qore and Qore modules only; not Qorus) | Pass | Python native tests invoke the existing local schema-resource driver. The test environment prepends the repository binary build and qlib before installed modules. |
| 16. External module dependencies use `%try-module` — except modules delivered with the project itself (Qore ex: DataProvider, ConnectionProvider, QUnit, etc.) which use hard `%requires` | Pass | The existing driver hard-requires the same-repository xml module and guards external json. No new user module dependency is introduced. |
| 17. No filesystem operations (fopen, open, creat, unlink, remove, rename, mkdir, rmdir, stat, chmod) without sandbox checks | Pass | No new filesystem access in module C++; dependency URI helpers only transform memory. Original source/archive integrity and strict offline callback resolution are checked. |
| 18. No network operations (connect, bind, socket, getaddrinfo, gethostbyname) without sandbox checks | Pass | HTTP loading retains the existing PO_NO_NETWORK and Qore transport policy checks, TLS verification and redirect restrictions. The seven resource tests recheck denial and recovery. |
| 19. If filesystem/network ops exist, verify `QoreSandboxManagerHelper` usage | Pass | Existing Qore transport checks remain responsible for destination policy; only the URL representation changes. Catalog/file checks and sandbox helper are unchanged and covered by resource tests. |
| 20. No `File::`, `Dir::`, `Socket::`, `HTTPClient::` usage without justification | N/A | No Qore implementation source changes. Existing fixture driver and independent local peers are justified by actual resource-selection verification. |
| 21. All `for`/`while` loops that could iterate >100 times have `qore_check_cancel()` checks | N/A | No added or modified C++ loop. Patched third-party C URI loops are pure linear memory transformations without Qore API linkage; existing Qore resource cancellation boundaries remain tested. |
| 22. Uses `qore_check_cancel()` (NOT deprecated `qore_check_io_interrupt()`) | N/A | No cancellation API call is added or changed; no deprecated API introduced. |
| 23. Check frequency: every 100 iterations for tight loops, every 10 for expensive iterations | N/A | No new Qore/C++ cancellation polling loop. Existing attachment/resource checks and interrupted stream regressions remain in the affected suite. |
| 24. No blocking operations without cancellation support | Pass | The HTTP path continues through cancellation-aware Qore transport and existing resource read callbacks. No new blocking operation is added. Native local-file I/O review remains explicitly P9. |
| 25. Every action has `display_name`, `short_desc` (plain text, <80 chars), `desc` (markdown) | N/A | No DataProvider action, app, fields, factory, JNI dependency or corresponding build registration change. |
| 26. Every action has `options` populated via `getActionOptionFromFields()` — without this, the action shows an empty, unusable form | N/A | No DataProvider action, app, fields, factory, JNI dependency or corresponding build registration change. |
| 27. Every action has `output_type` set to a typed data type constant (e.g., `MyResponseDataType`) — not omitted | N/A | No DataProvider action, app, fields, factory, JNI dependency or corresponding build registration change. |
| 28. DPAT_API actions: provider has `"supports_request": True` and implements `doRequestImpl()` | N/A | No DataProvider action, app, fields, factory, JNI dependency or corresponding build registration change. |
| 29. DPAT_FIND actions: every option exists in `SearchOptions`, `getRecordTypeImpl()` returns `*hash<string, AbstractDataField>` | N/A | No DataProvider action, app, fields, factory, JNI dependency or corresponding build registration change. |
| 30. Scheme-based apps (with `"scheme"` in registerApp): actions use `"path"` and do NOT use `"cls"` — having both `scheme` and `cls` causes a runtime error | N/A | No DataProvider action, app, fields, factory, JNI dependency or corresponding build registration change. |
| 31. Single-key hash slices use trailing comma: `Fields{"key",}` (without trailing comma, `Fields{"key"}` returns the value, not a hash) | N/A | No DataProvider action, app, fields, factory, JNI dependency or corresponding build registration change. |
| 32. **Typed data type classes exist** for request and response types — inherit `HashDataType`, have `const Fields` hash, call `addQoreFields(Fields)` in constructor, export public constant at bottom (e.g., `public const MyDataType = new MyDataType();`) | N/A | No DataProvider action, app, fields, factory, JNI dependency or corresponding build registration change. |
| 33. Request/input types use `public` Fields (enables `ClassName::Fields` in action registration) | N/A | No DataProvider action, app, fields, factory, JNI dependency or corresponding build registration change. |
| 34. Response/output types use `private` Fields | N/A | No DataProvider action, app, fields, factory, JNI dependency or corresponding build registration change. |
| 35. Each field in data types has `display_name`, `type`, and `desc` (markdown-formatted) | N/A | No DataProvider action, app, fields, factory, JNI dependency or corresponding build registration change. |
| 36. Input fields have `example_value` where useful (string fields, endpoint URIs, SQL queries, etc.) | N/A | No DataProvider action, app, fields, factory, JNI dependency or corresponding build registration change. |
| 37. Fields with finite allowed values use `allowed_values` with `AllowedValueInfo` containing both `value` and `display_name` (Title Case, human-readable) — never bare values, never described only in text | N/A | No DataProvider action, app, fields, factory, JNI dependency or corresponding build registration change. |
| 38. Password/secret fields have `"sensitive": True` | N/A | No DataProvider action, app, fields, factory, JNI dependency or corresponding build registration change. |
| 39. `groups` uses `AppGroup` enum values from `qlib/DataProvider/AppGroup.qc` | N/A | No DataProvider action, app, fields, factory, JNI dependency or corresponding build registration change. |
| 40. App `logo` stored as separate file, loaded at module level in `Priv` namespace | N/A | No DataProvider action, app, fields, factory, JNI dependency or corresponding build registration change. |
| 41. App `desc` uses markdown: bullet list of capabilities, links to project website, business-language explanation of value | N/A | No DataProvider action, app, fields, factory, JNI dependency or corresponding build registration change. |
| 42. `display_name` is user-friendly ("Apache Avro" not "avro") | N/A | No DataProvider action, app, fields, factory, JNI dependency or corresponding build registration change. |
| 43. `short_desc` is plain text, under 80 chars, single sentence — no markdown | N/A | No DataProvider action, app, fields, factory, JNI dependency or corresponding build registration change. |
| 44. `desc` uses markdown: backticks for code/field refs (`` `field_name` ``, `` `True` ``, `` `pdf` ``), `\n\n` for paragraphs, `- ` bullet lists for enumerations, `**bold**` for caveats | N/A | No DataProvider action, app, fields, factory, JNI dependency or corresponding build registration change. |
| 45. Descriptions use plain business language relating to common challenges — not just technical "what" but "why" and "when to use" | N/A | No DataProvider action, app, fields, factory, JNI dependency or corresponding build registration change. |
| 46. No bare `True`/`False`/`NOTHING` — must be backtick-wrapped in `desc` | N/A | No DataProvider action, app, fields, factory, JNI dependency or corresponding build registration change. |
| 47. No bare field/option names in prose — must use backticks | N/A | No DataProvider action, app, fields, factory, JNI dependency or corresponding build registration change. |
| 48. Long descriptions (>500 chars) use bold section headers and bullet lists | N/A | No DataProvider action, app, fields, factory, JNI dependency or corresponding build registration change. |
| 49. **Factory registration in Qore repo**: every factory name registered in `qlib/DataProvider/DataProvider.qc` → `FactoryMap` (without this, module loads but doesn't appear in Qorus apps) | N/A | No DataProvider action, app, fields, factory, JNI dependency or corresponding build registration change. |
| 50. **`getRecordTypeImpl()` signature**: must be `private *hash<string, AbstractDataField> getRecordTypeImpl(*hash<auto> search_options)` — NOT returning `*AbstractDataProviderType` | N/A | No DataProvider action, app, fields, factory, JNI dependency or corresponding build registration change. |
| 51. **Dependency JARs committed** (for JNI modules): JAR files in `qlib/*/jar/` may be gitignored — use `git add -f` to ensure they're tracked, otherwise CI compilation fails | N/A | No DataProvider action, app, fields, factory, JNI dependency or corresponding build registration change. |
| 52. JAR install rules in CMakeLists.txt for all dependency JARs | N/A | No DataProvider action, app, fields, factory, JNI dependency or corresponding build registration change. |
| 53. **No workarounds**: No TODOs, FIXMEs, stubs, or partially-implemented features | Pass | Native parsing, normalization, schema assembly and HTTP encoding defects are repaired at their owning boundaries. No source fixture rewrite, relaxed validation or disabled test. Six independently proven Xerces false negatives remain explicit. |
| 54. **Exception safety**: C++ uses `ReferenceHolder` for Qore allocations, `std::unique_ptr` for C++ allocations, `*xsink` checked after every fallible operation | Pass | xmlURIEscapeStr allocation is checked and immediately owned by ON_BLOCK_EXIT; fallible HTTP operations retain sink checks and existing ReferenceHolder ownership. Native URI/XML Base failures free allocations; the new mapper frees normalized/escaped buffers on every return. Injected failures, resource recovery and Valgrind pass. |
| 55. **Thread safety**: All mutable shared state protected by `std::lock_guard<std::mutex>` or documented as immutable-after-construction | Pass | New production helpers use only local state. Existing parser/callback context is thread-local and restored by RAII. Mutable allocation/probe hooks exist only in standalone single-threaded processes; Python peer state has deterministic teardown. |
| 56. **Type safety**: Strongly-typed `code<return(args)>` instead of untyped `code`; `static_cast` instead of C casts; typed hashdecls for results; enums where appropriate | Pass | C++ uses typed XML pointers and explicit reinterpret_cast for the byte-string boundary. The native C patch retains libxml2 types. Java uses URI-keyed typed maps, and no untyped production Qore callable is added. |
| 57. **Performance**: No O(n²) where O(n) is possible; no unnecessary copies; coordinate descent uses incremental residuals not full matrix multiply | Pass | Dot-segment removal and whitespace normalization are linear: consumed characters are copied or removed once. URI resolution performs required bounded passes without repeated whole-path normalization. No extra network round trip. |
| 58. **Error handling**: All inputs validated (dimensions, empty data, unfitted models); C++ I/O handles EAGAIN/EINTR if applicable | Pass | Malformed percent escapes, absent resources, aliases, empty/query references, bad schema content, allocation failures, transport failures and recovery are tested. Existing client handles interruptible I/O; no raw syscall is introduced. |
| 59. **Documentation**: Doxygen `@param`, `@return`, `@throw` on all public methods; `@par Example` with realistic business scenarios; `@note` for important caveats | Pass | Durable implemented design, URI example, provider README, reproducible commands, release notes and separate validator-defect evidence are included. Existing public QPP signatures, arguments and flags are unchanged. |
| 60. **QPP flags**: `[flags=CONSTANT]` on methods that never throw; `[flags=RET_VALUE_ONLY]` on methods that throw but have no side effects | N/A | No QPP method signature, implementation contract or flags change; only private schema-warning callbacks change logging. |
| 61. **Security**: No user-controlled format strings; no buffer overflows; bounds checking on array indices; no credentials in code | Pass | URI reserved escapes retain identity, strict malformed syntax remains rejected, and buffers have explicit ownership. The new warning logger uses a literal format string. Policies/TLS are preserved; offline oracle never falls back to network/filesystem. |
| 62. **Correctness**: Algorithms verified against reference implementations; edge cases tested (empty data, single sample, all-zero features) | Pass | 142 direct normative URI/component/hint checks, 29 allocation checks, six live URI methods in all modes, 300 independent QName documents with exact values, complete XML and unchanged both-version reports substantiate correctness. External XML Base defects and later-phase failures are not counted as conformance passes. |
