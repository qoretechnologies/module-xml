# Element-order port audit

Copyright (C) 2026 Qore Technologies, s.r.o.

Date: 2026-09-12. Scope: WSDL, both order regressions, the imported fixture,
snapshot worker, particle design, release/evidence documents and current reports
since `5e3e074`. Pending P5-13 native/CMake files are outside this commit.
The complete audit-changes skill and all five referenced guides were applied.
All 62 checks are resolved individually; no audit failure remains.

[Validation inventory](../element-order-port-validation.json) records commands,
logs, source/runtime hashes and the exact remaining corpus outcomes. The complete
128-suite gate preceded the release-note-only edit; the final source and rebuilt
AOT module both pass the new order suite again. No C++ change or new Valgrind run
belongs to this Qore-only commit. The main Qore workspace contains concurrent
Azure/OpenAPI development and is outside this commit.

| # | Check | Status | Evidence |
| --- | --- | --- | --- |
| 1 | Entry exists in `doxygen/lang/120_modules.dox.tmpl` (for modules in the Qore repo; N/A for external module repos) | N/A | External module repository; no Qore catalog entry is added. |
| 2 | Entry exists in `doxygen/lang/900_release_notes.dox.tmpl` (for modules in the Qore repo; external modules have release notes in their .qm) | Pass | WSDL 0.5.8 release notes describe native field order, required empty-record validation and normalization work. |
| 3 | `qore_user_module()` or `qore_external_user_module()` call in `CMakeLists.txt` | N/A | No new module or build registration; existing WSDL qmod is rebuilt with the deployed qcc. |
| 4 | Module added to QMOD list in `CMakeLists.txt` | N/A | No module or QMOD-list entry is added. |
| 5 | `.qm` file has `@section <lowercasemodname>intro` as first doc section — **must be all lowercase** (e.g., `avrodataproviderintro`, not `AvroDataProviderintro`) | Pass | The existing WSDL module retains its lowercase introduction and identity. |
| 6 | `%modern` in `.qm` file — no redundant `%new-style`, `%require-types`, `%strict-args`, `%enable-all-warnings` | Pass | The existing WSDL module uses %modern with no redundant parse directives added. |
| 7 | No parse directives (`%requires`, `%modern`, `%new-style`) in separated `.qc` files (check OUTSIDE of `@code` blocks only) | N/A | No separated .qc file is changed. |
| 8 | No `%include` usage (deprecated for modules) | Pass | No deprecated %include usage is introduced. |
| 9 | Copyright 2026 on all new files | Pass | All new authored tests, worker, design/evidence and inventory carry Copyright 2026. The imported 2.x WSDL is preserved byte-for-byte with its original notice. |
| 10 | Directory layout: `.qm` inside `qlib/<ModuleName>/` directory (not at `qlib/<ModuleName>.qm` for multi-file modules) | N/A | Existing single-file WSDL module layout is unchanged. |
| 11 | No second `.qm` for the same module at `qlib/<ModuleName>.qm` | N/A | No duplicate module entry point is added. |
| 12 | `ns=Qore::XX` matches the QoreNamespace constructor path | N/A | No QPP class is added. |
| 13 | `%modern` directive present | Pass | Both qtests and the snapshot worker explicitly use %modern. |
| 14 | Executable permission set (`chmod +x`) | Pass | Both qtests and the snapshot worker have executable permissions. |
| 15 | Uses %prepend-module-path  before %requires for in-repo modules (Qore and Qore modules only; not Qorus) | Pass | Tests prepend the local qlib and use relative in-repository requirements. Additional runs load the rebuilt WSDL qmod explicitly. |
| 16 | External module dependencies use `%try-module` — except modules delivered with the project itself (Qore ex: DataProvider, ConnectionProvider, QUnit, etc.) which use hard `%requires` | Pass | Project xml and core QUnit use hard requirements; the snapshot worker guards external json with an explicit missing-dependency error. |
| 17 | No filesystem operations (fopen, open, creat, unlink, remove, rename, mkdir, rmdir, stat, chmod) without sandbox checks | N/A | No C++ filesystem change in this commit. |
| 18 | No network operations (connect, bind, socket, getaddrinfo, gethostbyname) without sandbox checks | N/A | No C++ network change in this commit. |
| 19 | If filesystem/network ops exist, verify `QoreSandboxManagerHelper` usage | N/A | No native resource operation is added. |
| 20 | No `File::`, `Dir::`, `Socket::`, `HTTPClient::` usage without justification | Pass | Production changes use only in-memory values. The imported regression and snapshot worker read explicit fixture paths; include tests use a complete offline xsd_cache. |
| 21 | All `for`/`while` loops that could iterate >100 times have `qore_check_cancel()` checks | N/A | No C++ loop changes; Qore runtime cancellation is exercised by the existing particle model suite. |
| 22 | Uses `qore_check_cancel()` (NOT deprecated `qore_check_io_interrupt()`) | N/A | No native cancellation API changes. |
| 23 | Check frequency: every 100 iterations for tight loops, every 10 for expensive iterations | N/A | No native cancellation-check frequency changes. |
| 24 | No blocking operations without cancellation support | N/A | No blocking production operation is added; external validation runners use subprocess deadlines. |
| 25 | Every action has `display_name`, `short_desc` (plain text, <80 chars), `desc` (markdown) | N/A | No DataProvider action, application, field schema, factory or JNI dependency is changed; existing ordered provider metadata is checked by the imported regression and recursive corpus snapshots. |
| 26 | Every action has `options` populated via `getActionOptionFromFields()` — without this, the action shows an empty, unusable form | N/A | No DataProvider action, application, field schema, factory or JNI dependency is changed; existing ordered provider metadata is checked by the imported regression and recursive corpus snapshots. |
| 27 | Every action has `output_type` set to a typed data type constant (e.g., `MyResponseDataType`) — not omitted | N/A | No DataProvider action, application, field schema, factory or JNI dependency is changed; existing ordered provider metadata is checked by the imported regression and recursive corpus snapshots. |
| 28 | DPAT_API actions: provider has `"supports_request": True` and implements `doRequestImpl()` | N/A | No DataProvider action, application, field schema, factory or JNI dependency is changed; existing ordered provider metadata is checked by the imported regression and recursive corpus snapshots. |
| 29 | DPAT_FIND actions: every option exists in `SearchOptions`, `getRecordTypeImpl()` returns `*hash<string, AbstractDataField>` | N/A | No DataProvider action, application, field schema, factory or JNI dependency is changed; existing ordered provider metadata is checked by the imported regression and recursive corpus snapshots. |
| 30 | Scheme-based apps (with `"scheme"` in registerApp): actions use `"path"` and do NOT use `"cls"` — having both `scheme` and `cls` causes a runtime error | N/A | No DataProvider action, application, field schema, factory or JNI dependency is changed; existing ordered provider metadata is checked by the imported regression and recursive corpus snapshots. |
| 31 | Single-key hash slices use trailing comma: `Fields{"key",}` (without trailing comma, `Fields{"key"}` returns the value, not a hash) | N/A | No DataProvider action, application, field schema, factory or JNI dependency is changed; existing ordered provider metadata is checked by the imported regression and recursive corpus snapshots. |
| 32 | **Typed data type classes exist** for request and response types — inherit `HashDataType`, have `const Fields` hash, call `addQoreFields(Fields)` in constructor, export public constant at bottom (e.g., `public const MyDataType = new MyDataType();`) | N/A | No DataProvider action, application, field schema, factory or JNI dependency is changed; existing ordered provider metadata is checked by the imported regression and recursive corpus snapshots. |
| 33 | Request/input types use `public` Fields (enables `ClassName::Fields` in action registration) | N/A | No DataProvider action, application, field schema, factory or JNI dependency is changed; existing ordered provider metadata is checked by the imported regression and recursive corpus snapshots. |
| 34 | Response/output types use `private` Fields | N/A | No DataProvider action, application, field schema, factory or JNI dependency is changed; existing ordered provider metadata is checked by the imported regression and recursive corpus snapshots. |
| 35 | Each field in data types has `display_name`, `type`, and `desc` (markdown-formatted) | N/A | No DataProvider action, application, field schema, factory or JNI dependency is changed; existing ordered provider metadata is checked by the imported regression and recursive corpus snapshots. |
| 36 | Input fields have `example_value` where useful (string fields, endpoint URIs, SQL queries, etc.) | N/A | No DataProvider action, application, field schema, factory or JNI dependency is changed; existing ordered provider metadata is checked by the imported regression and recursive corpus snapshots. |
| 37 | Fields with finite allowed values use `allowed_values` with `AllowedValueInfo` containing both `value` and `display_name` (Title Case, human-readable) — never bare values, never described only in text | N/A | No DataProvider action, application, field schema, factory or JNI dependency is changed; existing ordered provider metadata is checked by the imported regression and recursive corpus snapshots. |
| 38 | Password/secret fields have `"sensitive": True` | N/A | No DataProvider action, application, field schema, factory or JNI dependency is changed; existing ordered provider metadata is checked by the imported regression and recursive corpus snapshots. |
| 39 | `groups` uses `AppGroup` enum values from `qlib/DataProvider/AppGroup.qc` | N/A | No DataProvider action, application, field schema, factory or JNI dependency is changed; existing ordered provider metadata is checked by the imported regression and recursive corpus snapshots. |
| 40 | App `logo` stored as separate file, loaded at module level in `Priv` namespace | N/A | No DataProvider action, application, field schema, factory or JNI dependency is changed; existing ordered provider metadata is checked by the imported regression and recursive corpus snapshots. |
| 41 | App `desc` uses markdown: bullet list of capabilities, links to project website, business-language explanation of value | N/A | No DataProvider action, application, field schema, factory or JNI dependency is changed; existing ordered provider metadata is checked by the imported regression and recursive corpus snapshots. |
| 42 | `display_name` is user-friendly ("Apache Avro" not "avro") | N/A | No DataProvider action, application, field schema, factory or JNI dependency is changed; existing ordered provider metadata is checked by the imported regression and recursive corpus snapshots. |
| 43 | `short_desc` is plain text, under 80 chars, single sentence — no markdown | N/A | No DataProvider action, application, field schema, factory or JNI dependency is changed; existing ordered provider metadata is checked by the imported regression and recursive corpus snapshots. |
| 44 | `desc` uses markdown: backticks for code/field refs (`` `field_name` ``, `` `True` ``, `` `pdf` ``), `\n\n` for paragraphs, `- ` bullet lists for enumerations, `**bold**` for caveats | N/A | No DataProvider action, application, field schema, factory or JNI dependency is changed; existing ordered provider metadata is checked by the imported regression and recursive corpus snapshots. |
| 45 | Descriptions use plain business language relating to common challenges — not just technical "what" but "why" and "when to use" | N/A | No DataProvider action, application, field schema, factory or JNI dependency is changed; existing ordered provider metadata is checked by the imported regression and recursive corpus snapshots. |
| 46 | No bare `True`/`False`/`NOTHING` — must be backtick-wrapped in `desc` | N/A | No DataProvider action, application, field schema, factory or JNI dependency is changed; existing ordered provider metadata is checked by the imported regression and recursive corpus snapshots. |
| 47 | No bare field/option names in prose — must use backticks | N/A | No DataProvider action, application, field schema, factory or JNI dependency is changed; existing ordered provider metadata is checked by the imported regression and recursive corpus snapshots. |
| 48 | Long descriptions (>500 chars) use bold section headers and bullet lists | N/A | No DataProvider action, application, field schema, factory or JNI dependency is changed; existing ordered provider metadata is checked by the imported regression and recursive corpus snapshots. |
| 49 | **Factory registration in Qore repo**: every factory name registered in `qlib/DataProvider/DataProvider.qc` → `FactoryMap` (without this, module loads but doesn't appear in Qorus apps) | N/A | No DataProvider action, application, field schema, factory or JNI dependency is changed; existing ordered provider metadata is checked by the imported regression and recursive corpus snapshots. |
| 50 | **`getRecordTypeImpl()` signature**: must be `private *hash<string, AbstractDataField> getRecordTypeImpl(*hash<auto> search_options)` — NOT returning `*AbstractDataProviderType` | N/A | No DataProvider action, application, field schema, factory or JNI dependency is changed; existing ordered provider metadata is checked by the imported regression and recursive corpus snapshots. |
| 51 | **Dependency JARs committed** (for JNI modules): JAR files in `qlib/*/jar/` may be gitignored — use `git add -f` to ensure they're tracked, otherwise CI compilation fails | N/A | No DataProvider action, application, field schema, factory or JNI dependency is changed; existing ordered provider metadata is checked by the imported regression and recursive corpus snapshots. |
| 52 | JAR install rules in CMakeLists.txt for all dependency JARs | N/A | No DataProvider action, application, field schema, factory or JNI dependency is changed; existing ordered provider metadata is checked by the imported regression and recursive corpus snapshots. |
| 53 | **No workarounds**: No TODOs, FIXMEs, stubs, or partially-implemented features | Pass | The authoritative particle graph determines order; no choice_index mirror or fixture-name branch is added. Required empty records reach normal validation. Independent whitespace schema defects are recorded below for the immediately following increment, as allowed by the execution prompt. |
| 54 | **Exception safety**: C++ uses `ReferenceHolder` for Qore allocations, `std::unique_ptr` for C++ allocations, `*xsink` checked after every fallible operation | Pass | Source conversion owns its fresh parse result until successful return; namespace maps remain local copy-on-write values. Decode builds a local hash and publishes no partial result on conversion failure. Existing cancellation/reconstruction tests pass in all execution modes. |
| 55 | **Thread safety**: All mutable shared state protected by `std::lock_guard<std::mutex>` or documented as immutable-after-construction | Pass | Changes add no shared mutable state. Source normalization precedes schema publication; decode only reads completed declarations and uses local maps. |
| 56 | **Type safety**: Strongly-typed `code<return(args)>` instead of untyped `code`; `static_cast` instead of C casts; typed hashdecls for results; enums where appropriate | Pass | Reference<auto> preserves the XML sum-type contract; namespace, identity, field-presence and occurrence maps remain typed. New test helpers have concrete parameter and return types. |
| 57 | **Performance**: No O(n²) where O(n) is possible; no unnecessary copies; coordinate descent uses incremental residuals not full matrix multiply | Pass | Normalization rebuilds only non-compositor hashes that require suffix merging; no regex key helper or per-child full declaration scan is added. Field projection is linear in declarations and supplied fields. Paired Release normalization measurements retain their exact inputs and environment. |
| 58 | **Error handling**: All inputs validated (dimensions, empty data, unfitted models); C++ I/O handles EAGAIN/EINTR if applicable | Pass | Tests enforce missing/duplicate choices, unknown fields, invalid empty XML, restricted choices, required attributes and namespace-aware imports. An explicit empty flat record no longer disappears before required-particle validation. |
| 59 | **Documentation**: Doxygen `@param`, `@return`, `@throw` on all public methods; `@par Example` with realistic business scenarios; `@note` for important caveats | Pass | Release notes and durable particle design describe final behavior and compatibility. The nested ENUM provisioning example executes in the new test. qdx and Doxygen complete without warnings; there is no new public production API. |
| 60 | **QPP flags**: `[flags=CONSTANT]` on methods that never throw; `[flags=RET_VALUE_ONLY]` on methods that throw but have no side effects | N/A | No QPP methods or flags change. |
| 61 | **Security**: No user-controlled format strings; no buffer overflows; bounds checking on array indices; no credentials in code | Pass | Format strings are fixed; fixture data contains no credentials. Names originate in parsed XML, and string position results are checked before slicing. Snapshot exceptions preserve error categories and cancellation. |
| 62 | **Correctness**: Algorithms verified against reference implementations; edge cases tested (empty data, single sample, all-zero features) | Pass | The original 34-assertion regression passes with equivalent particle diagnostics and the existing attribute metadata field. The new suite passes 284 assertions, including both bindings/directions, standalone/imported schemas and reconstruction. All 128 suites and 13 supplements pass; the 1000-model oracle has 15132 rows. Corpus outcomes are unchanged. |

Result: {'N/A': 43, 'Pass': 19}, 0 Fail.
