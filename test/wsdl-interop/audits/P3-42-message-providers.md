# P3-42 audit: complete message providers

Copyright (C) 2026 Qore Technologies, s.r.o.

The full audit-changes skill and its module-structure, sandboxing, cancellation,
DataProvider checklist and development guides were applied to the final changes.
All 62 checks are resolved: **20 Pass / 42 N/A / 0 Fail**.

Review corrected component reconstruction, removed-field projections and direct
construction of the message adapter. Additional tests verify private metadata
shape, shared declarations, namespace ownership and interruption recovery.
The exact test/source/artifact manifest is `/tmp/wsdl-p3-42-final-manifest.json`;
see [verification evidence](../message-providers-evidence.md).

| Check | Status | Evidence |
| --- | --- | --- |
| 1. Entry exists in `doxygen/lang/120_modules.dox.tmpl` (for modules in the Qore repo; N/A for external module repos) | N/A | No new module, separated source file, QPP class, layout or module registration. |
| 2. Entry exists in `doxygen/lang/900_release_notes.dox.tmpl` (for modules in the Qore repo; external modules have release notes in their .qm) | Pass | WSDL 0.5.8 release notes describe instance-aware message providers and retained copies/reconstruction. |
| 3. `qore_user_module()` or `qore_external_user_module()` call in `CMakeLists.txt` | Pass | Existing external WSDL registration remains correct; docs-module completes without warnings or errors. |
| 4. Module added to QMOD list in `CMakeLists.txt` | N/A | No new module, separated source file, QPP class, layout or module registration. |
| 5. `.qm` file has `@section <lowercasemodname>intro` as first doc section — **must be all lowercase** (e.g., `avrodataproviderintro`, not `AvroDataProviderintro`) | N/A | No new module, separated source file, QPP class, layout or module registration. |
| 6. `%modern` in `.qm` file — no redundant `%new-style`, `%require-types`, `%strict-args`, `%enable-all-warnings` | Pass | Existing WSDL and the new executable qtest use %modern, with no redundant directives added. |
| 7. No parse directives (`%requires`, `%modern`, `%new-style`) in separated `.qc` files (check OUTSIDE of `@code` blocks only) | N/A | No new module, separated source file, QPP class, layout or module registration. |
| 8. No `%include` usage (deprecated for modules) | Pass | No %include directive introduced. |
| 9. Copyright 2026 on all new files | Pass | New test, design, evidence and audit carry 2026 notices; the WSDL copyright already includes 2026. |
| 10. Directory layout: `.qm` inside `qlib/<ModuleName>/` directory (not at `qlib/<ModuleName>.qm` for multi-file modules) | N/A | No new module, separated source file, QPP class, layout or module registration. |
| 11. No second `.qm` for the same module at `qlib/<ModuleName>.qm` | N/A | No new module, separated source file, QPP class, layout or module registration. |
| 12. `ns=Qore::XX` matches the QoreNamespace constructor path | N/A | No new module, separated source file, QPP class, layout or module registration. |
| 13. `%modern` directive present | Pass | The new qtest explicitly uses %modern; reused Python workers are unchanged. |
| 14. Executable permission set (`chmod +x`) | Pass | test/wsdl-message-providers.qtest has executable permissions. |
| 15. Uses %prepend-module-path  before %requires for in-repo modules (Qore and Qore modules only; not Qorus) | Pass | The qtest prepends ../qlib and requires ../qlib/WSDL.qm; local xml and the frozen local DataProvider are selected by the recorded environment. |
| 16. External module dependencies use `%try-module` — except modules delivered with the project itself (Qore ex: DataProvider, ConnectionProvider, QUnit, etc.) which use hard `%requires` | Pass | Project xml and core QUnit use hard requirements. Reused interop workers retain explicit guarded external-json requirements. |
| 17. No filesystem operations (fopen, open, creat, unlink, remove, rename, mkdir, rmdir, stat, chmod) without sandbox checks | N/A | No C++/QPP, native I/O, native loop or native method flags changed. The native module hash is unchanged; no new Valgrind run is required. |
| 18. No network operations (connect, bind, socket, getaddrinfo, gethostbyname) without sandbox checks | N/A | No C++/QPP, native I/O, native loop or native method flags changed. The native module hash is unchanged; no new Valgrind run is required. |
| 19. If filesystem/network ops exist, verify `QoreSandboxManagerHelper` usage | N/A | No C++/QPP, native I/O, native loop or native method flags changed. The native module hash is unchanged; no new Valgrind run is required. |
| 20. No `File::`, `Dir::`, `Socket::`, `HTTPClient::` usage without justification | Pass | No production I/O added. The new tests use embedded offline WSDLs, bounded Queue/Counter synchronization and Program-scoped interruption; the documented example reads a caller-supplied local WSDL. |
| 21. All `for`/`while` loops that could iterate >100 times have `qore_check_cancel()` checks | N/A | No C++/QPP, native I/O, native loop or native method flags changed. The native module hash is unchanged; no new Valgrind run is required. |
| 22. Uses `qore_check_cancel()` (NOT deprecated `qore_check_io_interrupt()`) | N/A | No C++/QPP, native I/O, native loop or native method flags changed. The native module hash is unchanged; no new Valgrind run is required. |
| 23. Check frequency: every 100 iterations for tight loops, every 10 for expensive iterations | N/A | No C++/QPP, native I/O, native loop or native method flags changed. The native module hash is unchanged; no new Valgrind run is required. |
| 24. No blocking operations without cancellation support | N/A | No C++/QPP, native I/O, native loop or native method flags changed. The native module hash is unchanged; no new Valgrind run is required. |
| 25. Every action has `display_name`, `short_desc` (plain text, <80 chars), `desc` (markdown) | N/A | This is a schema-generated datatype adapter, with dynamic WSDL fields. It adds no application/action/factory registration, fixed business request/response catalog, credential field or JNI dependency. |
| 26. Every action has `options` populated via `getActionOptionFromFields()` — without this, the action shows an empty, unusable form | N/A | This is a schema-generated datatype adapter, with dynamic WSDL fields. It adds no application/action/factory registration, fixed business request/response catalog, credential field or JNI dependency. |
| 27. Every action has `output_type` set to a typed data type constant (e.g., `MyResponseDataType`) — not omitted | N/A | This is a schema-generated datatype adapter, with dynamic WSDL fields. It adds no application/action/factory registration, fixed business request/response catalog, credential field or JNI dependency. |
| 28. DPAT_API actions: provider has `"supports_request": True` and implements `doRequestImpl()` | N/A | This is a schema-generated datatype adapter, with dynamic WSDL fields. It adds no application/action/factory registration, fixed business request/response catalog, credential field or JNI dependency. |
| 29. DPAT_FIND actions: every option exists in `SearchOptions`, `getRecordTypeImpl()` returns `*hash<string, AbstractDataField>` | N/A | This is a schema-generated datatype adapter, with dynamic WSDL fields. It adds no application/action/factory registration, fixed business request/response catalog, credential field or JNI dependency. |
| 30. Scheme-based apps (with `"scheme"` in registerApp): actions use `"path"` and do NOT use `"cls"` — having both `scheme` and `cls` causes a runtime error | N/A | This is a schema-generated datatype adapter, with dynamic WSDL fields. It adds no application/action/factory registration, fixed business request/response catalog, credential field or JNI dependency. |
| 31. Single-key hash slices use trailing comma: `Fields{"key",}` (without trailing comma, `Fields{"key"}` returns the value, not a hash) | N/A | This is a schema-generated datatype adapter, with dynamic WSDL fields. It adds no application/action/factory registration, fixed business request/response catalog, credential field or JNI dependency. |
| 32. **Typed data type classes exist** for request and response types — inherit `HashDataType`, have `const Fields` hash, call `addQoreFields(Fields)` in constructor, export public constant at bottom (e.g., `public const MyDataType = new MyDataType();`) | N/A | This is a schema-generated datatype adapter, with dynamic WSDL fields. It adds no application/action/factory registration, fixed business request/response catalog, credential field or JNI dependency. |
| 33. Request/input types use `public` Fields (enables `ClassName::Fields` in action registration) | N/A | This is a schema-generated datatype adapter, with dynamic WSDL fields. It adds no application/action/factory registration, fixed business request/response catalog, credential field or JNI dependency. |
| 34. Response/output types use `private` Fields | N/A | This is a schema-generated datatype adapter, with dynamic WSDL fields. It adds no application/action/factory registration, fixed business request/response catalog, credential field or JNI dependency. |
| 35. Each field in data types has `display_name`, `type`, and `desc` (markdown-formatted) | N/A | This is a schema-generated datatype adapter, with dynamic WSDL fields. It adds no application/action/factory registration, fixed business request/response catalog, credential field or JNI dependency. |
| 36. Input fields have `example_value` where useful (string fields, endpoint URIs, SQL queries, etc.) | N/A | This is a schema-generated datatype adapter, with dynamic WSDL fields. It adds no application/action/factory registration, fixed business request/response catalog, credential field or JNI dependency. |
| 37. Fields with finite allowed values use `allowed_values` with `AllowedValueInfo` containing both `value` and `display_name` (Title Case, human-readable) — never bare values, never described only in text | Pass | Finite choices still pass through XsdDataFieldHelper and existing typed field implementations. The complete union/QName/facet/provider regression suites pass. |
| 38. Password/secret fields have `"sensitive": True` | N/A | This is a schema-generated datatype adapter, with dynamic WSDL fields. It adds no application/action/factory registration, fixed business request/response catalog, credential field or JNI dependency. |
| 39. `groups` uses `AppGroup` enum values from `qlib/DataProvider/AppGroup.qc` | N/A | This is a schema-generated datatype adapter, with dynamic WSDL fields. It adds no application/action/factory registration, fixed business request/response catalog, credential field or JNI dependency. |
| 40. App `logo` stored as separate file, loaded at module level in `Priv` namespace | N/A | This is a schema-generated datatype adapter, with dynamic WSDL fields. It adds no application/action/factory registration, fixed business request/response catalog, credential field or JNI dependency. |
| 41. App `desc` uses markdown: bullet list of capabilities, links to project website, business-language explanation of value | N/A | This is a schema-generated datatype adapter, with dynamic WSDL fields. It adds no application/action/factory registration, fixed business request/response catalog, credential field or JNI dependency. |
| 42. `display_name` is user-friendly ("Apache Avro" not "avro") | N/A | This is a schema-generated datatype adapter, with dynamic WSDL fields. It adds no application/action/factory registration, fixed business request/response catalog, credential field or JNI dependency. |
| 43. `short_desc` is plain text, under 80 chars, single sentence — no markdown | N/A | This is a schema-generated datatype adapter, with dynamic WSDL fields. It adds no application/action/factory registration, fixed business request/response catalog, credential field or JNI dependency. |
| 44. `desc` uses markdown: backticks for code/field refs (`` `field_name` ``, `` `True` ``, `` `pdf` ``), `\n\n` for paragraphs, `- ` bullet lists for enumerations, `**bold**` for caveats | N/A | This is a schema-generated datatype adapter, with dynamic WSDL fields. It adds no application/action/factory registration, fixed business request/response catalog, credential field or JNI dependency. |
| 45. Descriptions use plain business language relating to common challenges — not just technical "what" but "why" and "when to use" | N/A | This is a schema-generated datatype adapter, with dynamic WSDL fields. It adds no application/action/factory registration, fixed business request/response catalog, credential field or JNI dependency. |
| 46. No bare `True`/`False`/`NOTHING` — must be backtick-wrapped in `desc` | N/A | This is a schema-generated datatype adapter, with dynamic WSDL fields. It adds no application/action/factory registration, fixed business request/response catalog, credential field or JNI dependency. |
| 47. No bare field/option names in prose — must use backticks | N/A | This is a schema-generated datatype adapter, with dynamic WSDL fields. It adds no application/action/factory registration, fixed business request/response catalog, credential field or JNI dependency. |
| 48. Long descriptions (>500 chars) use bold section headers and bullet lists | N/A | This is a schema-generated datatype adapter, with dynamic WSDL fields. It adds no application/action/factory registration, fixed business request/response catalog, credential field or JNI dependency. |
| 49. **Factory registration in Qore repo**: every factory name registered in `qlib/DataProvider/DataProvider.qc` → `FactoryMap` (without this, module loads but doesn't appear in Qorus apps) | N/A | This is a schema-generated datatype adapter, with dynamic WSDL fields. It adds no application/action/factory registration, fixed business request/response catalog, credential field or JNI dependency. |
| 50. **`getRecordTypeImpl()` signature**: must be `private *hash<string, AbstractDataField> getRecordTypeImpl(*hash<auto> search_options)` — NOT returning `*AbstractDataProviderType` | N/A | This is a schema-generated datatype adapter, with dynamic WSDL fields. It adds no application/action/factory registration, fixed business request/response catalog, credential field or JNI dependency. |
| 51. **Dependency JARs committed** (for JNI modules): JAR files in `qlib/*/jar/` may be gitignored — use `git add -f` to ensure they're tracked, otherwise CI compilation fails | N/A | This is a schema-generated datatype adapter, with dynamic WSDL fields. It adds no application/action/factory registration, fixed business request/response catalog, credential field or JNI dependency. |
| 52. JAR install rules in CMakeLists.txt for all dependency JARs | N/A | This is a schema-generated datatype adapter, with dynamic WSDL fields. It adds no application/action/factory registration, fixed business request/response catalog, credential field or JNI dependency. |
| 53. **No workarounds**: No TODOs, FIXMEs, stubs, or partially-implemented features | Pass | Message providers reuse schema instance conversion after field conversion. No global mode, changed union fallback, declaration invention, fixture special case, stub or TODO added. Native sample generation and the already assigned P4/P6/P7 failures remain explicit separate work. |
| 54. **Exception safety**: C++ uses `ReferenceHolder` for Qore allocations, `std::unique_ptr` for C++ allocations, `*xsink` checked after every fallible operation | Pass | Local namespace copies and XsdQNameOutputScope restore state on success and exceptions. Returned immutable values keep caller ownership. Tests cover controlled schema exceptions, THREAD-CANCELLED, actual program interruption, reconstruction failures and subsequent reuse. |
| 55. **Thread safety**: All mutable shared state protected by `std::lock_guard<std::mutex>` or documented as immutable-after-construction | Pass | Providers retain schema components that must be finalized before sharing, as documented. Per-call namespace allocation is private. Four synchronized threads check shared-provider QName identity; existing capture concurrency tests also pass. |
| 56. **Type safety**: Strongly-typed `code<return(args)>` instead of untyped `code`; `static_cast` instead of C casts; typed hashdecls for results; enums where appropriate | Pass | Part metadata uses hash<ArgInfo>; fields, namespace contexts and reconstructed particle nodes have concrete types. Temporary wire containers are explicitly converted to the private recursive hashdecl. Test closures have declared types. |
| 57. **Performance**: No O(n²) where O(n) is possible; no unnecessary copies; coordinate descent uses incremental residuals not full matrix multiply | Pass | One schema conversion per retained supplied part follows field conversion; the adapter does not emit a SOAP message. Namespace state is copied once per call. Reconstruction preserves shared component identity. Particle metadata traversal is linear in its tree; a schema callback count confirms one invocation. |
| 58. **Error handling**: All inputs validated (dimensions, empty data, unfitted models); C++ I/O handles EAGAIN/EINTR if applicable | Pass | Missing/invalid metadata, invalid ENTITY selections and mandatory fields are rejected in the intended categories. Optional/soft/projection/container behavior, default QName identity and valid later datatype alternatives pass. Schema callback/control errors propagate and recovery succeeds. |
| 59. **Documentation**: Doxygen `@param`, `@return`, `@throw` on all public methods; `@par Example` with realistic business scenarios; `@note` for important caveats | Pass | All new public methods document arguments/results and applicable failures. The durable design defines ownership, copy/projection behavior and reconstruction with a tested catalog example. README, release notes, evidence and execution record are updated; Doxygen passes. |
| 60. **QPP flags**: `[flags=CONSTANT]` on methods that never throw; `[flags=RET_VALUE_ONLY]` on methods that throw but have no side effects | N/A | No C++/QPP, native I/O, native loop or native method flags changed. The native module hash is unchanged; no new Valgrind run is required. |
| 61. **Security**: No user-controlled format strings; no buffer overflows; bounds checking on array indices; no credentials in code | Pass | No new credentials, raw buffers, user-controlled format strings or external resource access. Metadata types and flags are checked. SOAP declaration restrictions are retained. |
| 62. **Correctness**: Algorithms verified against reference implementations; edge cases tested (empty data, single sample, all-zero features) | Pass | Final verification covers 93 suites, 957 cases and 36,372 assertions, with the new 9-case/226-assertion suite passing AST/IR/JIT/tiered. The independent matrix passes 12,096 outcomes and 8,280 document verdicts. All 2,411 raw rows, 293 coverage cases and strict results are unchanged. The two known P6 assertions remain failures. |
