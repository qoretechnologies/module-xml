# P5-13 native wildcard types and schema whitespace audit

Copyright (C) 2026 Qore Technologies, s.r.o.

Audited on 2026-09-12 against parent `5c283bf` with the entire audit-changes
skill and all five referenced guides. The scope includes both native CMake
corrections/probes, the phase-specific allocation regression, WSDL declaration
normalization and annotation metadata, and their tests/documentation.
No production allocation change or main-Qore edit is included.

All 62 checks are resolved below. Final staging replaced four literal trailing
spaces in the whitespace test with equivalent escaped string fragments, preserving
its XML bytes. Both final source and AOT runs pass all five cases/85 assertions;
production source and native binaries are unchanged from the complete gates. [Validation inventory](../P5-13-validation.json)
and [evidence](../native-wildcard-types-evidence.md) record exact source/runtime
hashes, commands and test outcomes. All required local checks pass; CI execution
and the remaining P5-P9 work are not claimed complete. Nothing was installed or
pushed. Existing test/cmake/__pycache__ is outside this change.

| # | Check | Status | Evidence |
| --- | --- | --- | --- |
| 1 | Entry exists in `doxygen/lang/120_modules.dox.tmpl` (for modules in the Qore repo; N/A for external module repos) | N/A | No new module, separated class file, module registration or directory layout. The existing WSDL module remains registered and its AOT artifact was rebuilt. |
| 2 | Entry exists in `doxygen/lang/900_release_notes.dox.tmpl` (for modules in the Qore repo; external modules have release notes in their .qm) | Pass | WSDL 0.5.8 and native xml 2.3.0 release notes describe declaration text, preserved annotations and strict instance-type assessment; implemented design and shipment example are included. |
| 3 | `qore_user_module()` or `qore_external_user_module()` call in `CMakeLists.txt` | N/A | No new module, separated class file, module registration or directory layout. The existing WSDL module remains registered and its AOT artifact was rebuilt. |
| 4 | Module added to QMOD list in `CMakeLists.txt` | N/A | No new module, separated class file, module registration or directory layout. The existing WSDL module remains registered and its AOT artifact was rebuilt. |
| 5 | `.qm` file has `@section <lowercasemodname>intro` as first doc section — **must be all lowercase** (e.g., `avrodataproviderintro`, not `AvroDataProviderintro`) | Pass | Existing single-file WSDL module retains wsdlintro as its first section. |
| 6 | `%modern` in `.qm` file — no redundant `%new-style`, `%require-types`, `%strict-args`, `%enable-all-warnings` | Pass | WSDL.qm retains %modern without redundant strictness directives. |
| 7 | No parse directives (`%requires`, `%modern`, `%new-style`) in separated `.qc` files (check OUTSIDE of `@code` blocks only) | N/A | No new module, separated class file, module registration or directory layout. The existing WSDL module remains registered and its AOT artifact was rebuilt. |
| 8 | No `%include` usage (deprecated for modules) | Pass | No %include directive introduced; native test preprocessor includes are C compile inputs. |
| 9 | Copyright 2026 on all new files | Pass | Every authored new file has Copyright (C) 2026 Qore Technologies, s.r.o.; third-party notices and downloaded source remain unchanged. |
| 10 | Directory layout: `.qm` inside `qlib/<ModuleName>/` directory (not at `qlib/<ModuleName>.qm` for multi-file modules) | N/A | No new module, separated class file, module registration or directory layout. The existing WSDL module remains registered and its AOT artifact was rebuilt. |
| 11 | No second `.qm` for the same module at `qlib/<ModuleName>.qm` | N/A | No new module, separated class file, module registration or directory layout. The existing WSDL module remains registered and its AOT artifact was rebuilt. |
| 12 | `ns=Qore::XX` matches the QoreNamespace constructor path | N/A | No QPP class, native public method or method flags changed. |
| 13 | `%modern` directive present | Pass | Both new qtests and both native/schema workers use %modern. |
| 14 | Executable permission set (`chmod +x`) | Pass | Both qtests, workers and Python matrices are executable (0755). |
| 15 | Uses %prepend-module-path  before %requires for in-repo modules (Qore and Qore modules only; not Qorus) | Pass | New qtests/workers prepend local qlib before requires; WSDL references are relative. AOT supplements explicitly load the rebuilt WSDL.qmod. |
| 16 | External module dependencies use `%try-module` — except modules delivered with the project itself (Qore ex: DataProvider, ConnectionProvider, QUnit, etc.) which use hard `%requires` | Pass | The external json module is guarded by %try-module; xml belongs to this repository and QUnit to core, so those use hard requires. |
| 17 | No filesystem operations (fopen, open, creat, unlink, remove, rename, mkdir, rmdir, stat, chmod) without sandbox checks | Pass | No native production filesystem operation added; CMake writes only checked build-tree copies. File-backed manifests are test inputs. |
| 18 | No network operations (connect, bind, socket, getaddrinfo, gethostbyname) without sandbox checks | Pass | No native network operation added. Provider selection retains the pinned HTTPS archive and integrity verification; integration services are loopback tests. |
| 19 | If filesystem/network ops exist, verify `QoreSandboxManagerHelper` usage | N/A | No native production filesystem/network operation or sandbox-helper boundary added. |
| 20 | No `File::`, `Dir::`, `Socket::`, `HTTPClient::` usage without justification | Pass | Production adapter does no I/O; ReadOnlyFile in workers reads explicit offline test manifests. Existing sandboxed schema/resource APIs are exercised. |
| 21 | All `for`/`while` loops that could iterate >100 times have `qore_check_cancel()` checks | N/A | No C++ loop changed; private libxml2 C is independent of Qore. Existing cleanup traversal gains no new loop, and standalone probe/fault loops are finite tests. Existing Qore cancellation boundaries are verified. |
| 22 | Uses `qore_check_cancel()` (NOT deprecated `qore_check_io_interrupt()`) | N/A | No C++ loop changed; private libxml2 C is independent of Qore. Existing cleanup traversal gains no new loop, and standalone probe/fault loops are finite tests. Existing Qore cancellation boundaries are verified. |
| 23 | Check frequency: every 100 iterations for tight loops, every 10 for expensive iterations | N/A | No C++ loop changed; private libxml2 C is independent of Qore. Existing cleanup traversal gains no new loop, and standalone probe/fault loops are finite tests. Existing Qore cancellation boundaries are verified. |
| 24 | No blocking operations without cancellation support | Pass | No blocking operation added. Native cancellation, error recovery and concurrent readers pass; HTTP consumers use bounded deterministic completion. |
| 25 | Every action has `display_name`, `short_desc` (plain text, <80 chars), `desc` (markdown) | N/A | No DataProvider action/app/datatype/factory registrations or JNI dependencies changed. Existing provider metadata, saved graphs and HTTP consumers are verified in source and AOT runs. |
| 26 | Every action has `options` populated via `getActionOptionFromFields()` — without this, the action shows an empty, unusable form | N/A | No DataProvider action/app/datatype/factory registrations or JNI dependencies changed. Existing provider metadata, saved graphs and HTTP consumers are verified in source and AOT runs. |
| 27 | Every action has `output_type` set to a typed data type constant (e.g., `MyResponseDataType`) — not omitted | N/A | No DataProvider action/app/datatype/factory registrations or JNI dependencies changed. Existing provider metadata, saved graphs and HTTP consumers are verified in source and AOT runs. |
| 28 | DPAT_API actions: provider has `"supports_request": True` and implements `doRequestImpl()` | N/A | No DataProvider action/app/datatype/factory registrations or JNI dependencies changed. Existing provider metadata, saved graphs and HTTP consumers are verified in source and AOT runs. |
| 29 | DPAT_FIND actions: every option exists in `SearchOptions`, `getRecordTypeImpl()` returns `*hash<string, AbstractDataField>` | N/A | No DataProvider action/app/datatype/factory registrations or JNI dependencies changed. Existing provider metadata, saved graphs and HTTP consumers are verified in source and AOT runs. |
| 30 | Scheme-based apps (with `"scheme"` in registerApp): actions use `"path"` and do NOT use `"cls"` — having both `scheme` and `cls` causes a runtime error | N/A | No DataProvider action/app/datatype/factory registrations or JNI dependencies changed. Existing provider metadata, saved graphs and HTTP consumers are verified in source and AOT runs. |
| 31 | Single-key hash slices use trailing comma: `Fields{"key",}` (without trailing comma, `Fields{"key"}` returns the value, not a hash) | N/A | No DataProvider action/app/datatype/factory registrations or JNI dependencies changed. Existing provider metadata, saved graphs and HTTP consumers are verified in source and AOT runs. |
| 32 | **Typed data type classes exist** for request and response types — inherit `HashDataType`, have `const Fields` hash, call `addQoreFields(Fields)` in constructor, export public constant at bottom (e.g., `public const MyDataType = new MyDataType();`) | N/A | No DataProvider action/app/datatype/factory registrations or JNI dependencies changed. Existing provider metadata, saved graphs and HTTP consumers are verified in source and AOT runs. |
| 33 | Request/input types use `public` Fields (enables `ClassName::Fields` in action registration) | N/A | No DataProvider action/app/datatype/factory registrations or JNI dependencies changed. Existing provider metadata, saved graphs and HTTP consumers are verified in source and AOT runs. |
| 34 | Response/output types use `private` Fields | N/A | No DataProvider action/app/datatype/factory registrations or JNI dependencies changed. Existing provider metadata, saved graphs and HTTP consumers are verified in source and AOT runs. |
| 35 | Each field in data types has `display_name`, `type`, and `desc` (markdown-formatted) | N/A | No DataProvider action/app/datatype/factory registrations or JNI dependencies changed. Existing provider metadata, saved graphs and HTTP consumers are verified in source and AOT runs. |
| 36 | Input fields have `example_value` where useful (string fields, endpoint URIs, SQL queries, etc.) | N/A | No DataProvider action/app/datatype/factory registrations or JNI dependencies changed. Existing provider metadata, saved graphs and HTTP consumers are verified in source and AOT runs. |
| 37 | Fields with finite allowed values use `allowed_values` with `AllowedValueInfo` containing both `value` and `display_name` (Title Case, human-readable) — never bare values, never described only in text | N/A | No DataProvider action/app/datatype/factory registrations or JNI dependencies changed. Existing provider metadata, saved graphs and HTTP consumers are verified in source and AOT runs. |
| 38 | Password/secret fields have `"sensitive": True` | N/A | No DataProvider action/app/datatype/factory registrations or JNI dependencies changed. Existing provider metadata, saved graphs and HTTP consumers are verified in source and AOT runs. |
| 39 | `groups` uses `AppGroup` enum values from `qlib/DataProvider/AppGroup.qc` | N/A | No DataProvider action/app/datatype/factory registrations or JNI dependencies changed. Existing provider metadata, saved graphs and HTTP consumers are verified in source and AOT runs. |
| 40 | App `logo` stored as separate file, loaded at module level in `Priv` namespace | N/A | No DataProvider action/app/datatype/factory registrations or JNI dependencies changed. Existing provider metadata, saved graphs and HTTP consumers are verified in source and AOT runs. |
| 41 | App `desc` uses markdown: bullet list of capabilities, links to project website, business-language explanation of value | N/A | No DataProvider action/app/datatype/factory registrations or JNI dependencies changed. Existing provider metadata, saved graphs and HTTP consumers are verified in source and AOT runs. |
| 42 | `display_name` is user-friendly ("Apache Avro" not "avro") | N/A | No DataProvider action/app/datatype/factory registrations or JNI dependencies changed. Existing provider metadata, saved graphs and HTTP consumers are verified in source and AOT runs. |
| 43 | `short_desc` is plain text, under 80 chars, single sentence — no markdown | N/A | No DataProvider action/app/datatype/factory registrations or JNI dependencies changed. Existing provider metadata, saved graphs and HTTP consumers are verified in source and AOT runs. |
| 44 | `desc` uses markdown: backticks for code/field refs (`` `field_name` ``, `` `True` ``, `` `pdf` ``), `\n\n` for paragraphs, `- ` bullet lists for enumerations, `**bold**` for caveats | N/A | No DataProvider action/app/datatype/factory registrations or JNI dependencies changed. Existing provider metadata, saved graphs and HTTP consumers are verified in source and AOT runs. |
| 45 | Descriptions use plain business language relating to common challenges — not just technical "what" but "why" and "when to use" | N/A | No DataProvider action/app/datatype/factory registrations or JNI dependencies changed. Existing provider metadata, saved graphs and HTTP consumers are verified in source and AOT runs. |
| 46 | No bare `True`/`False`/`NOTHING` — must be backtick-wrapped in `desc` | N/A | No DataProvider action/app/datatype/factory registrations or JNI dependencies changed. Existing provider metadata, saved graphs and HTTP consumers are verified in source and AOT runs. |
| 47 | No bare field/option names in prose — must use backticks | N/A | No DataProvider action/app/datatype/factory registrations or JNI dependencies changed. Existing provider metadata, saved graphs and HTTP consumers are verified in source and AOT runs. |
| 48 | Long descriptions (>500 chars) use bold section headers and bullet lists | N/A | No DataProvider action/app/datatype/factory registrations or JNI dependencies changed. Existing provider metadata, saved graphs and HTTP consumers are verified in source and AOT runs. |
| 49 | **Factory registration in Qore repo**: every factory name registered in `qlib/DataProvider/DataProvider.qc` → `FactoryMap` (without this, module loads but doesn't appear in Qorus apps) | N/A | No DataProvider action/app/datatype/factory registrations or JNI dependencies changed. Existing provider metadata, saved graphs and HTTP consumers are verified in source and AOT runs. |
| 50 | **`getRecordTypeImpl()` signature**: must be `private *hash<string, AbstractDataField> getRecordTypeImpl(*hash<auto> search_options)` — NOT returning `*AbstractDataProviderType` | N/A | No DataProvider action/app/datatype/factory registrations or JNI dependencies changed. Existing provider metadata, saved graphs and HTTP consumers are verified in source and AOT runs. |
| 51 | **Dependency JARs committed** (for JNI modules): JAR files in `qlib/*/jar/` may be gitignored — use `git add -f` to ensure they're tracked, otherwise CI compilation fails | N/A | No DataProvider action/app/datatype/factory registrations or JNI dependencies changed. Existing provider metadata, saved graphs and HTTP consumers are verified in source and AOT runs. |
| 52 | JAR install rules in CMakeLists.txt for all dependency JARs | N/A | No DataProvider action/app/datatype/factory registrations or JNI dependencies changed. Existing provider metadata, saved graphs and HTTP consumers are verified in source and AOT runs. |
| 53 | **No workarounds**: No TODOs, FIXMEs, stubs, or partially-implemented features | Pass | Both native rejection/cleanup roots and the Qore declaration view are corrected directly. Strict validation remains strict; optional reset assertions are phase-specific. Existing upstream comments in exact replacement context are not new stubs. |
| 54 | **Exception safety**: C++ uses `ReferenceHolder` for Qore allocations, `std::unique_ptr` for C++ allocations, `*xsink` checked after every fallible operation | Pass | Native change introduces no allocation or ownership transfer. Schema cleanup retains whole annotation subtrees; the allocation harness checks 131 faults through private/public paths, baseline live counts and recovery. All affected Valgrind runs have zero errors/lost blocks. |
| 55 | **Thread safety**: All mutable shared state protected by `std::lock_guard<std::mutex>` or documented as immutable-after-construction | Pass | No shared production state added. Native validation contexts remain per call, namespace maps are local copy-on-write values, and concurrent reader/provider/HTTP regressions pass. |
| 56 | **Type safety**: Strongly-typed `code<return(args)>` instead of untyped `code`; `static_cast` instead of C casts; typed hashdecls for results; enums where appropriate | Pass | Qore conversion uses typed references and namespace maps; schema text is type-checked. Allocation phases use an enum; C test records and native schema pointers retain concrete types. |
| 57 | **Performance**: No O(n²) where O(n) is possible; no unnecessary copies; coordinate descent uses incremental residuals not full matrix multiply | Pass | Source normalization retains conditional suffix merging and reference traversal. Whitespace is scanned once; native cleanup skips annotation subtrees. No additional traversal, numeric expansion or global cache is introduced. |
| 58 | **Error handling**: All inputs validated (dimensions, empty data, unfitted models); C++ I/O handles EAGAIN/EINTR if applicable | Pass | Significant declaration text, non-XML whitespace, unbound/unknown types, invalid facets/content/attributes, abstract types and forbidden namespaces reject with the required API errors. Failed schema additions and validation recovery are tested. |
| 59 | **Documentation**: Doxygen `@param`, `@return`, `@throw` on all public methods; `@par Example` with realistic business scenarios; `@note` for important caveats | Pass | No new public API. WSDL/native release notes, README, durable designs, allocation-phase explanation and an executed shipment example document the behavior and diagnostic boundaries; qdx/Doxygen are clean. |
| 60 | **QPP flags**: `[flags=CONSTANT]` on methods that never throw; `[flags=RET_VALUE_ONLY]` on methods that throw but have no side effects | N/A | No QPP class, native public method or method flags changed. |
| 61 | **Security**: No user-controlled format strings; no buffer overflows; bounds checking on array indices; no credentials in code | Pass | Only static test templates are format strings, snprintf results are bounded, source hashes are checked before applying fixes, and no credentials or new resource access are introduced. |
| 62 | **Correctness**: Algorithms verified against reference implementations; edge cases tested (empty data, single sample, all-zero features) | Pass | All 130 suites, 27 mode/AOT/matrix/consumer supplements, 43 provider tests, independent 684-document and 96-schema matrices, Valgrind, documentation and unchanged both-version corpus outcomes pass. Original invalid inputs and validator disagreements remain explicit. |

Result: {'N/A': 40, 'Pass': 22}, 0 Fail.
