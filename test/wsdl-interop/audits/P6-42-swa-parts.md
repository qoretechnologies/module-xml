# P6-42 SOAP attachment part audit

Copyright (C) 2026 Qore Technologies, s.r.o.

Applied `/home/david/.codex/skills/audit-changes/SKILL.md` to the working-tree production modules, tests, fixtures and documentation. Reviewed the applicable module-structure and DataProvider design guidance. No C++ files or main Qore files changed. The provider is a runtime WSDL type, so static app/action catalog requirements do not apply.

Root-cause fixes found during review preserve single-attachment public keys and keep qualified header identities separate from attachment maps. Provider examples now generate binding-compatible raw media, and repeated body membership scans use hash lookup. The original pinned CXF fixture remains unchanged; its separately named derivative removes one invalid binary-media annotation, checked exactly by the independent test.

| # | Check | Status | Evidence |
| --- | --- | --- | --- |
| 1 | Entry exists in `doxygen/lang/120_modules.dox.tmpl` (for modules in the Qore repo; N/A for external module repos) | N/A | Existing external modules and layout; no registration or installation changes. Module and repository release notes are updated. |
| 2 | Entry exists in `doxygen/lang/900_release_notes.dox.tmpl` (for modules in the Qore repo; external modules have release notes in their .qm) | N/A | Existing external modules and layout; no registration or installation changes. Module and repository release notes are updated. |
| 3 | `qore_user_module()` or `qore_external_user_module()` call in `CMakeLists.txt` | N/A | Existing external modules and layout; no registration or installation changes. Module and repository release notes are updated. |
| 4 | Module added to QMOD list in `CMakeLists.txt` | N/A | Existing external modules and layout; no registration or installation changes. Module and repository release notes are updated. |
| 5 | `.qm` file has `@section <lowercasemodname>intro` as first doc section — **must be all lowercase** (e.g., `avrodataproviderintro`, not `AvroDataProviderintro`) | Pass | WSDL, SoapClient, SoapHandler and SoapDataProvider introduction identifiers remain lowercase. |
| 6 | `%modern` in `.qm` file — no redundant `%new-style`, `%require-types`, `%strict-args`, `%enable-all-warnings` | Pass | Changed entry modules retain %modern without redundant parse directives. |
| 7 | No parse directives (`%requires`, `%modern`, `%new-style`) in separated `.qc` files (check OUTSIDE of `@code` blocks only) | Pass | SoapRequestDataProvider.qc contains no parse directives. |
| 8 | No `%include` usage (deprecated for modules) | Pass | No %include introduced. |
| 9 | Copyright 2026 on all new files | Pass | All new Qore, Python, Java, WSDL, JSON and Markdown files contain copyright 2026. |
| 10 | Directory layout: `.qm` inside `qlib/<ModuleName>/` directory (not at `qlib/<ModuleName>.qm` for multi-file modules) | N/A | Existing external modules and layout; no registration or installation changes. Module and repository release notes are updated. |
| 11 | No second `.qm` for the same module at `qlib/<ModuleName>.qm` | N/A | Existing external modules and layout; no registration or installation changes. Module and repository release notes are updated. |
| 12 | `ns=Qore::XX` matches the QoreNamespace constructor path | N/A | No C++ or QPP changes; native sandbox, cancellation, allocation and method-flag checks do not apply. |
| 13 | `%modern` directive present | Pass | The new qtest and Qore peer use %modern. |
| 14 | Executable permission set (`chmod +x`) | Pass | The qtest, Qore peer and Python test runner have executable permission. |
| 15 | Uses %prepend-module-path  before %requires for in-repo modules (Qore and Qore modules only; not Qorus) | Pass | Qore tests prepend local qlib before relative module requirements; validation uses QORE_MODULE_DIR=build-debug:qlib. |
| 16 | External module dependencies use `%try-module` — except modules delivered with the project itself (Qore ex: DataProvider, ConnectionProvider, QUnit, etc.) which use hard `%requires` | Pass | External json uses %try-module; local and core modules use hard requirements. |
| 17 | No filesystem operations (fopen, open, creat, unlink, remove, rename, mkdir, rmdir, stat, chmod) without sandbox checks | N/A | No C++ or QPP changes; native sandbox, cancellation, allocation and method-flag checks do not apply. |
| 18 | No network operations (connect, bind, socket, getaddrinfo, gethostbyname) without sandbox checks | N/A | No C++ or QPP changes; native sandbox, cancellation, allocation and method-flag checks do not apply. |
| 19 | If filesystem/network ops exist, verify `QoreSandboxManagerHelper` usage | N/A | No C++ or QPP changes; native sandbox, cancellation, allocation and method-flag checks do not apply. |
| 20 | No `File::`, `Dir::`, `Socket::`, `HTTPClient::` usage without justification | Pass | No new production I/O primitive. Test file reads and loopback HTTP listeners serve fixtures; readiness/STOP events and bounded completion control teardown. |
| 21 | All `for`/`while` loops that could iterate >100 times have `qore_check_cancel()` checks | N/A | No C++ or QPP changes; native sandbox, cancellation, allocation and method-flag checks do not apply. |
| 22 | Uses `qore_check_cancel()` (NOT deprecated `qore_check_io_interrupt()`) | N/A | No C++ or QPP changes; native sandbox, cancellation, allocation and method-flag checks do not apply. |
| 23 | Check frequency: every 100 iterations for tight loops, every 10 for expensive iterations | N/A | No C++ or QPP changes; native sandbox, cancellation, allocation and method-flag checks do not apply. |
| 24 | No blocking operations without cancellation support | N/A | No C++ or QPP changes; native sandbox, cancellation, allocation and method-flag checks do not apply. |
| 25 | Every action has `display_name`, `short_desc` (plain text, <80 chars), `desc` (markdown) | N/A | Existing dynamic SOAP provider registration and actions are unchanged; no new app, scheme, option form, logo or factory. |
| 26 | Every action has `options` populated via `getActionOptionFromFields()` — without this, the action shows an empty, unusable form | N/A | Existing dynamic SOAP provider registration and actions are unchanged; no new app, scheme, option form, logo or factory. |
| 27 | Every action has `output_type` set to a typed data type constant (e.g., `MyResponseDataType`) — not omitted | N/A | Existing dynamic SOAP provider registration and actions are unchanged; no new app, scheme, option form, logo or factory. |
| 28 | DPAT_API actions: provider has `"supports_request": True` and implements `doRequestImpl()` | N/A | Existing dynamic SOAP provider registration and actions are unchanged; no new app, scheme, option form, logo or factory. |
| 29 | DPAT_FIND actions: every option exists in `SearchOptions`, `getRecordTypeImpl()` returns `*hash<string, AbstractDataField>` | N/A | Existing dynamic SOAP provider registration and actions are unchanged; no new app, scheme, option form, logo or factory. |
| 30 | Scheme-based apps (with `"scheme"` in registerApp): actions use `"path"` and do NOT use `"cls"` — having both `scheme` and `cls` causes a runtime error | N/A | Existing dynamic SOAP provider registration and actions are unchanged; no new app, scheme, option form, logo or factory. |
| 31 | Single-key hash slices use trailing comma: `Fields{"key",}` (without trailing comma, `Fields{"key"}` returns the value, not a hash) | N/A | Existing dynamic SOAP provider registration and actions are unchanged; no new app, scheme, option form, logo or factory. |
| 32 | **Typed data type classes exist** for request and response types — inherit `HashDataType`, have `const Fields` hash, call `addQoreFields(Fields)` in constructor, export public constant at bottom (e.g., `public const MyDataType = new MyDataType();`) | Pass | WsdlSoapMimeDataType inherits HashDataType and describes requests/responses from the selected binding. Fields depend on runtime WSDL declarations, so a static Fields constant or singleton would be incorrect; the constructor adds typed fields. |
| 33 | Request/input types use `public` Fields (enables `ClassName::Fields` in action registration) | N/A | No static action input/output Fields declarations; binding fields are dynamic per WSDL operation. |
| 34 | Response/output types use `private` Fields | N/A | No static action input/output Fields declarations; binding fields are dynamic per WSDL operation. |
| 35 | Each field in data types has `display_name`, `type`, and `desc` (markdown-formatted) | Pass | New body/header/attachment fields supply display_name, typed providers and Markdown desc. Body/header schema fields retain their existing metadata. |
| 36 | Input fields have `example_value` where useful (string fields, endpoint URIs, SQL queries, etc.) | Pass | Binding-aware getExampleValue generates schema values and raw attachment bytes, chooses concrete media alternatives, validates the complete example, and explicitly rejects wildcard-only examples. Saved/soft examples are exercised. |
| 37 | Fields with finite allowed values use `allowed_values` with `AllowedValueInfo` containing both `value` and `display_name` (Title Case, human-readable) — never bare values, never described only in text | N/A | Raw media values and schema XML are not finite enumerations; this increment adds no finite option field. |
| 38 | Password/secret fields have `"sensitive": True` | N/A | No credential or secret fields. |
| 39 | `groups` uses `AppGroup` enum values from `qlib/DataProvider/AppGroup.qc` | N/A | Existing dynamic SOAP provider registration and actions are unchanged; no new app, scheme, option form, logo or factory. |
| 40 | App `logo` stored as separate file, loaded at module level in `Priv` namespace | N/A | Existing dynamic SOAP provider registration and actions are unchanged; no new app, scheme, option form, logo or factory. |
| 41 | App `desc` uses markdown: bullet list of capabilities, links to project website, business-language explanation of value | N/A | Existing dynamic SOAP provider registration and actions are unchanged; no new app, scheme, option form, logo or factory. |
| 42 | `display_name` is user-friendly ("Apache Avro" not "avro") | N/A | Existing dynamic SOAP provider registration and actions are unchanged; no new app, scheme, option form, logo or factory. |
| 43 | `short_desc` is plain text, under 80 chars, single sentence — no markdown | N/A | Existing dynamic SOAP provider registration and actions are unchanged; no new app, scheme, option form, logo or factory. |
| 44 | `desc` uses markdown: backticks for code/field refs (`` `field_name` ``, `` `True` ``, `` `pdf` ``), `\n\n` for paragraphs, `- ` bullet lists for enumerations, `**bold**` for caveats | Pass | New descriptions mark selector/field references with backticks; descriptions are short and require no paragraph/list structure. |
| 45 | Descriptions use plain business language relating to common challenges — not just technical "what" but "why" and "when to use" | Pass | Descriptions distinguish raw upload data from schema XML and explain when media selection is needed. |
| 46 | No bare `True`/`False`/`NOTHING` — must be backtick-wrapped in `desc` | Pass | New field descriptions contain no bare boolean/NOTHING literals. |
| 47 | No bare field/option names in prose — must use backticks | Pass | New selector and field names use backticks in provider descriptions. |
| 48 | Long descriptions (>500 chars) use bold section headers and bullet lists | N/A | No new field description exceeds 500 characters. |
| 49 | **Factory registration in Qore repo**: every factory name registered in `qlib/DataProvider/DataProvider.qc` → `FactoryMap` (without this, module loads but doesn't appear in Qorus apps) | N/A | Existing dynamic SOAP provider registration and actions are unchanged; no new app, scheme, option form, logo or factory. |
| 50 | **`getRecordTypeImpl()` signature**: must be `private *hash<string, AbstractDataField> getRecordTypeImpl(*hash<auto> search_options)` — NOT returning `*AbstractDataProviderType` | Pass | Existing getRecordTypeImpl retains private *hash<string, AbstractDataField> getRecordTypeImpl(*hash<auto> search_options). |
| 51 | **Dependency JARs committed** (for JNI modules): JAR files in `qlib/*/jar/` may be gitignored — use `git add -f` to ensure they're tracked, otherwise CI compilation fails | N/A | No dependency additions; the independent CXF runner verifies the already committed pinned JAR inventory and compiles with Java 17 and -Xlint:all -Werror. |
| 52 | JAR install rules in CMakeLists.txt for all dependency JARs | N/A | No dependency additions; the independent CXF runner verifies the already committed pinned JAR inventory and compiles with Java 17 and -Xlint:all -Werror. |
| 53 | **No workarounds**: No TODOs, FIXMEs, stubs, or partially-implemented features | Pass | Attachment parts now serialize independently of body selection. Element/type media behavior follows WS-I AP, with no fixture fallback or abstract-type coercion. Shared XML serialization replaces duplicate logic. General nested MIME layouts and reference protocols remain separate plan requirements, not stubs in this codec. |
| 54 | **Exception safety**: C++ uses `ReferenceHolder` for Qore allocations, `std::unique_ptr` for C++ allocations, `*xsink` checked after every fallible operation | Pass | Results, namespace copies and MIME entities stay call-local until complete; exceptions cannot publish partial messages. Provider validation preserves input values and rethrows cancellation. Negative HTTP tests verify recovery and listener cleanup. |
| 55 | **Thread safety**: All mutable shared state protected by `std::lock_guard<std::mutex>` or documented as immutable-after-construction | Pass | No mutable state is added to published operation/schema graphs. Provider construction is local; call-local encoding/selection is exercised concurrently across three charsets. |
| 56 | **Type safety**: Strongly-typed `code<return(args)>` instead of untyped `code`; `static_cast` instead of C casts; typed hashdecls for results; enums where appropriate | Pass | Typed Mime::MessageInfo entities, part maps, WSOperation/SoapBinding references and callbacks. Generic values are intentional for raw bytes, XML and explicit wrappers. Native and retained XML APIs preserve distinct value contracts. |
| 57 | **Performance**: No O(n²) where O(n) is possible; no unnecessary copies; coordinate descent uses incremental residuals not full matrix multiply | Pass | Part/CID association uses hash lookup. Audit replaced repeated body-part list scans with field lookup. Header and attachment walks are linear. Provider validation intentionally adds one serialization pass without I/O, documented in the durable design. |
| 58 | **Error handling**: All inputs validated (dimensions, empty data, unfitted models); C++ I/O handles EAGAIN/EINTR if applicable | Pass | Required/missing/empty media, malformed/duplicate identifiers, incompatible media, XML roots, unknown explicit values and ambiguous selections are tested. Audit fixed single-attachment flattening and qualified-header placement; both SOAP versions, saved/detached graphs, provider examples and negative recovery pass. |
| 59 | **Documentation**: Doxygen `@param`, `@return`, `@throw` on all public methods; `@par Example` with realistic business scenarios; `@note` for important caveats | Pass | Public methods include parameters, returns where applicable, errors and examples/caveats. Module and repository release notes, provider examples and durable design are updated. Documentation gate checks warnings. No DGC behavior changed and this repository has no design/dgc.md. |
| 60 | **QPP flags**: `[flags=CONSTANT]` on methods that never throw; `[flags=RET_VALUE_ONLY]` on methods that throw but have no side effects | N/A | No C++ or QPP changes; native sandbox, cancellation, allocation and method-flag checks do not apply. |
| 61 | **Security**: No user-controlled format strings; no buffer overflows; bounds checking on array indices; no credentials in code | Pass | Constant format strings, bounded identifier slices, no embedded credentials or new production I/O. Independent peers bind loopback; malformed input never selects another media representation. |
| 62 | **Correctness**: Algorithms verified against reference implementations; edge cases tested (empty data, single sample, all-zero features) | Pass | Final validation is recorded in P6-42-validation.json: debug Qore suites, independent Python/CXF peers, docs, astparser and unchanged semantic corpus reports. All four changed modules compile to QMOD; the attachment suite also passes when those compiled modules are loaded. The focused attachment matrix covers 19 cases / 876 assertions; CXF contributes 24 live calls and 60 additional negative/recovery exchanges. |

All 62 checks: **27 Pass / 35 N/A / zero Fail**. Validation: [P6-42-validation.json](../P6-42-validation.json).
