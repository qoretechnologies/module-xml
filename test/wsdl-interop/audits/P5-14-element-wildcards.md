# P5-14 element wildcard values audit

Copyright (C) 2026 Qore Technologies, s.r.o.

2026-09-12, parent `8101f81`. Applied the complete audit-changes skill and all
five referenced module structure, sandboxing, cancellation and provider guides
to the final source, tests, documentation and refreshed diagnostic reports.
All 62 checks are resolved below. The deployed Qore prerequisite `783ecefc0`
fixes shared-container cycle collection; no module-xml ownership workaround was
introduced. Original failing evidence remains in `/tmp/wsdl-wildcard-registry-leak/`
and in the validation inventory's history.

All 133 functional suites (1,305 cases/64,748 reported assertions), 27 mode/AOT/
independent supplements, documentation and both-version corpus gates pass on the
final source and deployed runtime. All six memory runs have zero errors/lost
blocks. The unchanged HTTP suite passes Valgrind in isolation after a deadline
failure under competing heavy test load. The HTTP fstat(-1) warning is traced to
system libnss_sss, the existing P9 environment finding from P3-36; it is retained,
not suppressed. P5 and P6-P9 acceptance are not claimed complete.

[Validation inventory](../P5-14-validation.json) records exact hashes/commands;
[requirement evidence](../wildcard-elements-evidence.md) records the tested scope.

| # | Check | Status | Evidence |
| --- | --- | --- | --- |
| 1 | Entry exists in `doxygen/lang/120_modules.dox.tmpl` (for modules in the Qore repo; N/A for external module repos) | N/A | No new module entry point, registration, separated source file or directory layout; WSDL retains existing build and install rules. |
| 2 | Entry exists in `doxygen/lang/900_release_notes.dox.tmpl` (for modules in the Qore repo; external modules have release notes in their .qm) | Pass | WSDL 0.5.8 release notes and README describe wildcard assessment, retained XML, providers, samples and the shared registry. |
| 3 | `qore_user_module()` or `qore_external_user_module()` call in `CMakeLists.txt` | N/A | No new module entry point, registration, separated source file or directory layout; WSDL retains existing build and install rules. |
| 4 | Module added to QMOD list in `CMakeLists.txt` | N/A | No new module entry point, registration, separated source file or directory layout; WSDL retains existing build and install rules. |
| 5 | `.qm` file has `@section <lowercasemodname>intro` as first doc section — **must be all lowercase** (e.g., `avrodataproviderintro`, not `AvroDataProviderintro`) | Pass | The existing wsdlintro section and %modern remain; no redundant parse directive or %include was added. |
| 6 | `%modern` in `.qm` file — no redundant `%new-style`, `%require-types`, `%strict-args`, `%enable-all-warnings` | Pass | The existing wsdlintro section and %modern remain; no redundant parse directive or %include was added. |
| 7 | No parse directives (`%requires`, `%modern`, `%new-style`) in separated `.qc` files (check OUTSIDE of `@code` blocks only) | N/A | No new module entry point, registration, separated source file or directory layout; WSDL retains existing build and install rules. |
| 8 | No `%include` usage (deprecated for modules) | Pass | The existing wsdlintro section and %modern remain; no redundant parse directive or %include was added. |
| 9 | Copyright 2026 on all new files | Pass | New tests, worker, Python matrix, design and evidence files carry 2026 Qore copyright notices. |
| 10 | Directory layout: `.qm` inside `qlib/<ModuleName>/` directory (not at `qlib/<ModuleName>.qm` for multi-file modules) | N/A | No new module entry point, registration, separated source file or directory layout; WSDL retains existing build and install rules. |
| 11 | No second `.qm` for the same module at `qlib/<ModuleName>.qm` | N/A | No new module entry point, registration, separated source file or directory layout; WSDL retains existing build and install rules. |
| 12 | `ns=Qore::XX` matches the QoreNamespace constructor path | N/A | No C++/QPP source, native I/O, cancellation loop or public QPP method flags changed. |
| 13 | `%modern` directive present | Pass | All three qtests and the worker use %modern, executable permissions, local module paths and relative WSDL/consumer requires. External json is guarded; same-repository xml and core QUnit use hard requires. |
| 14 | Executable permission set (`chmod +x`) | Pass | All three qtests and the worker use %modern, executable permissions, local module paths and relative WSDL/consumer requires. External json is guarded; same-repository xml and core QUnit use hard requires. |
| 15 | Uses %prepend-module-path  before %requires for in-repo modules (Qore and Qore modules only; not Qorus) | Pass | All three qtests and the worker use %modern, executable permissions, local module paths and relative WSDL/consumer requires. External json is guarded; same-repository xml and core QUnit use hard requires. |
| 16 | External module dependencies use `%try-module` — except modules delivered with the project itself (Qore ex: DataProvider, ConnectionProvider, QUnit, etc.) which use hard `%requires` | Pass | All three qtests and the worker use %modern, executable permissions, local module paths and relative WSDL/consumer requires. External json is guarded; same-repository xml and core QUnit use hard requires. |
| 17 | No filesystem operations (fopen, open, creat, unlink, remove, rename, mkdir, rmdir, stat, chmod) without sandbox checks | N/A | No C++/QPP source, native I/O, cancellation loop or public QPP method flags changed. |
| 18 | No network operations (connect, bind, socket, getaddrinfo, gethostbyname) without sandbox checks | N/A | No C++/QPP source, native I/O, cancellation loop or public QPP method flags changed. |
| 19 | If filesystem/network ops exist, verify `QoreSandboxManagerHelper` usage | N/A | No C++/QPP source, native I/O, cancellation loop or public QPP method flags changed. |
| 20 | No `File::`, `Dir::`, `Socket::`, `HTTPClient::` usage without justification | Pass | Production changes perform no new I/O. The offline manifest worker reads explicit test files; real HTTP tests use existing sandboxed core/server APIs on loopback. |
| 21 | All `for`/`while` loops that could iterate >100 times have `qore_check_cancel()` checks | N/A | No C++/QPP source, native I/O, cancellation loop or public QPP method flags changed. |
| 22 | Uses `qore_check_cancel()` (NOT deprecated `qore_check_io_interrupt()`) | N/A | No C++/QPP source, native I/O, cancellation loop or public QPP method flags changed. |
| 23 | Check frequency: every 100 iterations for tight loops, every 10 for expensive iterations | N/A | No C++/QPP source, native I/O, cancellation loop or public QPP method flags changed. |
| 24 | No blocking operations without cancellation support | Pass | No blocking production operation added. Qore cancellation propagates through assessment/provider calls; deterministic bounded HTTP consumer cancellation and recovery pass. |
| 25 | Every action has `display_name`, `short_desc` (plain text, <80 chars), `desc` (markdown) | N/A | No fixed action/app request schema, action registration, secrets, FactoryMap change, getRecordTypeImpl method or JNI dependency. Schema-generated record fields use the existing provider framework. |
| 26 | Every action has `options` populated via `getActionOptionFromFields()` — without this, the action shows an empty, unusable form | N/A | No fixed action/app request schema, action registration, secrets, FactoryMap change, getRecordTypeImpl method or JNI dependency. Schema-generated record fields use the existing provider framework. |
| 27 | Every action has `output_type` set to a typed data type constant (e.g., `MyResponseDataType`) — not omitted | N/A | No fixed action/app request schema, action registration, secrets, FactoryMap change, getRecordTypeImpl method or JNI dependency. Schema-generated record fields use the existing provider framework. |
| 28 | DPAT_API actions: provider has `"supports_request": True` and implements `doRequestImpl()` | N/A | No fixed action/app request schema, action registration, secrets, FactoryMap change, getRecordTypeImpl method or JNI dependency. Schema-generated record fields use the existing provider framework. |
| 29 | DPAT_FIND actions: every option exists in `SearchOptions`, `getRecordTypeImpl()` returns `*hash<string, AbstractDataField>` | N/A | No fixed action/app request schema, action registration, secrets, FactoryMap change, getRecordTypeImpl method or JNI dependency. Schema-generated record fields use the existing provider framework. |
| 30 | Scheme-based apps (with `"scheme"` in registerApp): actions use `"path"` and do NOT use `"cls"` — having both `scheme` and `cls` causes a runtime error | N/A | No fixed action/app request schema, action registration, secrets, FactoryMap change, getRecordTypeImpl method or JNI dependency. Schema-generated record fields use the existing provider framework. |
| 31 | Single-key hash slices use trailing comma: `Fields{"key",}` (without trailing comma, `Fields{"key"}` returns the value, not a hash) | N/A | No fixed action/app request schema, action registration, secrets, FactoryMap change, getRecordTypeImpl method or JNI dependency. Schema-generated record fields use the existing provider framework. |
| 32 | **Typed data type classes exist** for request and response types — inherit `HashDataType`, have `const Fields` hash, call `addQoreFields(Fields)` in constructor, export public constant at bottom (e.g., `public const MyDataType = new MyDataType();`) | Pass | XsdWildcardContentDataType inherits HashDataType and is selected for resolved wildcard content. Fields are generated from the schema, so a fixed const Fields/public singleton cannot describe them. Source/AOT, ordinary/native/soft and HTTP consumers exercise the resulting record types. |
| 33 | Request/input types use `public` Fields (enables `ClassName::Fields` in action registration) | N/A | No fixed action/app request schema, action registration, secrets, FactoryMap change, getRecordTypeImpl method or JNI dependency. Schema-generated record fields use the existing provider framework. |
| 34 | Response/output types use `private` Fields | N/A | No fixed action/app request schema, action registration, secrets, FactoryMap change, getRecordTypeImpl method or JNI dependency. Schema-generated record fields use the existing provider framework. |
| 35 | Each field in data types has `display_name`, `type`, and `desc` (markdown-formatted) | Pass | Declared fields retain XsdDataFieldHelper metadata and typed allowed-value records; wildcard fields use XML expanded names. Attribute metadata/alias/allowed-value regressions remain in the full gate. |
| 36 | Input fields have `example_value` where useful (string fields, endpoint URIs, SQL queries, etc.) | Pass | Mandatory wildcard providers generate checked bounded examples through getTypeExample; strict samples carry an available builtin type. Tests cover namespace choices, global-name collisions and sample limits. |
| 37 | Fields with finite allowed values use `allowed_values` with `AllowedValueInfo` containing both `value` and `display_name` (Title Case, human-readable) — never bare values, never described only in text | Pass | Declared fields retain XsdDataFieldHelper metadata and typed allowed-value records; wildcard fields use XML expanded names. Attribute metadata/alias/allowed-value regressions remain in the full gate. |
| 38 | Password/secret fields have `"sensitive": True` | N/A | No fixed action/app request schema, action registration, secrets, FactoryMap change, getRecordTypeImpl method or JNI dependency. Schema-generated record fields use the existing provider framework. |
| 39 | `groups` uses `AppGroup` enum values from `qlib/DataProvider/AppGroup.qc` | N/A | No fixed action/app request schema, action registration, secrets, FactoryMap change, getRecordTypeImpl method or JNI dependency. Schema-generated record fields use the existing provider framework. |
| 40 | App `logo` stored as separate file, loaded at module level in `Priv` namespace | N/A | No fixed action/app request schema, action registration, secrets, FactoryMap change, getRecordTypeImpl method or JNI dependency. Schema-generated record fields use the existing provider framework. |
| 41 | App `desc` uses markdown: bullet list of capabilities, links to project website, business-language explanation of value | N/A | No fixed action/app request schema, action registration, secrets, FactoryMap change, getRecordTypeImpl method or JNI dependency. Schema-generated record fields use the existing provider framework. |
| 42 | `display_name` is user-friendly ("Apache Avro" not "avro") | N/A | No fixed action/app request schema, action registration, secrets, FactoryMap change, getRecordTypeImpl method or JNI dependency. Schema-generated record fields use the existing provider framework. |
| 43 | `short_desc` is plain text, under 80 chars, single sentence — no markdown | N/A | No fixed action/app request schema, action registration, secrets, FactoryMap change, getRecordTypeImpl method or JNI dependency. Schema-generated record fields use the existing provider framework. |
| 44 | `desc` uses markdown: backticks for code/field refs (`` `field_name` ``, `` `True` ``, `` `pdf` ``), `\n\n` for paragraphs, `- ` bullet lists for enumerations, `**bold**` for caveats | Pass | New provider descriptions explain partner extensions and retained XML in plain language with code-formatted names and types. No bare boolean/NOTHING prose or long unstructured description was added. |
| 45 | Descriptions use plain business language relating to common challenges — not just technical "what" but "why" and "when to use" | Pass | New provider descriptions explain partner extensions and retained XML in plain language with code-formatted names and types. No bare boolean/NOTHING prose or long unstructured description was added. |
| 46 | No bare `True`/`False`/`NOTHING` — must be backtick-wrapped in `desc` | Pass | New provider descriptions explain partner extensions and retained XML in plain language with code-formatted names and types. No bare boolean/NOTHING prose or long unstructured description was added. |
| 47 | No bare field/option names in prose — must use backticks | Pass | New provider descriptions explain partner extensions and retained XML in plain language with code-formatted names and types. No bare boolean/NOTHING prose or long unstructured description was added. |
| 48 | Long descriptions (>500 chars) use bold section headers and bullet lists | N/A | No fixed action/app request schema, action registration, secrets, FactoryMap change, getRecordTypeImpl method or JNI dependency. Schema-generated record fields use the existing provider framework. |
| 49 | **Factory registration in Qore repo**: every factory name registered in `qlib/DataProvider/DataProvider.qc` → `FactoryMap` (without this, module loads but doesn't appear in Qorus apps) | N/A | No fixed action/app request schema, action registration, secrets, FactoryMap change, getRecordTypeImpl method or JNI dependency. Schema-generated record fields use the existing provider framework. |
| 50 | **`getRecordTypeImpl()` signature**: must be `private *hash<string, AbstractDataField> getRecordTypeImpl(*hash<auto> search_options)` — NOT returning `*AbstractDataProviderType` | N/A | No fixed action/app request schema, action registration, secrets, FactoryMap change, getRecordTypeImpl method or JNI dependency. Schema-generated record fields use the existing provider framework. |
| 51 | **Dependency JARs committed** (for JNI modules): JAR files in `qlib/*/jar/` may be gitignored — use `git add -f` to ensure they're tracked, otherwise CI compilation fails | N/A | No fixed action/app request schema, action registration, secrets, FactoryMap change, getRecordTypeImpl method or JNI dependency. Schema-generated record fields use the existing provider framework. |
| 52 | JAR install rules in CMakeLists.txt for all dependency JARs | N/A | No fixed action/app request schema, action registration, secrets, FactoryMap change, getRecordTypeImpl method or JNI dependency. Schema-generated record fields use the existing provider framework. |
| 53 | **No workarounds**: No TODOs, FIXMEs, stubs, or partially-implemented features | Pass | Assessment delegates to resolved declarations/types; emission uses the complete particle and retains original XML fragments. No fixture-name condition, suppression, validation weakening or ownership workaround. Existing declared-anyType behavior remains explicitly tracked in the next P5 generic-value increment. |
| 54 | **Exception safety**: C++ uses `ReferenceHolder` for Qore allocations, `std::unique_ptr` for C++ allocations, `*xsink` checked after every fallible operation | Pass | Qore 783ecefc0 fixes shared-container reference-cycle collection and stale recursive-reference snapshots. The original minimal reproducer in default/AST modes, complete wildcard attribute suite and all three new suites pass Valgrind with zero errors/lost blocks. Registry rollback, failed reconstruction and retained consumer lifetimes are tested; no ownership workaround is added. |
| 55 | **Thread safety**: All mutable shared state protected by `std::lock_guard<std::mutex>` or documented as immutable-after-construction | Pass | Registry maps are published only during schema construction; detached components retain them, and failed schema additions restore maps. Concurrent reads use per-call output namespace state. Both SOAP binding concurrency suites pass. |
| 56 | **Type safety**: Strongly-typed `code<return(args)>` instead of untyped `code`; `static_cast` instead of C casts; typed hashdecls for results; enums where appropriate | Pass | Typed registry maps, component checks, wildcard enums, scoped QName/native-type state and typed references are used. Inferred hash values are widened only locally before adding heterogeneous XML metadata, with caller-preservation regressions. |
| 57 | **Performance**: No O(n²) where O(n) is possible; no unnecessary copies; coordinate descent uses incremental residuals not full matrix multiply | Pass | Completed registries use expanded-name hash lookup. Each wildcard subtree retains inherited bindings at its root; wire normalization is reused for assessment/emission. Existing bounded particle/sample algorithms and budgets remain in force. |
| 58 | **Error handling**: All inputs validated (dimensions, empty data, unfitted models); C++ I/O handles EAGAIN/EINTR if applicable | Pass | Positive/negative cases cover strict/lax/skip, unknown/abstract/unbound types, namespace constraints, required attributes, malformed registry metadata, inferred hashes, lexical child QNames, invalid XML carrier roots, DTD/PI rejection and recovery. Error categories are asserted. |
| 59 | **Documentation**: Doxygen `@param`, `@return`, `@throw` on all public methods; `@par Example` with realistic business scenarios; `@note` for important caveats | Pass | Public APIs have parameter/return/error documentation as applicable; three realistic @par Example descriptions, retained XML and type-wrapper code examples, release notes and durable implemented design are included. Final qdx/Doxygen validation is recorded separately. |
| 60 | **QPP flags**: `[flags=CONSTANT]` on methods that never throw; `[flags=RET_VALUE_ONLY]` on methods that throw but have no side effects | N/A | No C++/QPP source, native I/O, cancellation loop or public QPP method flags changed. |
| 61 | **Security**: No user-controlled format strings; no buffer overflows; bounds checking on array indices; no credentials in code | Pass | No credentials, untrusted format strings or new resource access introduced. Particle positions are asserted; namespace/duplicate-name and XML fragment restrictions are checked before emission. A syscall stack traces the HTTP fstat(-1) warning to system libnss_sss during c-ares service lookup, matching the P3-36 environment finding assigned to P9. |
| 62 | **Correctness**: Algorithms verified against reference implementations; edge cases tested (empty data, single sample, all-zero features) | Pass | All 133 suites and 27 mode/AOT/independent supplements pass on unchanged final source; the independent matrix checks 684 documents across 36 schemas, output validity and name/value/order preservation. Six Valgrind runs have zero errors/lost blocks. All 144 selected WSDLs/1,388 directions pass; the only corpus changes reject two invalid strict-wildcard inputs. Lifecycle correctness is verified on the deployed Qore fix. |

Result: 28 Pass, 34 N/A, 0 Fail. The P5-14 increment may be committed.
