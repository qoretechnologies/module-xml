# P3-38: nested union composition evidence

Copyright (C) 2026 Qore Technologies, s.r.o.

The root cause and implemented example are in
[the union composition design](../../design/xsd-union-composition.md).
The baseline selected a double for `2` after wrongly applying a nested union's
restrictions to its integer member. `/tmp/wsdl-p3-38-composition-before.log`
records the explicit primitive-type assertion failure. Dependencies are now fully
validated before their union definitions participate in composition.

The independent matrix comprises five definitions, 15 schema shapes and 37
positive/negative lexical cases. Its canonical JSON schema/expectation SHA-256 is
`556a35b010be6fb209b3fa9d15e172955e14dc6872c6c6146bbefb76c37d1183` (UTF-8, sorted keys, compact separators, no trailing newline).
The captured fixture file is `/tmp/wsdl-p3-38-composition-fixtures.json`.
The matrix is generated by the committed test source; no upstream corpus changes.

| Check | Result | Evidence |
| --- | --- | --- |
| Focused scalar tests | 4 cases / 108 assertions pass | `/tmp/wsdl-p3-38-composition-expanded.log` |
| Execution modes | AST, IR, JIT and tiered pass; 16 cases / 432 assertions | `/tmp/wsdl-p3-38-composition-modes.json` |
| Independent binding/consumer matrix | 444 inputs, 252 valid binding outputs; 2,608 consumer outcomes including 1,584 valid outputs/examples; all expected errors and values pass | `/tmp/wsdl-p3-38-composition-interop.log` |
| Independent validators | Xerces-J 2.12.2 on Java 25.0.4.1+1 and Python libxml2 2.12.10 agree on all assessed schema/documents, without warnings | Same two Python methods, both directions and both bindings |
| Full XML suites | 85 suites / 906 cases / 33,429 reported assertions pass without warnings | `/tmp/wsdl-p3-38-composition-xml-gate.json` |
| Survey unit tests | 15 methods pass | `/tmp/wsdl-p3-38-composition-survey-unit.log` |
| Coverage unit tests | 13 methods; exactly the two known P6-selected-binding-version subcases fail | `/tmp/wsdl-p3-38-composition-coverage-unit.log` |
| Both-version corpus | Raw and strict results unchanged from P3-37 except the WSDL source hash | `/tmp/wsdl-p3-38-composition-xml-{survey,coverage}.json` |
| Documentation build | Completes without warnings/errors | `/tmp/wsdl-p3-38-composition-docs.log` |

The shared graph test has 32 levels with two references to the previous level,
checks shared object identity and bounded provider metadata, and verifies member
order before and after reconstruction. Invalid nested restrictions and cycles
still fail, and subsequent schema construction succeeds. Direct restricted types,
atomic/list members and restrictions on the composed union retain their facets.
The initial qtest temporary-method-reference lifetime error was fixed by retaining
the provider object in the test; the original failure log is preserved.

No C++ or native module source changes are included, so Valgrind is not required
for this increment. The isolated core and native module remain the tested P3-37
artifacts. The existing SOAP reported-assertion accounting discrepancy is retained
in the reported total. Known P9 environment findings remain open. The complete
[audit](audits/P3-38-union-composition.md) applies to this independent increment;
QName namespace-bearing instance/provider/list integration remains in scratch.
